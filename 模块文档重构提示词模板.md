# 模块文档重构提示词 · 通用模板 v0.1（待人工审核）

> **用途**：以 01/02 工作流等定稿文档为基准，逐个重构 `docs/模块功能/` 下 11 个模块文档的统一提示词。占位符 `{{N}}-{{模块名}}` 按目标模块替换。
> **状态**：v0.1 草案（2026-08-18），待人工审核定稿。

---

## 任务

重构 `docs/模块功能/{{N}}-{{模块名}}.md`，使其简洁回答三问：

**目标是什么（§1）、现状在哪（§2）、差什么（§3）**。

具体如何实现不赘述，由后续单独工作完成。

---

## 基准（只读，禁反向修改）

1. `docs/模块功能/01-标定工作流.md`（🔒 定稿流程图 + D1–D8 定案）
2. `docs/模块功能/02-扫描工作流.md`（🔒 定稿流程图 + D1–D7 定案）
3. `框架整体.md`（§1 五层架构 / §2 会话与状态机 / §3 数据流）
4. `模块功能目录.md` §二 对象对口速查 + §三 设计对象功能基准（🔒）

---

## 产出结构（四段式）

### §1 目标功能

从基准提取本模块须满足的对象/职责/交互清单：

- 对象逐个标注归属：**【公】**app 存活件 / **【私】**会话件 / —
- 与 01/02 流程图步骤号挂钩（如「02-③ 按键电报接收」）
- 以目录 §三 基准为准，不自行发明职责

### §2 现状对齐

与代码简洁对齐：

- 文件/类 + 一句话核心能力，只列核心，不展开实现细节
- 此节 AI 可随代码演进更新（同 01/02 §3 规则）

### §3 差距清单

三类逐项列（每项一句话 + 依据）：

- **【增加】** 基准要求而代码没有
- **【修改】** 代码有但与基准不符（写明：现状 → 改为 → 依据）
- **【删除】** 代码有但基准不再需要

### §4 附录

外部协议/接口规格/参考资料（如有则留，无则省略）。

---

## 规则

| # | 规则 |
|---|---|
| R1 | 只描述目标/现状/差距，不写实现方案、接口草案、实施批次 |
| R2 | 发现模块文档与基准冲突 → 报告人工裁决，禁止改基准迁就 |
| R3 | 旧文档被删减的内容 → 列清单报人工确认去向（附录/后续工作/删），不静默丢失 |
| R4 | §2 现状须如实反映代码（引用真实文件路径），禁止把目标写成现状 |
| R5 | 全文用**通俗简洁语言**：短句、大白话、少术语堆叠；必要的术语首次出现时用一句话点明含义；协议/报文格式等规格性附录除外（保持精确） |
