# 模块文档提示词模板 v0.3

> **用途**：重构/维护 `docs/模块功能/{{N}}-{{模块名}}.md` 的统一提示词。占位符 `{{N}}-{{模块名}}` 按目标模块替换。
> **文档只回答两问：目标是什么（§1）、代码做到哪（§2）**。工程过程信息（变更/裁决/差距/批次）一律不写。

---

## 任务

按下方产出结构重写目标模块文档，使其简洁回答两问，人类可读、排版整齐、只体现代码已有的。

**§2 动笔前，先完成「核心维度分析」（见下节），维度组合经人工确认后才开写。**

---

## 使用前置：核心维度分析（供人工决策）

§2「实现现状」写哪些维度，因模块而异。动笔前 AI 须依次完成三步：

1. **通读**：目标模块代码（`modules/{{NN}}_*/`、`app/`）＋旧版文档
2. **提案**：给出该模块的核心维度组合＝**通用基底**（必选）＋**模块特有**（从维度库选或新提，每项附一句理由）
3. **等确认**：人工确认维度组合后，§2 按确认的维度逐行成文

### 维度库（提案参考，可新提）

| # | 维度 | 回答什么 | 典型适用 |
|---|---|---|---|
| D1 职责能力 ★ | 这个类/文件做什么（一句话） | 全模块（基底） |
| D4 数据流交互 ★ | 谁生产/谁消费、上下游接口 | 全模块（基底） |
| D2 生命周期归属 | 【公】app 存活件／【私】会话件 | 06/07/08 |
| D3 线程/并发 | 线程模型、锁策略、P核/E核/GPU 调度 | 06/07/08 |
| D5 错误/降级 | fail/degraded/warning、故障码、兜底路径 | 06/08/10 |
| D6 协议/契约 | 命令字/报文格式、接口契约 | 08/09 |
| D7 测试覆盖 | tests/ 有无、通过情况 | 07/09 |
| D8 编译归属 | 编入哪个库/exe、关键依赖 | 03/06 |

★＝通用基底，全模块必选（另加「状态」三档列）；其余维度按模块提案选用，未选中的不写。

---

## 信息来源

| 段 | 来源 |
|---|---|
| §1 功能目标 | **原文档（旧版）的目标段**——提取其目标内容，压缩成表但不改写语义；原文档无目标段的才回退查 `模块功能目录.md` §2＋`框架整体.md` |
| §2 实现现状 | 目标模块真实代码：`modules/{{NN}}_*/`、`app/`（引用真实文件路径） |
| §3 附录 | 原文档附录中仍有效的规格/协议/规范链接；已失效或过程性的丢弃 |

---

## 产出结构（两段式）

> 排版总则见下方「排版规范」节——**适用于整篇文档**（§1–§4 全部），此处骨架仅列段落构成。

```markdown
# 模块{{N}} · {{模块名}}

> 一句话：本模块的通俗定位（是什么、给谁用）
> 返回索引：[`模块功能目录.md`](../../模块功能目录.md)

## 1. 功能目标

（提取自原文档目标段；对象粒度对齐 模块功能目录.md §2 速查；跨模块契约一行带过，规格细节入附录）

### 1.1 对象A
- 做什么：…（一句话）
- 交互：01-② 产出；02-① 消费

## 2. 实现现状（以代码为准）

（维度行按人工确认的维度组合生成，基底三行固定；本节随代码演进更新）

### 2.1 `modules/{{NN}}_*/xxx.h`（类名）
- 核心能力：…（一句话）
- 交互：01-② → 02-①（与 §1 目标一致时写「—」）
- 状态：✅／◐／预留

## 3. 附录（可选，无则省略）

外部协议、接口规格、相关规范文档链接。

## 4. 修改记录（初始空，后期修改追加于此）

| 日期 | 改了什么 | 依据/备注 |
|---|---|---|
| | | |
```

---

## 排版规范（适用于整篇文档，§1–§4 全部）

| # | 规范 |
|---|---|
| P1 | **数字编号划分内容**：章（## 1.）—节（### 1.1）—行（维度行）三层，层级连续不跳号，编号即阅读索引 |
| P2 | **禁用多列宽表格**：内容逐对象小节＋每维度一行；两列轻表（键值对）可用；仅 §4 修改记录表除外 |
| P3 | **每维度单独一行**：一个维度不与另一维度同行；同对象多维用 `- 维度：值` 列表行 |
| P4 | **尽量用流程图**：流程步骤、数据流向、对象装配关系用 ASCII 流程图表达，不用文字/表格平铺 |
| P5 | **通俗短句**：一句话点明含义；术语首次出现给一句解释；协议/报文规格除外（保持精确） |

---

## 规则

| # | 规则 |
|---|---|
| R1 | 只写「目标＋现状」两段；差距清单、待办、人工裁决、设计方案引用一律不写 |
| R2 | 文档头部禁止变更日志、重构记录、对齐记录、批次号、日期流水 |
| R3 | 现状必须与代码逐一对齐（真实文件路径），禁止把目标写成现状 |
| R4 | 每对象一句话，不展开实现细节；细节由源码与 `docs/` 专题文档承载 |
| R5 | 排版执行「排版规范」节 P1–P5（整篇适用） |
| R6 | 旧文档中的差距/报备/裁决内容不迁移，直接丢弃；仍反映代码的实质信息须保留 |
| R7 | §2 动笔前须先提案核心维度组合（基底＋特有）交人工确认；确认后按维度逐行成文，未选维度不写 |
| R8 | §1 目标提取自原文档目标段：目标语义不变、只压排版，禁止自行发明、增删或改写目标；目标段中内嵌的裁决号/批次号/日期等过程引用一并剥离 |
| R9 | 修改记录只写 §4 表内（日期＋改了什么＋依据），正文各段禁插变更流水；§4 新文档初始为空表 |
