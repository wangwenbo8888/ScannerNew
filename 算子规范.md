# 算子规范（流水线契约 + 工程集成）

> **版本**: v2.0
> &emsp;&emsp;v1.9 → v2.0：合并原《算子合并检查清单》（`算子规范2.md`）为 **§7 工程集成与合并规范**；§0–§6 单算子契约规则不变，重叠项（内存/异常/CUDA 分离）改为交叉引用去重。
>
> **用途**: 所有算子（现有改造 + 新增）必须遵守的硬性契约。与「算子提炼模板」(描述卡) 分离。适用于**扫描流水线**（`scanning/流水线方案*.md`）与**标定姿态流水线**（`calibration/姿态判断流水线方案.md`）共用算子。§1–§6 规定**单个算子长什么样**；§7 规定**多个算子合并到同一构建产物时的工程一致性**。
>
> **条款来源**: 两类——① 流水线约束：必须能回溯到 §0.1 事实表，回溯不到即为过约束；② 算子工程规范：算子本身应满足的工程要求（如 §3.4、§7），在条款内另行标注，不回溯到 §0.1。

> **🎯 重点条款索引**（后续实施重点执行）
>
> | 重点类别 | 对应章节 | 核心要求 |
> |---|---|---|
> | **接口规定** | §2【重点】 | 三元组 + `Execute()`/`Destroy()` + PascalCase + pImpl + GPU 资源实例隔离（§2.6） |
> | **并行规定** | §1【重点】 | `Stream&` 流亲和 + 非阻塞 + 每实例非线程安全 + 禁算子内多线程（§1.5）+ 禁 static 可变状态（§1.6） |
> | **数据传递规定** | §3【重点】 | 共享类型集中 + GpuMat 走 `shared_ptr` + Result move-only + 大数据禁值传递 + Params 值语义（§3.5）+ 只读共享（§3.6） |
> | **工程集成规定** | §7【重点】 | 符号唯一 + 导出控制 + 头文件组织 + 依赖/链接一致 + CUDA 分离 + 内存统一（合并到同库时） |

---

## 0. 适用范围

覆盖 `core/`（共享算子库）、`scanning/`（扫描应用算子）、`calibration/`（标定应用算子）下全部算子。CPU 与 CUDA 算子均适用；CUDA 算子有附加条款（标注 🔵）。

> **双流水线覆盖**：`core/` 算子（如 `mask_extract`、`ccl`、`image_split`…`point_reconstruct`、`frame_fuse`/`optical_flow_fuse`）被扫描流水线与标定姿态流水线**复用**，须同时满足两条流水线的实例隔离要求（两者一致：每路流水线独占 1 实例）。`scanning/` 独有算子（如 `mask_separation`、`laser_match_scan`、`laser_cloud_fuse`）和 `calibration/` 独有算子（如 `pose_estimate`、`intrinsic_calib`、`plane_map` 等）仅各属一条流水线，但同样遵守本规范全部条款。

---

## 0.0 硬件约束背景（摘录自流水线方案 §1.2）

> **说明**：本节为背景参考，不直接作为算子契约依据。对算子的契约要求见 §0.1。两条流水线共享 C1/C3/C5，C2/C4/C6/C7 因流水线而异：

| 编号 | 约束（扫描流水线） | 约束（标定姿态流水线） | 共同点 |
|------|------|------|------|
| C1 | 硬件为 Intel CPU（X 个 P 核 + Y 个 E 核）+ NVIDIA GPU | 同左 | **一致** |
| C2 | 1 个 P 核锁定给 Windows 系统 + 200fps 双目图像采集；同帧双目图打包为环形缓冲区的一个单元 | 1 个 P 核锁定给 Windows 系统 + 采集线程；**采集周期单元**（标记点帧 + N 个激光管帧 + 温度）打包写入环形缓冲区 | 均由 P0 写环形缓冲 |
| C3 | X-1 个 P 核 + X 个 E 核锁定给流水线；每流水线 = 1 P 核 + 1 E 核；E 核负责调度与单帧融合 | 同左（E 核额外负责姿态判断） | **一致** |
| C4 | 流水线空闲时自动从环形缓冲区抓取**最新**图像 | 流水线**顺序消费每一帧**（不跳帧，反压策略，防漏检姿态） | 均从环形缓冲取帧 |
| C5 | 自动分配并行 X 个流水线；流水线数 = 分配给流水线的 P 核数 + 1（防 P 核空闲） | 同左 | **一致** |
| C6 | GPU 分配 **2** 个并发槽；扣除显示渲染与系统必须后，剩余 CUDA 核心平分给两个槽 | GPU 分配 **1** 个槽即可（姿态链 GPU 工作量轻：仅 mask_extract + ccl） | 均需 GPU 槽管理 |
| C7 | 速度优先 | 姿态优先（不丢帧） | 均为最高优先级目标 |

---

## 0.1 源头：从流水线提取的事实

规范的每一条款必须能回溯到下表。下表是两条流水线方案对算子的全部约束。

| 流水线事实 | 对算子的契约要求 | 落到维度 |
|---|---|---|
| 帧内 GPU 工作串行在**同一 stream** 上（不让出槽） | CUDA 算子必须接受外部 `cv::cuda::Stream&`，不得内部自建默认流 | §1 |
| E 核 ↔ P 核跨线程传递结果（标记点链 future） | Result 必须 move-only（禁 copy），跨线程安全转移 | §3 |
| 每路流水线独占 1 E 核 + 1 实例 | 每实例非线程安全；并发靠"每路一实例"，不靠锁 | §1 |
| 流水线对线程做 **P 核 / E 核绑定**（E 核跑编排、P 核跑重算） | 算子内部**禁止**自建 host CPU 多线程（避免与 P/E 核绑定抢占） | §1 |
| **扫描**：融合-01 读 `PrevFrameState` 快照、融合-02 持哈希表；**标定**：2-12 融合读 `PrevFrameState` 快照、2-13 姿态命中写 `g_collected[25]`/`g_postureData[25]` | 算子必须显式声明状态归属 | §4 |
| GPU 槽 acquire 阻塞、warmup 避免冷启动 | 必须有 `warmup()` 预分配；运行时不得触发重分配 | §2 |
| **扫描**：失败帧被丢弃（丢帧策略、错误处理表）；**标定**：不丢帧（反压策略），但失败的处理结果仍可安全丢弃（环形缓冲数据不受影响） | 单一失败模型 `success=false + qualityFlag`；失败 Result 可安全丢弃 | §5 |
| **扫描**：预处理-01 输出供两条链（CPU + CUDA）复用；**标定**：单分支（仅标记点链），但 core 共享类型仍需集中管理 | 共享类型集中在 `common/`，GpuMat 用 shared_ptr 延长生命 | §3 |

---

## 1. 执行与资源【重点·并行规定】

| 条款 | 规则 |
|---|---|
| 1.1 平台标记 | 每算子必须声明运行平台：CPU 或 CUDA |
| 1.2 流亲和 🔵 | CUDA 核心方法必须接受 `cv::cuda::Stream&`（**引用**，非值），且不得内部使用默认流 / 自建流。允许提供无 stream 重载作向后兼容，但流水线调用必须显式传 stream |
| 1.3 非阻塞 | 算子内部不得执行 host 端阻塞等待。同步由调用方（E 核编排）在帧边界做 |
| 1.4 实例并发模型 | 每实例**非线程安全**；并发通过"每路流水线独占一个实例"实现。Debug 模式建议 atomic 断言检测并发调用 |
| 1.5 算子内并行模型 | 算子内部**禁止**创建或使用 host CPU 多线程（`std::thread` / `std::async` / OpenMP / TBB / `pthread` 等）。🔵 CUDA kernel 内部的 GPU 并行不受此约束（由 §1.2 管控）。 |
| 1.6 实例隔离 | 算子内部**禁止** static 可变变量、全局可变状态、单例；仅允许 `static const` / `constexpr` 只读查表。多实例并行下任何共享可变状态都会被踩踏。 |

> **⚠️ §1.2 / §1.3 实施门槛（内部同步移除属高风险，不得自动实施）**
>
> 落实 §1.3「非阻塞」及 §1.2「不得自建流」时，**移除算子内部 `cudaStreamSynchronize` / `stream.waitForCompletion()` 等 host 端阻塞同步**是本规范中风险最高的改造，**禁止机械/自动批量实施**，必须满足以下条件之一方可动手：
>
> 1. **人工逐点确认该同步为冗余**（safe-remove：同步后未读取 host 值，或同 stream 自然有序），并经代码审查签字；或
> 2. **已配套算法重构**消除 host-feedback 依赖（count/size 留 GPU、定长输出、两遍 device-side 调度等），**并补针对性并发测试**（多路流水线并发 + 数据量压力，覆盖单线程测试无法触发的 race window）。
>
> **原因**：现存内部同步多数为**承重同步**——`D2H` 下载 host 值（如有效点数）后立即读取以决定后续步骤。盲目移除会导致**静默数据竞争**（读到未完成拷贝的脏数据），且**单线程测试无法可靠捕捉**，仅在 §0.0 C5 多路流水线并发 + 特定数据量下间歇暴露，产线定位极难。
>
> **不满足上述条件时**：保持现状，在该同步处加注释标记为「§1.3 已知偏离 · 算法债 · 待重构」，把沉默违规转为有据可查的工程决策。
>
> **性质定位**：本条款（内部同步移除）属**性能 / 算法债**范畴，**非功能正确性问题**——同步是保守、保正确的，留着不影响输出正确性，仅影响吞吐。标定/姿态路径（低频，§0.0 C6 单槽）收益≈0；扫描热路径（200fps）应在**实测出现吞吐瓶颈且定位到这些同步**时才优先处理（首推同步最密集的算子）。

**改造对象**：`RegionAnalyzerCUDA::analyze(stream)` 当前 stream 取值传递并带默认 `Stream::Null()`，与 `StegerExtractorCUDA::extract(Stream&)` 取引用且无默认不一致 → 统一为 `Stream&` 引用。

---

## 2. 接口形状【重点·接口规定】

| 条款 | 规则 |
|---|---|
| 2.1 三元组 | 每算子由 `XxxParams` / `XxxResult` / `XxxOperator` 三部分构成 |
| 2.2 构造与配置 | 构造函数接受 `const XxxParams&`（带默认值）；提供 `SetParams()` 动态更新 + `GetParams()` |
| 2.3 预热 | 必须提供 `Warmup(int rows, int cols)` 和 `Warmup(const WarmupConfig&)`。Warmup 之后运行期**不得**触发 GPU 缓冲区重分配。非流水线批量算子（如标定阶段 3/4 的 `intrinsic_calib`、`stereo_rectify` 等）可提供空实现以统一接口形状 |
| 2.4 CUDA 隔离 🔵（必要实现约定） | CUDA 算子必须用 pImpl 模式，公开头文件不含 CUDA 类型，保证 `.cpp` 桥接 + `.cu` 实现分离 |
| 2.5 执行/销毁接口 | 见下表（§2.5 子条款） |
| 2.6 资源实例隔离 | `Warmup()` 预分配的 GPU 缓冲必须**每实例私有**；禁用全局/静态 GPU 资源池。多实例并行需各自独立 GPU 资源。 |

### 2.5 子条款：执行/销毁接口契约

| 子条款 | 规则 |
|---|---|
| 2.5.1 执行入口 | 统一命名为 `Execute(...)`；返回 `XxxResult`（move-only，与 §3.3 一致）。🔵 CUDA 算子末参必须为 `cv::cuda::Stream&`（与 §1.2 一致，禁止默认流/自建流、禁止内部 `cudaStreamSynchronize`） |
| 2.5.2 销毁入口 | 统一命名为 `Destroy()`；所有资源的释放**只在** `Destroy()` 中进行；析构函数 `= default`，**不**承担资源释放责任 |
| 2.5.3 调用契约 | 调用方责任：**先 `Destroy()` 再析构**对象。`Destroy()` 必须 idempotent（多次调用安全、无副作用）。未先调 `Destroy()` 即析构视为调用方 bug（资源泄漏由调用方负责） |
| 2.5.4 命名风格 | 算子对外公开方法统一 **PascalCase**：`Execute` / `Destroy` / `Warmup` / `SetParams` / `GetParams`。私有成员不受此约束。<br>**[来源 ② 算子工程规范]** 不回溯到 §0.1 |

**目的**：让缺失算子（扫描 M-01~M-04 等）及标定新增算子补齐时有统一骨架。

**改造对象**：
- 现有算子核心方法全面重命名为 PascalCase：`RegionAnalyzerCUDA::analyze` → `Execute`、`StegerExtractorCUDA::extract` → `Execute`、`setParams`/`getParams`/`warmup` → `SetParams`/`GetParams`/`Warmup`。
- 所有算子补齐 `Destroy()`（含 CPU 算子——即便无 GPU 资源，也提供空实现以统一接口形状）。
- CUDA 版 `Execute` 取 `Stream&`（与 §1 改造对象合并处理：`RegionAnalyzerCUDA` 当前 stream 值传递 + 默认 `Stream::Null()`，统一为 `Stream&` 引用）。

---

## 3. 数据与端口【重点·数据传递规定】

| 条款 | 规则 |
|---|---|
| 3.1 共享类型集中 | 跨算子复用的类型只放 `common/`，**禁止**重复定义 |
| 3.2 GpuMat 传递 🔵 | GPU 数据跨算子传递用 `std::shared_ptr<cv::cuda::GpuMat>`（引用计数延长生命）；`const GpuMat&` 重载仅作向后兼容 |
| 3.3 Result 所有权 | `XxxResult` 必须 move-only（显式 `delete` copy，`default` move）。跨 E 核 ↔ P 核传递靠 move |
| 3.4 大数据传递 | 算子间传递**非平凡拷贝对象**（拷贝成本高的对象）**禁止值传递**；只允许：`const T&`（只读入参）、`T*`（出参）、`std::shared_ptr<T>`（跨算子共享持有）、move（所有权转移）。标量 / 小 POD / `Params` 等轻量对象不受此约束。<br>**[来源 ② 算子工程规范]** 不回溯到 §0.1 |
| 3.5 Params 值语义 | `XxxParams` 必须值语义（可深拷贝）；禁止内部持有外部数据裸指针/引用，防止 `SetParams()` 后外部修改穿透到算子。 |
| 3.6 只读共享资源 | LUT / 模型 / 标定等只读共享资源用 `std::shared_ptr<const T>` 或 `const T&` 传入；算子内部不得写入。 |

**改造对象**：现有 `common/quality_flag.h` 与 `common/calib_types.h` 中 `QualityFlag` **重复定义** → 合并保留一处。`StegerExtractorCUDA` 缺 shared_ptr 重载。

---

## 4. 状态模型

每算子必须**显式声明**属于以下三类之一（声明位置：算子提炼模板 §G 约束，或头文件类注释）：

| 类别 | 定义 | 契约 |
|---|---|---|
| 无状态 | 调用之间不保留任何数据 | 可任意并发（仍受 §1.4 实例约束） |
| 调用方持有 | 算子需要跨帧状态，但状态由调用方管理（算子只读写入参） | 状态通过参数传入传出；算子实例本身仍无状态 |
| 自持久化 | 算子内部持有跨帧持久数据 | 必须提供 `clear()` 重置；必须声明并发策略（互斥串行或原子 CAS，视场景而定） |

**多实例状态边界**：自持久化算子必须显式声明状态是"每实例私有"还是"跨实例共享"；跨实例共享必须提供同步原语（扫描：对齐融合-02 的 `commitMutex` 互斥模式；标定：对齐 `g_collected[25]` 原子 CAS 模式）。

**目的**：状态归属此前隐式藏在文档里，现强制声明，消除"实例能否多路复用"的歧义。

---

## 5. 失败与质量

| 条款 | 规则 |
|---|---|
| 5.1 单一错误模型 | 每个 Result 必须含 `bool success` + `calib::QualityFlag qualityFlag` + `std::string message`。**禁止**第二种错误机制 |
| 5.2 失败可丢弃 | `success=false` 的 Result 必须能被流水线安全丢弃（不破坏后续帧、不泄漏资源）。Result 构造需自洽 |
| 5.3 质量降级语义 | Normal / Degraded / Warning 三档；Degraded / Warning 仍 `success=true` 并提交下游 |
| 5.4 参数非法 | 仅参数校验失败允许抛 `std::invalid_argument`；运行期错误一律走 §5.1 模型 |

**改造对象**：`StegerExtractorCUDA` 当前同时有 `SE_*` int 错误码和 `bool success` 双机制 → 移除 int 码，统一为 `success + qualityFlag`。

---

## 6. 一致性检查清单（单算子接入 / 改造）

新接入 / 改造**单个**算子前必过（多算子合并的工程检查见 §7.1）：

- [ ] 平台已标记？🔵 CUDA 接受 `Stream&` 且不内部阻塞？
- [ ] 算子内部未自建 host CPU 多线程？
- [ ] 算子内部无 static 可变变量 / 全局可变状态 / 单例？
- [ ] `Params` / `Result` / `Operator` 三元组 + `Warmup` 齐全？
- [ ] 执行入口统一 `Execute()`？🔵 CUDA 版末参带 `Stream&`？
- [ ] 销毁入口统一 `Destroy()` 且 idempotent？析构 `= default`？
- [ ] 公开方法命名 PascalCase（`Execute` / `Destroy` / `Warmup` / `SetParams` / `GetParams`）？
- [ ] Warmup 分配的 GPU 资源每实例私有？无全局/静态 GPU 资源池？
- [ ] 共享类型取自 `common/`，无重复定义？
- [ ] Result move-only？🔵 GpuMat 走 `shared_ptr`？
- [ ] 大数据未值传递？走 `const T&` / `T*` / `shared_ptr` / move？
- [ ] Params 值语义，无外部裸指针/引用？
- [ ] 只读共享资源用 `shared_ptr<const T>` / `const T&`？
- [ ] 状态类别已声明？（自持久化的话有 `clear()`？已声明状态边界 + 同步原语？）
- [ ] 错误只有 `success + qualityFlag` 一种？

---

## 7. 工程集成与合并规范【重点·原《算子合并检查清单》】

> **定位**：§1–§6 规定**单个算子**的契约；§7 规定**多个算子从独立开发合并到同一构建产物**（库 / DLL）时的工程一致性要求。两者互补。本节原为独立文档 `算子规范2.md`（《算子合并检查清单》），现合并于此。重叠项（内存管理、异常处理、CUDA 分离）以交叉引用 §2.3 / §2.4 / §2.6 / §5.1 为准，不再重复约束。

### 7.1 合并前检查清单

**符号检查**
- [ ] 所有类名/函数名在命名空间内唯一
- [ ] 无全局命名空间的符号
- [ ] 检查导出符号：`dumpbin /exports Algorithm.dll`

**依赖检查**
- [ ] 第三方库版本一致（OpenCV、Eigen 等，统一到《环境配置汇总.md》声明版本）
- [ ] 编译选项一致（/MT vs /MD、优化级别等）
- [ ] 预处理定义一致（`BUILD_CUDA`、`WIN32` 等）

**内存检查**
- [ ] 无内存泄漏（DrMemory / AddressSanitizer）
- [ ] GPU 内存正确释放（cuda-memcheck / Compute Sanitizer）
- [ ] 统一内存池配置

**链接检查**
- [ ] 无循环依赖
- [ ] 所有符号都有定义
- [ ] 链接顺序正确

**头文件检查**
- [ ] 无绝对路径
- [ ] include guard 正确（`#pragma once`）
- [ ] 公共头文件最小化

### 7.2 符号唯一性与导出控制（🔴 高优先级）

**问题**：各算子独立编译时各自的 `Result` / `Params` 等同名类型，合并后链接器看到重复符号。

**两种解决路线（工程已选「路线 B」）：**

```cpp
// 路线 A：每算子独立子命名空间
namespace calib::mask_extract     { struct Result { ... }; }
namespace calib::region_analyze   { struct Result { ... }; }

// 路线 B（本工程采用）：统一命名空间 calib:: + 类型名前缀
namespace calib {
    struct MaskExtractResult      { ... };   // mask_extract
    struct RegionAnalysisResult   { ... };   // region_analyze
    struct PoseOptimizeResult     { ... };   // pose_optimize
}
```

> **工程现状**：全部算子位于扁平命名空间 `calib::`，类型名带算子前缀（`MaskExtractResult`、`LaserMatchResult`、`PoseOptimizeResult`…）。新增算子沿用此前缀约定，**禁止**在 `calib::` 内放入同名类型。

**导出符号控制**：公开接口用导出宏（如 `ALGORITHM_API` / `JMW_API`）显式标注，私有成员不导出，避免符号污染。

```bash
# Windows：检查导出符号
dumpbin /exports Algorithm.dll | findstr "YourClass"
# Linux：检查符号表
nm -D libalgorithm.so | grep "T " | c++filt
```

### 7.3 头文件组织与目录结构（🔴 高优先级）

**问题**：独立编译时头文件路径不一致。

```cpp
// ❌ 绝对路径
#include "E:/workfold/20260509intergrate/Algorithm/calib/mask_extract.h"
// ❌ 深层相对路径
#include "../../../../../calib/mask_extract.h"
// ✅ 算子自目录作 include 根（构建期把每个含头目录加入 -I，见 §7.9）
#include "mask_extract_cuda.h"
```

**工程实际目录结构**（单库 `mod_operatorlib`，未做 include/ 与 src/ 物理分离；每算子一个自目录，头文件 + 实现混放，靠 -I 支持跨算子引用）：

```
modules/09_operatorlib/            # 单一静态库 mod_operatorlib（core+calibration+scanning 合并）
├── core/                          # 共享算子（对齐 §0）
│   ├── common/                    # 共享类型（对齐 §3.1）：calib_types.h / quality_flag.h
│   ├── vision/
│   │   ├── mask_extract/          # mask_extract_cuda.h + .cpp + _impl.cu
│   │   └── ccl/                   # region_analyze
│   ├── marker/  laser/            # 标记点链 / 激光链算子
│   └── scheduler/                 # prev_frame_state 等
├── calibration/laser_calib/       # 标定算子：plane_map/ pose_optimize/ endpoint_extract/ ...
├── calibration/posture/           # pose_estimate 等
└── scanning/                      # 扫描算子：fusion/ laser/ preprocess/ global_optim/
```

### 7.4 依赖与链接一致性（🟡 中优先级）

**依赖版本统一**：各算子不得各自声明第三方库版本，统一在根 `CMakeLists.txt` 经 `find_package` 锁定（《环境配置汇总.md》为版本基准）。

```cmake
find_package(OpenCV 4.13 REQUIRED COMPONENTS core imgproc calib3d cudaimgproc cudaarithm cudafilters flann)
target_link_libraries(mod_operatorlib PUBLIC ${OpenCV_LIBS} Eigen3::Eigen ...)
```

**链接顺序 / 循环依赖**：算子间依赖必须**单向无环**。若出现 `A→B→C→A`，要么重设计为单向，要么把三者并入同一库（库内部不互相链接）。

> **工程现状**：`core/`+`calibration/`+`scanning/` 已合并进**单一静态库** `mod_operatorlib`（同库内部目标不互相链接），故本约束目前落到 **`#include` 关系无环**（避免头文件循环引用）；若日后拆分为多库，再适用库级链接顺序约束。

### 7.5 CUDA 代码分离（🟡 中优先级）

**问题**：`.cu` 文件需 nvcc 单独编译，链接器不理解 PTX。

**与 §2.4 的关系**：§2.4（pImpl）管**头文件纯净**（公开头不含 CUDA 类型）；本节管**编译/链接分离**（`.cu` 由 nvcc 编对象、`.cpp` 由 cl 编对象、最后 link.exe 统一链接）。两者共同保证"公开接口与 CUDA 实现解耦"。

工程实际做法（现代 CMake CUDA，非旧 `cuda_add_library`）：

```cmake
enable_language(CUDA)
set(CMAKE_CUDA_ARCHITECTURES "75;86;87")   # 见《环境配置汇总.md》
add_library(mod_operatorlib STATIC ${OP_SOURCES})   # .cpp + .cu 混合源
target_compile_options(mod_operatorlib PRIVATE
    $<$<COMPILE_LANGUAGE:CXX>:/bigobj>
    $<$<COMPILE_LANGUAGE:CUDA>:-Xcompiler=/bigobj>) # 重模板算子需 /bigobj
```

> ⚠️ 旧式 `find_package(CUDA)` + `cuda_add_library` 在 CMake ≥ 3.27 已移除（CMP0146），不得使用。

### 7.6 内存管理统一（🟡 中优先级）

**与 §2.3 / §2.6 的关系**：本节是 §2.3（Warmup 预分配、运行期不重分配）与 §2.6（每实例私有、禁全局/静态 GPU 资源池）的落地示例，不另立约束。

```cpp
class MaskExtractCUDA {
    // 所有 GPU 缓冲 = 实例成员（非 static / 非全局）——对齐 §2.6
    cv::cuda::GpuMat inputBuffer_, outputBuffer_, tempBuffer_;

    void Warmup(int rows, int cols) {
        // 一次性预分配，运行期不重分配——对齐 §2.3
        inputBuffer_.create(rows, cols, CV_8UC1);
        outputBuffer_.create(rows, cols, CV_32FC1);
        tempBuffer_.create(rows, cols, CV_8UC1);
    }
};
```

> 混用 `cv::cuda::GpuMat`（GPU 显存）、`std::vector`（系统内存）、自定义内存池时，须在各算子边界明确所有权与生命周期，禁止跨算子传递裸 GPU 指针（对齐 §3.2：GpuMat 走 `shared_ptr`）。

### 7.7 编译选项与异常处理统一（🟢 低优先级）

**编译选项**：所有算子统一编译选项（优化级别、`/MD`/`/MDd`、`BUILD_CUDA` 等预处理宏），不得各算子各异。

**异常处理 → 统一到 §5.1**：**禁止**算子各搞一套错误机制（try-catch / 错误码 / `std::optional` 混用）。统一采用 §5.1 单一错误模型：

```cpp
struct MaskExtractResult {
    bool success;                   // 对齐 §5.1
    calib::QualityFlag qualityFlag;
    std::string message;
    // ... 业务字段
};
// 仅参数非法允许抛 std::invalid_argument（§5.4）；运行期错误一律填 success=false
```

### 7.8 版本管理（🟢 低优先级）

```cpp
#define OPERATORLIB_VERSION_MAJOR 1
#define OPERATORLIB_VERSION_MINOR 0
#define OPERATORLIB_VERSION_PATCH 0

struct OperatorInfo {
    const char* name;
    int version;
    OperatorType type;
};
extern "C" JMW_API OperatorInfo* getOperatorLibInfo();
```

### 7.9 推荐构建流程

工程实际根 `CMakeLists.txt` 已采用现代 CMake（`enable_language(CUDA)` + `CMAKE_CUDA_ARCHITECTURES` + FetchContent 依赖），**以此为准**；下面仅作参考骨架：

```cmake
cmake_minimum_required(VERSION 3.24)
project(OperatorLib LANGUAGES CXX CUDA)
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CUDA_STANDARD 17)
set(CMAKE_CUDA_ARCHITECTURES "75;86;87")

find_package(OpenCV 4.13 REQUIRED COMPONENTS core imgproc calib3d
                                                cudaimgproc cudaarithm cudafilters flann)
find_package(Eigen3 3.4 REQUIRED)

file(GLOB_RECURSE OP_CPP "core/*.cpp" "calibration/*.cpp" "scanning/*.cpp")
file(GLOB_RECURSE OP_CU  "core/*.cu"  "calibration/*.cu"  "scanning/*.cu")
add_library(mod_operatorlib STATIC ${OP_CPP} ${OP_CU})

target_include_directories(mod_operatorlib PUBLIC
    ${CMAKE_CURRENT_SOURCE_DIR}
    ${OP_INC_DIRS})                 # 各算子自目录作 -I，支持跨算子 include
target_link_libraries(mod_operatorlib PUBLIC ${OpenCV_LIBS} Eigen3::Eigen)

# 导出符号控制（合并为 DLL 时启用；当前为静态库可省）
set_target_properties(mod_operatorlib PROPERTIES
    WINDOWS_EXPORT_ALL_SYMBOLS OFF
    CXX_VISIBILITY_PRESET hidden
    VISIBILITY_INLINES_HIDDEN ON)
```

### 7.10 检查优先级总结（工程集成）

| 优先级 | 问题 | 影响 | 检查方法 | 关联条款 |
|--------|------|------|----------|----------|
| 🔴 高 | 符号冲突 / 命名 | 链接失败 | dumpbin /exports | §7.2 |
| 🔴 高 | 导出符号控制 | 符号污染 | 检查导出宏 | §7.2 |
| 🔴 高 | 头文件组织 | 编译失败 | 检查 include 路径 | §7.3 |
| 🟡 中 | 依赖库版本 | 运行时错误 | 检查 CMake / 环境配置汇总.md | §7.4 |
| 🟡 中 | 链接顺序 / 循环 | 链接失败 | 检查链接器日志 | §7.4 |
| 🟡 中 | CUDA 分离 | 编译失败 | 检查 nvcc 配置 | §7.5、§2.4 |
| 🟡 中 | 内存管理 | 内存泄漏 | DrMemory / Compute Sanitizer | §7.6、§2.3、§2.6 |
| 🟢 低 | 编译选项 | 行为不一致 | 检查编译日志 | §7.7 |
| 🟢 低 | 异常处理 | 错误处理混乱 | 代码审查 | §7.7、§5.1 |
| 🟢 低 | 版本管理 | 兼容性问题 | 检查版本宏 | §7.8 |

---

## 附录 A：与「算子提炼模板」的边界

| 模板段落 | 是否进规范 | 说明 |
|---|---|---|
| A 标识、C 算法、D 依赖、D2 衔接、F 参数、G 约束、H 风险、I 状态 | ✗ 留模板 | 属"算子描述"，逐个不同 |
| E 架构（三元组、pImpl） | ✓ 进规范 §2 | 接口契约 |
| J 环境（库依赖） | ✗ 留模板（统一版本见《环境配置汇总.md》） | 工程层，非算子契约 |
| K 质量（QualityFlag 语义） | ✓ 进规范 §5 | 错误模型契约 |

> 规范 = 所有算子必须满足的契约；模板 = 满足契约后对单个算子的描述。两者互补，不重叠。

## 附录 B：合并来源说明

本文档由以下两份文档合并而成（v2.0）：

| 原文档 | 内容 | 现位置 |
|---|---|---|
| `2026-06-21-算子规范-design.md`（v1.9） | 单算子契约 §0–§6 + 附录 A | §0–§6、附录 A（原样保留） |
| `算子规范2.md`（《算子合并检查清单》） | 多算子合并工程检查 | §7 工程集成与合并规范（重叠项改为交叉引用 §2.3/§2.4/§2.6/§5.1） |

合并后两份原文档可删除，以本文档（`算子规范.md`）为唯一基准。
