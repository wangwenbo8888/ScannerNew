# 数据管理分类原则

> 一句话原则：数据按「生命周期跨度 × 共享范围」分类归属——**算子内部用完即弃的数据由算子自管（不上提）；跨阶段/跨工作流共享的公共数据由统一机制管理（不下沉）。**
>
> 本文件是归属心智模型，非代码改动基准。L1/L2 的算子级实现细则见 `算子规范.md §4`；L4 的落点见 `docs/模块功能/06-文件管理.md`（WorkflowArtifactStore 设计项）+ `docs/流水线/客户端扫描流水线.md`（ADR 7.10 数据交接）；数据怎么流见 `框架整体.md §6`（端到端追踪）。

---

## 1. 为什么需要这份原则

工程现状：对象所有权清晰（`AppContext` 用 `unique_ptr` 持有、`WorkflowContext` 注入裸指针），算子级数据流清晰（`算子规范.md §4` 三类状态模型 + 流水线 §二 逐算子衔接表），但**跨阶段/跨工作流的数据归属分散**——融合累积器归算子、逐帧 obs 归调度层、标定温度表内存常驻、编辑却直接改渲染 buffer。

本原则把"数据该由谁管"统一成可判定规则，避免新数据无主、旧数据归属漂移。

`框架整体.md §6`（2026-08-10 落地）建立了数据流的**描述层**（数据怎么流）；本原则是其**治理层**补充（数据归谁管）。两者正交。

---

## 2. 四层分类

| 层 | 名称 | 生命周期 | 共享范围 | 归属 | 管理方式 |
|---|---|---|---|---|---|
| **L1** | 算子内暂存 | 单次 Execute 内 | 不出算子 | 算子 pImpl | 算子自管：惰性 create + 跨帧复用 + `Destroy()` 释放 |
| **L2** | 算子内累积器 | 跨帧（会话级） | 算子持有，下游借引用只读 | 算子实例 | 算子持有 + 会话层 `Clear()` + 借用契约文档化 |
| **L3** | 算子间逐帧 | 单帧跨算子 | 上游 → 下游 | Result 接收方 | move / `shared_ptr<GpuMat>`，禁拷贝 |
| **L4** | 跨阶段产物 | 跨工作流 / 跨会话 | 扫描↔GBA↔后处理、标定↔扫描 | WorkflowArtifactStore | 统一注册 / 检索 / 版本（待实现） |

> **两个维度别混**：「归属」列是**持有者**（谁拥有/谁负责），「共享范围」列是**多少人用**——两者独立。典型易混点：**L2 的持有者是单个算子**（不是流水线），但共享范围是"多算子借读"；流水线/会话只负责触发 `Clear()`，不持有数据。

各层实证（截至 2026-08-11）：

- **L1 典型**：steger 的 Hessian 导数图与临时 GpuMat（`steger_extract_cuda_pimpl.h`，Warmup 预分配 + Destroy 释放）、intrinsic_calib 的临时矩阵（栈上局部，每次 Execute 重建）。从不逃出算子。
- **L2 典型**：laser_cloud_fuse_cuda 的体素哈希表（Device 常驻，`laser_cloud_fuse_cuda_pimpl.h`）、marker_cloud_fuse_cpu 的 `fusedPoints_`（Host）。
- **L3 典型**：所有 Result struct（StegerResult、EllipseFitCPUResult、LaserReconstructResult…），通过 move 或 `shared_ptr<GpuMat>` 逐级传。
- **L4 典型**：标定温度补偿表（`StereoRectifyTempTable` / `LaserPlaneMapTempTable`，`calib_result_types.h`）、GBA 逐帧 obs / 修正位姿、existingMarkers 跨会话复用集。

---

## 3. 归属判定规则（新数据决策清单）

新产生一份数据时，依次自上而下回答，先命中先用：

1. **单次 Execute 内用完即弃、不出算子？** → L1：放 pImpl 暂存，不入 Result，不写对外接口。
2. **跨帧累积、且只此算子写、下游只读？** → L2：算子实例持有，提供只读借用接口（`GetDeviceContext()` / `const&`）。
3. **喂给下一算子、但只活当前帧？** → L3：放 Result，move 出去，禁拷贝。
4. **要被不同工作流阶段或跨会话消费？** → L4：进 WorkflowArtifactStore，不留在算子或调度层临时缓冲。

**优先下沉**（L1 > L2 > L3 > L4）：能用低层解决的绝不上提，避免公共层膨胀。只有当数据天然跨阶段/跨会话时，才升入 L4。

---

## 4. L2 灰色地带的处理（关键）

L2 是唯一"算子持有、语义上公共"的混合态——是二分法（算子内部 vs 公共）的边界模糊处。

**原则：所有权留在算子（不上提），借用契约文档化。**

理由：
- 累积器与融合算法内部强耦合（体素哈希布局、SoA 显存结构），上提破坏封装；
- DeviceContext 零拷贝是有意的性能设计（fuse 算子与 normal 算子读写同一显存）；
- "统一管理"指**归属规则统一**，不指物理位置集中。

对 L2 算子的硬约束：
- 必须提供只读借用接口（`GetDeviceContext()` / `GetFusedPoints()` 等）；
- 下游**不得跨帧缓存借用句柄**——每帧重新获取（fuse `Clear()` / 析构后即悬垂）；
- `Clear()` 时机由会话层显式调用（扫描开始 / GBA 重融合前）；
- 算子文档须标注"自持久化"状态类别 + 借用生命周期（`算子规范.md §4` 已落地，遵守即可）。

可选加固（后续）：RAII 会话守卫，防漏 `Clear()`。

---

## 5. 数据管理者归属总表（framework 退役后）

以 modules 为准绳：

| 数据 | 管理者 | 机制 |
|---|---|---|
| L1 算子内暂存 | 算子 pImpl | 自管，已就位 |
| L2 算子内累积器 | 算子实例（持有）+ 会话层（触发 `Clear()`） | 自管，需加借用护栏 |
| L3 算子间逐帧 | Result 接收方（move 接力） | 自管，已就位 |
| 帧流/点云/设备状态容器 | **modules/06**（从 `framework/data` 迁入） | Sink push + Ring/Snapshot |
| Sink 契约（IFrameSink/IPointCloudSink/IDeviceStateSink） | **modules/06**（随容器迁入） | 依赖倒置 |
| 标定参数 | **modules/01** + `calib_result_types.h` 契约 | CalibStore 删除 |
| 对象所有权 / DI | **app/AppContext** | WorkflowContext 退役后由 app 接管 |
| L4 跨阶段产物 | **WorkflowArtifactStore**（归 06，待建） | 统一注册/检索/版本 |

---

## 6. framework 退役与迁移路径

framework/ 已定为过时层（见 `framework/AGENTS.md`），整体退役。骨架（组合根 + Sink/Store/Buffer 分层）sound，不动；要做的是把承重数据迁出，让 framework 能删：

1. **迁容器**：`FrameBuffer`/`PointCloudBuffer`/`DeviceStateCache` + 三个 Sink 契约，从 `framework/data` 迁入 modules/06。
2. **删 CalibStore**：无生产者（`CalibrationWorkflow` 未编译），直接删；标定走 modules/01，并统一到 `calib_result_types.h` 契约（注：当前 modules/01 用自有 `CameraCalibResult`+JSON，**尚未接线契约**，迁移时需采纳）。
3. **退役 WorkflowContext**：AppContext 改用 modules 内 DI。
4. **删 framework 整树**：前三步完成后。
5. **L4 WorkflowArtifactStore**：归升格后的 06 数据管理模块，仍等消费者（扫描/GBA/后处理接线时再建，YAGNI）。

迁移完成后，06 由 "fileio" 升格为「数据管理」统一模块（运行时容器 + Sink + 落盘 + L4 产物仓库），`docs/模块功能/06-文件管理.md` 同步更名。

---

## 7. 与现有规范的关系（不重复造轮子）

| 现有规范 | 覆盖范围 | 本原则的关系 |
|---|---|---|
| `算子规范.md §4` 三类状态模型 | L1/L2/L3 的算子级实现细则 | 本原则补"为什么这么分"的归属逻辑 |
| `框架整体.md §6` 数据流程端到端追踪 | 数据怎么流（描述层） | 本原则是其治理层（归谁管） |
| `docs/模块功能/06-文件管理.md` + `docs/流水线/客户端扫描流水线.md`（ADR 7.10） | L4 的落点设计（WorkflowArtifactStore） | 本原则明确 L4 是"公共数据统一管理"的唯一落点 |
| `docs/应用层/02-AppContext装配.md` | 对象所有权（`unique_ptr`） | 本原则管数据语义归属，AppContext 管对象所有权，两者正交 |

---

## 8. 已知偏离点（记录，本原则不解决）

下列问题超出"确立分类原则"范围，仅登记待后续专项治理：

- **编辑/融合点云冲突**：编辑模块直接改渲染 Geometry buffer，权威源却是融合算子的 `fusedPoints_`，两套点云不同步（`docs/模块功能/05-编辑.md`）。
- **DeviceContext 裸指针无护栏**：仅靠文档约定防悬垂，无代码层强制。
- **Stream 同步策略不统一**：steger 用 cudaEvent，fuse 与 reconstruct 都用 `cudaMemcpyAsync` + `cudaStreamSynchronize` 阻塞——两类策略并存，破坏异步流水线。
- **部分 Warmup 空实现**：ellipse_fit、marker_cloud_fuse_cpu、laser_cloud_normal_cuda、intrinsic_calib 未真正预分配。

> 标定数据归属已在 §6 决策（删 CalibStore，走 modules/01），不再列为偏离。
