# 数据管理分类原则

> 一句话原则：数据按「生命周期跨度 × 共享范围」分类归属——**算子内部用完即弃的数据由算子自管（不上提）；跨阶段/跨工作流共享的公共数据由统一机制管理（不下沉）。**
>
> 本文件只含**原则**（规则，timeless）。代码现状 / 修改目标 / 待增加见 [`docs/模块功能/06-文件管理.md`](docs/模块功能/06-文件管理.md)。

---

## 1.1 核心原则

数据按「生命周期跨度 × 共享范围」两个维度分类归属：

- **算子内部用完即弃的数据** → 由算子自管（不上提）；
- **跨阶段/跨工作流共享的公共数据** → 由统一机制管理（不下沉）。

目的：把"数据该由谁管"统一成可判定规则，避免新数据无主、旧数据归属漂移。

## 1.2 两个维度（别混）

| 维度 | 含义 |
|---|---|
| **持有者**（归属） | 谁拥有 / 谁负责分配、释放、提供访问 |
| **共享范围** | 多少人用 |

两者独立。典型易混点：**L2 的持有者是单个算子**（不是流水线），但共享范围是"多算子借读"——流水线/会话只负责触发 `Clear()`，不持有数据。

## 1.3 四层分类

| 层 | 名称 | 生命周期 | 共享范围 | 归属（持有者） | 管理方式 |
|---|---|---|---|---|---|
| **L1** | 算子内暂存 | 单次 Execute 内 | 不出算子 | 算子 pImpl | 算子自管：惰性 create + 跨帧复用 + `Destroy()` 释放 |
| **L2** | 算子内累积器 | 跨帧（会话级） | 算子持有，下游借引用只读 | 算子实例 | 算子持有 + 会话层 `Clear()` + 借用契约文档化 |
| **L3** | 算子间逐帧 | 单帧跨算子 | 上游 → 下游 | Result 接收方 | move / `shared_ptr<GpuMat>`，禁拷贝 |
| **L4** | 跨阶段产物 | 跨工作流 / 跨会话 | 扫描↔GBA↔后处理、标定↔扫描 | WorkflowArtifactStore | 统一注册 / 检索 / 版本（待实现） |

## 1.4 归属判定规则（新数据决策清单）

新产生一份数据时，依次自上而下回答，先命中先用：

1. **单次 Execute 内用完即弃、不出算子？** → L1：放 pImpl 暂存，不入 Result，不写对外接口。
2. **跨帧累积、且只此算子写、下游只读？** → L2：算子实例持有，提供只读借用接口（`GetDeviceContext()` / `const&`）。
3. **喂给下一算子、但只活当前帧？** → L3：放 Result，move 出去，禁拷贝。
4. **要被不同工作流阶段或跨会话消费？** → L4：进 WorkflowArtifactStore，不留在算子或调度层临时缓冲。

**优先下沉**（L1 > L2 > L3 > L4）：能用低层解决的绝不上提，避免公共层膨胀。只有当数据天然跨阶段/跨会话时，才升入 L4。

## 1.5 L2 灰色地带的处理

L2 是唯一"算子持有、语义上公共"的混合态——是二分法（算子内部 vs 公共）的边界模糊处。

**原则：所有权留在算子（不上提），借用契约文档化。**

理由：
- 累积器与融合算法内部强耦合（体素哈希布局、SoA 显存结构），上提破坏封装；
- DeviceContext 零拷贝是有意的性能设计（fuse 算子与 normal 算子读写同一显存）；
- "统一管理"指**归属规则统一**，不指物理位置集中。

对 L2 算子的硬约束：
- 必须提供只读借用接口（`GetDeviceContext()` / `GetFusedPoints()` 等）；
- 下游**不得跨帧缓存借用句柄**——每帧重新获取（fuse `Clear()` / 析构后即悬垂）；
- `Clear()` 时机由会话层显式调用（扫描开始 / GBA 重融合前）；
- 算子文档须标注"自持久化"状态类别 + 借用生命周期（`算子规范.md §4` 已落地，遵守即可）。

可选加固（后续）：RAII 会话守卫，防漏 `Clear()`。
