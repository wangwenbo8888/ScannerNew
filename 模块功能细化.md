# 双目17线激光三维扫描仪 · 各模块功能细化

> **日期**: 2026-07-12
> **目标产品**: 双目17线激光三维扫描仪
> **硬件**: 2×大恒 VEN-301-125U3M-FPC 相机 · 17线激光器 · MCU串口(COM8/115200) · Intel CPU + NVIDIA GPU
> **采集规格**: 200fps 双目同步 · 180fps 标定姿态采集
> **依据**: 框架整体.md · 标定工作流.md · 扫描流水线260629.md · 扫描流程与输入输出数据.md · 姿态判断流水线方案.md

---

## 目录

**总览**
- [要点速览](#要点速览)
- [0. 硬件参数总表](#0-硬件参数总表)

**业务模块 (模块1 ~ 模块11)**
- [模块1 · 标定工作流](#模块1--标定工作流)
- [模块2 · 扫描工作流 (双模式)](#模块2--扫描工作流-双模式)
- [模块3 · 渲染显示](#模块3--渲染显示)
- [模块4 · 后处理](#模块4--后处理)
- [模块5 · 编辑 (含撤销/重做)](#模块5--编辑-含撤销重做)
- [模块6 · 文件管理](#模块6--文件管理)
- [模块7 · 会话管理](#模块7--会话管理)
- [模块8 · 设备管理 (含许可证/激活)](#模块8--设备管理-含许可证激活)
- [模块9 · 算子库](#模块9--算子库)
- [模块10 · 可观测性/运维](#模块10--可观测性运维)
- [模块11 · 安装部署 + 首次配置](#模块11--安装部署--首次配置)

**基础设施与规划**
- [框架基础设施](#框架基础设施)
- [优先级排序 (建议实施路线)](#优先级排序-建议实施路线)

---

## 要点速览

### 模块状态一览

| 模块 | 核心职责 | 当前状态 | 优先级 |
|------|---------|---------|--------|
| 1 · 标定工作流 | 25姿态采集 → 内外参/立体矫正/激光平面映射/温度补偿 | ✅ 核心已实现 (34算子接入) | — |
| 2 · 扫描工作流 | 200fps 双链并行 (标记点配准 ∥ 激光重建) | ⚠️ 单帧已实现, 并行调度待完善 | **P0** |
| 3 · 渲染显示 | 点云 CUDA-GL 零拷贝实时渲染 | ✅ display 已实现 | P1 增强 |
| 4 · 后处理 | 离线七阶段 (GBA→重融合→法线→封装→补洞→光顺→边界) → STL | ❌ 全空桩 (GBA/融合算子已有) | P1/P2 |
| 5 · 编辑 | 选中/删除/标记/裁剪/分离 + Undo/Redo 命令栈 | ❌ 全空桩 | P2 |
| 6 · 文件管理 | PLY/STL/标定参数 IO, 工程/工件组织 | ⚠️ 仅标定 JSON 落盘 | **P0** (PLY) |
| 7 · 会话管理 | 扫描启/停/暂停/恢复, 覆盖率, 断电续扫 | ❌ 全空桩 (线程模型已有) | **P0** (启停) |
| 8 · 设备管理 | 相机/MCU/CUDA 监控 + 许可证/激活 | ✅ 硬件侧已实现 (GxCamera+ScannerMCU) | P1/P3 |
| 9 · 算子库 | 42+ 纯计算算子 + GBA, 标定/扫描双模式复用 | ✅ 完整 (ctest 42/42 绿) | — |
| 10 · 可观测性 | 统一日志/性能监控/崩溃捕获 | ❌ 仅 spdlog 基础 | P1/P3 |
| 11 · 安装部署 | 安装包 + 首次配置向导 | ❌ 全空桩 | P3 |

### 关键要点

- **产品形态**: 双目17线激光三维扫描仪, 200fps 双目同步采集, 180fps 标定姿态采集
- **硬件**: 2×大恒相机 (2048×1536, USB3) + 17线激光器 + MCU串口 (COM8/115200) + Intel CPU + NVIDIA GPU
- **标定**: 25目标姿态 (5×5网格), 11×8 棋盘格 (方格15mm), 温度补偿范围 ±10°C
- **核心架构 (扫描)**: 标记点链 (CPU/P-core, 9算子) ∥ 激光链 (GPU/CUDA stream, 4算子) 双链并行, **分支点在 ccl**
- **配准策略**: 默认 optical_flow_fuse (光流) → 降级 frame_fuse (PFH+RANSAC) → 双失败继承上一帧 R/T
- **GPU 调度**: 2 并发 slot (扫描) / 1 slot (标定); Pipeline 数 X = P-core数 + 1 预备; RingBuffer 4×X slots; 丢帧阈值 2×X
- **零拷贝渲染**: 激光点云全程 VRAM (`LaserCloudFuseDeviceContext` → `point_expand_kernel` → GL VBO)
- **产物落盘**: `CalibrationArtifacts` JSON → 供扫描/后处理加载; 扫描结果 PLY/STL
- **算子规范**: pImpl 隐藏实现 · Result 携带 QualityFlag (不抛异常) · 无跨帧状态 · 双模式复用

### 实施路线摘要

- **P0 (产品可用底线)**: 模块2并行流水线 · 模块9扫描算子接入 · 模块6 PLY导出 · 模块7启停
- **P1 (质量提升)**: 模块4 GBA+重融合 · 模块10日志+监控 · 模块3统计+覆盖率 · 模块8热插拔
- **P2 (产品完善)**: 模块4网格算子 · 模块7覆盖率+续扫 · 模块5编辑 · 模块6工程管理
- **P3 (出厂级)**: 模块10崩溃捕获 · 模块8许可证 · 模块11安装向导 · 权限鉴权 · 多语言

---

## 0. 硬件参数总表

| 参数 | 值 | 来源 |
|------|-----|------|
| 相机 | 2× Daheng VEN-301-125U3M-FPC (2048×1536, USB3) | 实测 |
| 激光线数 | 17线 | 产品定义 |
| 采集帧率 | 200fps (扫描) / 180fps (标定, 含N管激光帧) | 扫描流水线文档 |
| 标定姿态数 | 25个目标姿态 (5×5网格) | 姿态判断文档 |
| 标定板 | 11×8 棋盘格, 方格15mm, 圆形标记点 | CalibrationParams |
| MCU串口 | COM8, 115200 baud, 命令格式 `N10 H{freq} B{bgLight} T{trigger} V{version} L{laserLight};` | 实测 |
| CPU | Intel X核P核 + Y核E核 (示例8+8) | 流水线文档 |
| GPU | NVIDIA, 2并发slot (扫描) / 1 slot (标定) | 流水线文档 |
| 温度传感器 | MCU读取, 参考温度25°C, 补偿范围±10°C | 标定工作流 |

---

## 模块1 · 标定工作流

### 职责
25姿态采集 → 解算相机内外参/立体矫正/激光平面映射表/温度补偿表，产出可复用标定参数。

### 当前状态: ✅ 核心已实现 (34算子全接入)

### 子功能细化

#### 1.1 姿态引导采集 (Stage 0/1/2)
- **硬件初始化**: 相机枚举/打开/增益曝光设置, MCU串口连接, 温度读取
- **采集周期**: 每周期 = 1帧标记点图像 + N帧激光管图像 + 温度值
- **姿态判断流水线** (速度优先, 不丢帧):
  - GPU: 2-1 mask_extract → 2-2 ccl (轻量, ~1-2ms, 1个GPU slot)
  - CPU: 2-3~2-11 标记点链 (image_split → zernike_edge → image_merge → undistort → ellipse_fit → marker_match → epipolar_intersect → edge_match → point_reconstruct)
  - 2-12 frame_fuse: 首帧建参考集, 后续帧PFH+RANSAC刚体配准
  - 2-13 pose_estimate: 比对25目标姿态, 原子CAS标记已采集, 保存完整采集周期
- **终止条件**: 25姿态全部采集完毕
- **背压策略**: 环形缓冲区满时阻塞采集线程, 不丢帧

#### 1.2 相机标定 (Stage 3)
- **3-1 inverse_distort**: 2-7椭圆中心逆畸变变换
- **3-2 intrinsic_calib**: 双目内参标定 (焦距/主点/畸变系数)
- **5-1 intrinsic_compensate**: 内参温度补偿
- **3-3 extrinsic_calib**: 双目外参标定 (R/T旋转平移矩阵)
- **5-2 extrinsic_compensate**: 外参温度补偿
- **3-4 stereo_rectify**: 立体矫正 (R1/R2/P1/P2/Q)
- **3-5 stereo_rectify_temp_table**: 温度补偿立体矫正参数表

#### 1.3 激光器标定 (Stage 4)
- **4-1~4-8 激光前端链路**: mask_separation → steger → undistort_cuda → epipolar_interp → laser_match → laser_reconstruct
- **4-9 endpoint_extract**: 激光线3D端点提取
- **4-10 virtual_camera_pose**: 虚拟相机光心求解
- **4-11 pose_optimize**: 虚拟相机外参优化 (Ceres非线性优化)
- **5-3 laser_extrinsic_compensate**: 激光外参温度补偿
- **4-12 plane_map**: 激光平面映射表 (17线各自平面方程)
- **4-13 plane_map_temp_table**: 温度补偿平面映射查找表

#### 1.4 产物存储
```
标定产物 (CalibrationArtifacts):
├── 内参: cameraMatrixL/R, distCoeffsL/R, intrinsicRms
├── 外参: R, T, E, F, stereoReprojError
├── 立体矫正: R1, R2, P1, P2, Q, validRoiL/R
├── 激光: virtualK/R/T, laserReprojError, plane_map (17线)
├── 温度表: intrinsicTempCoeff, extrinsicTempCoeff, laserTempCoeff
└── JSON落盘 → 供扫描/后处理加载
```

### 接口
```cpp
// 分步执行 (GUI三个独立按钮)
bool executeCameraCalib();  // 步骤1
bool executeLaserCalib();   // 步骤2 (前置: 步骤1)
bool executeTempCalib();    // 步骤3 (前置: 步骤1+2)
bool saveArtifacts(dir);    // JSON落盘
bool loadArtifacts(path);   // 加载已有标定
```

### 依赖
- 模块8 (GxCamera/ScannerMCU 硬件控制)
- 模块9 (全部34+标定算子)
- 模块6 (标定参数JSON IO)

### 待完善
- [ ] 真实标定板图像的mask_separation参数调优
- [ ] 姿态引导并行流水线 (当前串行, 需按姿态判断文档实现E-core编排)
- [ ] PrevFrameState原子快照 (optical_flow_fuse依赖)

---

## 模块2 · 扫描工作流 (双模式)

### 职责
实时扫描180-200fps: 标记点链(配准求R/T) ∥ 激光链(三维重建) 双链并行。

### 当前状态: ✅ 单帧流程已实现, ⚠️ 并行调度待完善

### 子功能细化

#### 2.1 预处理 (GPU)
- **mask_separation**: 激光/标记点掩膜分离 → `d_markingPointMask` + `d_laserMask`
- **ccl (连通域分析)**: 标记点区域标签 → **分支点**, 之后双链并行

#### 2.2 标记点链 (CPU, P-core)
```
03 image_split → 04 zernike_edge → 05 image_merge → 06 undistort_cpu
→ 07 ellipse_fit → 08 marker_match → 09 epipolar_intersect
→ 10 edge_match → 11 point_reconstruct
```
- 输出: 标记点3D坐标 + 法线 + 半径

#### 2.3 激光线链 (GPU, CUDA stream)
```
04 steger(Flat模式) → 05 undistort_cuda → 06 epipolar_interp
→ 07 laser_match_scan(全局查找) → 08 laser_reconstruct
```
- **17线**: laser_match_scan 通过温度补偿平面映射表全局查找确定line_id
- 输出: 激光3D点云 `d_points3d`

#### 2.4 单帧配准
- **默认: optical_flow_fuse** (光流配准, 需PrevFrameState)
- **降级: frame_fuse** (PFH特征配准)
- **双失败: 继承上一帧R/T**
- 输出: R(3×3) + T(3×1)

#### 2.5 并行流水线调度 (扫描流水线260629.md)
```
P0采集线程 ─200fps─► RingBuffer (双目同帧单元)
                        │
    ┌───────────────────▼──────────────────────────────┐
    │ Pipeline 0..X-1 (每个 = 1专用E-core + 1动态P-core + 共享GPU 2 slots) │
    │                                                     │
    │ E-core编排:                                          │
    │   ② GPU全链: mask_sep → ccl → laser 04~08 (串行)    │
    │   ③ ccl完成后双链并行:                                │
    │      GPU: 激光链继续                                  │
    │      P-core: 标记点链 03~11 + 配准                    │
    │   ④ 合并发布: FrameResult → 输出队列                  │
    │                                                     │
    │ Pipeline X (第+1): 预备态, P-core释放即接管           │
    └─────────────────────────┬──────────────────────────┘
                              ▼
    FrameResult输出队列 (lock-free MPMC)
    { frameId, R, T, markerPoints3D, laserPoints3D, temperature }
```

**关键参数**:
- GPU slot数: 2 (每slot半SM)
- Pipeline数: X = (X-1) + 1 (P-core数 + 1预备)
- RingBuffer slots: 4×X
- 丢帧阈值: 2×X (超过则跳到最新帧)
- PrevFrameState: `atomic<shared_ptr>` 最新快照模型

**纯标记点模式**: 关闭激光链, 仅标记点链 + 配准 (用于无激光场景)

### 接口
```cpp
// 单帧扫描
bool scanFrame(const Frame& input, FrameResult& output);
// 并行扫描
void startParallelScan(config);
void stopParallelScan();
// 输出回调
void setResultCallback(std::function<void(FrameResult&)>);
```

### 依赖
- 模块8 (相机采集 + MCU激光/补光控制)
- 模块9 (mask_separation + 标记点链 + 激光链 + 配准算子)
- 模块1 (标定参数: R1/P1/Q, plane_map, 温度表)

### 待完善
- [ ] P-core Broker动态租借 (当前ParallelScanPipeline为简化版)
- [ ] GpuService 2-slot信号量
- [ ] PrevFrameState原子快照
- [ ] moodycamel::ConcurrentQueue输出队列
- [ ] CPU拓扑检测 + 线程亲和性绑定

---

## 模块3 · 渲染显示

### 职责
点云实时3D渲染 (CUDA-GL零拷贝), 标记点/激光点云viewer。

### 当前状态: ✅ display/已实现

### 子功能细化

#### 3.1 激光点云渲染器 (CUDA-GL interop)
- **laser_cloud_renderer**: 直接读VRAM中`LaserCloudFuseDeviceContext.d_fusedXyz` + `d_fusedNormal`
- **point_expand_kernel**: CUDA展点kernel → GL VBO → glDraw
- **零拷贝**: 点云数据全程在GPU显存, 不回传CPU

#### 3.2 标记点渲染器 (CPU展点 + VBO)
- **marker_cloud_renderer**: 读`vector<MarkerCloudPoint>` → OSG VBO展点四边形 → glDraw
- 用途: 显示配准标记点位置, 辅助定位

#### 3.3 扫描查看器 (OSG场景图 + Shader)
- **scanner_viewer**: OSG窗口管理 + 场景图 + 着色器
- 功能: 旋转/平移/缩放/框选 + 实时预览扫描结果
- 坐标系: 世界坐标(标定板原点为参考)

#### 3.4 UI集成
- **OsgWidget**: QWidget内嵌OSG图形窗口 (已实现)
- 与jeamui主窗口集成 (已实现)
- 标定预览图像显示 (QImage Format_Indexed8 + 256灰度表)

### 数据来源
```
扫描输出 → 点云融合 → LaserCloudFuseDeviceContext(VRAM) → laser_cloud_renderer → 屏幕
                    → vector<MarkerCloudPoint>(CPU)      → marker_cloud_renderer → 屏幕
```

### 依赖
- 模块9 (laser_cloud_fuse_cuda + laser_cloud_normal_cuda 产出的DeviceContext)
- OSG 3.6.5 + CUDA-GL interop

### 待完善
- [ ] 实时帧率/点数统计显示
- [ ] 扫描覆盖率可视化 (已扫描区域着色)
- [ ] 多色彩渲染模式 (高度着色/法线着色/质量着色)

---

## 模块4 · 后处理

### 职责
离线batch七阶段: 全局标记点优化 → 重融合 → 法线计算 → 封装 → 补洞 → 光顺 → 边界优化, 出STL。

### 当前状态: ❌ 全空桩

### 子功能细化

#### 4.1 全局标记点优化 (GBA)
- **global_ba_cpu** (已实现, Ceres): 全局Bundle Adjustment
  - 输入: 所有帧的标记点3D + R/T
  - 优化: 联合优化所有相机位姿 + 标记点3D位置
  - 约束: Ceres稀疏求解, pose_graph_residual + ba_residuals
  - 输出: 优化后的全局位姿图

#### 4.2 重融合
- 用优化后的R/T重新跑点云融合
- **laser_cloud_fuse_cuda**: 体素哈希融合 (已有)
- **marker_cloud_fuse_cpu**: 标记点融合 (已有)
- 优势: 离线可多帧累积, 比实时融合更精确

#### 4.3 法线计算
- **laser_cloud_normal_cuda**: 协方差 + Jacobi特征值分解 (已有)
- 输出: 每个融合点的法线向量

#### 4.4 封装 (Poisson/Screenscreen)
- 泊松表面重建或屏幕空间封装
- 输入: 融合点云 + 法线
- 输出: 三角网格
- ⚠️ 需新建网格重建算子

#### 4.5 补洞
- 网格空洞检测 + 填充
- ⚠️ 需新建网格处理算子

#### 4.6 光顺
- Laplacian/Humphrey光顺滤波
- ⚠️ 需新建网格平滑算子

#### 4.7 边界优化
- 边界检测 + 精修
- ⚠️ 需新建算子

#### 4.8 输出
- STL格式导出 (供3D打印/CAD)
- PLY格式导出 (含法线/颜色)

### 优先级
- **高**: GBA + 重融合 (已有算子, 需编排)
- **中**: 法线计算 (已有算子)
- **低**: 封装/补洞/光顺/边界 (需新建网格算子)

### 依赖
- 模块9 (GBA + 融合 + 法线算子)
- 模块2 (扫描原始帧数据)
- 模块6 (STL/PLY文件IO)

---

## 模块5 · 编辑 (含撤销/重做)

### 职责
扫描时选中/标记/删除点云或标记点; 命令栈支持undo/redo + 操作历史可追溯。

### 当前状态: ❌ 全空桩

### 子功能细化

#### 5.1 点云编辑操作
- **选中**: 框选/套索/球选 点云区域
- **删除**: 删除选中点
- **标记**: 给选中点添加类别标签
- **裁剪**: 按平面/框/曲面裁剪
- **分离**: 将选中区域分离为独立对象

#### 5.2 命令栈 (Undo/Redo)
```cpp
class EditCommand {
    virtual void execute() = 0;
    virtual void undo() = 0;
    virtual void redo() = 0;
};
class EditCommandStack {
    std::vector<std::unique_ptr<EditCommand>> undoStack_;
    std::vector<std::unique_ptr<EditCommand>> redoStack_;
    void push(EditCommand*);  // execute + push to undo
    void undo();
    void redo();
};
```

#### 5.3 操作历史
- 记录每次编辑操作的元数据 (时间/类型/影响点数)
- 可导出为审计日志

#### 5.4 UI交互
- 工具栏: 选择/删除/标记/裁剪/分离 按钮
- 快捷键: Ctrl+Z(撤销) / Ctrl+Y(重做)
- 右键菜单: 上下文操作

### 依赖
- 模块3 (OSG拾取/框选交互)
- 模块9 (点云数据结构)

### 优先级
- **低** (产品可用后增加)

---

## 模块6 · 文件管理

### 职责
PLY/STL/标定参数导入导出, 工程/工件文件组织。

### 当前状态: ⚠️ 仅有标定参数JSON落盘

### 子功能细化

#### 6.1 点云文件IO
- **PLY**: ASCII/二进制, 支持xyz/normal/rgb (优先实现)
- **STL**: ASCII/二进制三角网格
- **PCD**: PCL兼容格式 (可选)
- **LAS**: 测绘格式 (可选)

#### 6.2 标定参数IO
- **JSON**: 内参/外参/矫正/plane_map/温度表 (已部分实现)
- **加载/保存**: `CalibrationArtifacts` 序列化/反序列化

#### 6.3 工程/工件管理
```
工程目录结构:
project/
├── project.jmw                    ← 工程配置 (JSON)
├── calibration/
│   ├── calibration.json           ← 标定参数
│   └── calibration_images/        ← 标定原始图像
├── scans/
│   ├── scan_001/                  ← 单次扫描
│   │   ├── scan_001.ply           ← 点云
│   │   ├── scan_001.stl           ← 网格 (后处理后)
│   │   ├── scan_001.meta.json     ← 元数据 (时间/参数/温度)
│   │   └── raw_frames/            ← 原始帧 (可选)
│   └── scan_002/
└── exports/                       ← 导出文件
```

#### 6.4 WorkflowArtifactStore
- 工作流产物统一存储接口
- 标定/扫描/后处理产物的注册/检索/版本管理

### 依赖
- 模块1 (标定参数序列化)
- 模块2/4 (点云/网格导出)
- 模块10 (操作日志)

### 优先级
- **高**: PLY导出 (扫描结果保存基本需求)
- **中**: 工程文件管理
- **低**: 版本管理

---

## 模块7 · 会话管理

### 职责
扫描启/停/暂停/恢复, 覆盖率统计, 断电续扫, 会话元数据。

### 当前状态: ❌ 全空桩 (Pipeline/Stage线程模型已有, 但无完整状态机)

### 子功能细化

#### 7.1 扫描状态机
```
                ┌──────────┐
            ───►│ Idle     │
           /    └────┬─────┘
  start() /         │ startScan()
        /           ▼
┌──────────┐  ┌──────────┐  pause()  ┌──────────┐
│ Idle     │  │ Scanning │──────────►│ Paused   │
└──────────┘  └────┬─────┘           └────┬─────┘
                   │ stop()               │ resume()
                   ▼                      ▼
              ┌──────────┐           ┌──────────┐
              │ Stopping │           │ Scanning │
              └────┬─────┘           └──────────┘
                   │
                   ▼
              ┌──────────┐
              │ Idle     │
              └──────────┘
```

#### 7.2 会话生命周期
```cpp
struct ScanSession {
    std::string sessionId;         // UUID
    std::string startTime;         // ISO8601
    std::string projectId;
    ScanConfig config;             // 采集参数
    ScanState state;               // 当前状态
    ScanStats stats;               // 统计
    std::string calibrationPath;   // 关联标定
    std::vector<FrameResult> frames; // 帧结果
};
```

#### 7.3 覆盖率统计
- 实时计算已扫描区域占总体积的比例
- 基于体素哈希: 已填充体素数 / 预估总体素数
- 可视化: 已扫描区域绿色, 未扫描区域灰色

#### 7.4 断电续扫
- 会话状态定期持久化 (每N帧)
- 启动时检测未完成会话 → 提示恢复
- 恢复: 加载已采集帧 + 继续采集

#### 7.5 故障处理
- 相机断连 → 自动暂停 + 告警
- MCU通信失败 → 告警 + 停止激光
- GPU OOM → 降级处理

### 依赖
- 模块2 (扫描控制)
- 模块8 (设备状态监控)
- 模块10 (日志/监控)

### 优先级
- **高**: 启/停/暂停/恢复 (基本扫描控制)
- **中**: 覆盖率统计
- **低**: 断电续扫

---

## 模块8 · 设备管理 (含许可证/激活)

### 职责
下位机/相机/CUDA/平台状态监控与设备控制; 含设备绑定许可证/激活。

### 当前状态: ✅ 硬件侧已实现 (GxCamera + ScannerMCU + MockCamera)

### 子功能细化

#### 8.1 相机管理
- **ICamera接口**: enumerate/open/close/startStream/stopStream/setExposure/setGain/captureFrame
- **GxCamera** (大恒实现):
  - 枚举: `IGXFactory::GetInstance().UpdateDeviceList()` (需先Init)
  - 双目同步: 硬件触发模式
  - 帧回调: `FramePairCollector` 按到达顺序配对L/R帧
  - 参数: 曝光/增益/ROI/像素格式(Mono8)
- **MockCamera**: 测试用, 生成合成图像

#### 8.2 MCU串口控制
- **ScannerMCU** (COM8, 115200):
  - 命令格式: `N10 H{freq} B{bgLight} T{trigger} V{version} L{laserLight};`
  - 补光灯亮度: `B0`~`B255`
  - 激光亮度: `L0`~`L255`
  - 触发模式: 单次/连续/外触发
  - 温度读取
  - 帧率设置

#### 8.3 设备状态监控
- **温度**: MCU实时读取, 供温度补偿用
- **帧率**: 相机实际帧率统计
- **按键**: MCU扫描仪按钮状态 (开始/停止/暂停)
- **故障**: USB断连/串口超时/温度异常

#### 8.4 许可证/激活 (出厂级)
- 加密狗/软授权检查
- 试用期管理
- 功能等级控制 (基础/专业版)
- ⚠️ 需新建

### 依赖
- 大恒Galaxy SDK
- Windows serial API

### 待完善
- [ ] 设备热插拔检测 (USB相机断连重连)
- [ ] 许可证系统
- [ ] 上电自检流程

---

## 模块9 · 算子库

### 职责
纯计算叶子, 42+算子 + GBA; 标定/扫描双模式复用。

### 当前状态: ✅ 完整 (42算子全实现, ctest 42/42绿)

### 算子分类

#### 9.1 核心视觉算子 (core/vision, 2个)
| 算子 | 平台 | 用途 |
|------|------|------|
| mask_extract | CUDA | 掩膜区域提取 (标定板标记点) |
| ccl (region_analyze) | CUDA | 连通域分析 (标记点区域标签) |

#### 9.2 核心标记点算子 (core/marker, 12个)
| 算子 | 平台 | 用途 |
|------|------|------|
| image_split | CPU | 标记点图像分割 (按ROI裁剪) |
| zernike_edge | CPU | Zernike椭圆边缘亚像素提取 |
| image_merge | CPU | 左右边缘点集合并 |
| undistort_cpu | CPU | 双目去畸变矫正 |
| ellipse_fit | CPU | 椭圆拟合中心提取 |
| marker_match | CPU | 标记点双目匹配 |
| epipolar_intersect | CPU | 椭圆边界极线交点 |
| edge_match | CPU | 椭圆边缘点双目匹配 |
| point_reconstruct | CPU | 标记点三维重建 |
| frame_fuse | CPU | 单帧PFH+RANSAC配准 |
| optical_flow_fuse | CPU | 标记点光流配准 (默认) |
| marker_cloud_fuse_cpu | CPU | 标记点体素哈希融合 |

#### 9.3 核心激光算子 (core/laser, 4个)
| 算子 | 平台 | 用途 |
|------|------|------|
| steger | CUDA | Steger激光中心亚像素提取 |
| undistort_cuda | CUDA | 激光点去畸变矫正 |
| epipolar_interp | CUDA | 极线插值 |
| laser_reconstruct | CUDA | 激光线三维重建 |

#### 9.4 标定算子 (calibration/, 16个)
| 算子 | 平台 | 用途 |
|------|------|------|
| inverse_distort | CPU | 逆畸变变换 |
| intrinsic_calib | CPU | 双目内参标定 |
| extrinsic_calib | CPU | 双目外参标定 |
| stereo_rectify | CPU | 立体矫正 |
| stereo_rectify_temp_table | CPU | 温度补偿立体矫正表 |
| intrinsic_compensate | CPU | 内参温度补偿 |
| extrinsic_compensate | CPU | 外参温度补偿 |
| laser_extrinsic_compensate | CPU | 激光外参温度补偿 |
| virtual_camera_pose | CUDA | 虚拟相机光心求解 |
| pose_optimize | CUDA | 虚拟相机外参优化 |
| endpoint_extract | CUDA | 激光线3D端点提取 |
| laser_label | CUDA | 激光线编号 |
| laser_match | CUDA | 激光线匹配 |
| plane_map | CUDA | 激光平面映射表 |
| plane_map_temp_table | — | 温度补偿平面映射查找表 |
| pose_estimate | CPU | 设备姿态估计 (25目标比对) |

#### 9.5 扫描算子 (scanning/, 6个)
| 算子 | 平台 | 用途 |
|------|------|------|
| mask_separation | CUDA | 激光/标记点掩膜分离 |
| laser_match_scan | CUDA | 激光线匹配(扫描模式, 全局查找) |
| laser_cloud_fuse | CPU | 激光点云体素哈希融合 (CPU版) |
| laser_cloud_fuse_cuda | CUDA | 激光点云体素哈希融合 (CUDA版) |
| laser_cloud_normal | CPU | 激光点云法线估计 (CPU版) |
| laser_cloud_normal_cuda | CUDA | 激光点云法线估计 (CUDA版) |

#### 9.6 全局优化 (global_optim)
| 算子 | 平台 | 用途 |
|------|------|------|
| global_ba_cpu | CPU/Ceres | 全局Bundle Adjustment |

### 设计规范
- **pImpl模式**: 所有算子使用pImpl隐藏实现
- **Result携带status**: 不抛异常, 返回QualityFlag (Normal/Degraded/Warning/Error)
- **无跨帧状态**: 算子为纯计算叶子 (融合哈希归Data层)
- **双模式复用**: 标定/扫描共用同一算子实例

### 依赖
- OpenCV 4.13 (core/imgproc/calib3d/cuda*)
- Eigen 3.4
- CUDA 12.6 (sm_75/86/87)
- Ceres 2.2.0 (仅GBA)

---

## 模块10 · 可观测性/运维

### 职责
统一日志(配置/轮转/分级), 性能监控(延迟/帧率/计数器dashboard), 崩溃捕获(minidump/crash)。

### 当前状态: ❌ 仅spdlog基础

### 子功能细化

#### 10.1 统一日志
- **spdlog升级**: 配置文件驱动 (级别/格式/轮转/多sink)
- **日志分级**: TRACE/DEBUG/INFO/WARN/ERROR/FATAL
- **多sink**: 控制台(彩色) + 文件(轮转 10MB×5) + 可选远程syslog
- **分类标签**: `[CALIB]` `[SCAN]` `[GPU]` `[HAL]` `[SESSION]`
- **CALIB_LOG_*宏**: 已有, 需扩展到所有模块

#### 10.2 性能监控
```cpp
struct PerfCounters {
    // 采集
    std::atomic<uint64_t> captureFps{0};        // 实际采集帧率
    std::atomic<uint64_t> dropCount{0};         // 丢帧数
    // 处理
    std::atomic<uint64_t> processFps{0};        // 处理帧率
    std::atomic<uint64_t> gpuLatencyMs{0};      // GPU链延迟
    std::atomic<uint64_t> cpuLatencyMs{0};      // CPU链延迟
    std::atomic<uint64_t> totalLatencyMs{0};    // 端到端延迟
    // 点云
    std::atomic<uint64_t> markerPointCount{0};  // 标记点数
    std::atomic<uint64_t> laserPointCount{0};   // 激光点数
    std::atomic<uint64_t> fusedPointCount{0};   // 融合后点数
    // 设备
    std::atomic<double> temperature{0};         // 当前温度
    std::atomic<uint64_t> gpuMemoryUsed{0};     // GPU显存使用
};
```
- **Dashboard**: 实时折线图 (帧率/延迟/温度)
- **告警**: 帧率低于阈值/温度过高/GPU OOM

#### 10.3 崩溃捕获
- **Windows**: `SetUnhandledExceptionFilter` + minidump (`MiniDumpWriteDump`)
- **自动重启**: 崩溃后可选自动重启 + 恢复会话
- **崩溃报告**: minidump + 日志 + 系统信息打包上传

### 依赖
- spdlog (已有)
- Windows DbgHelp (minidump)

### 优先级
- **高**: 统一日志配置 (调试必需)
- **中**: 性能监控 (调优必需)
- **低**: 崩溃捕获 (出厂级)

---

## 模块11 · 安装部署 + 首次配置

### 职责
安装包/卸载, 首运行向导(连接设备/选语言/初始标定引导)。

### 当前状态: ❌ 全空桩

### 子功能细化

#### 11.1 安装包
- **NSIS/Inno Setup**: Windows安装包生成
- **依赖打包**: 大恒SDK运行时 + CUDA运行时 + VC++红istribution + OSG + Qt
- **桌面快捷方式**: jeamui.exe
- **关联文件**: .jmw工程文件关联

#### 11.2 首次配置向导
```
步骤1: 语言选择 (中文/English)
步骤2: 设备连接检测 (相机枚举 + MCU通信 + GPU检测)
步骤3: 初始标定引导 (25姿态采集引导)
步骤4: 许可证激活 (加密狗/序列号)
步骤5: 完成 → 进入主界面
```

#### 11.3 卸载
- 清理用户数据 (可选保留)
- 注销许可证

### 优先级
- **低** (开发阶段用CMake部署, 出厂前实现)

---

## 框架基础设施

### Scheduler + Watchdog
- **当前**: 仅空类桩
- **Scheduler**: 线程池/CPU亲和/CUDA Stream管理
- **Watchdog**: Stage心跳检测 + 超时告警 + 自动恢复
- **ThreadAffinity**: Windows `SetThreadAffinityMask`抽象接口

### GpuRuntime
- **当前**: 仅空类桩
- **ICudaContext**: CUDA上下文管理 (多设备/多上下文)
- **ResourceLifecycleManager**: GPU显存生命周期 (算子间显存复用)

### EventBus
- **当前**: 仅空类桩
- 控制通知通道 (不携带载荷)
- 事件: SCAN_STARTED/STOPPED/PAUSED/RESUMED, CALIB_PROGRESS, DEVICE_CONNECTED/DISCONNECTED, FAULT

### 用户权限/鉴权
- **当前**: 未开始
- 角色: 管理员/操作员/访客
- 操作授权: 标定(管理员) / 扫描(操作员) / 导出(操作员) / 系统设置(管理员)

---

## 优先级排序 (建议实施路线)

### P0 — 产品可用最低要求
1. **模块2**: 并行扫描流水线 (P-core Broker + GpuService 2-slot + RingBuffer + 输出队列)
2. **模块9**: 扫描算子接入 (mask_separation + 激光链 + 标记点链 + 配准 + 融合)
3. **模块6**: PLY导出 (扫描结果保存)
4. **模块7**: 扫描启/停/暂停/恢复

### P1 — 产品质量提升
5. **模块4**: GBA + 重融合 (已有算子, 需编排)
6. **模块10**: 统一日志 + 性能监控
7. **模块3**: 实时帧率/点数显示 + 覆盖率可视化
8. **模块8**: 设备热插拔 + 故障处理

### P2 — 产品完善
9. **模块4**: 封装/补洞/光顺 (新建网格算子)
10. **模块7**: 覆盖率统计 + 断电续扫
11. **模块5**: 点云编辑 + Undo/Redo
12. **模块6**: 工程文件管理

### P3 — 出厂级
13. **模块10**: 崩溃捕获 + minidump
14. **模块8**: 许可证/激活系统
15. **模块11**: 安装包 + 首次配置向导
16. 用户权限/鉴权
17. 多语言支持
