# 下位机协议 260831 对齐（08 核心层）实施计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 将 modules/08_devicemgmt 串口协议从旧固件口径（N10–N16/$T/$K/$S/$A/ACK/回显）硬切到 260831 新协议（N10 七参/N11 H0/N12 T/N13 S ＋ G01/G02/G03 裸文本）。

**Architecture:** 按《协议260831对齐-08核心层-design》（同目录）§9 分批：批1 原子换代（编译耦合最强）→ 批2 可靠性机制物理删除 → 批3 参数域＋扫描模式扩展 → 批4 深编排＋故障对表 → 批5 回归＋文档。每批编译绿＋ctest 全绿后才进下一批。

**Tech Stack:** C++17 / CMake / GoogleTest（ctest 注册名＝测试文件名）/ Qt5 SerialPort / spdlog。

**配套文档（执行前必读）：**
- 设计＋已裁决决策 D1–D9：`docs/plans/2026-09-09-协议260831对齐-08核心层-design.md`
- 逐行取证清单（行号参考）：`docs/开发需要的信息/下位机和按键/下位机协议260831工程适配清单.md`
- 协议原文：`docs/开发需要的信息/下位机和按键/下位机通讯协议说明.md`

**构建/测试命令（Debug，AGENTS.md §8.5）：**
```powershell
cmake --build build --config Debug
$env:PATH = 'C:\opencv-cuda-4.13.0-debug\x64\vc17\bin;' + $env:PATH
ctest --test-dir build -C Debug --output-on-failure            # 全量
ctest --test-dir build -C Debug -R test_mcu_driver --output-on-failure   # 单测过滤
```
> 注意：`build/` 为 Debug-only（MSB8013 陷阱）；不要跑 Release。

**提交纪律：** 每任务一提交，格式对齐仓库惯例（中文 body，`feat(08):`/`refactor(08):` 前缀）。提交前 `git status --short` 核对只含本任务文件。**不得提交 build/ 产物**。

**已知欠账（批1 一并清算）：** 2026-09-05 命令空格化后 test_mcu_driver.cpp（08-21 断言）与 test_device_manager.cpp MockMcu 子串匹配已脱节——凡触及即按新口径重写，不留旧断言。

---

## 批 1（原子）：协议层换代＋按键直喂

> ⚠ 本批任务 1–7 编译耦合（FrameCodec::Version/RawKeyEvent/IMCU 方法集被交叉引用），**全部完成后一次性构建验证＋提交**。任务间不单独构建。

### Task 1: FrameCodec 收敛为裸 `;` 分帧

**Files:**
- Modify: `modules/08_devicemgmt/serial/FrameCodec.h`（整文件重写）
- Modify: `modules/08_devicemgmt/serial/FrameCodec.cpp`（整文件重写）
- Modify: `modules/08_devicemgmt/tests/test_frame_codec.cpp`（重写）

**Step 1: 重写 FrameCodec.h**

```cpp
#pragma once
// ============================================================================
// FrameCodec.h — 裸文本成拆帧器（协议 260831 定稿口径）
// 帧 = "载荷;"——ASCII、';' 结尾分帧、无校验无 seq（协议说明.md §一）。
// 载荷允许空格（"N10 H30 B60 ..." / "G02 A25.3 B25.4 ..."）。
// 半帧超时 50ms（advanceTimeout 推进——防御截断滞留，rx 线程驱动）。
// ============================================================================
#include <cstdint>
#include <string>
#include <vector>

namespace Scanner::device::serial {

class FrameCodec {
public:
    FrameCodec() = default;

    struct Frame { std::string payload; };        // 拆帧产物

    // 喂字节流（串口rx线程调）：内部累积+分帧，完整帧追加到 out
    void feed(const std::string& bytes, std::vector<Frame>& out);
    // 逻辑时钟推进（半帧超时）：超时丢弃挂起缓冲并复位（返回是否发生丢弃）
    // 线程契约：须与 feed 同线程调用（挂起缓冲无锁）
    bool advanceTimeout(int64_t ms);
    // 组帧：payload + ";"（载荷内 ';' 由调用方口径保证不出现；出现即直接追加，
    // 固件侧自然拒收——不做二次防御）
    std::string encode(const std::string& payload) const;

private:
    std::string pending_;     // 挂起缓冲（未收到 ';'）
    int64_t pendingAgeMs_ = 0;
};

} // namespace Scanner::device::serial
```

**Step 2: 重写 FrameCodec.cpp**

```cpp
// ============================================================================
// FrameCodec.cpp — 裸 ';' 分帧实现（契约见 FrameCodec.h）
// ============================================================================
#include "FrameCodec.h"

namespace Scanner::device::serial {

namespace {
constexpr int64_t kHalfTimeoutMs = 50;
}

void FrameCodec::feed(const std::string& bytes, std::vector<Frame>& out) {
    for (char ch : bytes) {
        if (ch == ';') {
            if (!pending_.empty()) out.push_back(Frame{std::move(pending_)});
            pending_.clear();
            pendingAgeMs_ = 0;
        } else {
            if (pending_.empty()) pendingAgeMs_ = 0;
            pending_.push_back(ch);
        }
    }
}

bool FrameCodec::advanceTimeout(int64_t ms) {
    if (pending_.empty()) return false;
    pendingAgeMs_ += ms;
    if (pendingAgeMs_ < kHalfTimeoutMs) return false;
    pending_.clear();
    pendingAgeMs_ = 0;
    return true;
}

std::string FrameCodec::encode(const std::string& payload) const { return payload + ";"; }

} // namespace Scanner::device::serial
```

**Step 3: 重写 test_frame_codec.cpp**（保留原 v2 `;` 分帧 4 例的语义＋空格载荷＋半帧超时；删全部 v3/CRC/seq 用例）

```cpp
// test_frame_codec.cpp — 裸 ';' 分帧：分帧/粘帧/分段/空帧/空格载荷/半帧超时/encode
#include "serial/FrameCodec.h"
#include <gtest/gtest.h>

namespace sd = Scanner::device::serial;

TEST(FrameCodec, SplitsOnSemicolon) {
    sd::FrameCodec c;
    std::vector<sd::FrameCodec::Frame> out;
    c.feed("G01 U1;G03 S500;", out);
    ASSERT_EQ(out.size(), 2u);
    EXPECT_EQ(out[0].payload, "G01 U1");
    EXPECT_EQ(out[1].payload, "G03 S500");
}

TEST(FrameCodec, SpacePayloadKept) {                     // 新协议载荷含空格
    sd::FrameCodec c;
    std::vector<sd::FrameCodec::Frame> out;
    c.feed("G02 A25.3 B25.4 C26.0 D24.5;", out);
    ASSERT_EQ(out.size(), 1u);
    EXPECT_EQ(out[0].payload, "G02 A25.3 B25.4 C26.0 D24.5");
}

TEST(FrameCodec, SegmentedFeedAccumulates) {             // 分段到达
    sd::FrameCodec c;
    std::vector<sd::FrameCodec::Frame> out;
    c.feed("N10 ", out);
    c.feed("H30", out);
    ASSERT_TRUE(out.empty());
    c.feed(" B60;", out);
    ASSERT_EQ(out.size(), 1u);
    EXPECT_EQ(out[0].payload, "N10 H30 B60");
}

TEST(FrameCodec, LeadingGarbageBeforeFirstFrameDropped) {// ';' 前缀噪声并入帧（口径同旧 v2：裸流无同步头）
    sd::FrameCodec c;
    std::vector<sd::FrameCodec::Frame> out;
    c.feed("xxG01 U1;", out);
    ASSERT_EQ(out.size(), 1u);
    EXPECT_EQ(out[0].payload, "xxG01 U1");               // 拆帧不判内容，解析层拒
}

TEST(FrameCodec, EmptyFrameNotEmitted) {                 // ";;" 空帧不出
    sd::FrameCodec c;
    std::vector<sd::FrameCodec::Frame> out;
    c.feed(";;G01 U1;;", out);
    ASSERT_EQ(out.size(), 1u);
    EXPECT_EQ(out[0].payload, "G01 U1");
}

TEST(FrameCodec, HalfFrameTimeoutDrops) {
    sd::FrameCodec c;
    std::vector<sd::FrameCodec::Frame> out;
    c.feed("G01 U1", out);
    EXPECT_FALSE(c.advanceTimeout(49));
    ASSERT_TRUE(c.advanceTimeout(1));                    // 50ms 丢挂起
    c.feed(";G03 S1;", out);                             // 迟到 ';' 只出一个 G03
    ASSERT_EQ(out.size(), 1u);
    EXPECT_EQ(out[0].payload, "G03 S1");
}

TEST(FrameCodec, EncodeAppendsSemicolon) {
    sd::FrameCodec c;
    EXPECT_EQ(c.encode("N10 H30 B60 T1 V1 C1 D1 L50"), "N10 H30 B60 T1 V1 C1 D1 L50;");
}
```

**Step 4: 构建（预期编译错——CommandChannel/MCUDriver 尚引用旧符号，属预期；本任务只保证本二文件自洽）**

---

### Task 2: McuFrame 换代 G01/G02/G03

**Files:**
- Modify: `modules/08_devicemgmt/serial/McuFrame.h`（重写）
- Modify: `modules/08_devicemgmt/serial/McuFrame.cpp`（重写）
- Modify: `modules/08_devicemgmt/tests/test_mcu_frame.cpp`（重写）

**Step 1: 重写 McuFrame.h**

```cpp
#pragma once
// ============================================================================
// McuFrame.h — MCU 上行帧类型 + 分流双有界环（协议 260831 口径）
//
// 上行三类：G01 手势 / G02 四路温度 / G03 触发计数（协议说明.md §三）。
// 环：手势/计数事件环容量 64，温度遥测环容量 8（满丢新+计数）。
// SPSC：串口 rx 线程生产 / 逻辑线程消费。
// ============================================================================
#include "base/types.h"
#include <array>
#include <atomic>
#include <cstdint>
#include <string>

namespace Scanner::device::serial {

enum class KeyId : uint8_t { Up, Left, Middle, Right };   // G01 键 U/L/M/R
enum class Gesture : uint8_t { Short = 1, Double = 2, Hold = 3 };  // G01 手势位

struct TempFrame      { double celsius[4]; TimestampMs ts; };      // G02 恒 4 路
struct GestureEvent   { KeyId key; Gesture gesture; TimestampMs ts; };  // G01（MCU 已判手势）
struct ShotCountFrame { uint64_t count; TimestampMs ts; };         // G03 硬件触发计数

// —— 载荷解析（入参已由 FrameCodec 剥去 ';'；非法一律 false，out 先归零）——
// G01: "G01 U1"   G02: "G02 A25.3 B25.4 C26.0 D24.5"   G03: "G03 S500"
bool parseGesturePayload(const std::string& payload, GestureEvent& out);
bool parseTempPayload(const std::string& payload, TempFrame& out);
bool parseShotCountPayload(const std::string& payload, ShotCountFrame& out);

// —— 满环丢新有界环（同旧实现，口径 D-T12a）——
template <typename T, size_t N>
class SpscRing {
public:
    bool push(T v) {
        if (full()) { dropCount_.fetch_add(1, std::memory_order_relaxed); return false; }
        buf_[tail_] = std::move(v); tail_.store((tail_+1)%N); return true; }
    bool pop(T& out) {
        if (empty()) return false; out = std::move(buf_[head_]); head_.store((head_+1)%N); return true; }
    bool empty() const { return head_.load(std::memory_order_acquire) == tail_.load(std::memory_order_acquire); }
    uint64_t dropped() const { return dropCount_.load(std::memory_order_relaxed); }
private:
    bool full() const { return (tail_+1)%N == head_.load(std::memory_order_acquire); }
    std::array<T, N> buf_{};
    std::atomic<size_t> head_{0}, tail_{0};
    std::atomic<uint64_t> dropCount_{0};
};

} // namespace Scanner::device::serial
```

**Step 2: 重写 McuFrame.cpp**

```cpp
// ============================================================================
// McuFrame.cpp — G01/G02/G03 载荷解析（协议 260831；契约见 McuFrame.h）
// ============================================================================
#include "McuFrame.h"

#include <charconv>
#include <cmath>

namespace Scanner::device::serial {

namespace {

bool parseDoubleFull(const std::string& s, double& out) {
    if (s.empty()) return false;
    double v = 0.0;
    const auto r = std::from_chars(s.data(), s.data() + s.size(), v);
    if (r.ec != std::errc{} || r.ptr != s.data() + s.size() || !std::isfinite(v)) return false;
    out = v;
    return true;
}

bool parseU64Full(const std::string& s, uint64_t& out) {
    if (s.empty()) return false;
    uint64_t v = 0;
    const auto r = std::from_chars(s.data(), s.data() + s.size(), v, 10);
    if (r.ec != std::errc{} || r.ptr != s.data() + s.size()) return false;
    out = v;
    return true;
}

} // namespace

// "G01 U1" —— 指令字＋空格＋键字母＋手势位
bool parseGesturePayload(const std::string& payload, GestureEvent& out) {
    out = GestureEvent{};
    if (payload.size() != 5 || payload.substr(0, 4) != "G01 " || payload[3] == ' ') return false;
    switch (payload[3]) {
        case 'U': out.key = KeyId::Up; break;
        case 'L': out.key = KeyId::Left; break;
        case 'M': out.key = KeyId::Middle; break;
        case 'R': out.key = KeyId::Right; break;
        default: return false;
    }
    if (payload[4] < '1' || payload[4] > '3') return false;
    out.gesture = static_cast<Gesture>(payload[4] - '0');
    return true;
}

// "G02 A25.3 B25.4 C26.0 D24.5" —— 四路必齐（协议恒 4 路），A/B/C/D 序可乱可重复？——
// 口径：按协议序 A B C D 逐项消费；缺路/重复/非法值一律 false。
bool parseTempPayload(const std::string& payload, TempFrame& out) {
    out = TempFrame{};
    if (payload.substr(0, 4) != "G02 ") return false;
    const char kKeys[4] = {'A', 'B', 'C', 'D'};
    size_t pos = 4;
    for (int i = 0; i < 4; ++i) {
        if (pos + 1 >= payload.size() || payload[pos] != kKeys[i] || payload[pos + 1] == ' ') return false;
        size_t end = payload.find(' ', pos + 1);
        if (end == std::string::npos) end = payload.size();
        double v = 0.0;
        if (!parseDoubleFull(payload.substr(pos + 1, end - pos - 1), v)) return false;
        out.celsius[i] = v;
        pos = (end == payload.size()) ? end : end + 1;
    }
    return pos == payload.size();
}

// "G03 S500" —— 指令字＋空格＋S＋十进制计数
bool parseShotCountPayload(const std::string& payload, ShotCountFrame& out) {
    out = ShotCountFrame{};
    if (payload.size() < 6 || payload.substr(0, 4) != "G03 " || payload[4] != 'S') return false;
    return parseU64Full(payload.substr(5), out.count);
}

} // namespace Scanner::device::serial
```

**Step 3: 重写 test_mcu_frame.cpp**

```cpp
// test_mcu_frame.cpp — G01/G02/G03 解析器合法/非法 ＋ SpscRing 满丢新
#include "serial/McuFrame.h"
#include <gtest/gtest.h>

namespace sd = Scanner::device::serial;
using SD = sd::Gesture;

TEST(G01Parse, AllTwelveGestures) {
    const char* keys[] = {"U", "L", "M", "R"};
    for (int k = 0; k < 4; ++k)
        for (int g = 1; g <= 3; ++g) {
            sd::GestureEvent ev;
            ASSERT_TRUE(sd::parseGesturePayload("G01 " + std::string(keys[k]) + std::to_string(g), ev));
            EXPECT_EQ(static_cast<int>(ev.key), k);
            EXPECT_EQ(static_cast<int>(ev.gesture), g);
        }
}

TEST(G01Parse, RejectsMalformed) {
    sd::GestureEvent ev;
    EXPECT_FALSE(sd::parseGesturePayload("G01 U0", ev));    // 手势位 0 非法
    EXPECT_FALSE(sd::parseGesturePayload("G01 U4", ev));    // 手势位 4 非法
    EXPECT_FALSE(sd::parseGesturePayload("G01 X1", ev));    // 未知键
    EXPECT_FALSE(sd::parseGesturePayload("G01U1", ev));     // 缺空格
    EXPECT_FALSE(sd::parseGesturePayload("K M1", ev));      // 旧 K 帧
    EXPECT_FALSE(sd::parseGesturePayload("", ev));
}

TEST(G02Parse, FourChannels) {
    sd::TempFrame t;
    ASSERT_TRUE(sd::parseTempPayload("G02 A25.3 B25.4 C26.0 D24.5", t));
    EXPECT_DOUBLE_EQ(t.celsius[0], 25.3);
    EXPECT_DOUBLE_EQ(t.celsius[1], 25.4);
    EXPECT_DOUBLE_EQ(t.celsius[2], 26.0);
    EXPECT_DOUBLE_EQ(t.celsius[3], 24.5);
}

TEST(G02Parse, RejectsMalformed) {
    sd::TempFrame t;
    EXPECT_FALSE(sd::parseTempPayload("G02 A25.3 B25.4 C26.0", t));      // 缺 D 路
    EXPECT_FALSE(sd::parseTempPayload("G02 A25.3 B25.4 C26.0 D", t));    // D 空值
    EXPECT_FALSE(sd::parseTempPayload("T25.3,24.8", t));                 // 旧 T 帧
    EXPECT_FALSE(sd::parseTempPayload("G02 A25.3 B25.4 C26.0 E24.5", t));// 键错
    EXPECT_FALSE(sd::parseTempPayload("G02 A B C D", t));                // 非数值
}

TEST(G03Parse, CountAndMalformed) {
    sd::ShotCountFrame f;
    ASSERT_TRUE(sd::parseShotCountPayload("G03 S500", f));
    EXPECT_EQ(f.count, 500u);
    EXPECT_TRUE(sd::parseShotCountPayload("G03 S0", f));                  // 0 合法
    EXPECT_EQ(f.count, 0u);
    EXPECT_FALSE(sd::parseShotCountPayload("G03 S", f));
    EXPECT_FALSE(sd::parseShotCountPayload("G03 S-1", f));
    EXPECT_FALSE(sd::parseShotCountPayload("S0A", f));                    // 旧 S 帧
}

TEST(SpscRing, FullDropsNew) {                     // 承旧用例语义
    sd::SpscRing<int, 3> r;
    EXPECT_TRUE(r.push(1)); EXPECT_TRUE(r.push(2));
    EXPECT_FALSE(r.push(3));                       // 容量 3 有效 2，满丢新
    EXPECT_EQ(r.dropped(), 1u);
    int v = 0;
    EXPECT_TRUE(r.pop(v)); EXPECT_EQ(v, 1);
    EXPECT_TRUE(r.push(4));
    EXPECT_FALSE(r.empty());
}
```

> 注：parseTempPayload 中 `payload[3] == ' '` 防御分支在 "G01 " 已由 substr 比较覆盖，G01 实现为 5 字符定长校验，保持简单。

---

### Task 3: IMCU.h 接口换代（B1/B2）

**Files:**
- Modify: `modules/08_devicemgmt/IMCU.h`（重写）

**Step 1: 重写 IMCU.h**

```cpp
#pragma once
// ============================================================================
// IMCU.h — 下位机 MCU 接口（HAL 层；协议 260831 口径）
//
// 下行 4 条（N10 采集/N11 停止/N12 温度周期/N13 加热）＋上行 G01/G02/G03 分流。
// 所有 send* 非阻塞：帧入写队列即返，完成回调同步回告写成败（无 ACK——盲发口径 D8）。
// ============================================================================
#include "base/types.h"
#include "serial/CommandChannel.h"
#include "serial/McuFrame.h"
#include <functional>
#include <string>

namespace Scanner::hal {

struct CaptureParams {              // N10 七参（协议表：H 1-200 / B 0-100 / T V C D 0|1 / L 0-100）
    int freqHz = 60;
    int bgLight = 10;
    int laserT = 1, laserV = 1, laserC = 0, laserD = 0;   // 四激光管开关（D7：归 ScanMode 映射）
    int laserLevel = 40;
};

struct McuUplink {                  // 上行分流出口（DeviceManager 注册；均在逻辑线程回调）
    std::function<void(const Scanner::device::serial::TempFrame&)> onTemp;
    std::function<void(const Scanner::device::serial::GestureEvent&)> onGesture;
    std::function<void(const Scanner::device::serial::ShotCountFrame&)> onShotCount;
};

class IMCU {
public:
    virtual ~IMCU() = default;
    virtual Scanner::Result open(const std::string& port) = 0;   // 内起 rx 线程
    virtual Scanner::Result close() = 0;
    virtual bool isOpen() const = 0;

    using DoneCb = Scanner::device::serial::CommandChannel::DoneCb;
    virtual void setCaptureParams(const CaptureParams&, DoneCb) = 0;   // "N10 H.. B.. T.. V.. C.. D.. L.."
    virtual void stopScan(DoneCb) = 0;                                 // "N11 H0"
    virtual void setTempReportPeriod(int periodMs, DoneCb) = 0;        // "N12 T<5-1000>"
    virtual void setHeatTarget(int celsius, DoneCb) = 0;               // "N13 S<0-80>"

    virtual void setUplink(McuUplink h) = 0;
    virtual Scanner::TimestampMs lastRxTime() const = 0;    // 通讯心跳时间戳
    virtual void pump() = 0;    // 逻辑线程调：排空 3 环→分流回调；回调内不得再调 pump/send

    // —— 观测（调试/巡检）——
    virtual uint64_t gestureRingDropped() const = 0;        // G01 手势环满丢新计数（0x0806）
    virtual uint64_t parseFailCount() const = 0;            // 上行载荷解析失败计数
    virtual uint64_t shotCount() const = 0;                 // 最近 G03 计数值快照（D6 记账）
};

} // namespace Scanner::hal
```

---

### Task 4: CommandChannel 最小适配（encode 新签名）

> 机制（挂表/重传/onAck/tick）批 2 物理删除；本任务只改到能编译。

**Files:**
- Modify: `modules/08_devicemgmt/serial/CommandChannel.cpp:20-23,49-51`（encode 调用去 seq 参数）
- Modify: `modules/08_devicemgmt/serial/CommandChannel.h:53`（nextSeq 删——sendFireAndForget 不再用）

**Step 1:** `CommandChannel.cpp` 中三处 `deps_.codec->encode(payload, seq)` → `deps_.codec->encode(payload)`（`writeFrame` 签名可保留 seq 形参但不再传入 encode，或直接去掉 seq 形参——选后者，同步改 `.h:70` 声明）；`sendFireAndForget` 内 `writeFrame(nextSeq(), payload)` → `writeFrame(payload)`；`send()` reliable 分支内 `writeFrame(nextSeq(), payload)` → `writeFrame(payload)`（表项 seq 留 0，批 2 整删）。
**Step 2:** `.h` 删 `uint16_t nextSeq();` 声明与 `.cpp` 实现（14-18 行）；`seq_` 成员批 2 删。

---

### Task 5: MCUDriver 命令缩并＋G 分流＋探测重造（B5/B8）

**Files:**
- Modify: `modules/08_devicemgmt/MCUDriver.h`（重写——保留写线程/测试缝骨架）
- Modify: `modules/08_devicemgmt/MCUDriver.cpp`（大改）

**Step 1: MCUDriver.h 重写要点**（骨架保留：WriteOverride/open/close/isOpen/写线程组/rxLoop/pump/testInjectRaw/setWireTap/lastRxTime/parseFailCount；变更如下）

- 删：`setProtocolVersion/applyVersion/version_/seqGapCount/channelTick/setAckTimeoutMs/ackTimeoutMs_/keyDropCount/probeDidSelfCheck/probeN12Sent_/sendEchoProbe/lastEchoPayload/lastEcho_/echoMtx_/hasTSeq_/lastTSeq_/accountTempSeq/ackRing_/statusRing_/feedTextLine/probeHit_`
- IMCU override 换新四方法＋两个观测口：`gestureRingDropped()`（=gestureRing_.dropped()）、`shotCount()`（=最近 G03 count，atomic）
- 环成员：`SpscRing<GestureEvent,64> gestureRing_; SpscRing<TempFrame,8> tempRing_; SpscRing<ShotCountFrame,64> shotRing_;`
- `codec_` 无参构造；`makeDeps()` 删 reliable 开关（恒 false 路径，批 2 收敛）
- 新增探测成员：`std::atomic<bool> gSeen_{false};`（任意 G 帧到达置位——探测凭据）

**Step 2: MCUDriver.cpp 变更**

typed 命令（替换 427-458 全部旧实现）：

```cpp
// ============================================================================
// typed 下行 4 条（协议 260831；参数间空格——对齐工厂帧格式）
// ============================================================================
void MCUDriver::setCaptureParams(const hal::CaptureParams& p, DoneCb cb) {
    channel_.send("N10 H" + std::to_string(p.freqHz) +
                  " B" + std::to_string(p.bgLight) +
                  " T" + std::to_string(p.laserT) +
                  " V" + std::to_string(p.laserV) +
                  " C" + std::to_string(p.laserC) +
                  " D" + std::to_string(p.laserD) +
                  " L" + std::to_string(p.laserLevel), std::move(cb));
}
void MCUDriver::stopScan(DoneCb cb)              { channel_.send("N11 H0", std::move(cb)); }
void MCUDriver::setTempReportPeriod(int ms, DoneCb cb) {
    if (ms < 5) ms = 5;                          // D9：串口预算安全下限
    if (ms > 1000) ms = 1000;
    channel_.send("N12 T" + std::to_string(ms), std::move(cb));
}
void MCUDriver::setHeatTarget(int celsius, DoneCb cb) {
    if (celsius < 0) celsius = 0;                // 协议域 0-80 钳制
    if (celsius > 80) celsius = 80;
    channel_.send("N13 S" + std::to_string(celsius), std::move(cb));
}
void MCUDriver::lightsOff() {                    // 熄灯收口＝N11 H0（新协议无独立灯控）
    if (!open_.load() || writeOverride_) return;
    stopScan(nullptr);
}
```

dispatchFrame（替换 310-364）：

```cpp
void MCUDriver::dispatchFrame(const serial::FrameCodec::Frame& f) {
    lastRx_.store(nowMs(), std::memory_order_release);       // 心跳
    notifyTap(false, f.payload);
    const std::string& p = f.payload;
    gSeen_.store(true, std::memory_order_release);           // 任意完整帧（探测凭据宽松版：
                                                             // 纯回环线回显自身 TX 不经 rx——
                                                             // 实际 rx 帧即 MCU 真发）
    serial::GestureEvent g;
    if (p.compare(0, 3, "G01") == 0) {
        if (serial::parseGesturePayload(p, g)) { if (!gestureRing_.push(g)) JMW_LOG_WARN("08-MCUDriver", "[MCUDriver] 手势环满丢新"); }
        else onParseFail(p);
        return;
    }
    serial::TempFrame t;
    if (p.compare(0, 3, "G02") == 0) {
        if (serial::parseTempPayload(p, t)) { if (!tempRing_.push(t)) JMW_LOG_WARN("08-MCUDriver", "[MCUDriver] 温度环满丢新"); }
        else onParseFail(p);
        return;
    }
    serial::ShotCountFrame s;
    if (p.compare(0, 3, "G03") == 0) {
        if (serial::parseShotCountPayload(p, s)) { lastShot_.store(s.count, std::memory_order_release); if (!shotRing_.push(s)) {} }
        else onParseFail(p);
        return;
    }
    onParseFail(p);                                           // 未知帧（旧 T/K/S/A 全落此）
}
```

> `nowMs()` 用现文件内时钟助手（保持原 lastRx_ 时间源）；`lastShot_` 为新增 `std::atomic<uint64_t>`。

pump（替换 395-412 附近）：

```cpp
void MCUDriver::pump() {
    if (!uplink_) return;
    serial::GestureEvent g;
    while (gestureRing_.pop(g)) { if (uplink_.onGesture) uplink_.onGesture(g); }
    serial::ShotCountFrame s;
    while (shotRing_.pop(s))   { if (uplink_.onShotCount) uplink_.onShotCount(s); }  // D6：默认无消费者
    serial::TempFrame t;
    while (tempRing_.pop(t))   { if (uplink_.onTemp) uplink_.onTemp(t); }            // 遥测最后（保事件优先）
}
```

rxLoop 简化：删 v2 行分支（'\n' 文本行路径整删——新协议纯 ';' 帧）；循环体＝read → `codec_.feed(bytes, frames)` → 逐帧 dispatchFrame → 周期 `codec_.advanceTimeout(实际间隔)`。lineBuf_ 删。

probeAutoPort 重造（替换 220-247）：

```cpp
// 自动搜口（协议 260831）：逐口 open → 发 N12 T100（开温度上报）→ 等 300ms
// 收到任意完整帧（gSeen_）即命中。纯回环线：TX 帧不经 rx 线程回流，不会误命中。
std::string MCUDriver::probeAutoPort(int baud) {
    ...枚举口（沿用现逻辑）...
    for (const std::string& port : ports) {
        if (openPortOnly(port, baud) != ok) continue;      // 开串口+起 rx/写线程（不发命令）
        gSeen_.store(false);
        channel_.sendFireAndForget("N12 T100");
        for (int waited = 0; waited < 300; waited += 10) { sleepMs(10); if (gSeen_.load()) return port; }
        closePortOnly();                                    // 未命中倒序关，试下一口
    }
    return "";
}
```

> 原 `openPortOnly/closePortOnly` 若无此拆分（现 open() 一条龙），按现结构提取：把「开串口＋起线程」与「探测后命令序列」分开；命中口直接续用（N12 T100 已生效，open() 后续不再重发——与旧 probeN12Sent_ 幂等口径同型）。

其余删除：echo 相关（sendEchoProbe/lastEcho_/echoMtx_）、`'N' 回显 default 分支`、seq 对账（accountTempSeq）、`'E' 分支`、probeHit_/feedTextLine。`open(port)` 与 `open(port, baud)` 保留；open 内若引用 version_/applyVersion 全删。

**Step 3:** 若 `JMW_LOG_WARN` 宏名与现工程不符，按 MCUDriver.cpp 现有日志宏对齐（文件内已有用法）。

---

### Task 6: KeyManager 退役＋KeySemantics 直喂（B3/B4）

**Files:**
- Delete: `modules/08_devicemgmt/KeyManager.h` / `KeyManager.cpp` / `tests/test_key_manager.cpp`
- Modify: `modules/08_devicemgmt/CMakeLists.txt:42`（删 `target_sources(mod_devicemgmt PRIVATE KeyManager.cpp)` 行）
- Modify: `modules/08_devicemgmt/KeySemantics.h`
- Modify: `modules/08_devicemgmt/KeySemantics.cpp`
- Modify: `modules/08_devicemgmt/tests/test_key_semantics.cpp`

**Step 1:** 删三文件＋CMakeLists 登记（tests 是 GLOB，test_key_manager.cpp 删除即自动消失）。

**Step 2: KeySemantics.h**——`#include "KeyManager.h"` → `#include "serial/McuFrame.h"`；`onGesture(const KeyGesture&, const MenuState&)` → `onGesture(const serial::GestureEvent&, const MenuState&)`。头注释更新：手势判定归 MCU（G01），本类只做语义映射；「预留」注释改「协议可达、产品未定义」。

**Step 3: KeySemantics.cpp**——入口处把 `GestureEvent.gesture`（1/2/3）映射到原 `KeyGesture::G`（Short/Double/Hold）后走原转移表；`KeyGesture` 本地定义删除，`key` 字段类型同为 `serial::KeyId`（枚举值序不变，转移表零改动）。

**Step 4: test_key_semantics.cpp**——注入模型从 `{key, G, mcuMs}` 换 `{key, gesture(1/2/3)}`；断言不变。18 例保留语义平移（删 mcuMs 相关断言）。

---

### Task 7: DeviceManager 批1 适配（编译贯通＋机械编排改）

**Files:**
- Modify: `modules/08_devicemgmt/DeviceManager.h`
- Modify: `modules/08_devicemgmt/DeviceManager.cpp`
- Modify: `modules/08_devicemgmt/tests/test_device_manager.cpp`（MockMcu＋断言机械适配）
- Modify: `modules/08_devicemgmt/tests/test_mcu_driver.cpp`（重写）

**Step 1: DeviceManager.h**
- 删 `#include "KeyManager.h"`（DeviceConfig.keys 成员随之删）、`serial::FrameCodec.h` include（protocol 字段删）
- DeviceConfig：删 `protocol/ackTimeoutMs/seqGapWarn`；新增 `int tempReportPeriodMs = 100;`（D9）
- DevFault 注释对表：0x0807 缩义「串口写失败」；0x0808 注释改「闲置（G03 对账预留）」
- 删成员 `keyMgr_`；`dispatchKeyGesture(const KeyGesture&)` → `dispatchGesture(const serial::GestureEvent&)`
- `captureSeqSteps` 注释改单 N10；startCapture 注释对新口径

**Step 2: DeviceManager.cpp**
- ctor：删 keyMgr_ 构造；uplink 三回调换签名（onGesture 直推 `pendingGestures_` 小向量，logicTick ③ drain→dispatchGesture）；onStatus 删、onShotCount 空实现（D6 记账已归 MCUDriver）
- open：探测段后发 `mcu_->setTempReportPeriod(cfg_.tempReportPeriodMs, ...)`（替换已删的 N12Z1 语义）；注释更新
- close：删 `exitSelfCheck` 行；**D8 前半**：close 尾 `mode_->reset()`（ModeController 若无 reset，加 `request+commit(Idle)` 直落板＋`setCapturing(false)`——按 ModeController 现接口最小实现）
- captureSeqSteps（412-425）：删 FLUSH 步后仍保留（N10 单步）；startCaptureOnLogic：相机先启→发 N10（原 sendSeq(captureSeqSteps) 结构不变，组内容只剩 N10）
- stopCaptureOnLogic：`mcu_->stopScan(...)` 保留，detail 改 `"N11 H0 "`；熄灯收口 N11 H0 语义不变
- enterScanOnLogic/enterCalibrationOnLogic/toIdleOnLogic：删 `standbyActive_` 与 N13E0/N13E1/N16B1 步——enterCalibrationOnLogic 组链清空改纯软件落板（`mode_->commit(Calibrating)`）；toIdleOnLogic 删命令步纯落板
- startWarmup（635-644）：`mcu_->setHeatTarget(targetC, ...)` 保留（载荷已变 N13S）；detail `"N14T"` → `"N13S"`；targetC 钳 0-80
- logicTick：删 ④ channelTick、⑥ v2 温度兜底（kV2TempFallbackMs/kV2TempQueryCh 常量删）；`keyMgr_->tick/drain` → pendingGestures_ drain
- 灯光三态封装（486-522）：`lightsBgAndCrossLaser` 删（A/B 组语义亡）；`setLights/lightsBgOnly/lightsAllOff` 按 ScanMode 管掩码重写（映射函数见 Task 9，本任务先留 `setLights` 按 `captureLaserOn_` 二值映射 T/V=on、C/D=0——Task 9 统一换）
- ParamStore specs（38-49）：`laserSelectA/B` 两键先删（键删除不迁移——批 3 补迁移规则）；freqHz 上限暂留 120（批 3 改 200）；`effectiveN10`（62-73）改七字段拼装
- startupSelfCheck/SelfCheckSm：mcuLink 项凭据改 `lastRxTime()>0`（N12 后 G02 到达）；bgLight/laser 项改「N10 H5 短采 800ms→N11 H0」两段式（expectEcho 逻辑删，stage 表对表重排：0 发 N10→1 保持→2 发 N11 H0→3 相机收帧）；report key 不变
- publishFault detail 串全表：`"N14T"`→`"N13S"`、`"N11H0 "`保留、`"N16B1"`等删

**Step 3: test_device_manager.cpp 机械适配**
- MockMcu（118-143）：接口缩并（四方法＋新签名）；`count(frame)` 子串匹配断言全按新载荷改（含空格：`"N10 H60 B10 T1 V1 C0 D0 L40"`、`"N11 H0"`、`"N12 T100"`、`"N13 S42"`）
- T 组链用例：删 N13E0/N16B1/N11H1 步断言；预热断言 `N14T42`→`N13 S42`
- 按键注入：`onKey(RawKeyEvent)` → `onGesture(GestureEvent)`；v2 兜底用例（T11:602 `N15V2`）删
- ACK 重传用例（T10）删（批 2 补直发版）

**Step 4: test_mcu_driver.cpp 重写**（9 例换新面）

```cpp
// 核心用例骨架（WriteOverride 注入捕获帧串＋testInjectRaw 回灌上行）：
// 1. TypedPayloads：setCaptureParams→"N10 H60 B10 T1 V1 C0 D0 L40;"
//    stopScan→"N11 H0;" setTempReportPeriod(100)→"N12 T100;"
//    setTempReportPeriod(2)→"N12 T5;"（下限钳） setHeatTarget(99)→"N13 S80;"（上限钳）
// 2. DispatchG：inject "G01 U1;"→pump→onGesture(Up,Short)；"G02 A1 B2 C3 D4;"→onTemp 四路；
//    "G03 S500;"→shotCount()==500
// 3. OldFrameRejected：inject "T25.3,24.8;" "KM1,1234;" "S0A;"→parseFailCount 增、无回调
// 4. Heartbeat：inject 任意帧→lastRxTime()>0
// 5. GestureRingDrop：灌 64+ 手势→dropped()>0
// 6. Reopen：open/close×2 无残留（沿用原用例改命令面）
```

**Step 5: 批 1 终验＋提交**

```powershell
cmake --build build --config Debug
$env:PATH = 'C:\opencv-cuda-4.13.0-debug\x64\vc17\bin;' + $env:PATH
ctest --test-dir build -C Debug --output-on-failure
```
预期：**全绿**（含 06/07/09/10 既有 95 基线——本批不应波及；test_key_manager 自动消失）。

```powershell
git add -A modules/08_devicemgmt
git commit -m "feat(08)!: 协议260831硬切批1——裸';'分帧/G01-G02-G03上行解析/下行缩并N10七参-N11H0-N12T-N13S/KeyManager退役(KeySemantics直喂MCU手势)/探测改N12T100+G帧凭据/自检闪灯改N10短采包裹——ACK/回显/seq对账遗迹随批删除"
```

---

## 批 2：可靠性机制物理删除

### Task 8: CommandChannel 降级＋ParamStore 去 confirmed

**Files:**
- Modify: `modules/08_devicemgmt/serial/CommandChannel.h`（重写）
- Modify: `modules/08_devicemgmt/serial/CommandChannel.cpp`（重写）
- Modify: `modules/08_devicemgmt/ParamStore.h/.cpp`
- Modify: `modules/08_devicemgmt/tests/test_command_channel.cpp`（重写）
- Modify: `modules/08_devicemgmt/tests/test_param_store.cpp`
- Modify: `modules/08_devicemgmt/DeviceManager.cpp`（sendSeq 调用点随新 Done 语义微调——组链仍保留「依序发、写败即整组败」）

**Step 1: CommandChannel.h 重写**

```cpp
#pragma once
// ============================================================================
// CommandChannel.h — 下行通道（协议 260831：无 ACK——盲发口径 D8）
// 职责收敛为：encode ＋ 写出口，同步回告写成败。sendGroup＝依序直发，
// 任一步写失败整组短路。单线程属主：逻辑线程。
// ============================================================================
#include "serial/FrameCodec.h"
#include <functional>
#include <string>
#include <vector>

namespace Scanner::device::serial {

class CommandChannel {
public:
    using DoneCb = std::function<void(bool ok, const std::string& payload)>;

    struct Deps {
        FrameCodec* codec = nullptr;
        std::function<bool(const std::string& frame)> write;   // 写帧（返回是否成功）
    };

    explicit CommandChannel(Deps d);
    void send(const std::string& payload, DoneCb onDone);      // 组帧+写，立即回告写成败
    void sendFireAndForget(const std::string& payload);
    void sendGroup(std::vector<std::string> payloads, DoneCb onGroupDone);  // 依序直发，写败短路

private:
    bool writeFrame(const std::string& payload);
    Deps deps_;
};

} // namespace Scanner::device::serial
```

**Step 2: CommandChannel.cpp 重写**（构造防护：write 空钳恒 false；send：`ok=writeFrame` → `onDone(ok, payload)`；sendGroup 循环版依序调 send、遇 false 短路回调）。

**Step 3: test_command_channel.cpp 重写**（~6 例：send 成功/写失败回告、fire-and-forget、组链全过/中败短路、空 codec 裸发、空 write 恒败）。

**Step 4: ParamStore**——`confirmed` 字段与 `SetAckConfirm/SetThreeFailReject/V2Immediate` 语义删：`Done` 简化为写成败直通（`onParamDispatch` 调用点同步）；test_param_store 三类 confirmed 用例改写、档迁移用例留批 3。

**Step 5:** 构建＋`ctest -R "test_command_channel|test_param_store|test_device_manager"` 绿后：

```powershell
git add -A modules/08_devicemgmt
git commit -m "refactor(08): 协议批2——CommandChannel物理降级为盲发直通(删ACK挂表/重传/3败/seq),ParamStore去confirmed语义"
```

---

## 批 3：参数域迁移＋扫描模式扩展（D7）

### Task 9: ScanMode 四值＋管掩码映射＋参数域＋旧档迁移

**Files:**
- Modify: `base/types.h:89-92`（ScanMode 扩枚举）
- Modify: `modules/08_devicemgmt/DeviceManager.cpp`（specs/effectiveN10/灯光封装/映射函数）
- Modify: `modules/08_devicemgmt/DeviceManager.h`（lightsBgAndCrossLaser 删；映射出口）
- Modify: `modules/08_devicemgmt/ParamStore.cpp`（旧档迁移）
- Modify: `app/AppContext.cpp` / `app/MainWindow.cpp` / `app/ScannerWindow.cpp`
- Modify: `modules/08_devicemgmt/tests/test_device_manager.cpp`、`tests/test_param_store.cpp`
- 可能 Modify: `modules/10_observability/StateMachine.cpp:68-75`（S4/S5 判别若 switch-exhaustive——只加 case 不改逻辑）

**Step 1: base/types.h**

```cpp
enum class ScanMode : uint8_t {
    MarkerOnly = 0,        // 标点扫描：不开激光线（T0 V0 C0 D0）
    MarkerPlusLaser = 1,   // 面片扫描：左右激光线（T1 V1 C0 D0）
    FineScan = 2,          // 精细扫描（原「点云扫描」）：C 管（T0 V0 C1 D0）
    DeepHoleScan = 3,      // 深孔扫描：D 管（T0 V0 C0 D1）
};
```

**Step 2: DeviceManager 映射函数**（cpp 本地）＋ effectiveN10 按 mode 组装四管：

```cpp
hal::CaptureParams effectiveN10(const ParamStore& params, Scanner::ScanMode mode) {
    hal::CaptureParams p;
    p.freqHz  = params.getOrDefault("freqHz");
    p.bgLight = params.getOrDefault("bgLight");
    p.laserLevel = (mode == Scanner::ScanMode::MarkerOnly) ? 0 : params.getOrDefault("laserLevel");
    p.laserT = (mode == Scanner::ScanMode::MarkerPlusLaser) ? 1 : 0;
    p.laserV = (mode == Scanner::ScanMode::MarkerPlusLaser) ? 1 : 0;
    p.laserC = (mode == Scanner::ScanMode::FineScan)        ? 1 : 0;
    p.laserD = (mode == Scanner::ScanMode::DeepHoleScan)    ? 1 : 0;
    return p;
}
```

`startCapture(bool laserOn)` 签名改 `startCapture(Scanner::ScanMode mode)`（AppContext 两调用点随改——`AppContext.cpp:434-439/469/519` 按 `lastScanMode_` 传值）；`captureLaserOn_` 成员换 `lastCaptureMode_`。

**Step 3: ParamStore specs 终态**：`freqHz 60, 1-200`（H 新域）；`tempReportPeriodMs 100, 5-1000`；`laserSelectA/B` 已批 1 删。旧档迁移（persist 回读处）：遇 `laserSelectA/laserSelectB` 键→丢弃＋info 日志；`laserLevel>100`→钳 100＋warn「旧档量纲迁移」。

**Step 4: app 三文件**
- MainWindow.cpp:1036 `"cloudscan-black-14","点云扫描"` → `"精细扫描"`；:761-762/:1078/:1175-1177 模式判定链扩四值（精细/深孔按钮 → `ScanMode::FineScan/DeepHoleScan`；深孔按钮若无现成 icon 资源，复用 cloudscan 图标＋文案，资源新增留待设计）；:1369 页面注释对表
- AppContext.cpp:433-446：`laserOn` 二值判定删，改按 mode 传参＋日志打四管掩码
- ScannerWindow.cpp:92-100：滑条运行时钳位 `freq 1-200 / bg 0-100 / laser 0-100`；:309 `MarkerPlusLaser` 不变
- ⚠ 批 3 只做 UI 映射就位；**精细/深孔的 07 激光链单管周期正确性未验证（⑨b 登记，代码注释注明）**

**Step 5:** 构建＋全量 ctest 绿后：

```powershell
git add -A base modules app
git commit -m "feat(base/08/app): 协议批3——ScanMode扩四值(精细=点云改名C管/深孔新增D管),N10按模式组装四管掩码,参数域freqHz 1-200+旧档迁移,UI量程对齐"
```

---

## 批 4：深编排＋故障对表（D8/⑧）

### Task 10: D8 恢复路径＋FaultPatrol 对表

**Files:**
- Modify: `modules/08_devicemgmt/DeviceManager.cpp`（open 重同步/close 复位/0x0802/0x0804/0x0806/0x0808 巡检段）
- Modify: `modules/08_devicemgmt/DeviceManager.h`（DevFault 注释终态）
- Modify: `modules/08_devicemgmt/tests/test_device_manager.cpp`（F 组用例）

**Step 1: D8 恢复路径**
- close：`mode_` 落板 Idle＋`setCapturing(false)`＋`standbyActive_` 类记账清零＋`lastTemps_{}` 清空＋`gSeen_` 类探测遗迹复位
- open（成功尾段）：重发 `N12 T<cfg>`＋若 `mode()!=Idle` 按当前模式重发 N10（参数重同步）＋日志「重开重同步」

**Step 2: 巡检对表**
- 0x0802：武装条件 `lastRx>0` → 改 open 成功即武装（`serialArmed_=true`）；超时判据 `heartbeatTimeoutMs` 保留 10s 默认（注释挂 D9 周期自适应：`max(10×tempReportPeriodMs, 10000)` 可选增强）
- 0x0804：速率判据 → **绝对差值**：同路相邻 G02 `|Δ|>tempSpikeAbsC`（默认 10.0℃/帧，新 DeviceConfig 字段）即报；删 Δt 分母
- 0x0806：`keyDropCount` 源改 `mcu_->gestureRingDropped()`
- 0x0808：巡检段删（码位注释「闲置——G03 帧对账预留」）；`lastSeqGap_` 成员删
- 0x0807：注释/detail 终态（「串口写失败」；组链写败仍归此码）

**Step 3: test_device_manager F 组**：F5（0x0806 手势环溢——改注入 G01 洪水）、F-seq 用例删、0x0802 用例改 open 即武装（无帧也报）；恢复路径新用例：`close→open→isCapturing()==false`＋重同步帧断言（MockMcu count "N12 T100" ≥2）。

**Step 4:** 构建＋全量 ctest 绿后：

```powershell
git add -A modules/08_devicemgmt
git commit -m "feat(08): 协议批4——D8恢复路径(close复位黑板/open重同步N12N10),故障对表(0x0802 open即武装/0x0804绝对差值/0x0806挂手势环/0x0808闲置)"
```

---

## 批 5：全量回归＋文档同步

### Task 11: 全量回归（Debug＋Release 双绿）＋文档同步

**Step 1: 双配置回归**

```powershell
cmake --build build --config Debug
$env:PATH = 'C:\opencv-cuda-4.13.0-debug\x64\vc17\bin;' + $env:PATH
ctest --test-dir build -C Debug --output-on-failure
cmake --build build-rel --config Release
ctest --test-dir build-rel -C Release --output-on-failure
```
预期：双绿（基线 95－test_key_manager 18＋新增解析器/通道用例数）。

**Step 2: 文档同步（最小集）**
- `docs/模块功能/08-设备管理.md`：协议章节按新口径重写（命令 4 条/G 帧/盲发/手势直喂/四管映射）
- `模块功能目录.md` §2 08 段（:383,:402-408,:422-428,:440 旧口径替换）
- `AGENTS.md:75`：「MCUDriver（typed N10–N16）」→「MCUDriver（typed N10/N11H0/N12T/N13S＋G01-G03 上行）」
- `开发进度.md`：快照更新（本对齐完成＋⑨b/⑨c 待办挂起＋H3 盲发残风险登记）
- 本计划文档头加「已完成」标记

**Step 3: 提交**

```powershell
git add -A docs AGENTS.md 模块功能目录.md 开发进度.md
git commit -m "docs: 协议260831对齐批5——08模块文档/AGENTS/进度快照同步(⑨b周期模型与⑨c四路温度待办挂起)"
```

---

## 验收清单（对照 design §0）

- [ ] 下行仅 4 条命令且载荷带空格（wireTap 日志核对）
- [ ] G01/G02/G03 三解析器合法/非法全测
- [ ] KeyManager 物理删除（全工程 grep 零引用）
- [ ] ACK/seq/CRC/回显遗迹物理删除（grep `nextSeq|onAck|crc16|lastEcho|RawKeyEvent` 零命中）
- [ ] ScanMode 四值映射正确（N10 四管掩码按模式）
- [ ] 旧档迁移：laserSelectA/B 丢弃＋laserLevel 钳位有日志
- [ ] close→open 恢复路径（isCapturing 复位＋N12 重同步）
- [ ] Debug＋Release ctest 全绿
- [ ] 文档五处同步

## 残风险移交（不阻塞验收）

- H3 盲发零反馈（固件静默拒收不可见——G03 对账后续新增）
- 精细/深孔单管周期 × 07 偶L奇R 配对（⑨b 门控）
- 自检短采包裹 H5 下相机出帧行为待真机验证
- N12 T<5ms 固件行为未知（PC 侧已钳下限）
