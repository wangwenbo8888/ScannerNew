# 设备管理模块 08 实施计划（S~A 五阶段 / T1–T18）

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 落地 `modules/08_devicemgmt/` 全模块——串口协议层三小层（v2/v3 开关）+ KeyManager 按键链 + DeviceManager 门面（6 小组件）+ CameraControl 收敛 + HardwareMonitor/SelfCheckCollector 监控全量 + app 装配收拢。

**Architecture:** 设计依据＝[`2026-08-20-设备管理模块08设计方案.md`](2026-08-20-设备管理模块08设计方案.md)（两轮审查定稿，§ 引用均指该文）。SerialPort（纯 IO）→ FrameCodec（v2 `;` / v3 `$…seq crc;` 成帧+CRC16+半帧超时）→ CommandChannel（非阻塞 seq/ACK/重传/命令组步链）+ 上行分流双有界环；门面沿用 [`2026-08-18-DeviceManager-design.md`](2026-08-18-DeviceManager-design.md)；监控产 `HealthMetrics`（base/types.h:95 已有）经 app 适配器喂 10-PerfMonitor。mod_devicemgmt 仍只链 base/OpenCV/spdlog(+Galaxy PRIVATE)；**08 不链 06/10**。

**Tech Stack:** C++17 / Win32 串口+PDH+NVML(动态加载) / spdlog / gtest（FetchContent 已有）/ CRC16-CCITT 查表。

**实施期补正（与设计方案的偏差，完成后回写）：**
1. 06 侧**零代码改动**：4 路温度用 DeviceStateCache 既有 deviceId 机制（每路一条 `MCU_T0..T3`，首路同步写 `MCU` 兼容 MainWindow.cpp:1614 现读法）——设计方案 §1「DeviceStateCache 扩字段」以更省方式达成。
2. 帧率三件套采集沿用 HardwareMonitor 既有 `setFrameCounter` 注入模式，另加 `setProcessCounter`/`setDropCounter` 注入（FrameBuffer 无计数器，不为此改 06）。

---

## 全局约定（每个任务都遵守）

1. **禁区**：`factory_calib/` 只读；🔒 定稿文档（01/02 §1§2、模块功能目录 §三）不改。不确定的接口先读头文件，**不猜**。
2. **构建/测试**（Debug，`build/` 已配置）：
   ```powershell
   cmake --build build --config Debug --target <test_exe>
   $env:PATH = 'C:\opencv-cuda-4.13.0-debug\x64\vc17\bin;' + $env:PATH
   ctest --test-dir build -C Debug -R "^<test_exe>$" --output-on-failure
   ```
   阶段收尾全量：`cmake --build build --config Debug` + `ctest --test-dir build -C Debug --output-on-failure`——**基线不得变红**（2026-08-20 提交 2e871c1 记 77/77，以实际跑数为准）。
3. **提交**：每 Task 一 commit，风格 `feat(mod08): S-T3 FrameCodec成帧拆帧`。**只 add 本任务文件**（工作区可能有他人未提交改动，严禁 `git add -A`）。
4. **风格**：`#pragma once` + 文件头注释块（仿 `modules/06_fileio/RingBuffer.h`）；08 命名空间 `Scanner::device`（serial 子层 `Scanner::device::serial`）；中文契约级注释；**不加多余注释**。
5. **测试注册**：`modules/08_devicemgmt/CMakeLists.txt` 加 BUILD_TESTS 段（GLOB `tests/test_*.cpp` + 每 exe 一 add_test + `jmw_deploy_crt`，仿 `modules/10_observability/CMakeLists.txt:16-31`）。
6. **app 侧改动**（Phase A）在 scan_demo 条件构建内；Phase S–H 不碰 app，保证每 commit 库+测试可独立编译。
7. **协议未定稿纪律**：v3 上行帧格式按设计 §8 默认落码（`$T25.3,24.8,…` 逗号分隔 / `$K<键><0|1>,<ms>` / `$S<hex码>` / `$A<被确认seq><自身seq>` 双 seq），全部集中写在 `McuFrame.h`/`FrameCodec.cpp` 的**常量与解析函数**里，协议方改口径只动一处。

---

# Phase S — 串口协议层地基

## Task 1: McuFrame 上行帧类型 + 双有界环

**Files:**
- Create: `modules/08_devicemgmt/serial/McuFrame.h`
- Modify: `modules/08_devicemgmt/CMakeLists.txt`（暂只加注释行说明 serial/ 归属，源文件 T5 起登记）

**Step 1: 写头文件**（header-only，无测试——纯数据结构；环的并发正确性在 T12 集成测覆盖）：

```cpp
#pragma once
// McuFrame.h — MCU 上行帧类型 + 分流双有界环（设计方案 §2.4）
// 事件环（K/S/A，容量64，满丢最旧+计数）；遥测环（T，容量8，覆盖旧）。
// SPSC：串口rx线程生产 / 逻辑线程消费。协议默认口径见 §8（未定稿，改只改这里）。
#include "base/types.h"
#include <array>
#include <atomic>
#include <cstdint>
#include <string>

namespace Scanner::device::serial {

enum class KeyId : uint8_t { Up, Left, Middle, Right };   // U/L/M/R
enum class FrameKind : uint8_t { Temperature, Key, Status, Ack };

struct TempFrame   { double celsius[4]; uint8_t channels; uint16_t seq; TimestampMs ts; };
struct RawKeyEvent { KeyId key; bool pressed; uint32_t mcuMs; uint16_t seq; TimestampMs ts; };
struct StatusFrame { uint8_t code; uint16_t seq; TimestampMs ts; };  // 码表待协议方（§8-8 占位）
struct AckFrame    { uint16_t ackedSeq; uint16_t seq; };

// —— 位解析（v3 默认口径；v2 由 MCUDriver 兜底路径绕过）——
// T: "$T25.3,24.8<seq><crc>;"  K: "$KM1,1234<seq><crc>;"  S: "$S0A<seq><crc>;"  A: "$A0B<seq><crc>;"
bool parseTempPayload(const std::string& payload, TempFrame& out);
bool parseKeyPayload(const std::string& payload, RawKeyEvent& out);
bool parseStatusPayload(const std::string& payload, StatusFrame& out);
bool parseAckPayload(const std::string& payload, AckFrame& out);

// —— 丢最旧有界环（复用语义同 06 RingBuffer，但 08 自带轻量实现——分层铁律 C2）——
template <typename T, size_t N>
class SpscRing {
public:
    bool push(T v) {               // 生产者：满则丢最旧
        if (full()) { popCount_.fetch_add(1, std::memory_order_relaxed); head_.store((head_+1)%N); }
        buf_[tail_] = std::move(v); tail_.store((tail_+1)%N); return true; }
    bool pop(T& out) {             // 消费者
        if (empty()) return false; out = std::move(buf_[head_]); head_.store((head_+1)%N); return true; }
    bool empty() const { return head_.load(std::memory_order_acquire) == tail_.load(std::memory_order_acquire); }
    uint64_t dropped() const { return popCount_.load(std::memory_order_relaxed); }
private:
    bool full() const { return (tail_+1)%N == head_.load(std::memory_order_acquire); }
    std::array<T, N> buf_{};
    std::atomic<size_t> head_{0}, tail_{0};
    std::atomic<uint64_t> popCount_{0};
};

} // namespace Scanner::device::serial
```

**Step 2: Commit** `feat(mod08): S-T1 McuFrame上行类型+SPSC双环`

## Task 2: FrameCodec v2/v3 成帧拆帧（TDD）

**Files:**
- Create: `modules/08_devicemgmt/serial/FrameCodec.h` / `serial/FrameCodec.cpp`
- Create: `modules/08_devicemgmt/tests/test_frame_codec.cpp`
- Modify: `modules/08_devicemgmt/CMakeLists.txt`（登记 FrameCodec.cpp + BUILD_TESTS 段）

**Step 1: 写失败测试**（用例=设计 §2.1 全约束 + §7 单元行）：

```cpp
// test_frame_codec.cpp — FrameCodec 单测（回灌字节流，无真机）
#include "serial/FrameCodec.h"
#include <gtest/gtest.h>
using Scanner::device::serial::FrameCodec;

// v3 CRC16-CCITT 查表已知值自校验（"123456789" → 0x29B1）
TEST(Crc16, KnownVector) { EXPECT_EQ(FrameCodec::crc16ccitt("123456789", 0), 0x29B1); }

TEST(FrameCodecV3, HappyPath) {          // "$T25.3,24.80AF12;" → 载荷 "T25.3,24.8"
    FrameCodec c(FrameCodec::Version::V3);
    std::vector<FrameCodec::Frame> out;
    c.feed("$T25.3,24.8", out);           // 先喂前半（无尾符挂起）
    EXPECT_TRUE(out.empty());
    c.advanceTimeout(60);                 // 未到超时前喂尾
    c.feed("0AF1;", out); c.advanceTimeout(60);
    // 注：seq/crc 由测试内组帧器生成——见 Step3 组帧 API encodeFrame()
}
// 其余用例（逐个写成独立 TEST）：粘连两帧一次吐出 / 无'$'垃圾字节被丢 /
// 半帧超时50ms丢弃后复位 / 帧长>32B丢弃 / CRC错丢弃 / seq不匹配丢弃 /
// v2模式：';'分帧直通无校验 / 载荷含'$'或';'组帧被拒（v3）
```

组帧/拆帧共用一套 API（测试与 CommandChannel 都用组帧侧）：

```cpp
// FrameCodec.h 契约
class FrameCodec {
public:
    enum class Version { V2, V3 };
    explicit FrameCodec(Version v);
    struct Frame { std::string payload; uint16_t seq; };        // 拆帧产物（CRC 已验）
    void feed(const std::string& bytes, std::vector<Frame>& out); // 喂字节流，吐完整帧
    void advanceTimeout(int64_t ms);                             // 逻辑时钟推进（半帧超时 A3）
    // 组帧：payload 内禁 '$' ';'（V3 校验拒绝）；seq 由调用方（CommandChannel）给定
    std::string encode(const std::string& payload, uint16_t seq) const;
    static uint16_t crc16ccitt(const std::string& fromDollarToSeq, uint16_t init); // 查表，0x1021/0xFFFF
};
```

**Step 2:** 建测试 → 跑 → **确认编译失败**（FrameCodec 不存在）。
**Step 3: 实现** FrameCodec（CRC16 查表 256 项；v3 解析顺序：`$` 起始 → 收全到 `;` → 剥尾部 seq2+crc4（hex）→ 重算 CRC 比对 → 返回载荷+seq；v2：`[^;]*;` 直通）。
**Step 4:** 跑测试 → 全绿。
**Step 5: Commit** `feat(mod08): S-T2 FrameCodec（v2/v3+CRC16+半帧超时+32B上限）`

## Task 3: SerialPort 纯 IO（并发写拒绝）

**Files:**
- Create: `modules/08_devicemgmt/serial/SerialPort.h` / `serial/SerialPort.cpp`

**Step 1: 实现**（从 `MCUDriver.cpp:26-108` 抽取 CreateFile/DCB/COMMTIMEOUTS/ReadFile/WriteFile 逻辑）：

```cpp
// SerialPort.h 契约（Win32 句柄封装；无协议知识）
class SerialPort {
public:
    Result open(const std::string& port, int baud);   // \\.\ 前缀 + DCB 8N1 + 超时
    Result close();
    bool isOpen() const;
    int read(char* buf, int cap);                     // 返回实读字节数（≤0=超时/错）
    Result write(const std::string& bytes);           // ★只许一个线程调——第二线程调用直接
                                                      //   fail("串口并发写拒绝")（R2-A1 铁律强制点）
    // 缓冲在 open 内 SetupComm(h, 4096, 4096) 调大（C5），不设外部接口
};
```

**Step 2:** 编译通过（Win32 不可单测，行为由 T12 假串口接口 `ISerialIo` 绕过）。
**Step 3: Commit** `feat(mod08): S-T3 SerialPort纯IO（SetupComm4KB+并发写拒绝）`

## Task 4: CommandChannel 非阻塞对账 + 命令组步链（TDD）

**Files:**
- Create: `modules/08_devicemgmt/serial/CommandChannel.h` / `serial/CommandChannel.cpp`
- Create: `modules/08_devicemgmt/tests/test_command_channel.cpp`
- Modify: `modules/08_devicemgmt/CMakeLists.txt`（登记源文件+测试）

**Step 1: 写失败测试**（假 IO：lambda 回灌 ACK；假钟：注入 `nowMs`）。用例=设计 §7 单元行 + R2-A2：
- `send` 立即返回且不调 write 等待（**T10 守护**：send 后 write 只发生 1 次）
- ACK 命中销项 → 完成回调 ok（恰一次）
- ACK 超时重传：假钟推进 100ms → 第 2 次 write；300ms 累计 4 次 write → 完成回调 fail + Fault 回调
- 表容量 8：连续 send 9 条 → 最旧判超时
- 查询类 `sendFireAndForget` 不挂表不重传
- **命令组**：`sendGroup({A,B,C})` → A 的 ACK 后才 write B；B 处 3 败 → C 永不发 + 组失败回调一次；全 ACK → 组成功回调一次
- v2 模式：send 恒 ok「未确认」，无重传无 Fault

**Step 2:** 跑 → 编译失败确认。
**Step 3: 实现**：

```cpp
// CommandChannel.h 契约
struct PendingCmd { uint16_t seq; std::string payload; int retries; int64_t dueMs; };
using DoneCb = std::function<void(bool ok, const std::string& payload)>;

class CommandChannel {
public:
    struct Deps { FrameCodec* codec; std::function<Result(const std::string&)> write;
                  std::function<int64_t()> nowMs; bool reliable;   // reliable=false ⇒ v2 降级
                  int ackTimeoutMs = 100; int maxRetries = 3; };
    explicit CommandChannel(Deps d);
    uint16_t nextSeq();                                            // 0~255 循环
    void send(const std::string& payload, DoneCb onDone);          // 关键命令：挂表+写
    void sendFireAndForget(const std::string& payload);            // 查询（N15）
    void sendGroup(std::vector<std::string> payloads, DoneCb onGroupDone); // 步链：前条 ACK→发下条
    void onAck(uint16_t ackedSeq);                                 // 逻辑线程分流 A 帧时回填
    void tick();                                                   // 逻辑线程 10ms：超时/重传/3败
    std::function<void(const std::string&, int)> onFault;          // Fault 事件出口（DeviceManager 接）
};
```

**Step 4:** 跑 → 全绿。**Step 5: Commit** `feat(mod08): S-T4 CommandChannel（非阻塞ACK对账/重传3败/命令组步链/v2降级）`

## Task 5: IMCU.h 重写 + MCUDriver 瘦身（组合壳 + N10–N16 typed）

**Files:**
- Modify: `modules/08_devicemgmt/IMCU.h`（整文件替换）
- Modify: `modules/08_devicemgmt/MCUDriver.h` / `MCUDriver.cpp`（整文件替换）
- Modify: `modules/08_devicemgmt/CMakeLists.txt`（serial/ 源全登记）

**Step 1: 重写 IMCU.h**——typed 命令（设计 §2.2 表逐一对应）+ 结构化上行：

```cpp
// IMCU.h 契约（hal 层抽象；MCUDriver 实现组合 SerialPort/FrameCodec/CommandChannel）
struct CaptureParams { int freqHz; int bgLight; int laserSelectA; int laserSelectB; int laserLevel; }; // H/B/T/V/L
struct UplinkHandler {
    std::function<void(const serial::TempFrame&)> onTemp;
    std::function<void(const serial::RawKeyEvent&)> onKey;
    std::function<void(const serial::StatusFrame&)> onStatus;
    // ACK 由 MCUDriver 内部消化喂 CommandChannel，不上业务
};
class IMCU {
public:
    virtual Result open(const std::string& port) = 0;      // 内起 rx 线程
    virtual Result close() = 0;
    virtual bool isOpen() const = 0;
    // N10–N16 typed（均经 CommandChannel；onDone=ACK 完成回调，v2 恒立即 ok）
    virtual void setCaptureParams(const CaptureParams&, DoneCb) = 0;   // $N10H..B..T..V..L..
    virtual void startScan(DoneCb) = 0;  virtual void stopScan(DoneCb) = 0;      // N11 H1/H0
    virtual void enterSelfCheck(DoneCb) = 0; virtual void exitSelfCheck(DoneCb) = 0; // N12 Z1/Z0
    virtual void enterStandby(DoneCb) = 0; virtual void exitStandby(DoneCb) = 0; // N13 E1/E0
    virtual void setHeatTarget(int celsius, DoneCb) = 0;                            // N14 T..
    virtual void queryTemperature(int v0to2) = 0;                                   // N15（发不等）
    virtual void enterCalibration(DoneCb) = 0; virtual void exitCalibration(DoneCb) = 0; // N16 B1/B0
    virtual void setUplink(UplinkHandler h) = 0;
    virtual void setProtocolVersion(FrameCodec::Version v) = 0;  // 开关（open 前配；DeviceConfig 注入）
    virtual TimestampMs lastRxTime() const = 0;                  // 通讯心跳时间戳（§4-4）
    virtual uint64_t seqGapCount() const = 0;                    // seq 跳变丢帧计数（对账）
};
```

**Step 2: 重写 MCUDriver**——自有 rx 线程：`SerialPort.read → FrameCodec.feed → 按首字符 T/K/S/A → 双环`；逻辑线程侧 `pump()`（DeviceManager 逻辑线程调）排空环出 UplinkHandler + `onAck` 回填。**删净旧代码**：`sendSoftwareTrigger/setHardwareTriggerMode/setLaserOn/setLaserPower/setLedOn/emergencyStop/isEmergencyStop/getTemperature/registerCallback/startScan 拼帧/parseReceived/E1` 全部不留。
**Step 3:** 全量编译（HardwareMonitor.cpp:43-49 引用旧 IMCU 方法会报错——错误即 T16 改造范围；**本任务顺手最小修补**：HardwareMonitor 的 `mcu_->getTemperature()/isOpen()` 两处暂改为空实现 `#include` 兼容，T16 正式改造）。
**Step 4: Commit** `feat(mod08): S-T5 IMCU重写+MCUDriver瘦身（typed N10-N16/删急停软触发错牌）`

---

# Phase K — 按键链（v3 专属；v2 整链停用 §2.5）

## Task 6: KeyManager 手势判定（TDD）

**Files:**
- Create: `modules/08_devicemgmt/KeyManager.h` / `KeyManager.cpp`
- Create: `modules/08_devicemgmt/tests/test_key_manager.cpp`
- Modify: `modules/08_devicemgmt/CMakeLists.txt`

**Step 1: 写失败测试**（设计 §3 时序规则；事件全用合成 MCU 时刻，时钟注入）：
- 短按：按下@0 → 松开@300 → 静默至@700 → 出 `S`（恰一次）
- 双击：按下@0/松开@100/按下@350/松开@420 → @420 即出 `D`（无 S）
- 长按：按下@0 持续 → @800 即出 `H`（不等松开）；随后松开不再出事件
- 消抖：按下@0 → 松开@15 → 按下@18（<20ms）→ 视为同一次按压
- 互斥：一次物理操作只出 S/D/H 之一
- 乱序/超期（mcuMs 早于已处理）→ 丢弃
- 四键并发各自独立状态

**Step 2:** 编译失败确认 → **Step 3: 实现**：

```cpp
// KeyManager.h 契约
struct GestureThresholds { int debounceMs = 20; int shortMs = 400; int doubleWindowMs = 400; int holdMs = 800; };
struct KeyGesture { KeyId key; enum class G { Short, Double, Hold } gesture; uint32_t mcuMs; };
class KeyManager {
public:
    explicit KeyManager(GestureThresholds t = {});
    void onRawEvent(const serial::RawKeyEvent&);        // 逻辑线程喂
    void tick(int64_t nowMs);                           // 逻辑线程 10ms：S 静默窗到期/长按达阈
    std::vector<KeyGesture> drain();                    // 取走已判定手势
};
```

**Step 4:** 全绿 → **Step 5: Commit** `feat(mod08): K-T6 KeyManager手势判定（短/双/长击+消抖+互斥）`

## Task 7: MenuLogic 菜单记账本（TDD）

**Files:**
- Create: `modules/08_devicemgmt/MenuLogic.h` / `MenuLogic.cpp`
- Create: `modules/08_devicemgmt/tests/test_menu_logic.cpp`

**Step 1: 写失败测试**（2026-08-18 设计 §2.3，纯记账无判断）：
- 默认态：layer=1、cursor=①、adjustCtx=None、modeCursor=1
- `apply(EnterMenu)/apply(ExitMenu)/apply(CursorLeft/Right)`（①←④环绕）
- `apply(CycleMode)`：1→2→3→1
- `apply(CycleAdjustCtx)`：None→View→Brightness→None
- 全字段只经 apply 变化（无裸 setter）——纯函数式表驱动可全遍历

**Step 2-4:** 失败确认 → 实现（enum LedgerOp + 显式状态字段 + apply 转移）→ 全绿。
**Step 5: Commit** `feat(mod08): K-T7 MenuLogic显式账本（层/游标/调节/模式光标）`

## Task 8: KeySemantics 按键裁判（TDD）

**Files:**
- Create: `modules/08_devicemgmt/KeySemantics.h` / `KeySemantics.cpp`
- Create: `modules/08_devicemgmt/tests/test_key_semantics.cpp`

**Step 1: 写失败测试**（转移表=08 文档 §4.2.3 弃奇偶版；门禁/黑板/账本全 Mock lambda）：

| 用例 | 输入态 | 手势 | 期望动作 |
|---|---|---|---|
| 采集开中按启停 | capturing=true, layer=1 | M/S | 发 `CaptureToggle`（→N11 H0） |
| 就绪按启停 | capturing=false, layer=1 | M/S | 发 `CaptureToggle`（→N11 H1） |
| 菜单第2层中键 | layer=2, cursor=③ | M/S | `MenuSelect`（选中退出：③=扫描完成） |
| 双击切模式 | 任意 | M/D | `CycleMode`（账本 modeCursor 1→2） |
| 上键开关菜单 | layer=1 / layer=2 | U/S | `EnterMenu` / `ExitMenu` |
| 双击换调节 | adjustCtx=None | U/D | `CycleAdjustCtx` |
| 左右键移游标 | layer=2 | L/S,R/S | `CursorLeft/Right` |
| 左右键调值 | adjustCtx=Brightness | L/S,R/S | `AdjustDown/AdjustUp`（经 ParamStore） |
| 门禁拒绝 | gate()=false | 任意 | **丢弃**（无任何动作、无账本变） |
| 预留 | 任意 | 任意 H | 丢弃 |

**Step 2-4:** 失败确认 → 实现（`std::function<bool()> gate` + `struct Sources{ bool capturing; Ledger ledger; }` + `struct Actions{…callbacks}` 全注入，纯函数映射无内部状态）→ 全绿。
**Step 5: Commit** `feat(mod08): K-T8 KeySemantics转移表裁判（门禁→查状态源→动作，无奇偶）`

---

# Phase D — 门面（2026-08-18 设计 + 设计方案 §4 五增量）

## Task 9: ModeController 模式黑板（TDD）

**Files:**
- Create: `modules/08_devicemgmt/ModeController.h` / `ModeController.cpp`
- Create: `modules/08_devicemgmt/tests/test_mode_controller.cpp`

**测试**：三态+采集子态读写原子；`request(newMode)` 先问注入门禁（拒→fail 带原因/过→返回 ok 但**不落板**——落板 `commit(newMode)` 由 DeviceManager 在命令组成功回调调（§4-1））；commit 广播回调。全 Mock。
**Commit** `feat(mod08): D-T9 ModeController模式黑板（门禁先问/ACK后落板）`

## Task 10: ParamStore 参数账本（TDD）

**Files:**
- Create: `modules/08_devicemgmt/ParamStore.h` / `ParamStore.cpp`
- Create: `modules/08_devicemgmt/tests/test_param_store.cpp`

**测试**（确认更新制 §4-5）：默认值→`set(key,val)` 走下发回调（注入）→ ACK ok 路径改账+广播；3 败路径保旧值+弹回事件；v2 模式立即改账 `confirmed=false`；两入口（按键/UI）同队列串行（apply 排队不交错）；`persist()/load()` 只经注入的存取回调（06 无直链）；采集中改 N10 参数触发重发标记。
**实现要点**：`std::map<std::string, ParamEntry{value, confirmed, source}>`；命令下发经注入 `std::function<void(const std::string&, double, DoneCb)>`（DeviceManager 接 CommandChannel）。
**Commit** `feat(mod08): D-T10 ParamStore确认更新制账本（双入口串行/落盘注入）`

## Task 11: WarmupSequence 预热看火员（TDD）

**Files:**
- Create: `modules/08_devicemgmt/WarmupSequence.h` / `WarmupSequence.cpp`
- Create: `modules/08_devicemgmt/tests/test_warmup_sequence.cpp`

**测试**（2026-08-18 §2.2）：合成温度序列——先升后稳（连续10s 变化≤0.1℃ 且离目标≤2℃）→ `onStable` 恰一次；永不稳 → 超时（默认15min，测试缩至秒级注入）`onTimeout` 一次且**无停止加热动作**；状态机 待命→加热→稳/超。
**Commit** `feat(mod08): D-T11 WarmupSequence看火员（稳判/超时只报不停）`

## Task 12: DeviceManager 门面 + 逻辑线程（集成 TDD）

**Files:**
- Create: `modules/08_devicemgmt/DeviceManager.h` / `DeviceManager.cpp`
- Create: `modules/08_devicemgmt/tests/test_device_manager.cpp`
- Modify: `modules/08_devicemgmt/CMakeLists.txt`

**Step 1: 写失败集成测**（假串口：MockSerialIo 回灌脚本+假钟；T1–T8 件全真配、IMCU 用假实现）。用例（设计 §7）：
- **T1** open 第3步失败 → 倒序关闭无泄漏 + Fault
- **T2** 门禁拒切扫描 → 无任何 write
- **T3/T4** 预热稳定/超时回调恰一次
- **T5** 采集中菜单键丢弃；启停键按黑板发 N11 H0/H1；连按同值幂等无害
- **T6** 菜单全遍历（4键×3手势×2层×mode1/2/3）账本全覆盖
- **T7** 按键洪峰 100 条 → 丢最旧+警告+逻辑线程不卡
- **T8** 采集中相机断连 → Fault 且**无自主停采**
- **T9** v2/v3 开关切换重连
- **T10** ACK 丢失 → 重传链路 + send 不阻塞逻辑线程（发令后 tick 继续）
- **T11** v2 降级全链（不判败/匿名 K 丢/被动收温+超周期 N15 兜底）
- **T12** enterScan 三连发中段 3 败 → 不擦板+Fault；全 ACK → 擦板+广播

**Step 2: 实现门面**（~200 行目标，组合根）：

```cpp
// DeviceManager.h 对外窗口（两条铁规：不漏零件指针/门禁回调注入——2026-08-18 §1.2）
struct DeviceConfig {
    std::string serialPort;  int baud = 115200;
    FrameCodec::Version protocol = FrameCodec::Version::V3;
    int ackTimeoutMs = 100;  GestureThresholds keys{};
    // 相机/预热阈值/磁盘阈值……构造可换机型
};
class DeviceManager {
public:
    using GateQuery = std::function<bool(const std::string& op)>;
    DeviceManager(DeviceConfig, GateQuery, infra::EventBus*);
    Result open();   Result close();                    // 一条龙/倒着收（含 N12 Z1 自检开——§4-2）
    void enterCalibration(); void enterScan(); void toIdle();   // 命令组步链（N13E0→N16B1 等）
    void startWarmup(int targetC, std::function<void(bool)> done);
    bool isDeviceReady() const;  TempFrame getLastTemperatures() const;
    bool isCapturing() const;    // S4/S5 子态真相源（10 设计 §2.5）
    // 相机薄转发（窗口不漏零件，但转发允许）：
    bool isCameraOpen() const;  Result setCameraExposure(double ms);
    Result startFrameStream(hal::FrameCallback);  Result stopFrameStream();
    void startCapture(); void stopCapture();      // ★采集编排：N10(ParamStore)→N11 H1→相机开流
    ParamStore& params();   // UI 双向同步入口（引用非指针，参数唯一真相源）
    void logicTick();       // 供测试手动驱动；内部线程同函数
private:
    void logicLoop();       // 10ms：pump 上行环→分流→按键链→裁判→账本→CC.tick→warmup.tick→v2 兜底
    // 组合（全 unique_ptr 内部持有，不外泄）：MCUDriver/KeyManager/MenuLogic/KeySemantics/
    // ParamStore/WarmupSequence/ModeController/CameraControl/HardwareMonitor(引用)
};
```

逻辑线程内 Fault 出口统一 `publishFault(code, detail)`（EventBus `FaultOccurred` + spdlog warn，回调短平快铁律 R2-A3）。
**Step 3:** 全绿 + 全量基线绿。
**Step 4: Commit** `feat(mod08): D-T12 DeviceManager门面+逻辑线程（T1-T12集成用例）`

## Task 13: 故障 10 类接线（补全 §6.2 表）

**Files:**
- Modify: `modules/08_devicemgmt/DeviceManager.cpp`（fault 表补全）+ `HardwareMonitor.cpp`（心跳检查挂点预留）

**内容**：§6.2 表 1–7（2026-08-18 七类）+8 ACK 3 败（T4 已有 onFault）+9 seq 跳变（`seqGapCount` 超阈值）+10 心跳丢失（`lastRxTime` 超 10s——检查点在 HardwareMonitor 巡检，经注入回调上报，不发命令 R2-A1）。每类一条 warn 单测（假源触发→EventBus 收到 FaultOccurred）。
**Commit** `feat(mod08): D-T13 故障10类接线（心跳/seq跳变/ACK败→FaultOccurred）`

---

# Phase H — 相机收敛 + 监控全量

## Task 14: CameraControl 收敛

**Files:**
- Modify: `modules/08_devicemgmt/IScannerCamera.h`（删 `setFrameRate/setLaserOn/setLaserPower` 三纯虚）
- Modify: `modules/08_devicemgmt/CameraControl.h/.cpp`（删三实现；`setGain` 真实现 GainRaw；`loadCalibration` 改注入式 `setCalibration(const hal::CameraIntrinsics&, const hal::CameraIntrinsics&, const hal::StereoExtrinsics&)`——08 不解析 json 不做第二真相源 B3）

**Step 1** 改接口 → 全量编译，收集外部引用错误（应为 0——已核实 setFrameRate/setLaser* 全库无 08 外调用者；若有则一并改）。
**Step 2** `setGain`：两侧 `GainRaw` int 特征写入（对照 `applySideParams` 现有曝光写法，CameraControl.cpp:applySideParams 附近）。
**Step 3: Commit** `feat(mod08): H-T14 CameraControl收敛（setGain实装/标定注入式/删三越界方法）`

## Task 15: SelfCheckCollector 上位机自检（TDD——可测部分）

**Files:**
- Create: `modules/08_devicemgmt/SelfCheckCollector.h` / `SelfCheckCollector.cpp`
- Create: `modules/08_devicemgmt/tests/test_self_check_collector.cpp`

**实现**（Win32，磁盘/内存可测、CPU/GPU 尽力而为字段化）：
- 内存：`GlobalMemoryStatusEx` → memPercent
- 磁盘：`GetDiskFreeSpaceExA`（工作盘）→ diskFreeGB（**停写/预警阈值判定归 10-PerfMonitor，08 只采集**——P3）
- CPU 占用：PDH `\Processor(_Total)\% Processor Time`（首次调 0 返回，二次起有效——单测断言 -1 或 [0,100]）
- CPU 温度：WMI `MSAcpi_ThermalZoneTemperature` 尽力而为，不可得=-1（Honest：不编数）
- GPU：`LoadLibraryA("nvml.dll")` 动态加载（失败=-1 全字段，无链接依赖）→ nvmlDeviceGetHandleByIndex(0) → temp/mem
- 线程心跳：注册表 `registerHeartbeat(name)` + `beat(name)` + 超时清单
- `snapshot()` 合成 `HealthMetrics`（base/types.h:95）——**单测**：内存/磁盘字段 >0；GPU/CPU 温度字段允许 -1；心跳超时判定注入假钟

**Commit** `feat(mod08): H-T15 SelfCheckCollector（PDH/NVML动态加载/磁盘双防线数据源）`

## Task 16: HardwareMonitor 升级

**Files:**
- Modify: `modules/08_devicemgmt/HardwareMonitor.h` / `HardwareMonitor.cpp`

**改造**：
- 删 `setMCU`/IMCU 依赖（T5 遗留兼容层一并清）——心跳时间戳由 DeviceManager 注入 `std::function<TimestampMs()>`
- 删轮询取温路径；温度读 `DeviceStateCache`（08 侧 DeviceManager 写入）
- 增 `setProcessCounter/setDropCounter` 注入（帧率三件套；采集帧率沿用现有 `setFrameCounter`）
- 增 `setSelfCheck(SelfCheckCollector*)`；巡检循环组装 `HealthMetrics snapshot() const`（**供 app 适配器喂 10-PerfMonitor**）
- 巡检只采集广播（cache push + EventBus），阈值判定归 10（P3）；心跳超 10s 只**经注入回调上报**（R2-A1 不发命令）
**Commit** `feat(mod08): H-T16 HardwareMonitor（删IMCU轮询/三件套/HealthMetrics快照）`

---

# Phase A — app 装配收拢（scan_demo 条件构建内）

## Task 17: AppContext/ScannerWindow/MainWindow 收口

**Files:**
- Modify: `app/AppContext.h/.cpp`、`app/WorkflowContext.h/.cpp`、`app/ScannerWindow.h/.cpp`、`app/MainWindow.cpp`

**Step 1: AppContext**（§3.4 三行目标）：
- `camera_/mcu_/hwMonitor_` 三成员与装配（AppContext.cpp:143-155）→ 收进 `DeviceManager::open()`；AppContext 增 `deviceManager_`（构造+open+close 三行）
- `shutdown()`（AppContext.cpp:198-207）改为 `deviceManager_->close()`
- FaultHandler `setSafeStopCallback`（AppContext.cpp:46 TODO）→ 接 `deviceManager_->toIdle()`
- 10 侧 P3 接线：app 内小适配器 `struct HealthProviderAdapter : service::IHealthProvider { … snapshot() → hwMonitor->snapshot() }` 注入 `perfMonitor_->setProvider(...)`（08 不链 10，适配在组合根）；`poll()` 挂 QTimer 1s 或巡检回调
- S1 自检（AppContext.cpp:209-228）：`notifySelfCheckItem("serialPort")` 改由 DeviceManager open 结果回填
**Step 2: WorkflowContext**——删 `setMCU/mcu()/mcu_`（已核实全库无消费者；AppContext.cpp:166 注入行同删）
**Step 3: ScannerWindow**——
- 删 `m_cam` 裸指针与 `m_serialPort1/2`/`buildStartCommand`/`sendData`（**下位机旁路私摸串口**——08 红线）；onStartScanner（ScannerWindow.cpp:295-351）改：`deviceManager->startCapture()`（内含 N10/N11+相机开流）+ 既有 gate submit；onStopScanner 同理 `stopCapture()`
- 曝光滑条 → `deviceManager->params()`（ParamStore 双向同步入口）
**Step 4: MainWindow**——`series->getCameraControl()` 三处（MainWindow.cpp:200,251,439）改门面薄转发（`isCameraOpen/grabFrame`）；状态栏温度读 `MCU_T0..T3`（保留 `MCU` 兼容）
**Step 5:** 全量构建含 scan_demo（`cmake --build build --config Debug`）→ 基线绿。
**Step 6: Commit** `feat(mod08): A-T17 app装配收拢（三行门面/串口旁路收口/HealthProvider接线）`

## Task 18: 收尾全量验证 + 文档回写

**Step 1:** 全量构建 + ctest（Debug）全绿；`ctest --test-dir build-rel -C Release` 抽验（可跳：无 CUDA 依赖改动）。
**Step 2: 过线标准核验**（设计 §7.2）：
```powershell
# 对外头文件 grep 不到子零件类型
rg "CameraControl|MCUDriver|SerialPort|FrameCodec|CommandChannel" modules/08_devicemgmt/DeviceManager.h
# 期望：0 命中（前向声明除外）
rg -c "deviceManager_" app/AppContext.cpp   # 装配 ≤3 行语义核验
```
**Step 3: 文档回写**：设计方案头注「已实施」+ 本计划头部补「已完成」；`docs/模块功能/08-设备管理.md` §2 现状表按落地事实刷新（DeviceManager/KeyManager 由「⚠没造」改为实况；§3 差距清单核销）——**四段式结构不动，仅现状/差距更新**。
**Step 4: Commit** `docs(mod08): A-T18 实施收尾（08文档现状刷新/设计计划回写）`

---

## 依赖与顺序（执行纪律）

```
S: T1 → T2 → T3 → T4 → T5（T5 依赖 T1-T4 全部）
K: T6 ∥ T7 → T8（T8 依赖 T6/T7 类型）——K 可与 S 尾部并行
D: T9 ∥ T10 ∥ T11 → T12（汇合）→ T13
H: T14 ∥ T15 → T16（T16 依赖 T15；T14 独立）
A: T17（依赖全部）→ T18
每 Task 一 commit、每 Phase 收尾跑全量基线；任何基线变红先修再继续。
```
