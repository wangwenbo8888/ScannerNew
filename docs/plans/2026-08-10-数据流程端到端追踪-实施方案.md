# 数据流程章节实施计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 在 `框架整体.md` 新增 §6「数据流程（端到端追踪）」，基于算子库代码与标定/扫描两条主工作流，梳理从相机帧到点云产物的端到端数据流，含每环节数据类型与内存/显存流转关系。

**Architecture:** 单文件编辑——在 `框架整体.md` §5 状态机之后、§6「后续」之前插入新 §6（6 个小节），当前 §6 顺延为 §7。内容已在设计文档 `docs/plans/2026-08-10-数据流程端到端追踪-design.md` 中详细规定。

**Tech Stack:** Markdown / ASCII 图表（沿用文档现有风格）

**设计文档:** `docs/plans/2026-08-10-数据流程端到端追踪-design.md`（权威内容来源）

---

## 前置：通读设计文档

**Step 0: 读设计文档**

读取 `docs/plans/2026-08-10-数据流程端到端追踪-design.md` 全文，确认各小节内容规格。
读取 `框架整体.md` 当前 §5 结尾（行 329 附近）和 §6 开头（行 331 附近），确认插入点。

---

## Task 1: 章节编号顺延（§6→§7）

**Files:**
- Modify: `框架整体.md`（§6「后续」→§7，§6.1→§7.1）

**Step 1: 修改 §6 标题**

将 `## 6. 后续` 改为 `## 7. 后续`

**Step 2: 修改 §6.1 标题**

将 `### 6.1 修订遗留待复审项` 改为 `### 7.1 修订遗留待复审项`

**Step 3: 验证编号连续性**

Grep `框架整体.md` 确认 §1~§5 不变，新 §6 待插入位（§5 之后），§7 已就位。确认无其他 `## 6.` 或 `### 6.1` 残留。

---

## Task 2: 插入 §6.0 数据旅程总览 + §6 导读

**Files:**
- Modify: `框架整体.md`（在 §5.7 落地依赖结束后、原§6 之前插入）

**Step 1: 写 §6 标题 + 导读 + §6.0 总览图**

内容来源：设计文档 §3.1。包含：
- `## 6. 数据流程（端到端追踪）` 标题
- 导读段（§2 静态架构 / §5 控制流 / §6 数据流 三者正交）
- §6.0 三段式横向总览图（标定→交接→扫描）
- 标注要素：层归属、hot/cold、两条独立标定流水线、A/B 变点、GlobalOptimStage、skip-layer

**Step 2: 验证**

Read 插入区域，确认 §5 → 新§6 → §7 顺序正确，分隔线 `---` 齐全。

---

## Task 3: 插入 §6.1 标定数据流追踪

**Files:**
- Modify: `框架整体.md`（§6.0 之后追加）

**Step 1: 写 §6.1 导读 + §6.1.1 第一流水线**

内容来源：设计文档 §3.2 §6.1.1。包含：
- §6.1 导读（离线一次性，产出两张温度表）
- §6.1.1 Stage0/1/2 描述 + 姿态判断链算子表（14 算子，含设备/输入类型(域)/输出类型(域)/跨域标注）
- 标注 GPU→CPU 分界在 2-2→2-3

**Step 2: 写 §6.1.2 第二流水线**

内容来源：设计文档 §3.2 §6.1.2。包含：
- Stage3 相机标定表（3-1~3-5/5-1/5-2，全 CPU）
- Stage4 激光标定表（4-1~4-8/PJC/4-12/4-13/5-3，GPU→CPU）
- 标注 4-8→聚合→PJC 的 D2H download；Stage3→Stage4 数据依赖

**Step 3: 验证数据类型**

对照设计文档核对每个 Result struct 名拼写（`MaskExtractResult`/`RegionAnalysisResult`/`StereoUndistortResult` 等），确认与代码头文件一致。

---

## Task 4: 插入 §6.2 标定→扫描交接契约

**Files:**
- Modify: `框架整体.md`（§6.1 之后追加）

**Step 1: 写 §6.2 交接契约表**

内容来源：设计文档 §3.3。包含：
- 导读（全系统唯一跨工作流数据耦合，契约在 `calib_result_types.h`）
- 三行表：StereoRectifyTempTable / LaserPlaneMapTempTable / 补偿表
- 列：产出算子/消费算子/数据类型/查表域/存储路径
- 补充说明（温度阶梯/lookup 不抛异常/模块8喂温度/已知集成缺口 leftToRightMap）

**Step 2: 验证**

确认查表域标注正确（表1=Host内存，表2=Device显存），与设计文档一致。

---

## Task 5: 插入 §6.3 扫描数据流追踪

**Files:**
- Modify: `框架整体.md`（§6.2 之后追加）

**Step 1: 写入口 + 预处理分叉**

内容来源：设计文档 §3.4。包含：
- 导读（运行时流式 180fps，热路径）
- 入口（HAL→RingBuffer→双副本）
- 预处理表（mask_separation + ccl）

**Step 2: 写标记点分支 + 激光分支**

内容来源：设计文档 §3.4。包含：
- 标记点分支表（03~11+配准，全 CPU，标注 06 查表1）
- 激光分支表（04~08，全 CUDA，标注 05 查表1、07 查表2、①变点）

**Step 3: 写融合 + 显示 + GlobalOptimStage + A/B 变点**

内容来源：设计文档 §3.4。包含：
- 融合表（marker_cloud_fuse Host / laser_cloud_fuse_cuda Device，标注持久性）
- 显示表（marker 上传VBO / 激光零拷贝 interop）
- GlobalOptimStage（逐帧obs输入，批处理，display冻结）
- A/B 变点标注（①激光链跳过）

**Step 4: 验证**

确认每个查表引用（§6.2表1/表2）指向正确；确认 ① 变点标注与扫描流水线文档一致。

---

## Task 6: 插入 §6.4 数据类型字典 + §6.5 内存/显存流转图谱

**Files:**
- Modify: `框架整体.md`（§6.3 之后追加）

**Step 1: 写 §6.4 数据类型字典**

内容来源：设计文档 §3.5。包含 4 组类型表：
- 标定→扫描契约（calib_result_types.h）
- 标记点链（zitai_result_types.h）
- 管线聚合（pipeline_types.h）
- 扫描融合（各算子头文件）
- 每个类型标：定义头文件/产出/消费/内存域

**Step 2: 写 §6.5 内存/显存流转图谱**

内容来源：设计文档 §3.6。包含：
- 双副本投递机制图
- 关键跨域转换点表（T1~T6）
- 跨帧持久缓冲表（Host/Device 分列）
- 查表路径表（Host内存表/Device显存表）

**Step 3: 验证**

确认 d_keys/d_fusedIdx/d_fusedXyz/d_fusedNormal 拼写与 `laser_cloud_fuse_cuda.h:59-68` 一致；确认 MarkerCloudPoint 字段与 `marker_cloud_fuse_cpu.h:33-41` 一致。

---

## Task 7: 更新文档头部修订日志

**Files:**
- Modify: `框架整体.md`（头部 `> **本次修订要点**` 区域）

**Step 1: 追加修订日志条目**

在头部修订日志追加：
```
> - §6 新增「数据流程（端到端追踪）」：基于算子库代码+标定/扫描两条主工作流，端到端追踪相机帧→点云产物的数据流，含每环节数据类型与内存/显存流转（2026-08-10）
> - 原 §6「后续」顺延为 §7
```

**Step 2: 更新日期行**

修订日期补充 2026-08-10 增补。

---

## Task 8: 全文验证

**Step 1: 编号连续性**

Grep `框架整体.md` 的所有 `## N.` 和 `### N.N` 标题，确认 §1~§7 连续无跳号。

**Step 2: 数据类型抽检**

随机抽 5 个 Result struct 名，grep 算子库头文件确认存在且拼写一致：
- `StereoRectifyTempTable` → calib_result_types.h
- `PointReconstructCPUResult` → zitai_result_types.h
- `LaserCloudFuseDeviceContext` → laser_cloud_fuse_cuda.h
- `MarkerCloudPoint` → marker_cloud_fuse_cpu.h
- `CclOutput` → pipeline_types.h

**Step 3: 交叉引用完整性**

确认 §6.3 中对「§6.2表1/表2」的引用、§6.5 中对「§6.2」的引用均指向正确。

**Step 4: 阅读完整性**

Read 新 §6 全文，确认叙事连贯（总览→标定→交接→扫描→字典→内存图谱），无断层。

---

## 完成标志

- [ ] §6 六个小节全部就位，内容与设计文档一致
- [ ] §7（原§6）编号顺延完成
- [ ] 头部修订日志已更新
- [ ] 数据类型拼写与代码头文件一致
- [ ] 章节编号 §1~§7 连续无跳号
