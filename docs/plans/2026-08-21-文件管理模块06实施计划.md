# 文件管理模块 06 实施计划（批 A/B/C · T1–T16）

> 2026-08-21 计划审查修订 17 处（I1 载荷 Mat 形状对齐目标类型（P1/P2=3×4、Q=4×4）/ I2 销毁序判据修正（session_ 声明在 pipeline_ 之前）/ I3 T12→T13 中间态警告成文 / I4 T14 补 existingMarkers 注入 / I5 T12 补姿态环切换（删 01 自有 cycleRing_）/ I6 ScanSessionData 槽位构造期定死 / I7 T2 补 §8-7 口径销账 / I8 补并发写读用例 + M1-M9：分支步骤成文、rg 精确模式、许可证行勿动、tmp 路径、Release 成本提示、T9 include 验证等，已逐条并入正文）。

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 落地 [`2026-08-21-文件管理模块06设计方案.md`](2026-08-21-文件管理模块06设计方案.md)（两轮审查定稿）——仓库两层立起来（CalibrationRepository＋点云仓库三用途）、会话件两版（ScanSessionData/CalibSessionData＋出口查表接线）、file_io 收库解 OSG、死代码清退。

**Architecture:** 三批递进各独立全绿——批 A 清退（只删零消费者 Pipeline＋文档销账）；批 B 仓库（CalibrationRepository JSON 单格式、PointCloudBuffer 正名扩容、file_io 收库，新旧并存）；批 C 会话件与切换（01/02/app 装配切仓库、删 CalibStore/mod_calibration、三套存取收敛）。设计 § 引用均指设计方案文档。

**Tech Stack:** C++17 / nlohmann_json（FetchContent 已有，目标名 `nlohmann_json::nlohmann_json`）/ OpenCV 4.13 / gtest。

**分支操作（批 A 首步执行）：** 当前在 feat/mod07-pipelinemgmt，先 `git switch -c feat/mod06-fileio`（人工指定则从其指定）。

---

## 全局约定（每个任务都遵守）

1. **禁区**：`factory_calib/` 只读；🔒 定稿文档（01/02 §1§2、模块功能目录 §三）不改。不确定的接口先读头文件，**不猜**。
2. **构建/测试**（Debug，`build/` 已配置）：
   ```powershell
   cmake --build build --config Debug --target <test_exe>
   $env:PATH = 'C:\opencv-cuda-4.13.0-debug\x64\vc17\bin;' + $env:PATH
   ctest --test-dir build -C Debug -R "^<test_exe>$" --output-on-failure
   ```
   阶段收尾全量：`cmake --build build --config Debug` ＋ 全量 ctest——**基线 90/90 只增不减**。scan_demo 仅在 Qt/stubs 齐备时构建（本机已齐）。
3. **提交**：每 Task 一 commit，风格 `feat(mod06): B-T6 CalibrationRepository写入落盘`。**只 add 本任务文件**（严禁 `git add -A`）。
4. **风格**：`#pragma once` ＋ 文件头注释块（仿 `modules/06_fileio/SlotRing.h`）；命名空间 `Scanner::data`（fileio 子空间 `Scanner::data::fileio`）；中文契约级注释；**不加多余注释**。
5. **测试注册**：`modules/06_fileio/CMakeLists.txt` 加 BUILD_TESTS 段（GLOB ＋ 每 exe 一 add_test ＋ `jmw_deploy_crt`，仿 `modules/07_pipelinemgmt/CMakeLists.txt:39-54`）——T3 一次建好，后续任务零 CMake 改动。
6. **Mat JSON 格式**（仓库解析必须匹配，`09/json_utils.h:55-65`）：二维嵌套数组、CV_64F 行主序（`[[r0c0,r0c1...],[r1...]...]`）。
7. **销毁顺序铁律（设计 §4.4）**：停采集→停 07 流水线→再销毁会话件（ring 最后死）——T14 装配时核对。

---

# 批 A — 清退（可独立全绿）

## Task 1: 删 Pipeline.{h,cpp}（Stage 零消费者）

**Files:**
- Delete: `modules/06_fileio/Pipeline.h`
- Delete: `modules/06_fileio/Pipeline.cpp`
- Modify: `modules/06_fileio/CMakeLists.txt:7`（去 `Pipeline.cpp`）

**Step 1:** 确认零引用（精确模式——裸 `Pipeline.h` 会误报全部 `*Pipeline.h`）：
```powershell
rg -l '#include "Pipeline\.h"|workflow::Pipeline' --glob "!build*/**"
```
**Step 2:** 删两文件；CMakeLists `add_library` 源列表去掉 `Pipeline.cpp`。
**Step 3:** 全量构建＋全量 ctest（90/90 绿——`IWorkflow.h` 不动，01/02/04 仍实现它）。
**Step 4:** `git add modules/06_fileio && git commit -m "refactor(mod06): A-T1 删零消费者Pipeline/Stage原语(D3)"`

## Task 2: 文档销账（D2/D3/§8-7 口径兑现）

**Files:**
- Modify: `框架整体.md` §1（:97 附近「〔模块8〕设备状态缓存＋许可证存储」——**只改设备状态缓存归属为〔模块6〕，许可证存储〔模块8〕不动**）
- Modify: `docs/模块功能/06-文件管理.md`

**Step 1:** `框架整体.md` §1 DataStore 框内「设备状态缓存」归属标注〔模块8〕→〔模块6〕（前缀式写法照原文格式改，勿动许可证行）。
**Step 2:** 06 文档四处：§3 待裁决表两行改为「已裁（2026-08-21 设计方案 D2/D3）：留 06 改蓝图／删 Stage-Pipeline 留 IWorkflow」；§1「流水线原语」行去掉 Pipeline/Stage 只留 IWorkflow＋ParameterManager；**§1/§2/§3「ScanConfig（06 会话件/开工三件套）」口径改「配置归 07 ScanConfig（ScanTypes.h），06 会话件只管装表＋ring（设计方案 §8-7）」**；§2 对应现状行同步（Pipeline 已删）。文头加一行修订记录。
**Step 3:** `git add 框架整体.md docs/模块功能/06-文件管理.md && git commit -m "docs(mod06): A-T2 两项裁决销账+蓝图标注(D2/D3)"`

---

# 批 B — 仓库（新旧并存，可独立全绿）

## Task 3: file_io 收库＋类型/命名空间改造（TDD）

**Files:**
- Create: `modules/06_fileio/tests/test_file_io.cpp`
- Modify: `modules/06_fileio/file_io.h`（全量重写头）
- Modify: `modules/06_fileio/file_io.cpp`（namespace＋osg→cv）
- Modify: `modules/06_fileio/CMakeLists.txt`（源＋file_io＋BUILD_TESTS 段＋链 nlohmann_json 顺带 T6 用）
- Modify: `app/CMakeLists.txt:60-63,81`（删 MOD_FILEIO 组与引用）
- Modify: `app/MainWindow.cpp:789,811,889,899,908-909`
- Modify: `modules/03_rendering/OSGWidget.cpp:3,1548-1550`
- Modify: `modules/04_postprocessing/PostProcessWorkflow.cpp:13,33-45`

**Step 1: 写失败测试** `tests/test_file_io.cpp`：

```cpp
#include <gtest/gtest.h>
#include "file_io.h"
#include <cstdio>
#include <fstream>
#include <filesystem>

using namespace Scanner::data;

static std::string tmp(const char* name) {
    auto p = std::filesystem::temp_directory_path() / name;
    return p.string();
}

TEST(FileIo, PlyAsciiRoundtrip) {
    std::vector<cv::Point3f> pts = {{1.f,2.f,3.f},{4.f,5.f,6.f}};
    auto p = tmp("t06_io.ply");
    ASSERT_TRUE(fileio::exportPLY(p, pts));
    std::vector<cv::Point3f> out;
    ASSERT_TRUE(fileio::importPLY(p, out));
    ASSERT_EQ(out.size(), 2u);
    EXPECT_FLOAT_EQ(out[1].x, 4.f);
}
// 同构再写：Xyz / Pcd / StlMesh / ObjMesh / Markers 五个往返 TEST（STL/OBJ 走
// fileio::MeshData{vertices,indices,normals}；markers 走 exportMarkers/importMarkers）
TEST(FileIo, AutoDetectImport) {
    // .xyz 后缀文件经 importPointCloud 自动分派
}
TEST(FileIo, BadFileReturnsFalse) {
    std::vector<cv::Point3f> out;
    EXPECT_FALSE(fileio::importPointCloud("Z:/no/such.ply", out));
    std::ofstream(tmp("t06_bad.ply")) << "not a ply";
    EXPECT_FALSE(fileio::importPLY(tmp("t06_bad.ply"), out));
}
```

**Step 2:** CMakeLists：`add_library` 加 `file_io.cpp`；尾部加 BUILD_TESTS 段（整段照抄 07/CMakeLists.txt:39-54，把 `mod_pipelinemgmt` 换 `mod_fileio`）；同时加 `target_link_libraries(mod_fileio PUBLIC nlohmann_json::nlohmann_json)`（T6 用，一并落地）。
**Step 3:** 构建 test_file_io → **预期编译失败**（`Scanner::data::fileio` 不存在）。
**Step 4: 改造 file_io**——`file_io.h` 全量重写：`#include <opencv2/core.hpp>` 换 `<osg/Vec3>`；`namespace file_io` → `namespace Scanner::data::fileio`；全部 `osg::Vec3` → `cv::Point3f`（MeshData 同步）。`.cpp` 跟改 namespace 与实现内 osg 用法（读写逻辑不动，仅类型）。
**Step 5:** 构建全量——**预期 3 处调用面红**，逐一适配：
- MainWindow：`file_io::` → `Scanner::data::fileio::`；导入出的 `std::vector<cv::Point3f>` 就地转 `osg::Vec3(p.x,p.y,p.z)` 填 UI 容器；导出前反向转换
- OSGWidget:1548-1550：`fileio::MeshData` 后 `mesh.vertices` 循环转 osg
- PostProcessWorkflow:33-45：`exportStlViaFileIo` 直接填 `fileio::MeshData{cv::Point3f}`（07 mesh 的 float xyz 直填，注释「app 编译单元限制」句删）
**Step 6:** app/CMakeLists 删 `set(MOD_FILEIO ...)` 组（:60-63）与 `${MOD_FILEIO}`（:81）。
**Step 7:** test_file_io 绿＋全量构建绿＋全量 ctest 绿。
**Step 8:** commit `feat(mod06): B-T3 file_io收库+解OSG+入data命名空间`

## Task 4: PointCloudBuffer marker 通道（TDD）

**Files:**
- Modify: `modules/06_fileio/PointCloudBuffer.h/.cpp`
- Create: `modules/06_fileio/tests/test_cloud_warehouse.cpp`

**Step 1: 失败测试**：
```cpp
#include <gtest/gtest.h>
#include "PointCloudBuffer.h"
using namespace Scanner::data;

TEST(CloudWarehouse, MarkersRoundtrip) {
    PointCloudBuffer wh;
    std::vector<MarkerRecord> in = {{7, {1.f,2.f,3.f}, {0.f,0.f,1.f}}};
    wh.setMarkers(in);
    std::vector<MarkerRecord> out;
    uint64_t v = 0;
    EXPECT_TRUE(wh.snapshotMarkers(v, out));
    ASSERT_EQ(out.size(), 1u);
    EXPECT_EQ(out[0].globalId, 7u);
    EXPECT_FLOAT_EQ(out[0].pos.y, 2.f);
}
TEST(CloudWarehouse, MarkersVersionBumps) { /* setMarkers 两次 → version 递增 */ }
TEST(CloudWarehouse, ConcurrentMarkerWriteRead) {
    // 渲染线程模拟：写线程 setMarkers 循环 × 读线程 snapshotMarkers 循环 2s 无死锁无崩
    // （marker 通道独立 shared_mutex 的并发正确性验证点——设计 §7 用例）
}
```
**Step 2:** 跑测试确认编译失败。**Step 3: 实现**——`PointCloudBuffer.h` 加：
```cpp
// 点云仓库 marker 通道（续扫基准，设计 §3）：会话尾产物走专有方法，不入 IPointCloudSink
struct MarkerRecord {
    uint32_t globalId = 0;      // 07 MarkerCloudPoint 的 globalId（06 不链 09，自有轻量结构）
    cv::Point3f pos;
    cv::Vec3f normal;
};
// 类内：
void setMarkers(const std::vector<MarkerRecord>& markers);   // 独立 marker 版本号
bool snapshotMarkers(uint64_t& version, std::vector<MarkerRecord>& out) const;
```
`.cpp`：marker 专用 `std::shared_mutex` ＋独立 `std::atomic<uint64_t> markerVersion_`（与点云版本分家，互不干扰）。
**Step 4:** 测试绿 → commit `feat(mod06): B-T4 点云仓库marker通道(globalId保真)`。

## Task 5: PointCloudBuffer 按需导出（TDD）

**Files:** 同 T4 两文件

**Step 1: 失败测试**：`exportCloud(tmp.ply)` 后 `fileio::importPLY` 读回点数一致；`exportMarkers(tmp.json)` 后 `fileio::importMarkers` 读回数量一致；空仓库导出返回 true（空文件）或 false——按实现定并断言之（推荐 true）。
**Step 2:** 实现：`exportCloud(const std::string& path)`（锁内拷贝 points/colors → `fileio::exportPointCloud(path, pts)`；.ply/.xyz/.pcd 由 fileio 按扩展名分派）＋ `exportMarkers(const std::string& path)`（MarkerRecord.pos → fileio 标志点格式）。
**Step 3:** 绿 → commit `feat(mod06): B-T5 点云/标志点按需导出(02-D5唯一出口)`。

## Task 6: CalibrationRepository 骨架——write/load/原子落盘（TDD）

**Files:**
- Create: `modules/06_fileio/CalibrationRepository.h/.cpp`
- Create: `modules/06_fileio/tests/test_calib_repository.cpp`

**Step 1: 失败测试**——手工构造与 07 `serializeCalib` 真键一致的载荷（键名证据：`CalibSerialize.cpp:23-43,113-128`）：
```cpp
#include <gtest/gtest.h>
#include "CalibrationRepository.h"
using namespace Scanner::data;

// 构造最小合法载荷（真实键名照抄 CalibSerialize.cpp；Mat 形状对齐目标类型——
// P1/P2=3×4、Q=4×4、其余 3×3，T7 matxFromJson 逐元素转换的前提）
static std::string samplePayload() {
    auto mat3 = [](double v){ return nlohmann::json::array({nlohmann::json::array({v,0,0}),{0,v,0},{0,0,1}}); };
    auto mat34 = [](double v){ return nlohmann::json::array({{v,0,0,0},{0,v,0,0},{0,0,v,0}}); };
    auto mat44 = [](double v){ return nlohmann::json::array({{v,0,0,0},{0,v,0,0},{0,0,v,0},{0,0,0,1}}); };
    nlohmann::json j = {
      {"stereo", {{"cameraMatrixL", mat3(1000)}, {"distCoeffsL", nlohmann::json::array({0,0,0,0,0})},
                  {"cameraMatrixR", mat3(1001)}, {"distCoeffsR", nlohmann::json::array({0,0,0,0,0})},
                  {"R", mat3(1)}, {"T", nlohmann::json::array({10,0,0})},
                  {"R1", mat3(1)}, {"R2", mat3(1)}, {"P1", mat34(1)}, {"P2", mat34(1)}, {"Q", mat44(1)},
                  {"reprojError", 0.5}}},
      {"tempTables", {
        {"intrinsic", {{"success", true}, {"table", nlohmann::json::array()}}},
        {"extrinsic", {{"success", true}, {"table", nlohmann::json::array()}}},
        {"rectify", {{"success", true}, {"table", nlohmann::json::array({
            {{"temperature", 25.0}, {"R1", mat3(1)}, {"R2", mat3(1)}, {"P1", mat34(1)}, {"P2", mat34(1)}, {"Q", mat44(1)}}})}}},
        {"laserExtrinsic", {{"success", true}, {"table", nlohmann::json::array()}}}}},
      {"pjc", {{"success", true}}},
      {"planeMap", {{"map", {{"success", true}}}, {"tempTable", {{"success", true}, {"table", nlohmann::json::array({
          {{"temperature", 25.0}, {"deltaT", 0.0}, {"totalPairs", 10}}})}}}}},
      {"quality", {{"ok", true}, {"items", nlohmann::json::array()}}}};
    return j.dump();
}

TEST(CalibRepository, WriteLoadRoundtrip) {
    CalibrationRepository repo;
    auto path = tmp("t06_repo.json");                  // 显式 tmp 路径（勿用默认相对路径污染构建树）
    ASSERT_TRUE(repo.write(samplePayload(), cv::Size(2560, 1440), path).success);
    CalibrationRepository repo2;
    ASSERT_TRUE(repo2.load(path).success);
    EXPECT_EQ(repo2.stereo().imageSize, cv::Size(2560, 1440));
    EXPECT_EQ(repo2.stereoTempTable().tiers.size(), 1u);   // T7 起有此断言
}
TEST(CalibRepository, BadJsonFailsNotCrash) {
    CalibrationRepository repo;
    EXPECT_FALSE(repo.write("{ not json", cv::Size(1,1)).success);
}
TEST(CalibRepository, AtomicWriteNoHalfFile) { /* write 后文件可完整 parse */ }
```
**Step 2:** 失败确认。**Step 3: 实现头** `CalibrationRepository.h`：
```cpp
#pragma once
// CalibrationRepository.h — 标定结果仓库（app 存活件；设计 §2）
// 一格仓库两副面孔：结果仓库(01-⑦落盘持久)+参数仓库(02-①门禁查询)。
// JSON＝07 serializeCalib 载荷原样＋外层 meta{version,createdAt,calibId,imageSize}。
// GpuMat 设备数据不落盘(07 已定)；激光档表=温补相机参数原文(无网格数据，§8-1)。
#include "base/types.h"
#include "TempTableTypes.h"
#include <nlohmann/json.hpp>
#include <opencv2/core.hpp>
#include <mutex>
#include <string>
#include <vector>

namespace Scanner::data {

struct StereoData {                       // stereo 键 + meta.imageSize 汇总
    cv::Mat cameraMatrixL, cameraMatrixR, distCoeffsL, distCoeffsR;
    cv::Mat R, T, R1, R2, P1, P2, Q;      // R/T＝外参
    cv::Size imageSize;
};

struct ReadyReport {
    bool ready = false;
    std::vector<std::string> missing;     // 缺哪样报哪样
};

class CalibrationRepository {
public:
    // 写入端（01 适配 ICalibRepoWriter）：解析→校验→填内存→临时文件+原子改名落盘
    Scanner::Result write(const std::string& payloadJson, cv::Size imageSize,
                          const std::string& path = "calibration.json");
    Scanner::Result load(const std::string& path);
    // typed getters（T7/T8 实现）
    StereoData stereo() const;
    StereoTempTable stereoTempTable() const;         // tempTables.rectify.table → 06 档类型
    PlaneMapTempTableRef planeMapTiers() const;      // planeMap.tempTable.table 档温索引
    nlohmann::json planeMapTempTableRaw() const;     // 激光档表参数原文(温补相机参数)
    nlohmann::json tempTablesRaw() const;            // 相机四表原文
    Scanner::Result readyForScan(ReadyReport& out) const;
    void clear();
    const std::string& lastPath() const { return lastPath_; }
private:
    mutable std::mutex mtx_;
    nlohmann::json doc_;                   // 载荷+meta 全量内存态
    bool hasData_ = false;
    std::string lastPath_;
};

} // namespace Scanner::data
```
`.cpp` 本任务实现 write/load/clear/lastPath：write＝`nlohmann::json::parse`(catch 返 fail)→校验五键存在→组 `{version,createdAt(ISO),calibId(uuid 简版),imageSize{w,h},…载荷五键}`→`path+".tmp"` 写出→`std::filesystem::remove(path,ec)`＋`rename`（Windows rename 不覆盖既有，先删后改）；load＝读文件→parse→填 doc_。
**Step 4:** 测试绿 → commit `feat(mod06): B-T6 CalibrationRepository写入/加载/原子落盘`。

## Task 7: typed getters（TDD）

**Files:** 同 T6

**Step 1: 失败测试**（追加）：`stereo()` 五 Mat 元素值往返（`jsonToMatAuto` 格式：二维数组）；`stereoTempTable().tiers[0].tempC==25.0` 且 R1 非零；`planeMapTiers().tiers.size()==1`；`planeMapTempTableRaw()` 含 `deltaT` 键；`tempTablesRaw()` 含四表键。
**Step 2:** 实现——.cpp 内静态助手 `matFromJson(const nlohmann::json&)`（**照抄 09 json_utils.h:55-65 语义自建**，06 不链 09）＋ `matxFromJson<T>`（Mat→Matx 逐元素）；getters 锁内从 `doc_` 取键转换。stereoTempTable：遍历 `tempTables.rectify.table[]`，每档 `{temperature, R1..Q}` → `StereoTempTier`（tempC/R1..Q 五 Matx），实现内防御按 tempC 升序。
**Step 3:** 绿 → commit `feat(mod06): B-T7 仓库typed getters(立体表Matx档/激光档索引)`。

## Task 8: readyForScan＋clear（TDD）

**Files:** 同 T6

**Step 1: 失败测试（缺样矩阵）**——深拷贝 samplePayload 后逐项抠键断言 `missing` 文案：抠 `stereo.cameraMatrixL`／抠 `stereo.R`（外参）／抠 `tempTables.rectify` 首档（空表）／抠 `planeMap.tempTable` 首档／write 时不给 imageSize 不可能（参数必填）→ 改测 meta.imageSize 缺失（手工改 doc 的路径：load 一个手工去掉 meta.imageSize 的文件）——各断言 `ready==false` 且 missing 含对应名；完整载荷 `ready==true`。`clear()` 后 `ready==false`。
**Step 2:** 实现 readyForScan（锁内逐项 `contains`＋rectify/planeMap 档数≥1）＋clear（doc_ 清空、hasData_=false）。
**Step 3:** 绿 → commit `feat(mod06): B-T8 readyForScan五样门禁(外参/档数判据)`。

## Task 9: WorkflowArtifactStore.h 接口预留

**Files:**
- Create: `modules/06_fileio/WorkflowArtifactStore.h`

**Step 1:** 纯头文件（零实现零链接——D1 口径「等接线时再落地」）：三方法声明 `put(tag, json, version)`/`get(tag)`/`list()`，文件头注明「L4 产物仓库预留（框架整体 §1 DataStore／扫描 ADR 7.10）——本次不实现，接线批落地」。
**Step 2:** `tests/` 下任一既有测试文件加 `#include "WorkflowArtifactStore.h"`（纯头无人 include 时不参与编译——加一行保证编译验证）；全量构建绿 → commit `feat(mod06): B-T9 L4产物仓库接口预留(纯头)"`。

---

# 批 C — 会话件与切换（牵动面最大）

## Task 10: ScanSessionData（TDD）

**Files:**
- Create: `modules/06_fileio/ScanSessionData.h/.cpp`
- Create: `modules/06_fileio/tests/test_scan_session.cpp`

**Step 1: 失败测试**：
```cpp
#include <gtest/gtest.h>
#include "ScanSessionData.h"
using namespace Scanner::data;

TEST(ScanSession, AssembleLoadsTables) {
    CalibrationRepository repo; /* T6 samplePayload 同款填充 */;
    ScanSessionData s;                                  // 默认 16 槽
    ASSERT_TRUE(s.assemble(repo).success);
    EXPECT_EQ(s.ringCapacity(), 16u);
}
TEST(ScanSession, PushFrameEnrichesIntoRing) {
    /* 仿 07 test_scan_enricher.cpp:32 自建档表（06 不跨模块取 07 夹具）；
       pushFrame 两帧不同温度 → ring read 回帧 snapshot.stereoTier 与档表一致 */
}
TEST(ScanSession, EnrichFailDropsFrame) { /* 空表装配 → pushFrame 返 fail/warning 且环空 */
}
```
**Step 2:** 实现 `ScanSessionData.h`：
```cpp
#pragma once
// ScanSessionData.h — 02 扫描会话件（设计 §4.2；02-②构造、⑩逆序销毁前先停07）
// 只管「装表＋ring＋pushFrame」；配置归 07 ScanConfig（§4.1，06 不造）。
// pushFrame 限采集回调单线程调用（SlotRing write 单生产者约定）。
// 注：槽位构造期定死（SlotRing 无 resize）——ringSlots 构造参数，assemble 只装表。
#include "CalibrationRepository.h"
#include "EnhancedFrame.h"
#include "SlotRing.h"
#include "TempTableTypes.h"
#include "base/types.h"
#include <opencv2/core.hpp>

namespace Scanner::data {
class ScanSessionData {
public:
    explicit ScanSessionData(size_t ringSlots = 16)
        : ring_(ringSlots, SlotRing<EnhancedFrame>::WriterMode::Overwrite),
          ringSlots_(ringSlots) {}
    Scanner::Result assemble(const CalibrationRepository& repo);
    Scanner::Result pushFrame(const cv::Mat& grayL, const cv::Mat& grayR,
                              double temperatureC, uint64_t frameId);
    SlotRing<EnhancedFrame>& ring() { return ring_; }
    size_t ringCapacity() const { return ringSlots_; }
private:
    StereoTempTable stereoTable_;          // 装配期内存 Matx 档
    PlaneMapTempTableRef laserTiers_;      // 档温索引（网格数据归 §8-1 协调项）
    size_t ringSlots_;
    SlotRing<EnhancedFrame> ring_;
};
} // namespace Scanner::data
```
`.cpp`：assemble＝repo 两 getter 拷入（空表返 fail「标定表未就绪」）；pushFrame＝`enrich(grayL,grayR,tempC,stereoTable_,laserTiers_,frameId,out)`→`ring_.write(std::make_shared<EnhancedFrame>(std::move(out)))`（enrich 非 ok：warn 计数＋透传 Result，不写环）。
**Step 3:** 绿 → commit `feat(mod06): C-T10 ScanSessionData出口查表入环(算子无查表)`。

## Task 11: CalibSessionData（TDD）

**Files:**
- Create: `modules/06_fileio/CalibSessionData.h/.cpp`
- Create: `modules/06_fileio/tests/test_calib_session.cpp`

**格式定案（§8-2 兑现，06 中立类型不链 07）**：JSON 三键——
```json
{ "initialParams": {...07 PostureInitialParams 字段原文，01 侧换算...},
  "targets":   [[16 个数] × 25],
  "boardPoints": [[x,y,z], ...] }
```
（targets 对齐 01 现有 `setTargets(vector<array<double,16>>)`——`CalibrationWorkflow.h:60,90`。）

**Step 1: 失败测试**：写临时 JSON 文件→`load(path)`→三字段数量/数值断言；坏文件返 fail。
**Step 2:** 实现 `CalibSessionData.h`：
```cpp
#pragma once
// CalibSessionData.h — 01 标定会话件（设计 §4.3）
// initialParams 以 JSON 原文透传（07 类型归 01 边界换算，06 不链 07）。
#include "CycleUnit.h"
#include "SlotRing.h"
#include "base/types.h"
#include <array>
#include <nlohmann/json.hpp>
#include <opencv2/core.hpp>
#include <vector>

namespace Scanner::data {
struct CalibSessionConfig {
    nlohmann::json initialParams;                   // 07 PostureInitialParams 原文
    std::vector<std::array<double, 16>> targets;    // 25 目标姿态（4×4 齐次）
    std::vector<cv::Point3f> boardPoints;           // 温补后板点
};
class CalibSessionData {
public:
    Scanner::Result load(const std::string& path, CalibSessionConfig& out) const;
    // 姿态环（Backpressure；07 PosturePipeline attachRing 注入）
    SlotRing<CycleUnit>& cycleRing() { return ring_; }
private:
    static constexpr size_t kSlots = 8;             // 沿用 01 现状槽位
    SlotRing<CycleUnit> ring_{kSlots, SlotRing<CycleUnit>::WriterMode::Backpressure};
};
} // namespace Scanner::data
```
**Step 3:** 绿 → commit `feat(mod06): C-T11 CalibSessionData配置装载+姿态环`。

## Task 12: 01 侧切仓库（RepositoryWriter＋会话件装配）

**Files:**
- Modify: `app/WorkflowContext.h:13,28,33,57`（**增** `calibRepo_` 成员与 `setCalibRepo/calibRepo`——旧 `calibStore_` 并存至 T15 删）
- Modify: `modules/01_calibration/CalibrationWorkflow.h/.cpp`
- Modify: `modules/01_calibration/CMakeLists.txt`（链 `mod_fileio`）

**Step 1:** WorkflowContext 增 `data::CalibrationRepository* calibRepo_ = nullptr;` ＋ getter/setter（前向声明补一行）。
**Step 2:** CalibrationWorkflow.cpp：删 `CalibStoreRepoWriter` 类与 `kCalibStorePath/kCalibJsonPath` 双落盘；新适配器：
```cpp
// RepositoryWriter — ICalibRepoWriter → 06 标定结果仓库（JSON 单格式，D4）
class RepositoryWriter final : public Scanner::pipeline::ICalibRepoWriter {
public:
    RepositoryWriter(WorkflowContext* ctx, cv::Size imageSize)
        : ctx_(ctx), imageSize_(imageSize) {}
    bool write(const std::string& json) override {
        auto* repo = ctx_ ? ctx_->calibRepo() : nullptr;
        if (!repo) return false;
        auto r = repo->write(json, imageSize_);          // meta.imageSize 此处补（§2.1）
        if (!r.success) spdlog::error("[CalibWorkflow] 仓库写入失败: {}", r.message);
        return r.success;
    }
private:
    WorkflowContext* ctx_; cv::Size imageSize_;
};
```
装配处（:212-217 区域）改 `calibRepo_ = std::make_unique<RepositoryWriter>(ctx, imageSize_)`（imageSize 来源与现状 setImageSize 同源——先读 :39,59 现状代码再改）。
**Step 3:** initialize 增配置装载：`CalibSessionData().load(path, cfg)`（路径＝`"calib_session.json"` 常量）→ `setInitialParams`(JSON→PostureInitialParams 字段映射：**先读 07 PosturePipeline.h:70-75 的 PostureInitialParams**——字段为 `K1,D1,K2,D2,R1,R2,P1,P2,Q` 九 Mat＋`imageWidth/imageHeight`＋`maskRatioThreshold`，全部可 JSON 映射，键名照抄)＋`setTargets(cfg.targets)`＋`setBoardPoints(cfg.boardPoints)`；文件缺省不存在时保持现状空参防言语义（`paramsReady()` 拦截），`paramsReady()` 判据不变。
**Step 4（姿态环切换，I5）**：删 01 自有 `cycleRing_` 成员（CalibrationWorkflow.h:98-102）与 `kCycleRingSlots`；`:143` 附近 `posture_->attachRing(cycleRing_)` 改挂会话件环——成员 `Scanner::data::CalibSessionData calibSession_;`（装配期 load）＋ `posture_->attachRing(calibSession_.cycleRing())`。
**Step 5:** CMakeLists：01 新增对 06 头的 include 经 `mod_fileio` PUBLIC 传递——`target_link_libraries(mod_calibration PUBLIC mod_fileio base spdlog::spdlog ${OpenCV_LIBS})`（mod_calibration T15 才删，此间链 06 无环）。
**Step 6:** 全量构建＋ctest 绿。
⚠ **中间态警告（I3）**：本任务删旧 RepoWriter 后、T13 装配 `calibRepo_` 前，若跑标定结果不落任何仓库（RepositoryWriter::write 的 calibRepo()==nullptr 恒返 false）——**T12→T13 必须连做，中间态不可人工验收/交付**。
**Step 6:** commit `feat(mod06): C-T12 01侧RepositoryWriter直写仓库+会话件装载`

## Task 13: app 侧切仓库（装配/谓词/转接/菜单）

**Files:**
- Modify: `app/AppContext.h:15,38,66` ＋ `app/AppContext.cpp:9,51,83-115,214`
- Modify: `app/MainWindow.cpp`（导出菜单两处改走仓库）

**Step 1:** AppContext：`calibStore_` → `calibRepo_`（`std::unique_ptr<data::CalibrationRepository>`）；`:51` 构造后紧跟 `calibRepo_->load("calibration.json")`（best-effort：文件不存在记 info 不失败——开机自检经 readyForScan 体现）。
**Step 2:** `:83-89` pre 谓词：`hasData()` → `readyForScan(rr)`，失败消息带 `rr.missing` 清单（「标定参数未就绪——缺：激光档表/…」）。
**Step 3:** `:97-111` 转接：`cs->cameraMatrixL()` 等 → `repo->stereo()` 一次取结构体逐字段赋 `ScanCalibration`（含 `c.imageSize = st.imageSize`）。
**Step 4:** `:214` `wfCtx_->setCalibStore(...)` → `setCalibRepo(calibRepo_.get())`。
**Step 5:** MainWindow 导出菜单：`file_io::exportPointCloud(spath, points)` 两处改 `wfCtx`/AppContext 仓库指针 `exportCloud(spath)`；`exportMarkers` 同改（UI 层从 osg 数据反填仓库的场景不存在——导出源就是仓库内存，直接调）。
**Step 6:** 全量构建＋ctest 绿＋手工冒烟：scan_demo 起/停（标定未做过 → start_scan 被谓词拦，提示缺样清单）。
**Step 7:** commit `feat(mod06): C-T13 app切仓库(谓词readyForScan+转接+导出菜单)`

## Task 14: 02 侧装配 ScanSessionData（ring 替换＋existingMarkers 注入）

**Files:**
- Modify: `modules/02_scanning/ScanWorkflow.h/.cpp`

**Step 1:** 先读 `ScanWorkflow.cpp:36-63`（assemblePipeline——**注意：`ring_`（.h:97-100，16 槽 Overwrite）与 `attachRing`（:51）已存在**，本任务是**替换**不是新建）与 `ScanPipeline.h:78-80`（attachRing 契约）。
**Step 2:** `.h`：删自有 `ring_`/`kRingSlots`（:97-100），增成员 `Scanner::data::ScanSessionData session_;`；`.cpp` `assemblePipeline()`：
- `session_.assemble(*calibRepo)`（repo 经 WorkflowContext 注入——02 现无该成员则同 T12 方式补，**读现状后选改动最小路**）
- **existingMarkers 注入（设计 §4.1，:46 TODO 兑现）**：`cloudWh->snapshotMarkers(v, recs)` 非空 → 逐条换算 `calib::MarkerCloudPoint{globalId, pos, normal}` 填 `cfg.existingMarkers`＋日志「续扫基准 N 点」；空 → warning 降级（日志一次，不阻断）——点云仓库指针经 WorkflowContext 注入
- `pipeline_->attachRing(session_.ring(), /*dropThreshold=*/0)`（:51 原调用改挂 session_ 环）
**Step 3:** 采集回调接线点（:49 TODO 处）：回调内 `session_.pushFrame(grayL, grayR, tempC, id)`（08 接入期真回调未通则保留 TODO 注释＋空实现——**本任务不造假帧源**）。
**Step 4:** 销毁顺序核对（§4.4 铁律——判据修正：C++ 成员**逆序**析构，故 `session_` 必须声明在 `pipeline_` **之前**，保证 pipeline_（先声明）先析构、ring（随 session_）最后死；运行期兜底仍靠 `stop()` 内 `pipeline_.reset()` 先于成员析构）。核对不满足则调整声明序并注释。
**Step 5:** 全量绿 → commit `feat(mod06): C-T14 02装配ScanSessionData(ring替换+existingMarkers注入+销毁序核对)`

## Task 15: 清退（CalibStore/mod_calibration/死代码/注释同步）

**Files:**
- Delete: `modules/01_calibration/CalibStore.h/.cpp`
- Modify: `modules/01_calibration/CMakeLists.txt`（删 add_library 整块→空文件或仅注释）
- Modify: `modules/01_calibration/calib_workflow.h:58-59` ＋ `.cpp:205-260`（删 saveCalibResult/loadCalibResult 死代码——零调用已核实）
- Modify: `app/CMakeLists.txt:101`（删 mod_calibration 链接）
- Modify: `app/WorkflowContext.h`（删 calibStore_ 残余成员/getter——T12 并存至此删）
- Modify: `modules/08_devicemgmt/IScannerCamera.h:65`、`CameraControl.h:101`、`CameraControl.cpp:274`（注释「01 CalibStore」→「06 标定结果仓库」）
- Modify: `modules/07_pipelinemgmt/pipelines/scan/ScanPipeline.h:81`、`pipelines/postprocess/PostProcessPipeline.h:72`（旧注释引用同步）

**Step 1:** 全工程检索残余：`rg -l "CalibStore|calibStore_|mod_calibration" --glob "!build*/**" --glob "!docs/**" --glob "!factory_calib/**"`——逐处清理（docs 引用保留历史记录不动）。
**Step 2:** 全量构建＋ctest 绿（此时全工程唯一标定存取＝CalibrationRepository）。
**Step 3:** commit `refactor(mod06): C-T15 CalibStore/mod_calibration清退+三套存取收敛(D6)`

## Task 16: 收尾回归＋文档回写

**Step 1:** 全量构建＋全量 ctest（记录总数，应 ≥90＋本计划新增 5 个测试 exe）。
**Step 2:** Release 重跑（`build-rel` 独立目录，命令见 AGENTS——**注意**：build-rel 是 framework 时代旧基线，本次为 07/08＋06 后首次全量 Release，含 CUDA 三架构＋FetchContent 重拉，**预期耗时数十分钟**；若 CUDA/网络环境受阻则记明原因降级为 Debug-only 验收，报人工知悉）。
**Step 3:** 设计方案文头状态改「已实施」＋文末附「实施落地记录」（偏差逐条记——08 计划同款做法）；`docs/模块功能/06-文件管理.md` §2 现状表按落地实况重写（新 5 件行、CalibStore 行删、IWorkflow 行改）。
**Step 4:** commit `docs(mod06): C-T16 实施落地记录+06文档现状对齐`

---

## 风险提示（实施期回报点）

| # | 情形 | 处置 |
|---|---|---|
| 1 | T7 Mat→Matx 转换遇非 double Mat（jsonToMatAuto 恒 CV_64F，理论不遇） | 断言 type==CV_64F，异常即返 fail |
| 2 | T12 PostureInitialParams 字段映射缺参（九 Mat＋宽高＋maskRatioThreshold 已核可映射） | 01 侧手写字段映射；字段不齐报 §8 风险，不猜键名 |
| 3 | T14 08 采集回调未通（真机联调待做） | attachRing 先行、pushFrame 留 TODO——不造假帧源 |
| 4 | 全量测试意外红 | 停下按 systematic-debugging 流程处置，不带病提交 |
| 5 | T12→T13 中间态标定写链空转（calibRepo 未装配） | 两任务连做，中间态不验收不交付（T12 末已警告） |
| 6 | T15 rg 兜底漏 02 侧旧 ring_（M7） | T14 已删；T15 Step1 检索模式补 `kRingSlots` 一并兜底 |
