# 2026-09-05 会话剩余工作 TODO

> 范围：编辑功能（05）实施计划剩余批次＋本会话遗留的跨模块事项。
> 进度基线：P1（05 域对象）✅ / P2（双账本契约）✅ / P3（就绪态，真机验证）✅ /
> **P4a（EditWorkflow＋账本访问链）已实现未提交**——接手先提交（见 §1.0）。
> 原始计划：`docs/plans/2026-09-05-编辑模块05实施计划.md`。

## 1. 编辑功能（05）收尾

### 1.0 立即项
- [ ] **提交 P4a**：IMarkerFuse::removePoints＋ScanPipeline/ScanWorkflow 访问链＋
      DeleteBatch 扩展＋EditWorkflow（新文件）＋app CMake（编入＋链 mod_editing）。
      回归已绿（5/5：undo_redo/selection/fuseconsumer/pipeline/obsaccum）。

### 1.1 P4b UI 接线（编辑会话端到端）
- [ ] **进入编辑**：就绪态下点套索/多段线钮 → `AppContext::canEnterEditSession()`
      → `EditWorkflow.enter(Deps)`（markerFuse/obs 经 `scanWorkflow()->markerFuse()/
      obsAccumulator()`；sceneFeed＝`sceneFeed()`；快照＝fusedPoints 拷贝）。
- [ ] **圈选路由**：OSGWidget 套索闭合改走编辑链——新增出口（多边形＋闭合时的
      view/proj/viewport），MainWindow 转 `editWorkflow->deleteByPolygon(...)`，
      **替代**现有显示级直删（highlightSelectedPoints/deletePointsInPolyline
      在编辑态旁路）。三栏配置从下侧工具栏三组 QButtonGroup 读取（对象类型 4 钮
      → ObjectType 映射：点云/三角面暂映射说明见 §4.3）。
- [ ] **撤销**：编辑态 Ctrl+Z → `editWorkflow->undo()`（优先于 OSGWidget
      undoDelete）；redo 快捷键（Ctrl+Y）。
- [ ] **退出编辑（D5⑤）**：续采/完成扫描/关闭窗口时若有未固化编辑 →
      确认框（应用/放弃/取消）→ `exit(discard)`；编辑会话期间禁止 resume/stop
      直通（先退出编辑）。
- [ ] 状态栏工作集统计（D4：标志点数/已删点数/可撤销步数——`markerCount()/
      stats()`）。

### 1.2 P5 端到端＋文档
- [ ] 真机全流程验证：扫描→就绪→进编辑→圈选删（融合云+obs 双账本）→undo→
      放弃/应用→完成扫描→GBA 快照不含已删点（日志验证 obs 过滤）。
- [ ] EditWorkflow 单测（fake IMarkerFuse/obs/sceneFeed：入账/undo 映射压缩与
      尾部追加/退出放弃恢复/应用落地）。
- [ ] 05 文档 §2 按实况更新（v2 文档追加实现状态）；`模块功能目录.md` §2 对象
      速查补 05 新对象。

### 1.3 编辑能力扩展（后续批次）
- [ ] **激光点云编辑**：候选/账本接 `LaserCloudFuseCuda`（P2 契约已备）——显示
      侧需累积云快照通道（现 loadLaserPoints 显示当帧块，与累积云索引不对应）；
      `canEnterEditSession` 补激光云非空检查（接 ILaserFuse 出口）。
- [ ] **三角面编辑**：网格账本（07-E MeshData）无删除契约——待网格四族算子
      落地后另立批次。
- [ ] 操作组五钮（全选/反选/取消/抛弃/删除）恢复显示＋实装（现已 hide 保留）。

## 2. 激光模式四组接线（2026-09-05 口径，注释已记 ScanChains.cpp）
- [ ] 状态机（10）扫描子模式账本（标点/面片/精细/深孔——现 canOperate("edit")
      app 级门禁同款问题）。
- [ ] 08 下发激光模式命令（协议命令字待定——v3 清单核对；左斜25/右斜25/精细7/
      深孔1 组别参数）。
- [ ] 07 收图按模式判定激光线种类（现奇偶近似仅左斜/右斜两组）。
- [ ] 右斜激光参数标定（现暂用左斜）；精细/深孔参数标定。

## 3. 09 算法遗留
- [ ] **mask_separation 线点分离改进**（面片扫描伪标志点根治——roisL 67 vs
      正常 13；07 配准失败门控（4c370cc）为止血，见该提交说明）。
- [ ] 配准失败率观察：16:00 前后真机标点扫描约半数帧走兜底/失败路径——与移动
      速度/视野标志点数的关系待查（不扩账门控前提下）。

## 4. 资产与 UI 杂项
- [ ] 图标占位替换（设计出图后）：点云（现用「点云和三角面选择(1)」）、
      三角面（现用 surfaceselect(1)）。
- [ ] 悬浮条 hover 态 (2) 号图标未启用（现仅 (1) 常态/(3) 选中两态切换）。
- [ ] 开扫温度兜底：开机即扫描时 MCU 温度帧未到→25.0℃ 兜底（16:20 实测）——
      开扫门禁加温度已收检查或 UI 提示等待。
- [ ] `reader_vs2022/` 未跟踪目录处置（入库或 gitignore，来历待确认）。

## 5. 存量测试失败（与本会话改动无关，stash 实证）
- [ ] `test_device_manager` T2/T3/T4（写线程投递竞态——断言先于 mock 收帧）。
- [ ] `test_mcu_driver`（08 断言不匹配，疑环境/时序）。
- [ ] `test_marker_match_cpu` YToleranceAboveOneThrows（09 既有）。

## 6. 文档同步
- [ ] `模块功能目录.md` 索引仍指 03/05 v1 文档——v2 验收后切换。
- [ ] 08 文档：严格帧号配对（13fbfa1）＋通信留痕（7908073）补进 §2。
- [ ] 06/07 文档：选档留痕（ac3caef）＋obs 剔除契约（bae4669）＋removePoints
      口径同步。
