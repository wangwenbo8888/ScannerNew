# 模块5 · 编辑工作流

> 一句话：本模块是扫描间隙的**点云修整台**——停止采集后、完成扫描前的就绪态里圈选杂点删掉（可撤销），删干净了再去做全局优化。
> 返回索引：[`模块功能目录.md`](../../模块功能目录.md)

> ⚠ **定稿保护**：§1 定案要点与两张图（工作流程图/对象装配与数据流图）为人工定稿，AI 不得自行修改；发现与代码或其他文档冲突时，须先向人说明冲突点、获得认可后才可改动。仅 §2（实现现状）随代码演进由 AI 更新。

---

## 1. 功能目标

### 1.1 设计定案（D1–D8）

- **D1 对象自治**：选择服务（05）、撤销/重做栈（05）、数据管理（06）、渲染（03）四对象封装自治；05 只做 ①门禁进入、②装配、③交互会话承载、④旁观显示、⑤停止回收（仅会话私有件）。
- **D2 编辑时机＝就绪态内、GBA 之前**：编辑是 02-⑥ 就绪态内的一个会话（非独立全局态）——用户停止采集（⑥）后、点「完成扫描」（⑦ GBA）之前进入；数据源＝融合累积器实时点云（未经 GBA 修正）；门禁前置＝处于 S4/S5 就绪态且融合累积器有点。
- **D3 三栏选择模型（正交开关）**：圈选配置三栏独立——工具类型（套索/多段线）× 选择类型（贯穿/只选第一层）× 对象类型（激光点/标记点/标记点和激光点），任意组合合法。
- **D4 删除＝软删除＋双账本作用**：删除只标记（工作集内 tombstone），不物理移除——undo/redo 的实现基础。应用时双账本作用：①融合累积器移除该批点（渲染/后续融合即不见）②逐帧观测累加器中相应观测标记剔除——⑦ GBA 求解不含已删点。
- **D5 撤销/重做栈**：操作粒度＝一次删除（含所选点集）；undo＝恢复该批点、redo＝再删；新动作清空 redo；栈深上限可配（默认 20，逐出最旧）；会话内存态不持久化。undo/redo 同步作用双账本（融合累积器＋obs 剔除标记双向）。
- **D6 记账归 05**：开账（会话 ID＋开始时间戳）/合账（结束时间戳＋编辑统计：删除次数/点数、undo/redo 次数）；不设独立会话对象（同 01-D7/02-D6 款）。
- **D7 所有权拆分**：app 存活件（⑤ 不销毁）＝渲染基座（03）＋EventBus；存活期＝扫描会话（02 私有件，05 只接线不销毁）＝融合累积器＋obs 累加器；编辑会话私有件（② 构造、⑤ 逆序销毁）＝场景内容供给器[编辑层]（03）＋选择服务＋撤销/重做栈。判据同 01-D8/02-D7。
- **D8 编辑不终结扫描工作流，编辑后可续采**：编辑会话退出回 S4/S5 就绪态——就绪态三个出口任选：再按键续采（回 02-③④⑤，同一工作流继续累积）/ 再进编辑 / 点「完成扫描」（进 ⑦ GBA，此时才终结）。编辑成果经双账本自然延续到 GBA 与重融合。

### 1.2 工作流程图

> 图例：✓ = 该模块对应能力已存在；⚠ = 尚不存在或部分缺失（规划中）。
> 调用与现状均只到**模块级**（子模块归属见各模块文档）。

```
（上游：02-⑥ 停止采集，系统处于 S4/S5 就绪态——见 02 文件）
                ┌────────────────────┐
                │ S4/S5 就绪状态       │
                │ （对象不销毁，数据保留）│
                └────┬───────────────┘
┌ ① 进入编辑会话（就绪态内）────────────────────  ★D2
│ 做什么：用户点击「编辑」按钮 → 状态门禁（当前为
│      S4/S5 就绪态）通过 + **可编辑检查**（融合累
│      积器非空）→ 创建编辑会话并开账：会话 ID +
│      开始时间戳
│ 调用：app 应用层（UI 命令入口）
│      + 10 可观测性运维（状态门禁检查——编辑不占
│        全局态，仅查就绪态）
│      + 06 数据管理（融合累积器可编辑检查）
│ 现状：app UI ⚠；10 门禁 ⚠；06 ⚠（累积器外部
│      访问契约未实现）
└───────────────────────────────────────────────
                     ▼
┌ ② 装配对象（注入存活件 + 构造编辑会话私有件）──  ★D7
│ 做什么：05 装配四个封装对象（所有权拆分 ★D7）：
│      【注入·存活期=扫描会话（02 私有件），05 只
│       接线，⑤ 不销毁】
│      · 融合累积器（07·02 会话私有）——工作集数据
│        源（实时融合点云：激光点+标记点+法线）
│      · 逐帧观测累加器（07·02 会话私有）——GBA 输
│        入账本（编辑剔除作用的第二账本，D4）
│      · 渲染基座（03·app 存活）——三维视窗 + 场景
│        切换器
│      【构造·编辑会话私有，05 new、⑤ 逆序销毁】
│      · 场景内容供给器[编辑层]（03）——编辑模式点
│        云显示（高亮选中集/隐藏已删集）+ 圈选曲线
│        叠加层，挂载到渲染基座
│      · 选择服务（05）——三栏选择引擎（D3）：屏幕
│        曲线→空间点集
│      · 撤销/重做栈（05）——软删除操作账本（D4/D5）
│      接线关系见 §1.3「对象装配与数据流」图
│ 调用：06/07（数据注入）+ 03 + 05 内部
│ 现状：全部 ⚠（见 §2）
└───────────────────────────────────────────────
                     ▼ ══ 此后进入交互会话（用户主导，05 承载）══
┌ ③ 交互会话：圈选 → 删除 → 撤销/重做（循环）──  ★D3/D4/D5
│ ┌───────────────────────────────────────────┐
│ │ ③a 配置三栏（进入时默认，随时可切）：        │
│ │  栏1 圈选工具类型：套索 | 多段线             │
│ │  栏2 选择类型：贯穿 | 只选第一层             │
│ │  栏3 对象类型：激光点 | 标记点 | 标记点和激光点│
│ └───────────────────────────────────────────┘
│ 运行者：选择服务（05）+ 场景内容供给器（03）+
│      撤销/重做栈（05）
│ 子流程：
│  [圈选] 用户在三维视窗画曲线（套索=自由手绘闭合；
│         多段线=逐点落子，双击/回车闭合）→ 选择
│         服务将屏幕曲线换算为选中点集：
│         · 对象过滤（栏3）：仅候选所选类型
│         · 屏幕投影包含测试：候选点投影落入闭合
│           曲线内
│         · 栏2 分叉：贯穿=命中即选（任意深度）；
│           只选第一层=仅取视线方向最近可达点
│           （深度遮挡剔除）
│         → 选中集高亮（渲染叠加层实时反馈）
│  [删除] 用户确认删除 → 撤销栈入账 {Delete, 点集}；
│         **双账本作用（D4）**：融合累积器移除该批
│         点（渲染即时隐藏）+ obs 累加器相应观测打
│         剔除标记
│  [撤销] undo → 弹栈顶 {Delete, 点集} → **双账本
│         反向恢复**（累积器恢复点+obs 剔除标记清
│         除）→ 压入重做栈 → 渲染即时恢复显示
│  [重做] redo → 弹重做栈顶 → 双账本重新作用 → 压
│         回撤销栈 → 渲染即时隐藏
│ 05 角色：承载（转发 UI 事件给选择服务/撤销栈；
│      记账：删除/撤销/重做计数）
│ 现状：05 ⚠（全未实现）；03 ⚠（高亮/叠加层未实现）；
│      07 ⚠（累积器/obs 外部操作契约未实现）
└───────────────────────────────────────────────
                     ▼ 用户点击「退出编辑」
┌ ④ 界面显示编辑状态 ─────────────────────────
│ 做什么：全程渲染对象驱动三维视窗（实时融合点云 +
│      选中高亮 + 圈选曲线叠加 + 已删点隐藏）；状
│      态栏显示工作集统计（激光点数/标记点数/已删
│      点数/可撤销步数）
│ 调用：05 编辑工作流（UI 编入 app）
│      + 03 渲染（三维视窗，编入 app）
│ 现状：05 ⚠；03 ⚠
└───────────────────────────────────────────────
                     ▼
┌ ⑤ 完成编辑，停止会话私有件并退回就绪态 ────────  ★D6/D7/D8
│ 做什么：05 收到退出指令 →
│      若存在**未应用的编辑**（撤销栈非空且有未落
│      地软删除）→ 弹确认框：应用 / 放弃 / 取消退出
│        · 应用：软删除双账本物理落地（累积器移除
│          +obs 剔除固化）
│        · 放弃：丢弃软删除（双账本还原到进入编辑
│          时快照）
│        · 取消退出：回到 ③ 继续编辑
│      （无编辑或已应用 → 直接走合账）
│      合账（结束时间戳+编辑统计存档）→ 逆序停止
│      并销毁**编辑会话私有件**（场景内容[编辑层]/
│      选择服务/撤销栈）；注入的存活件不销毁（★D7）
│      → **退回 S4/S5 就绪态**（★D8——不终结扫描
│      工作流；就绪态三出口任选：续采/再编辑/完成
│      扫描进 GBA，编辑成果经双账本自然延续）
│ 调用：10 可观测性运维（就绪态确认）
│ 现状：10 ⚠；07 ⚠
└───────────────────────────────────────────────
                     ▼
                ┌────────────────────┐
                │ S4/S5 就绪状态       │──▶（出口①再按键→续采 02-③④⑤）
                │ （02 扫描工作流仍在） │──▶（出口②再编辑→回本文件 ①）
                └────────────────────┘──▶（出口③「完成扫描」→02-⑦ GBA：
                                            GBA 求解已按 obs 剔除标记
                                            排除已删点观测；重融合累积
                                            器已无该批点）
```

### 1.3 对象装配与数据流

```
 存活件（⑤ 不销毁）：
 ┌──────────────────────────────────────────────────────────────┐
 │ 07 融合累积器 + 07 逐帧观测累加器（存活期=扫描会话·02 私有件）      │
 │ 03 渲染基座 · base EventBus（app 存活）                          │
 └──────────────────────────────────────────────────────────────┘

       ②装配                 ③交互会话(圈选/删除/undo/redo)     ⑤停止/销毁
 05 ─────────▶ 编辑会话私有件 ──────────────────▶ 逆序回收
 编辑工作流    （构造私有件+接线                            （仅编辑会话私有件；
               存活件）                                     存活件留存→退回就绪态）

 交互期数据流（05 承载，双账本作用经撤销栈记账）：

   ┌────────────────────┐ 实时点云快照 ┌──────────────┐ {Delete,点集} ┌──────────────────┐
   │ 07 融合累积器        │ ───────────▶ │ 选择服务(05)  │ ────────────▶ │ 撤销/重做栈(05)    │
   │ (扫描会话·数据源)    │              │ 三栏引擎       │               │ 双账本操作账本      │
   │ 07 obs 累加器        │ ◄───────────────────────────────────────────┤ 应用/恢复/重删      │
   │ (扫描会话·GBA输入)   │        双账本作用（删除/恢复，经栈记账）        └──────────────────┘
   └─────────┬──────────┘                                                   │
             │ 工作集状态（选中/已删）                                          │选中集/圈选曲线
             ▼                                                              ▼
      ┌──────────────────┐                                          ┌──────────────────┐
      │ 03 场景内容供给器   │ ◄── 圈选曲线(屏幕坐标) ── 05 UI ◄──── 三栏配置切换 │
      │ [编辑层](会话私有)  │                                          └──────────────────┘
      │ 高亮/隐藏/叠加      │ ── 30fps 刷新（03-D4）──▶ 渲染基座 ─▶ 屏幕
      └──────────────────┘
             └──── 编辑统计事件 ─▶ base EventBus ─▶ app UI(④)

 编辑成果流向（⑤ 退出后自然延续，无显式交接动作）：
   · 退回就绪态→续采：累积器已无已删点，后续融合不再累积该批点
   · 退回就绪态→「完成扫描」→ 02-⑦ GBA：obs 累加器的剔除标记 → GBA 求解
     排除已删点观测 → 修正位姿 → 重融合（累积器已无该批点）
```

> **要点**：渲染是**纯消费者**（同 01/02）；选择服务只读累积器快照、只经撤销栈写双账本；编辑会话不新建独立工作集副本——**直接作用于 02 会话的两个账本**（软删除＝账本上的标记态，物理落地＝应用时的固化）；GBA 前编辑的成果经账本状态自然进入 ⑦（无「编辑结果文件/版本」这类交接物）。

### 1.4 目标文件构成（05 域对象粒度）

- `modules/05_editing/SelectionService.h/.cpp` — 三栏选择引擎：屏幕曲线→空间点集（投影包含＋深度遮挡）。
- `modules/05_editing/UndoRedoStack.h/.cpp` — 双账本软删除操作账本：入账/undo/redo/栈深上限/应用固化。
- `modules/05_editing/EditWorkflow.h/.cpp` — 编辑会话主体（①—⑤ 承载，编入 app）。
- `modules/03_rendering/` 编辑层 — 场景内容供给器[编辑层]：高亮/隐藏/圈选曲线叠加（03 域）。
- 07 侧前置 — 融合累积器/obs 累加器的外部操作契约（按点移除/观测剔除标记）。

## 2. 实现现状（以代码为准）

> 一句话：**零实现**——模块仅 INTERFACE 占位库；点云编辑能力现存于 03 OSGWidget（纯显示级，不触任何账本），05 域对象（选择服务/撤销栈/工作流）与 07 账本操作契约均未落地。

### 2.1 `modules/05_editing/CMakeLists.txt`（mod_editing）
- 核心能力：INTERFACE 占位库，仅保 CMake 目标名，无源码。
- 编译归属：不参与任何链接产物。
- 状态：预留

### 2.2 相关既有资产（现状注记）
- 03 OSGWidget（`modules/03_rendering/OSGWidget.cpp`）— 套索交互已实装 ◐：enterLassoMode 圈选（2D 多边形＋HitTestCullCallback 捕获 MVP 命中）、highlightSelectedPoints 高亮保原色、deletePointsInPolyline 删除（顶点色 alpha=0＋GL_BLEND）、undoDelete 撤销栈——**纯显示级**：只改渲染几何的颜色数组，不经融合累积器/obs 任何账本（D4 双账本未触）；三栏模型（D3）未实现（无贯穿/第一层分叉与对象类型过滤）。
- 07 FrameObsAccumulator（`modules/07_pipelinemgmt/pipelines/scan/FrameObsAccumulator.h`）— push/snapshot/clear 已有（D 全局优化消费快照）；**无观测剔除/按点移除 API**——D4 第二账本作用的前置缺口。
- 07 融合累积器（marker/laser 融合算子内部，经 FuseConsumer 持有）— 有 seed() 已有点预填；无移除点 API。
- 02 ScanWorkflow（`modules/02_scanning/ScanWorkflow.h/.cpp`）— 无编辑会话挂点（就绪态「进编辑」出口未接）。
- 10 StateMachine — canOperate("edit") 门禁键已登记（现查 S2）；D2 的 S4/S5 就绪态前置未实现。

## 3. 附录

- 套索能力现状载体：[`03-渲染显示.md`](03-渲染显示.md) §2.1（OSGWidget 套索功能组）
- 双账本的消费端：[`07-流水线管理.md`](07-流水线管理.md)（FuseConsumer/FrameObsAccumulator/GlobalOptimObject）

## 4. 修改记录

| 日期 | 改了什么 | 依据/备注 |
|---|---|---|
| | | |
