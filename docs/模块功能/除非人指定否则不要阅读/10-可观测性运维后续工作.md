# 10-可观测性运维 · 后续工作（产品化路线图）

> 状态：**产品化路线图（2026-08-15，拆分自 06-11 合并稿）**
> 定位：系统最终形态是产品；本文件 = 10 模块从当前状态到产品状态的距离
> 上游：`10-可观测性运维.md`（含 §0 门禁状态机——Phase 0 已迁入）· `07拆解与状态机迁移方案.md`（已执行）
> 批次代号：**G 批次**

---

## 一、现状（已核）

- FaultHandler：订阅/上报/清除 + 状态机转态（Phase 0 后与 StateMachine 同库，`10→07` 依赖已消）
- StateMachine：9 态 + EventBus 广播；真实接线仅"设备连接/断开"与"故障/清除"；**扫描启停零调用者**（待 H1）
- 日志：控制台 spdlog + CALIB_LOG_* 仅算子域；无文件轮转/崩溃捕获/性能计数

## 二、缺口（G 批次）

| # | 单元 | 服务于 | 依赖 |
|---|---|---|---|
| G3 | **状态机 9→7 态对齐** + ScanResumed/EmergencyReset 事件补全（E 盘 framework 已验证实现可移植）+ canOperate 表更新（含"扫描前置=已标定"）+ Editing 态与可编辑点云前置（E 盘已实现同款） | **H1 的前置**（门禁表不对=三域启动时序错） | 无 |

> **G3 移植注意（纵横审查 M6）**：E 盘 framework 转换表把 `SessionStarted` 兼用作"Calibrating→Calibrated"转换事件——本工程 base 语义中 SessionStarted 属会话组（记账语义），**移植时必须剥离**，标定完成用独立 `CalibrationCompleted`（0x0506，见产品化总图 §五事件表）。转换表以**总图 §六映射表**为准（工作流 Phase↔全局态↔事件）。
| G1 | **故障主动响应链**：08 检测源（温度超限/USB 断连/GPU OOM）→ FaultHandler.reportFault → 转态 S7 + 安全停机（相机停/补光关/激光关） | 三域稳定性（S7 语义落地；07 文档 §3.1 既定项） | F 检测源（F4/F7） |
| G2 | `FrameStats` 原子计数（processed/dropped/fps）——PerfCounters 首件 | 02 S1 观测（M4 里程碑） | 无 |
| G4 | 统一日志升级：文件 sink + 轮转（10MB×5）+ CALIB_LOG_* 推广三主域 | 出厂排障（M5 报告附件） | 无 |
| G5 | 崩溃捕获：SetUnhandledExceptionFilter + MiniDumpWriteDump + 崩溃重启恢复 | 产品级最低保障 | 无 |
| G6 | Performance Dashboard 数据源（G2 扩展：GPU 显存/各阶段延迟/调度指标） | M4 | G2 + H2 |

> G3 排最前（H1 前置）；G1 是 S7 闭环的最后一块（检测源在 08）。

## 三、实施顺序与验收

```
G3 ──► H1（07 Phase 1，解锁三域启动时序）
G2/G4/G5 ── 可与 E 批并行（无依赖小件）
G1 ◄── F4/F7 检测源就绪
G6 ◄── G2 + H2（调度指标）
```

| 里程碑 | 验收 |
|---|---|
| GM1（G3） | 7 态转换表全通（含 S7 恢复续作语义）；canOperate 三域前置正确；ctest 绿 |
| GM2（G2/G4/G5） | 帧率/丢帧可观测；日志落文件轮转；崩溃产 minidump |
| GM3（G1） | 温度超限真机触发 → S7 安全停机 → 自检恢复链全通 |

## 四、风险

| 风险 | 缓解 |
|---|---|
| 9→7 态对齐破坏既有接线（ScannerWindow 连接/断开） | 连接触发映射到 S1→S2 语义调整；E 盘实现已趟过同类改动 |
| S7 恢复续作依赖检查点（loadCheckpoint TODO） | GM1 仅落转换表；续作实链随 01 W8/E8 二期 |
| 安全停机命令与 W0 协议耦合 | G1 实现待 W0（与 F1 同窗口）；转态逻辑先行 |
