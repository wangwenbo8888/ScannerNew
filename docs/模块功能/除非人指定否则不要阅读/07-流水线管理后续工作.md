# 07-流水线管理 · 后续工作（产品化路线图）

> 状态：**产品化路线图（2026-08-15，拆分自 06-11 合并稿）**
> 定位：系统最终形态是产品；本文件 = 07_pipelinemgmt（新模块）从零到产品状态的距离
> 上游：`docs/plans/流水线管理模块07设计方案.md`（v3，D1~D5/Q1/Q2/Q4/Q5 已定案）· `07-流水线管理.md`（模块文档）
> 批次代号：**H 批次**（= 设计方案 Phase 框架）；Phase 0 已执行（状态机迁 10，commit a451380）
> 现状：**代码零行**（modules/07_pipelinemgmt/ 未创建；旧 07_session 仅剩 SessionService 待迁入）

---

## 一、与三主域的对接点（Phase 1 内容扩充——三域梳理倒逼）

| Phase | 三主域新增要求（并入实施） |
|---|---|
| Phase 1 | **Params 通道细化**：三 Kind 工厂参数不再是空壳——01（warmupSec/poseTablePath/tubeN）、02（**ScanConfig**：mode A/B/existingMarkers/projectName）、04（**PostProcessConfig**：五阶段参数/输出格式）；`cancel()` 透传（04 契约，E7） |
| Phase 1 | **门禁接 G3 后的 7 态表**：S2→S3 标定 / S4/S5 扫描二选一（ScanMode）/ S6 后处理；后处理**免设备门禁**但需前置"存在 B 模式扫描产物"（查 ArtifactStore/E8 一期） |
| Phase 1 | **会话记账扩展**：01 标定账本（W8-2 checkpoint 扩展——SessionService 迁入后实现） |
| Phase 1 | **按键双入口照应**：08 KeyManager 的 ActionSink 终点即 WorkflowManager（F-K3）；§3.2 门禁时序的"UI 图标"单入口补按键入口（同权同终点） |
| Phase 2/3 | 调度层双模式（扫描 X 流水线 + 姿态 Cycle 反压）——02/01 已留挂点（processFrame/run 接口不变） |

## 二、工作分解（H 批次 = Phase 1~4）

| # | 单元 | 说明 | 依赖 |
|---|---|---|---|
| H1（=Phase 1） | 目录/CMake/WorkflowManager 骨架 + SessionService 迁入 + 旧 07_session 删除 + 三 Kind 注册（Params 通道 + cancel 透传）+ 门禁接 10 + 记账驱动 + 事件发布；**含 V10 清理：移除 ScanWorkflow/CalibrationWorkflow 现状代码内部自发的 publishEvent/转态（ScanStarted 等）——统一命令通道后事件唯一发布者为 07** | P0"接线断点三连"闭环；可独立验收 | **G3**（7 态门禁表）；E 批首批 |
| H2（=Phase 2） | 调度组件：Topology/GpuService/PCoreBroker/SchedRingBuffer/FrameResultQueue + vendored moodycamel + 单测 | 纯基建无业务 | H1 |
| H3（=Phase 3） | PipelineScheduler + 双模式对接：扫描（MarkerChainTask/GpuChainTask，退化配置先行）+ 姿态（Cycle 单元/反压/1 槽） | Q4=b 双模式同批 | H2 + 02 C2~C5 + 01 B3~B4（挂点就绪） |
| H4（=Phase 4） | 文档同步（设计方案 §6 清单）+ 开发进度 | — | H3 |

**Q3（scanLoop 过渡）仍挂起**：当前实现冻结原则下留 Phase 3 实施时拍板；倾向 b（并存一版做回归基线）。

## 三、实施顺序与验收

```
G3(状态机7态) ──► H1 ──► H2 ──► H3 ◄── 并行等待: 01 B3-B4 / 02 C2-C5 挂点
                    │              └──► H4
                    └─► 解锁: F-K3(按键派发) / 三主域启动时序全通
```

| 里程碑 | 验收 |
|---|---|
| HM1（H1） | 三工作流经统一命令通道启停（屏幕+按键双入口）；状态机转态有真实调用者；旧 07_session 目录消失；ctest 绿 |
| HM2（H2） | 调度组件单测绿（拓扑/槽/队列/缓冲） |
| HM3（H3） | 扫描+姿态在调度层运行；退化配置回归通过；吞吐指标采集 |
| HM4（H4） | 全文档一致（AGENTS/框架整体/02/07 文档） |

## 四、风险

| 风险 | 缓解 |
|---|---|
| H1 未做先接业务（三域先行启动） | 三域单线程版不依赖 07；H1 落地后切换入口即可（挂点接口已约定） |
| G3 迁移引入回归 | E 盘 framework 已验证实现可移植 + 本工程 ctest 兜底 |
| 双模式调度抽象不匹配（Q4 风险） | SchedulerConfig 三参数（gpuSlots/dropPolicy/dualBranch）+ SchedRingBuffer<T> 模板已预留；H3 先退化配置回归再并行 |
| Params 结构随三域演化漂移 | Params 用各域 Config 结构体直传（02 ScanConfig/04 PostProcessConfig），07 不复制字段 |
