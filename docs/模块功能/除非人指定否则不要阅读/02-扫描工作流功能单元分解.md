# 02-扫描工作流 · 功能单元分解表

> 状态：**实现蓝本（2026-08-15）**——伪代码拆到最小可独立实现/测试单元；约定同标定版（FU/☠/输入/输出/逻辑/依赖/测试）
> 批次 C1~C8 对应《02-扫描工作流实现梳理.md》§九

---

## FU 总索引

| 组 | 单元数 | 批次 |
|---|---|---|
| G1 配置与骨架 | 4 | C1 |
| G2 ScanFramePipeline | 14 | C2~C5 |
| G3 配准状态 | 4 | C4 |
| G4 观测累加 | 2 | C6 |
| G5 主控两阶段 | 5 | C1 |
| G6 GlobalOptimStage | 5 | C7 |
| G7 跨模块小件 | 4 | C7/C8 |
| **合计** | **38** | |

---

## G1. 配置与骨架（C1）

### FU-2.1.1 ScanConfig 结构
- 纯数据：mode/enableFinalBA/existingMarkers/projectName + `enableLaser()`
- 测试：A/B 构造、正交性（existing 与 mode 无关）

### FU-2.1.2 温度表加载与注入
- 输入：CalibStore
- 逻辑：load 表1（必需 ☠）/表2（B 模式必需 ☠）→ pipeline 注入（表2 经 SetTempTable）
- 测试：缺表1 → 明确失败；A 模式缺表2 → 不报错

### FU-2.1.3 ScanWorkflow 骨架改造
- 两阶段 Phase 枚举 + 保留 IWorkflow 契约 + 会话/事件接线（现状 start/pause/resume/stop 的 session 调用保留，stop 扩展 GBA 语义）
- **含 ScanPipeline.h 处置**（实现梳理 §3.1 既定）：吸收其 193 行算子声明骨架 → 新建 ScanFramePipeline，旧名退役；**已核实（2026-08-15 纵横审查 M5）：ScanPipeline.h 无 cpp、无任何 CMake/代码引用（孤立声明文件），直接吸收后删除即可**
- 测试：生命周期状态迁移单测

### FU-2.1.4 扫描参数注入接口
- `setConfig(ScanConfig)`（07 WorkflowManager 的 Params 通道）；替代现有 setScanMode/setCalibration 双入口
- 测试：setConfig 后 initialize 生效

---

## G2. ScanFramePipeline（C2~C5）

### FU-2.2.1 算子池构造与 GPU 预热
- 22 个算子成员构造 + stream + gpuWarmup（kernel 首跑）
- 测试：构造/析构显存平衡

### FU-2.2.2 mask_separation 调用（预处理-01，左右）
- 输入：gray → 输出：`d_markingPointMask + d_laserMask + d_grayImage`
- 测试：合成混合图（斑+线）→ 两掩膜分离正确

### FU-2.2.3 ccl 调用（标记点侧）
- 测试：N 斑 → N components

### FU-2.2.4 标记点链 9 算子串联（image_split→…→point_reconstruct）
- 与标定 FU-2.6~2.12 同族，差异点：undistort 参数**来自温补表 lookup**（非 InitialParams）
- 测试：合成双目标记帧端到端（复用标定 G2 测试图生成器）→ 3D 重投影误差 < 0.5mm

### FU-2.2.5 温度查表封装（表1 lookup）
- 输入：tempC → 输出：`StereoRectifyParams`（R1/R2/P1/P2/Q）
- 逻辑：lookup 失败 → 最近有效温度缓存 + Degraded
- 测试：表内温度精确命中；表外温度取边界；空表降级

### FU-2.2.6 steger Flat 调用（激光04，左右）
- 测试：断裂亮线段 → 中心点提取，line_id 统一占位

### FU-2.2.7 undistort_cuda（激光05，查表）
- 测试：零畸变参数恒等

### FU-2.2.8 epipolar_interp(false)（激光06）
- 测试：仅几何邻近生效（无同线判据）

### FU-2.2.9 laser_match_scan（激光07，查表2）
- 输入：左右插值点 + 当前温度（表已 SetTempTable 注入）
- 输出：匹配对 + **产出 line_id**
- 测试：合成查表对应线 → 匹配率与 line_id 正确

### FU-2.2.10 laser_reconstruct（激光08）
- 输入含 Q（查表）
- 测试：已知深度合成线 → Z 误差 < 0.5mm

### FU-2.2.11 laser_cloud_fuse_cuda + normal（融合-02/03）
- 跨帧累积语义（内部累积器）；Clear 重置
- 测试：两帧同位置点 → 体素去重后单点；法线非零

### FU-2.2.12 marker_cloud_fuse_cpu（融合-02M）
- 测试：跨帧累积 + groupIds 持久化在 fusedPoints

### FU-2.2.13 existingMarkers seed 注入
- 逻辑：`markerFuse_.seed(points)` + `regState_.setGlobalMarkers`
- 测试：seed 后第 0 帧渲染即可见已有点；新点不覆盖已有点（首落为代表）

### FU-2.2.14 processFrame 组装
- ①→⑥ 串联 + obs 旁路 + FrameResult 组装
- 依赖：FU-2.2.2~2.2.13 全部
- 测试：A/B 模式端到端单帧（B 含激光链产出 Device 点云）

---

## G3. RegistrationState（C4）

### FU-2.3.1 PrevFrameState 快照 store/load
- `atomic<shared_ptr>` 封装（C++17 atomic_load/store 自由函数——工程 C++17）
- 测试：并发 load/store 安全（TSAN）；旧快照引用保活

### FU-2.3.2 全局标记点集维护
- existingMarkers 预填 / 帧间更新 / frame_fuse 靶标提供
- 测试：seed 后匹配指向已有点

### FU-2.3.3 首帧配准分支（fuseFirstFrame）
- 逻辑：无快照 → 首帧初始化；挂 existing 时 bestIdx 靶标
- 输出：globalIds（首帧 i / bestIdx）
- 测试：空靶标 → globalId=0..n；有靶标 → 匹配点 bestIdx

### FU-2.3.4 光流主路径 + 兜底
- 逻辑：optical_flow.Execute(snapshot) → 失败/匹配<3 → frame_fuse；兜底 globalId=-1
- 测试：正常光流链；人为破坏光流（标记点剧变）→ 兜底触发且 globalId=-1

---

## G4. ScanObsCollector（C6）

### FU-2.4.1 onFrame 累加
- 测试：N 帧后 takeAll 长度 N、字段完整

### FU-2.4.2 takeAll/clear 语义
- 测试：取走后再取为空

---

## G5. 主控两阶段（C1）

### FU-2.5.1 streamingLoop（单线程版）
- 逻辑：pop→tempC→processFrame→session 记账→进度；Paused 检查；连续失败降级
- 测试：合成帧源跑 N 帧；pause/resume 生效

### FU-2.5.2 温度获取接线
- `hardwareMonitor_.getLatestTemperatures()` → 相机均值
- 测试：Mock 温度注入

### FU-2.5.3 stop() 两阶段语义（★Q5）
- 逻辑：join → 激光 OFF → GBA（阻塞）→ session/publish → Done/Error
- 测试：stop 期间进度回调收到 GBA 阶段事件；GBA 失败 → Error

### FU-2.5.4 单帧异常隔离
- try-catch 连续 N=30 帧失败 → Degraded 上报不终止
- 测试：注入毒帧序列

### FU-2.5.5 进度 detailJson
- 帧级（frameId/R-T 摘要）与 GBA 级（迭代/ rms）双格式
- 测试：解析侧字段齐全

---

## G6. GlobalOptimStage（C7）

### FU-2.6.1 GBA 输入组装
- obsFrames + highPrecisionGlobalIds（existing 中 id≥0 者）+ X_existing/σ
- 测试：globalId=-1 帧不入先验集

### FU-2.6.2 global_ba_cpu 调用与诊断
- 收敛/条件数/修正量提取
- 依赖：FU-2.7.1（09 软先验改造）
- 测试：合成小规模 BA（5 帧 10 点）收敛且先验点几乎不动（< σ 量级）

### FU-2.6.3 重融合循环
- Clear → seed(existing) → 逐帧修正位姿重跑双融合
- 测试：合成漂移场景 → 修正后点云更聚（体素密度提升）

### FU-2.6.4 逐帧激光缓存管理
- 显存/内存预算控制：全缓存 or 降采样（开放点：联调实测定）
- 测试：预算超限策略触发

### FU-2.6.5 publishReplace 覆盖发布
- 依赖：FU-2.7.2
- 测试：version++ 且渲染快照一次性切换

---

## G7. 跨模块小件（C7/C8）

### FU-2.7.1 09 global_ba_cpu 软先验扩展
- GlobalBAInput/Params 增字段 + Ceres 先验残差块（文档 §5.3 改造表）
- 测试：随 FU-2.6.2；先验点零观测时位置不变

### FU-2.7.2 06 PointCloudBuffer 覆盖发布
- `publishReplace(points, colors)`：写锁整体替换 + version++（现仅增量 push）
- 测试：替换后 getSnapshot 原子一致

### FU-2.7.3 08 温度缓存查询
- `getLatestTemperatures()`（若标定域 S1-3 已实现则复用——**与标定共享此单元**）
- 测试：Mock 上报缓存

### FU-2.7.4 A 模式前端分支预留
- `frontendA`：mask_extract 备选路径开关（文档 §3.4 开放决策，联调后定）
- 测试：两分支均可跑通合成帧

---

## 实现顺序与依赖图

```
C1: FU-2.1.1 → 2.1.4 → 2.1.3（含 ScanPipeline.h→ScanFramePipeline 改名迁移与旧引用清理）→ 2.5.x（骨架先行，streamingLoop 用旧链占位）
C2: FU-2.2.1 → 2.2.2/2.2.3 → 2.2.5 → 2.2.4（标记链补全，换掉旧占位）
C3: FU-2.2.6~2.2.10（激光链）→ 2.2.11
C4: FU-2.3.1~2.3.4（配准）
C5: FU-2.2.12/2.2.13 → 2.2.14（融合与组装）
C6: FU-2.4.1/2.4.2
C7: FU-2.7.1/2.7.2（并行）→ FU-2.6.1~2.6.5
C8: FU-2.7.3/2.7.4 + 端到端回归（A/B/兜底/退化）
```

## 测试策略分层（同标定版）

| 层 | 覆盖 | 手段 |
|---|---|---|
| L1 单元 | 每 FU 测试栏 | 合成数据 gtest |
| L2 链级 | FU-2.2.4/2.2.14/2.6.2 | 端到端合成帧 |
| L3 全流程 | C5/C7 完成后 | 合成 200 帧序列：流式→GBA→修正（含 existing/兜底路径） |
| L4 真机 | C9 之前置 | IntegrateTestDialog + 真实扫描物 |
| L5 调度版 | 07 Phase 3 | 同 L3 用例在 SchedulerConfig 下回归（接口不变验证） |
