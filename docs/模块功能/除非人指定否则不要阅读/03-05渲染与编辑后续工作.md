# 03-渲染显示 · 后续工作（联调清单）+ 05-编辑 · 立项预案

> 状态：**产品化路线图（2026-08-15，拆分自 06-11 合并稿）**
> 定位：系统最终形态是产品；03 = 联调型模块（无大新建）；05 = 产品功能最后一环（远期立项）
> 批次代号：**J 批次**

---

## 一、03-渲染显示（J1 联调组）

### 现状
OSGWidget 编入 scan_demo；扫描点云实时显示已有（版本号快照轮询）。

### 产品化联调项（无大新建，逐项随上游就位）

| # | 联调项 | 上游 | 时机 |
|---|---|---|---|
| J1-1 | GBA 期间冻结末帧确认（渲染层不拉新快照即天然冻结——验证 publishReplace 前后无闪变） | 02 C7 | C7 联调 |
| J1-2 | publishReplace 快照一次性切换（version++ 原子性——大点云无撕裂） | 02 C7/E6 | C7 联调 |
| J1-3 | 网格显示（04 产物 MeshData → OSG Geometry） | 04 D6 | D6 联调 |
| J1-4 | 姿态引导 3D 预览（01 UI 需求：当前设备姿态/目标姿态/已集清单的 3D 呈现，CalibDisplay 扩展） | 01 B3/B4 | 01 联调 |
| J1-5 | existingMarkers 已有点显示（seed 后第 0 帧可见——02 文档 §5.2 验收点） | 02 C5 | C5 联调 |
| J1-6 | 覆盖率可视化（体素覆盖率 → 扫描界面百分比，M 里程碑 UI） | 07 记账 + 02 | 后期 |

## 二、05-编辑（J2 立项预案）

### 现状
空目录（仅 CMakeLists INTERFACE 占位）。E 盘 framework 工程的状态机已为其预留 **Editing 态 + "存在可编辑点云"前置**（可移植）。

### 立项预案（三主域稳定后启动）

| 项 | 预案 |
|---|---|
| 产品功能 | 套索选点/框选、删除、裁剪、**撤销/重做（操作历史栈）**——07 旧文档"编辑服务（主层=Service）+操作历史"定位 |
| 数据契约 | 编辑直接作用于 GBA 修正后点云（publishReplace 快照）——**与扫描互斥**（状态机 Editing 态门禁） |
| 架构落点 | Service 层服务类（选择/删除/历史栈）+ UI 面（03 渲染交互拾取）；不新建算子 |
| 前置 | 03 渲染交互拾取（屏幕坐标→点索引）；G3 状态机 Editing 态 |
| 规模预估 | ~800 行（选择算法 + 撤销栈 + UI 接线） |

### 风险
- 编辑与"已发布点云"的一致性：编辑结果走独立 publishReplace 版本链，不污染扫描会话产物（ArtifactStore 分版本保存）。
- 大点云拾取性能：需空间索引（KDTree——04 域 mesh_wrap 已建同款可复用）。
