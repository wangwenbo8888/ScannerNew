# 模块10 · 可观测性/运维

> 返回索引：[`模块功能目录.md`](../../模块功能目录.md)
>
> ⚠ **全局门禁状态机已迁入（2026-08-15，Phase 0）**：`StateMachine.h/.cpp`、`IState.h`
> 自 07_session 迁至本模块（FaultHandler 与状态机同库，`10 → 07` 依赖消除）。
> 方案见 `docs/plans/07拆解与状态机迁移方案.md`。

## 职责
统一日志(配置/轮转/分级), 性能监控(延迟/帧率/计数器dashboard), 崩溃捕获(minidump/crash), 故障聚合处理(FaultHandler), **全局门禁状态机（1 号灯，自 07 迁入）**。

## 0. 全局门禁状态机（StateMachine，自 07 迁入）

### 0.1 代码位置

- `modules/10_observability/IState.h` — ScannerState 枚举（现 9 态）+ IState 接口
- `modules/10_observability/StateMachine.h/.cpp` — 实现：transition/canOperate/onStateChange + EventBus 广播 StateChanged

### 0.2 目标设计：7 态模型（S1–S7）

> 权威规格（自 07-会话管理.md §1.2 整节迁入，内容不变）。7 态**两两互斥**，描述扫描仪运行全部处境（操作员视角）。代码现 9 态半接线，对齐另排期（§0.2.6）。

**0.2.1 七个状态**

| 代号 | 状态 | 说明 |
|---|---|---|
| **S1** | 初始化 | 软件启动自检：通讯 / 加密狗 / 软件授权文件 |
| **S2** | 待机 | 空闲就绪，所有功能入口 |
| **S3** | 标定 | 多姿态采集→解算标定参数 |
| **S4** | 扫描标记点 | 纯标记点扫描（激光 OFF，ScanMode 二选一） |
| **S5** | 扫描标记点+激光 | 标记点+激光点扫描（激光 ON，ScanMode 二选一） |
| **S6** | 后处理 | 离线 batch 七阶段→出 STL |
| **S7** | 设备故障+自检中 | 设备故障（通讯/温度/GPU）触发；安全停机 + 自检恢复 |

**0.2.2 转换图**

```
                ┌──────────────────────────────┐
                │ S1 初始化                     │
                │ 自检: 通讯/加密狗/授权文件     │
                └──────────────┬───────────────┘
                     自检通过  │  设备故障 → S7
                               ▼
   ┌──────────────────────► S2 待机 ◄──────────────────┐
   │      启动标定/扫描/后处理 │                          │
   │      (扫描按 ScanMode     │                          │
   │       二选一 S4 或 S5)    ▼                          │
   ┌────┴───┐  ┌──────────┐  ┌───────────┐  ┌──────┐  完成/
   │ S3 标定│  │ S4 扫描   │  │ S5 扫描    │  │ S6   │ 停止
   │        │  │ (标记点)  │  │(标记点+激光)│  │后处理│(图标)
   └────┬───┘  └────┬─────┘  └─────┬─────┘  └──┬───┘
        └───────────┴──────────────┴────────────┘
                     (各状态完成→返回 S2)

   ┌──────────────────────────────────────────┐
   │ S1–S5 检测到设备故障 → S7 设备故障+自检中 │
   │   S7 自检通过 → 回"退出前状态"，续作同一会话│
   │   ※ S6 免疫：不进 S7（离线，无设备可停/自检）│
   └──────────────────────────────────────────┘
```

**0.2.3 转换表**（空 = 不允许，需经 S2）

| 从＼到 | S1 | S2 | S3 | S4 | S5 | S6 | S7 |
|---|---|---|---|---|---|---|---|
| **S1** Init | — | 自检通过 | | | | | 设备故障 |
| **S2** Standby | | — | 启动标定 | 启动扫描(标记点) | 启动扫描(标记+激光) | 启动后处理 | 设备故障 |
| **S3** Calib | | 标定完成 | — | | | | 设备故障 |
| **S4** ScanM | | 上位机图标 | | — | | | 设备故障 |
| **S5** ScanML | | 上位机图标 | | | — | | 设备故障 |
| **S6** Post | | 后处理完成 | | | | — | **—（免疫）** |
| **S7** Fault | （恢复目标之一） | （恢复目标之一） | （恢复目标之一） | （恢复目标之一） | （恢复目标之一） | | — |

> S7 恢复 = 回退出前状态（S1–S5 之一）并续作同一会话；**永不回 S6**。

**0.2.4 关键规则**

1. **互斥**：任意时刻只处于 7 状态之一；S3/S4/S5/S6 间无直接跳转，必经 S2
2. **扫描二选一**：S4、S5 由 ScanMode 在 S2 入口决定；切换需经 S2 重选
3. **故障恢复续作**：S7 自检通过后回退出前状态 + 续作同一会话。⚠ 依赖检查点机制（`loadCheckpoint`，当前 TODO）
4. **S6 免疫**：离线 batch 不走硬件采集路径，不进 S7；GPU/算法失败由 `Result.status` 返回、操作员处置
5. **S7 安全停机**：进入即下位机采集全停——相机停 + 补光灯关 + 激光关
6. **S1 非设备类失败**（加密狗/授权）不进 S7，留 S1 阻塞告警
7. **急停不采用**；扫描启停纯操作员驱动（覆盖率仅显示，不触发自动退出）

**0.2.5 S4/S5 子状态机（就绪/采集）**

```
            上位机图标（从 S2 进入）
                    │
                    ▼
             ┌─────────────┐  扫描仪按键   ┌─────────────┐
             │  就绪 Ready  │ ──────────► │ 采集 Capture │
             │ (持连接,不出帧)│ ◄────────── │(实时出帧+算子链)│
             └──────┬──────┘  扫描仪按键   └──────┬──────┘
                    │                             │
                    └──────── 上位机图标 ─────────┘
                         （就绪/采集任一，点击即退出→ S2）
```

- 就绪↔采集：扫描仪按键 toggle（MCU `KeyEvent`，已实现）；任一→S2：上位机图标（UI 待建）；任一子态可 → S7

**0.2.6 落地依赖（按优先级）**

1. 检查点机制（`loadCheckpoint`）— S7 续作硬依赖
2. 模块8 上电自检 + 许可证检查 — S1 实质功能
3. 故障主动响应链（08 → `FaultHandler::reportFault`）— S7 上报
4. 覆盖率计算（体素哈希）— S4/S5 显示
5. UI（自检画面/扫描图标/故障告警/覆盖率）
6. `ScannerState` 枚举对齐 7 态：含就绪/采集子态、删 EmergencyStop

### 0.3 代码现状

- 9 态 + 事件换态 + 操作权限 + EventBus 广播（与 07 文档 §2 描述一致）
- 真实接线：设备连接/断开（ScannerWindow）、故障/清除（本模块 FaultHandler）；扫描启停仍零调用者（待 07_pipelinemgmt WorkflowManager 接线）

## 1. 代码实际实现的（真实文件路径 + 简述）

### 1.1 FaultHandler（本模块主体）
位于 `modules/10_observability/`，构建为静态库 `mod_observability`：
- `modules/10_observability/FaultHandler.h` / `FaultHandler.cpp` — 故障处理器，命名空间 `Scanner::service`
- `modules/10_observability/StateMachine.h` / `StateMachine.cpp` / `IState.h` — 全局门禁状态机（自 07 迁入，见 §0）
- `modules/10_observability/CMakeLists.txt` — `mod_observability` STATIC，链接 `base` + `spdlog`（~~mod_session 链接已删除~~）

**依赖类型**：`FaultSeverity`、`EventType`（含 `FaultOccurred=0x0400` / `FaultCleared=0x0401`）定义于 `base/types.h:82,107`；`StateMachine`（本模块内，迁移后同库直引）；`EventBus`（`Scanner::infra`，头在 `base/EventBus.h`）。

**实际行为**（`FaultHandler.cpp`）：
- `start()`（`:11`）订阅 `EventType::FaultOccurred` → `spdlog::error` 记录 → 若 `severity >= Error` 调 `StateMachine::transition(FaultOccurred)` 转 Error 态 → **复用 `EmergencyStop` 通道** `publish` 一个红灯事件（`:26-31`）→ 触发已注册的 `faultCallback_`
- `reportFault(source, severity, message)`（`:45`）：手动上报 —— 记日志 + `publish(FaultOccurred)` + (>=Error) `transition` + 回调
- `clearFault(faultId)`（`:63`）：记录 + `publish(FaultCleared)` + `transition(FaultCleared)`
- `onFault(cb)`：注册故障回调；`setEventBus` / `setStateMachine`：注入依赖

**装配**：`app/AppContext.cpp:40-45` 实例化 `faultHandler_` → `setEventBus` + `setStateMachine` + `start()`；经 `AppContext.h:37` 的 `faultHandler()` 暴露。

### 1.2 散落在外的日志实现（非本模块目录，但属可观测性范畴）
- spdlog 基础：`app/main.cpp` 设 `spdlog::set_level(info)`，控制台输出
- `CALIB_LOG_*` 分类标签日志宏：`modules/09_operatorlib/core/common/calib_logging.h`（DEBUG/INFO/WARN/ERROR 自动附加算子标签），仅用于算子库与 `factory_calib`

> ⚠ 旧版文档称 FaultHandler 位于 `framework/service` —— **已过时**：framework/ 已退役删除，FaultHandler 现落点为 `modules/10_observability`。

## 2. 代码需要调整的

- **`reportFault` / `clearFault` 无任何外部调用者**（全仓 grep 仅命中定义与声明）—— 手动故障上报 API 形同虚设，需接线（见 §3 故障主动响应链）。
- **EventBus `FaultOccurred` 无探测器 `publish`** —— `start()` 的订阅链半接：当前仅 `reportFault` 手动触发，硬件/算子链路未发布故障事件，自动响应路径实际不激活。
- **红灯事件复用 `EmergencyStop` 通道**（`FaultHandler.cpp:28`）—— 语义混淆（故障≠急停），需引入独立的指示灯控制事件类型。
- **`reportFault` 发布的事件丢失 `source` 标识** —— `evt.param2=0`（`:52`），订阅侧日志只能打印 `source=0`；故障来源信息未进入事件流。
- **`CALIB_LOG_*` 分类标签仅在算子库可用** —— 未推广到其他模块，统一日志口径缺失。

## 3. 需要增加的

### 3.1 故障主动响应链（接线）
- 模块 7（会话/扫描）检测相机断连 / MCU 通信失败 / GPU OOM → 主动降级或暂停 → 调 `FaultHandler::reportFault`（当前模块 7 未接入）
- 模块 8（设备管理）检测 USB 断连 / 串口超时 / 温度异常 → 自动停激光 → 调 `FaultHandler::reportFault`（当前模块 8 未接入）
- 接入后 `start()` 订阅链方能真正自动触发（→ 转 Error 态 + 红灯 + 回调）

### 3.2 统一日志
- 配置文件驱动（级别 / 格式 / 轮转 / 多 sink）
- 日志轮转（文件 10MB × 5）
- 多 sink（控制台彩色 + 文件 + 可选远程 syslog）
- 分类标签（`CALIB_LOG_*`）推广到全部模块

### 3.3 性能监控
- `PerfCounters`（采集帧率 / 丢帧 / 处理帧率 / GPU·CPU·端到端延迟 / 标记点·激光点·融合点数 / 温度 / GPU 显存）
- 实时 Dashboard 折线图
- 告警（帧率低于阈值 / 温度过高 / GPU OOM）

### 3.4 崩溃捕获
- Windows 崩溃捕获（`SetUnhandledExceptionFilter` + `MiniDumpWriteDump`）
- 崩溃后自动重启 + 会话恢复
- 崩溃报告打包（minidump + 日志 + 系统信息）上传
