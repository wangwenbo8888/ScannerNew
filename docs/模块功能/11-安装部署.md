# 模块11 · 安装部署 + 首次配置

> 返回索引：[`模块功能目录.md`](../../模块功能目录.md)

## 职责
安装包/卸载, 首运行向导(连接设备/选语言/初始标定引导)。

## 已实现功能（以代码为准）

> `modules/11_deploy` 目录为占位；现有实现仅为开发阶段部署脚本。

### 开发阶段部署
- `app/copy_dlls.bat`：构建后拷贝运行时依赖（Qt / OSG / OpenCV DLL + Qt platform 插件，区分 Debug/Release）

## 待实现功能
### 安装包
- Windows 安装包生成（NSIS / Inno Setup）
- 依赖打包（大恒 Galaxy SDK 运行时 + CUDA 运行时 + VC++ redistribution + OSG + Qt）
- 桌面快捷方式（jeamui.exe）
- `.jmw` 工程文件关联

### 首次配置向导
- 步骤1：语言选择（中文 / English）
- 步骤2：设备连接检测（相机枚举 + MCU 通信 + GPU 检测）
- 步骤3：初始标定引导（多姿态采集）
- 步骤4：许可证激活（加密狗 / 序列号）
- 步骤5：完成 → 进入主界面

### 卸载
- 清理用户数据（可选保留）
- 注销许可证
