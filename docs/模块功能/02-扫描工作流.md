# 模块2 · 扫描工作流

> 返回索引：[`模块功能目录.md`](../../模块功能目录.md)
> 2026-08-17 重构：仅保留 §1 流程图 / §2 数据流 / §3 代码现状三部分；算子细节见 [`docs/流水线/客户端扫描流水线.md`](../流水线/客户端扫描流水线.md)，调度细节见 [`docs/流水线/扫描并行调度260629.md`](../流水线/扫描并行调度260629.md)。同日经人工确认补定**对象所有权拆分**（D7：app 存活件 / 会话私有件），§1 D1/②/⑧/⑩ 与 §2 图同步修订；2026-08-18 应人工指令，§1.2 排版优化为图文版（app 存活件前置 + 完整流程图[app 存活件与工作流 ①→⑩ 合一，逐步明细之公/私与 ✓/⚠ 现状已浓缩入图，明细表删除]），流程语义与各定案不变；同日人工确认增补：**状态机定为第 5 个 app 存活件**（app 构造持有的全局公共对象；实现代码按既定计划由 07 迁 10——「app 造实例、模块出代码」，与 EventBus/仓库同模式），D7 与 §1.2/§2 图同步修订

---

## ⚠ AI 读者必读（本文档使用规则）

> **§1 工作流程图是最终确定版（定稿 2026-08-17），AI 不得自行修改。**
>
> - 若发现流程图与其他文档或代码**存在冲突**：**必须先向人说明冲突点、征询意见，获得认可后**才可修改，禁止擅自改图或改其他文件迁就。
> - §2 数据流图同样受此保护（与 §1 同批定稿）。
> - 仅 §3（代码实际实现的）随代码演进可由 AI 更新，更新时须如实反映代码现状。

---

## 1. 工作流程图（对象自治版）🔒 最终确定版 · 2026-08-17（2026-08-18 图文版重排，语义不变）

### 1.1 定案要点（AI 一眼可见）

| # | 定案 | 内容 |
|---|---|---|
| **D1** | **对象自治** | 设备管理（DeviceManager/08）、扫描处理流水线（07）、全局优化（07）、数据管理（06）、渲染（03）五对象封装自治；**02 只做①门禁进入、②装配（注入+构造，见 D7）、⑨旁观显示、⑩停止回收（仅会话私有件，见 D7）** |
| **D2** | **硬件按键触发** | ③ 启动采集与 ⑥ 停止采集**只由硬件按键电报触发**（08 DeviceManager 接收 → 指令转发下位机），软件不直接启停采集 |
| **D3** | **GBA 软件触发** | ⑦ 由用户点击**软件界面**「完成扫描」触发；④⑤⑧ 为自动段（02 零干预） |
| **D4** | **生命周期边界** | **⑦ GBA 优化完成 = 工作流终结**。续扫必须从 ① 重开新工作流（旧点云经 existingMarkers 注入作基准点）；⑥ 后就绪态内再按键续采（回 ③④⑤）**仍属同一工作流** |
| **D5** | **结果不落盘** | ⑧ 优化后点云**留存内存**（点云缓冲），04 后处理从内存调用；落盘仅在明确请求时执行 |
| **D6** | **会话记账归 02** | 会话记账由 02 自己承担，不设独立会话对象：①开账（会话 ID+开始时间戳）、④⑤计数（帧数/覆盖率，经事件汇总只记数值）、⑩合账（结束时间戳+统计存档）（2026-08 定案；01 标定则不记帧数/姿态数）。**无自动定期存档**：断电续扫检查点不自动写——存档仅由**人工手动触发**，想存时手动存（与 D5「不主动落盘」一致） |
| **D7** | **对象所有权拆分** | 跨工作流存活件归 **app**（应用生命周期；02 只接线注入，⑩ 不销毁）：DeviceManager(08)、标定参数仓库+点云仓库(06)、渲染基座(03)、EventBus、**状态机（2026-08-18 人工确认增补为第 5 个 app 存活件：全局状态唯一且跨工作流互斥存活，故实例归 app 构造持有；实现代码按既定计划由 07 迁 10）**。**会话私有件归 02**（② 构造、⑩ 逆序销毁）：扫描处理流水线/全局优化对象(07)、帧环形缓冲实例(06)、场景内容供给器(03)。**点云仓库不随 ⑩ 销毁**——D5「留存内存供 04」由 app 仓库兑现，D4 续扫 existingMarkers 基准点亦从该仓库读取；DeviceManager 于 ⑩ 切回 idle。判据：数据跨工作流存活→容器归 app，仅会话内消费→归工作流（2026-08-17 人工确认补定） |

### 1.2 流程图

> 图例：✓ = 该模块对应能力已存在；⚠ = 尚不存在或部分缺失（规划中）。
> 调用与现状均只到**模块级**（子模块归属见各模块文档）。

#### 前置：app 存活件（应用启动即构造，先于本工作流存在）★D7

以下对象由 app 构造并持有整个应用生命周期——不归本工作流构造/销毁（② 只接线注入、⑩ 不销毁），但流程中反复出现，先列出便于阅读。判据（D7）：数据跨工作流存活 → 容器归 app；仅会话内消费 → 归工作流。

| app 存活件 | 公/私 | 通俗职责 |
|---|---|---|
| DeviceManager 设备管理器（08） | 公用 | 08 对外唯一门面：把相机 + 下位机 + 按键 + 温度监测封装成一个「采集服务」——接收硬件按键电报、向下位机转发指令；02 调 `enterScan()` 即切入扫描模式 |
| 数据管理（06） | 仓库公用 + 会话件私有（同一对象两副面孔） | **仓库（公用，app 持有）**：标定参数仓库（内参/矫正参数 + 两张温度补偿表，01 产出）+ 点云仓库（扫描结果的内存保管处，02→04 内存交接点）；**会话件（私有，② 构造、⑩ 销毁）**：扫描配置 ScanConfig + 帧环形缓冲实例，详见 ② |
| 渲染基座（03） | 公用 | 三维视窗 + 场景切换器；纯显示，不反向控制流程 |
| EventBus 事件总线（base） | 公用 | 全局事件订阅/发布通道；02 靠它「旁观」帧数/覆盖率/温度等事件（只记数值，不碰数据） |
| 状态机（代码由 07 迁 10） | 公用 | 全系统唯一一份「现在处于什么状态」的裁决者：① 门禁检查与 ③⑥⑩ 状态切换都问它；实例归 app（2026-08-18 增补，实例 app 造、代码模块出——与 EventBus/仓库同模式） |

#### 完整流程图（app 存活件 + 工作流 ①→⑩ 一张图）

> ★D1–D7 语义见 §1.1 定案表。【公】= app 存活件（跨工作流公用）；【私】= 会话私有件（② 构造、⑩ 逆序销毁）；每步末行（现状：…）为 ✓/⚠ 摘要。

```
━━━ app 存活件【公】 ┏ 应用启动即构造 · 全程存活 · ② 只接线注入 · ⑩ 不销毁 ★D7 ┓
  08 DeviceManager「采集服务」 —— 相机+下位机+按键+温度封装一体，08 对外唯一门面
  06 标定参数仓库 —— 存放 01 产出：内参/矫正参数 + 两张温度补偿表
  06 点云仓库 —— 扫描结果的内存保管处，供 04 调用、续扫作基准
  03 渲染基座 —— 三维视窗 + 场景切换器，纯显示不反向控制流程
  base EventBus —— 全局事件通道，02 靠它旁观帧数/覆盖率（只记数值 ★D6）
  状态机（代码迁 10） —— 全局唯一状态裁决：门禁/状态切换都问它（2026-08-18 增补第 5 件）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                   │
                   ▼
① 进入工作流（就绪态）
   用户点「扫描」 —— 软件入口命令（app UI）
   ↓ 状态机门禁【公】 —— 问 app 状态机「现在能进吗」，状态不合法不放行（代码 10）
   ↓ 06 参数就绪检查【公·仓库】 —— 开工前查库存：标定参数/温度表齐不齐
   ↓ 开账 —— 会话 ID + 开始时间戳（记账归 02 ★D6）
   ↓ 待命 —— 装配完成后不启动采集，等硬件按键
   （现状：app UI ✓；10 门禁 ⚠ 状态机迁 10 未完成；06 ⚠ 温度表契约未实现，仅简化参数注入）
                   │
                   ▼
② 装配 ★D7
   注入 4 个 app 存活件【公】 —— 只接线不 new；08 调 enterScan() 切扫描模式
   ↓ new 会话私有件【私】 —— ② 构造、⑩ 逆序销毁，共 4 件：
       · 07 扫描流水线 —— 标记点链CPU ‖ 激光链GPU + 配准 + 融合，核心引擎
       · 07 全局优化 GBA —— 收尾批处理优化，⑦「完成扫描」时触发
       · 06 会话件 —— ScanConfig（enableLaser 双模式 / existingMarkers 基准点 / BA 收尾开关）+ 帧环形缓冲实例
       · 03 场景内容供给器 —— 实时点云层，挂到公用渲染基座
   （现状：06 配置加载 ⚠；08 连续采集 ✓ 电报接收 ⚠；07 并行调度 ⚠ 算子 09 大体就绪；03 基座 ✓ 实时渲染 ⚠；D7 代码未落地）
                   │
                   ▼  ══ 此后 02 零干预，实时旁观 ══
③ 启动采集 ★D2
   用户按硬件按键 —— 唯一合法启动方式，软件不启停采集
   ↓ 08 接电报 → 转发下位机【公】 —— 设备自治处理，02 不掺和
   ↓ 进入采集态 —— 扫描流水线开始消费帧
   （现状：08 连续采集 ✓；电报接收/转发 ⚠ 协议待对齐）
                   │
                   ▼
④ 采图入缓冲
   08 持续产出双目帧 + 帧温【公】 —— 相机连拍，每帧自带温度
   ↓ 写入 06 环形缓冲【私·会话件】 —— 中转站：采图与计算解耦，互不等待
   ↓ lag 超限处理 —— 消费跟不上就跳最新丢中间，永远追新帧
   （02 记账 —— 只记帧数不碰帧数据 ★D6；现状：08 采图 ✓；FrameBuffer ✓；调度环形缓冲 ⚠）
                   │
                   ▼
⑤ 逐帧处理与融合（持续运行至用户结束）
   07 抓帧【私】 —— 从环形缓冲取帧，抓到即「增强帧」
   ↓ 06 出口查表【私·会话件】 —— 按帧温查两张补偿表，随帧附标定快照（算子无查表动作）
   ↓ 双链并行 —— 标记点链 CPU/P核 ‖ 激光链 GPU 槽，各跑各的
   ↓ 帧间配准 —— PrevFrameState 快照，算相邻帧相对位姿
   ↓ 融合累积 —— 逐帧拼进全局点云，越扫越全
   ↓ 推 03 实时刷新 —— 三维窗口实时看点云长大
   （02 记账 —— 只记覆盖率不碰点云 ★D6；现状：07 单线程调试骨架可跑标记点链+融合 ⚠；出口查表 ⚠；实时渲染 ⚠）
                   │
                   ▼
⑥ 停止采集 ★D2
   用户按硬件按键 —— 与 ③ 同源，硬件触发
   ↓ 08 接电报 → 转发下位机【公】 —— 设备自治停止
   ↓ 回就绪态 —— 对象不销毁，累积数据保留
   ├─ 就绪态再按键 → 回 ③④⑤ —— 续采，仍属同一工作流
   └─ 点软件「完成扫描」→ ⑦ —— 进收尾优化
   （现状：停止采集可用 ✓；电报接收/转发 ⚠）
                   │
                   ▼
⑦ GBA 全局优化 ★D3/D4
   软件界面触发 —— 区别于 ③⑥ 的硬件按键
   ↓ 读逐帧观测累加器【私】 —— ⑤ 攒下的观测数据在此兑现
   ↓ Ceres GBA + 位姿图闭环 —— 全局批优化全部位姿，消累积误差
   ↓ 修正位姿回写重融合 —— 用准位姿重拼点云；渲染冻结末帧
   （优化完成 ＝ 工作流终结 ★D4；现状：GBA 算子 09 已实现，工作流未接入 ⚠）
                   │
                   ▼
⑧ 结果留存内存 ★D5
   存入 06 点云仓库【公】 —— app 存活件，不随 ⑩ 销毁
   ↓ 04 后处理直接内存调用 —— 免文件中转
   （落盘仅在明确请求时；续扫从 ① 重开——旧点云 → existingMarkers 基准点；现状：点云缓冲 ✓；按需导出/与 04 交接契约 ⚠）
                   │
                   ▼
⑨ 显示扫描结果
   03 全程驱动三维视窗【公基座+私场景】 —— 一窗到底，三种画面
   ↓ 采集/处理中：实时点云 —— 看扫描进度
   ↓ GBA 期间：冻结末帧 —— 优化中不闪屏
   ↓ 优化后：最终点云 + 完成提示 —— 收工画面（02 UI）
   （现状：OSGWidget 基座 ✓；实时点云渲染 ⚠；02 UI ⚠）
                   │
                   ▼
⑩ 回收退待机 ★D6/D7
   02 合账 —— 结束时间戳 + 统计存档
   ↓ 逆序销毁会话私有件【私】 —— 流水线/GBA/帧缓冲/场景内容
   ↓ app 存活件不销毁【公】 —— 08 切 idle；点云仓库留存供 04（★D7）
   ↓ 状态机切回待机【公】 —— app 状态机裁决切态，等下一轮工作流（代码 10）
   （无自动存档 —— 存档仅人工手动触发；现状：状态机迁 10 未完成 ⚠）
                   │
                   ▼
                 待机
```

---

## 2. 对象装配与数据流 🔒 与 §1 同批定稿

```
 app 存活件（应用生命周期；② 接线注入，⑩ 不销毁——设备切 idle、点云仓库留存）：
 ┌─────────────────────────────────────────────────────────────────┐
 │ 08 DeviceManager（模式门面）· 06 标定参数仓库+点云仓库 · 03 渲染基座 · base EventBus · 状态机(07→10) │
 └─────────────────────────────────────────────────────────────────┘

         ②装配        ③按键电报→采集    ⑥按键电报→停止     ⑦完成扫描(软件)   ⑩停止/销毁
 02 ─────────▶ 会话私有件 ──────────▶ 持续运行 ─────────▶ GBA批处理 ─────────▶ 逆序回收
 扫描工作流  （构造私有件+接线 (08接电报→ （④⑤ 自动）        （⑧ 自动）      （仅会话私有件；
              app 存活件）     下位机)   ▲  (06 后回就绪)  (工作流终结)    点云仓库留存）  ▲
                                        │ 按键电报（硬件按键，08 接收）       │ 续扫=重开新工作流
                                        └─ 就绪态再按键：回 ③④⑤ ─┘          （旧点云→existingMarkers）

  运行期数据流（02 不参与数据流，只旁观事件）：

   ┌───────────────────┐ 双目帧+帧温 ┌───────────────────┐ 抓帧(增强帧) ┌────────────────────┐
   │ 08 DeviceManager  │ ─────▶ │ 06 环形缓冲容器实例 │ ─────▶ │ 扫描处理流水线对象   │
   │ (app·扫描模式)     │  写入   │ (会话私有·解耦)     │        │ (07·会话私有)       │
   │  接电报→指令      │        │ 出口查表：按帧温每帧 │        │ 双链并行→配准→融合   │
   └────────┬──────────┘        │ 查两张补偿表，随帧附 │        │（算子无查表动作）   │
          │                     │ 带标定快照           │        └──┬───────────┬────┘
          │        ▲ 停止/续采（按键电报，经 08 指令下位机；流水线随之暂停/续跑）
          │ 温度/状态事件      │融合点云     │逐帧观测累加
          ▼                  ▼           ▼ ⑦触发
      base EventBus     ┌──────────┐  ┌──────────────┐
          │             │ 03 渲染   │  │ 全局优化对象   │
          └─进度事件─▶   │ 基座(app) │  │ (07·私有)GBA  │
                        │+场景内容  │  │ 修正位姿回写   │
                        └──────────┘  └──────┬───────┘
                                             │ 优化后点云
                                             ▼
                                       ┌──────────────┐
                                       │ 06 点云仓库   │
                                       │ (app·留存内存)│
                                       │ 续扫累积 / 04 │
                                       │ 内存调用 / 按 │
                                       │ 需导出        │
                                       └──────────────┘
   装配期：数据管理会话件（06）向流水线注入标定参数（内参/温度表，取自 app 标定参数仓库）；查表在缓冲出口逐帧执行（表由会话件在装配期装载）
```

> **要点**：渲染是**纯消费者**（基座 app 持有，场景内容会话挂载；只消费点云刷新视窗，不反向控制流程）；对象间触发机制（帧缓冲 / EventBus）由实现期定，图不锁定。

---

## 3. 代码实际实现的（AI 可随代码演进更新此节）

### 3.1 ScanWorkflow 工作流骨架

- `modules/02_scanning/ScanWorkflow.h` / `ScanWorkflow.cpp` — 扫描工作流，命名空间 `Scanner::workflow`，实现 `IWorkflow`。核心类型：
  - `ScanWorkflow` — 工作流主体（`initialize/start/pause/resume/stop` + `scanLoop()` 单线程扫描循环 + `setScanMode/setCalibration`）。
  - `ScanCalibration` — 扫描所需标定参数（L/R 内参畸变 + `R1/R2/P1/P2/Q` + `imageSize`），由 CalibrationWorkflow 产出注入。
  - `ScanFrameResult` — 单帧结果（markerPoints3d / laserPoints3d / markerNormals / R / T）。

### 3.2 五个 Stage（Stage0~Stage4）

| Stage | 类 | 现状实现 |
|---|---|---|
| Stage0 Capture | `CaptureStage` | 从 `FrameBuffer::popFrame(100ms)` 取帧到 `latestFrame_` |
| Stage1 Preprocess | `PreprocessStage` | **CPU 手搓掩码**：`cv::adaptiveThreshold` + `morphologyEx` 生成左右标记点掩码（非 `mask_separation_cuda` 算子） |
| Stage2 Marker | `MarkerStage` | 标记点链：`connectedComponentsWithStats`(CCL) → `calib::ZernikeEdgeCPU` → `calib::EllipseFitCPU` → `calib::MarkerMatchCPU` → `calib::PointReconstructCPU`。算子对象在 Stage 构造时创建一次复用 |
| Stage3 Laser | `LaserStage` | **空壳**：`MarkerOnly` 直接返回 ok；否则记日志"需要 CUDA，跳过"。激光链完全未接入 |
| Stage4 Fuse | `FuseStage` | `calib::LaserCloudFuseCPU` 体素融合 → `PointCloudBuffer::pushPointCloud` |

`ScanWorkflow::scanLoop()`（`ScanWorkflow.cpp:335`）：单线程 `while(running_)` 同步串行跑 Capture→Preprocess→Marker→(Laser 跳过)→Fuse，每 100 帧打一条日志。**非** §1 设计目标的多流水线并行。

### 3.3 扫描算子（在 `modules/09_operatorlib/scanning/`，命名空间 `calib::`）

扫描【算子】物理位于 09 算子库，非 02 模块：

| 目录 | 算子 | 是否已接入 02 |
|---|---|---|
| `preprocess/mask_separation/` | `laser_markingpoint_mask_separation_cuda`（联合掩膜分离，CUDA） | 否（Stage1 用 OpenCV 替代） |
| `laser/laser_match_scan/` | `laser_match_scan_cuda`（激光线匹配扫描，查温度映射表） | 否（Stage3 空壳） |
| `fusion/laser_cloud_fuse/` | `laser_cloud_fuse_cpu`（体素哈希融合，CPU） | **是**（Stage4） |
| `fusion/laser_cloud_fuse_cuda/` | `laser_cloud_fuse_cuda`（CUDA 版融合） | 否 |
| `fusion/laser_cloud_normal/` | `laser_cloud_normal_cpu`（法线估计） | 否 |
| `fusion/laser_cloud_normal_cuda/` | `laser_cloud_normal_cuda` | 否 |
| `global_optim/` | `global_ba_cpu` + `ba_residuals.h` + `pose_graph_residuals.h`（GBA/PGO）；`gba_dataset_runner.cpp` 离线 exe | 否（算子已实现，工作流未接入） |

> 注：steger / undistort_cuda / epipolar_interp / laser_reconstruct 等【激光链算子】位于 `modules/09_operatorlib/core/laser/`，标记点链其余算子位于 `modules/09_operatorlib/core/marker/`（双链前端共用 `core/vision`）。详见 [09-算子库.md](09-算子库.md)。

### 3.4 编译归属 & 相关文件

- `ScanWorkflow.{h,cpp}` 由 `app/CMakeLists.txt:47-48` 编入 `jeammware.exe`。`modules/02_scanning/CMakeLists.txt` 仅占位 `add_library(mod_scanning INTERFACE)`，不构建 02 模块库。
- `app/ScannerWindow.{h,cpp,.ui}` — 扫描 UI 窗口（启停扫描、模式选择），**已迁 `app/`**，不在 02 模块。
- `base/types.h:89` — `enum class ScanMode { MarkerOnly=0, MarkerPlusLaser=1 }`。**ScanConfig.h 尚未建立**。
