# 模块功能目录

> 本文件为 [`docs/模块功能/`](docs/模块功能/) 下各模块文档的**目录与简介索引** + **设计对象速查基准**。

>
> **对象清单说明**：仅提取自 [`01-标定工作流.md`](docs/模块功能/01-标定工作流.md) 与 [`02-扫描工作流.md`](docs/模块功能/02-扫描工作流.md) 的**流程图（🔒 定稿版）**（07 后处理流水线对象依 2026-08-18 人工指令增补）。这些对象是**设计目标**（07 五对象与调度底座、10 状态机[+统一命令通道]已于 2026-08-20 落地；08 DeviceManager 于 2026-08-21 落地——其余多数尚未实现，如 05 编辑/03 场景内容供给器等）——后续以它们为基准，对各模块已有代码**重构或新增**。「01-②」= 01 文档流程图步骤②。
>
> **归属标注（2026-08-18 同步 D7/D8 定案）**：【公】= app 存活件（app 启动即构造、全程存活；工作流 ② 只接线注入、⑨/⑩ 不销毁）——共 **5 件**：DeviceManager(08)、数据管理·仓库(06)、渲染基座(03)、EventBus(base)、**状态机（2026-08-18 人工确认增补第 5 件；实例 app 造，实现代码已由 07_session 迁入 10〔2026-08-20 完成〕——「app 造实例、模块出代码」）**。【私】= 会话私有件（工作流 ② 构造、⑨/⑩ 逆序销毁）。
>
> **落地状态速记（2026-08-21，详见各模块文档 §2）**：07 五条流水线对象＋并行调度底座已落地（`mod_pipelinemgmt`，真机联调待做）；状态机代码已自 07_session 迁入 `mod_observability`（07_session 整目录退役）；01/02/04 旧帧处理链退役，改为装配 07 对象、自身只留编排+记账；10 可观测性六件已落地（7 态 CAS 状态机重写 + CommandGate 统一命令通道双口 + FaultHandler 故障档案表 + ObsLogger 文件轮转 + CrashHandler + PerfMonitor；app 自检清单与 01/02/04 门禁接线完成，04 无 UI 入口；08 指标源/poll 轮询已接通，故障完整链桥两待办见 10 文档 §3，UI 状态图标待做）；**08 设备管理已落地（2026-08-21）**——serial/ 协议层三小层（v2/v3 开关+CRC16+ACK 重传+命令组步链）+ KeyManager/MenuLogic/KeySemantics 按键链（弃奇偶）+ **DeviceManager 总门面**（逻辑线程 post 编队/故障 8 码/ParamStore 参数账本/Warmup 预热/ModeController 模式黑板）+ CameraControl 收敛 + SelfCheckCollector（PDH/NVML）+ HardwareMonitor 三件套 + app 三行装配收拢（ScannerWindow 串口旁路删净）；真机联调与 v3 协议定稿跟进待做；全量 ctest **90/90 绿**。
>
> **🔒 第三部分「设计对象功能基准」为人工锁定基准**：各模块重构/新增/迁移代码一律以它为准对齐；**该节内容未经人工审核批准不得修改**（AI 不得自行增删改，发现冲突先报告人工）。

---

## 一、模块目录

| # | 模块 | 简介 | 文档 |
|---|------|------|------|
| 1 | 标定工作流 | 相机/激光标定流程 | [`01-标定工作流.md`](docs/模块功能/01-标定工作流.md) |
| 2 | 扫描工作流 | 双模式扫描流水线 | [`02-扫描工作流.md`](docs/模块功能/02-扫描工作流.md) |
| 3 | 渲染显示 | OSG+Qt 3D 渲染（点云/网格/标志点加载显示） | [`03-渲染显示.md`](docs/模块功能/03-渲染显示.md) |
| 4 | 后处理 | 离线批处理五阶段（法线→封装→补洞→光顺→边界优化）出 STL；GBA/重融合归 02（Q5 定案） | [`04-后处理.md`](docs/模块功能/04-后处理.md) |
| 5 | 编辑 | 点云编辑（套索选中/删除/撤销），从模块3解耦 | [`05-编辑.md`](docs/模块功能/05-编辑.md) |
| 6 | 文件管理与数据管理 | 文件 IO + 运行时数据管理（容器/Sink 契约/产物仓库） | [`06-文件管理.md`](docs/模块功能/06-文件管理.md) |
| 7 | 流水线管理 | 并行调度底座 + 5 条业务流水线对象（姿态判断/标定计算/扫描处理/全局优化/后处理），供 01/02/04 会话装配 | [`07-流水线管理.md`](docs/模块功能/07-流水线管理.md) |
| 8 | 设备管理 | 相机/MCU/按键/温度监控与控制 | [`08-设备管理.md`](docs/模块功能/08-设备管理.md) |
| 9 | 算子库 | 纯计算叶子算子集合（详见算子目录.md） | [`09-算子库.md`](docs/模块功能/09-算子库.md) |
| 10 | 可观测性/运维 | 状态机门禁 + 统一命令通道 + 故障链 + 日志/性能/崩溃捕获（六件已落地，2026-08-20） | [`10-可观测性运维.md`](docs/模块功能/10-可观测性运维.md) |
| 11 | 安装部署 | 安装包/卸载，首运行配置向导 | [`11-安装部署.md`](docs/模块功能/11-安装部署.md) |

---

## 二、对象对口速查（设计基准 · 按模块顺序 · 01/02 流程图用到谁）

> 本表 = **设计需要的对象统计**（非已有代码），后续重构/新增以此为基准。空格 = 01/02 流程图暂未涉及该模块的对象。

| 模块 | 设计对象 | 归属 | 用在哪 |
|---|---|---|---|
| **01 标定工作流** | 标定工作流（编排者本身） + 会话记账 | 01 自身 | 01 全程：①门禁进入、②构造装配、③启动、⑧旁观显示、⑨停止回收；记账（①开账/⑨合账，帧数与姿态数不记——由算子内部处理） |
| **02 扫描工作流** | 扫描工作流（编排者本身） + 会话记账 | 02 自身 | 02 全程：①②⑨⑩；记账（①开账 / ④⑤计数帧数+覆盖率 / ⑩合账；无自动定期存档，存档仅人工手动触发） |
| **03 渲染显示** | 渲染基座（三维视窗+场景切换器） | 【公】app 存活件 | 01-②⑤⑧；02-②⑤⑨ |
| | 场景内容供给器（实时层，挂载到基座） | 【私】会话件 | 01-②⑤⑧；02-②⑤⑨ |
| **04 后处理** | 后处理工作流（编排者本身）+ 会话记账 | 04 自身 | 04 全程（离线五阶段出 STL；输入自 02-⑧ 点云仓库内存调用；不过设备门禁，S6 免疫 S7） |
| **05 编辑** | — | — | — |
| **06 文件管理** | 数据管理·仓库（01 标定结果仓库 / 02 标定参数+点云仓库） | 【公】app 存活件 | 01-②⑦；02-①②⑧ |
| | 数据管理·会话件（标定配置/姿态表/ScanConfig + 帧环形缓冲实例） | 【私】会话件 | 01-②④⑤；02-②④⑤ |
| **07 流水线管理** | 姿态判断流水线对象 | 【私】会话件 | 01-⑤ |
| | 标定计算流水线对象 | 【私】会话件 | 01-⑥ |
| | 扫描处理流水线对象 | 【私】会话件 | 02-⑤ |
| | 全局优化对象（GBA） | 【私】会话件 | 02-⑦ |
| | 后处理流水线对象（2026-08-18 人工补充） | 【私】会话件 | 04 后处理全程（04 无定稿流程图，不挂步骤号） |
| **08 设备管理** | DeviceManager 设备管理器（**已落地 2026-08-21**：总门面+serial 协议层+按键链+监控全量，`modules/08_devicemgmt/`） | 【公】app 存活件 | 01-②③④⑨；02-②③④⑥⑩ |
| **09 算子库** | （流程图不直接引用对象，算子由 07 各流水线对象调用） | — | 01-⑤⑥；02-⑤⑦（经流水线对象） |
| **10 可观测性运维** | 状态机（全局状态裁决；**已落地 10**：7 态 CAS 重写 + CommandGate 统一命令通道〔2026-08-20〕） | 【公】app 存活件（**第 5 件**，2026-08-18 增补） | 01-①⑨；02-①③⑥⑩ |
| **11 安装部署** | — | — | — |
| **base 共享内核** | EventBus 事件总线 | 【公】app 存活件 | 01-④⑧；02-④⑨ |

---

## 三、设计对象功能基准（每个对象干什么活——各模块开发的对齐基准）

> **⚠ 本节是设计基准（🔒 与 01/02 流程图同源同保护级别）**：各模块后续**重构 / 新增 / 迁移代码时，必须以本节为基准对齐**——对象职责边界、归属（【公】/【私】）、交互方式不得自行偏离。**未经人工审核批准，任何人（含 AI）不得修改本节内容**；若发现本节与流程图或代码现状冲突，须先向人说明冲突点、征得认可后方可改动。
>
> 2026-08-18 人工指令重排：分组改为按归属（【公】app 存活件 /【私】01·02 会话件 / 支撑约定），内容逐项对齐 01/02 流程图定稿补全（ScanConfig 三开关、出口查表增强帧、温度表分工、01 预热、PJC、GBA 观测累加器、点云仓库三用途等），并修正「连续命中才确认」表述。
>
> 2026-08-20 同步（依 07 文档登记的人工指令与迁移事实）：①状态机「迁 10」标注更新为已完成（代码现状，D8 语义不变）；②增补【私】04 后处理的会话件（后处理流水线对象——07 文档 §1.1-E 2026-08-18 人工补充，原「待基准同步」项）；③状态机行补落地状态标注（10 六件落地、7 态重写+命令通道——代码现状，职责/归属/交互语义不变，未经人工批准不再改内容）。
>
> 2026-08-21 同步（代码现状标注，沿用上条先例）：DeviceManager 行（§三【公】表）补落地标注——08 已落地（总门面+serial 协议层+按键链+小组件+故障 8 码）；职责/归属/交互语义一字未改，未经人工批准不再改内容。

### 【公】app 存活件（app 启动即构造 · 全程存活 · 5 件）

| 对象 | 职责与关键交互 |
|---|---|
| **DeviceManager 设备管理器**（08·**已落地 2026-08-21**：总门面 DeviceManager + serial 协议层三小层 + KeyManager/MenuLogic/KeySemantics 按键链 + ParamStore/Warmup/ModeController 小组件 + 故障 8 码——代码现状标注，职责/归属/交互语义即本基准，真机联调待做） | 设备总门面：一人管相机、下位机、按键、温度，外面只找它。01：调 `enterCalibration()` 切标定模式，先预热至热平衡再周期采集（全自动无按键）；02：调 `enterScan()` 切扫描模式，收硬件按键电报→指令转发下位机启停采集，持续产双目帧+帧温 |
| **数据管理·仓库**（06） | 跨工作流的「数据保险柜」。01 侧＝标定结果仓库：⑦ 收标定成果统一落盘（相机内/外参、立体矫正、激光平面映射、两张温度表）——持久基准量，与扫描「留内存」相反；02 侧＝标定参数仓库（01 产出，① 门禁「参数就绪」查它）+ 点云仓库（⑧ 优化后点云留内存：供 04 内存调用 / 续扫 existingMarkers 基准 / 按需导出） |
| **渲染基座**（03） | 三维视窗 + 场景切换器，全程常驻的「画框」：纯显示不反向控制流程，会话场景内容挂载其上 |
| **EventBus 事件总线**（base） | 广播站：温度/进度/完成等消息发出去谁关心谁订阅，发收双方互不认识；01/02 靠它旁观事件（只记数值不碰数据） |
| **状态机**（实例 app 造 · 实现代码已自 07_session 迁入 10 并按设计方案重写落地〔7 态表驱动 CAS + 统一命令通道，2026-08-20〕· 2026-08-18 增补第 5 件） | 红绿灯兼状态总账：全系统唯一一份「现在处于什么状态」。01：① 门禁、⑨ 切回待机；02：① 门禁、③⑥ 采集态进出、⑩ 切回待机——所有状态切换唯一经它。状态全局互斥（01/02 不能同时进）；「app 造实例、模块出代码」，与 EventBus/仓库同模式 |

### 【私】01 标定的会话件（② 构造 · ⑨ 逆序销毁）

| 对象 | 职责与关键交互 |
|---|---|
| **姿态判断流水线**（07） | 逐周期认姿态：从环形缓冲抓周期帧，完整算子链判断（非「盲收 N 帧」），**连续命中同一姿态才确认**；命中存完整周期帧，边认边推实时姿态+标志点检出状态给渲染，每确认一个 → 渲染切下一目标；集齐 25 姿态自动触发：停采＋销毁缓冲＋叫醒标定计算 |
| **标定计算流水线**（07） | 姿态集齐自动开工：相机链 ‖ 激光前段并行 → 激光后段（PJC 联合标定 `projector_joint_calib`，D4 定版）→ 质量门禁（合格与否先判一道） |
| **数据管理·会话件**（06） | 开工加载：标定配置 + 初始参数 + 25 目标姿态表；创建帧环形缓冲实例（周期帧＝标记点帧＋N 路激光管帧＋温度） |
| **场景内容供给器**（03） | 标定画面三件套：扫描仪实时姿态模型 + 目标姿态模型 + 标定板标志点检出状态，挂到公用渲染基座 |

### 【私】02 扫描的会话件（② 构造 · ⑩ 逆序销毁）

| 对象 | 职责与关键交互 |
|---|---|
| **扫描处理流水线**（07） | 主力干活的：从缓冲抓帧（抓到即增强帧）→ 标记点链 CPU/P核 ‖ 激光链 GPU 槽双链并行 → 帧间配准（PrevFrameState 快照）→ 融合累积，点云越滚越大边滚边推渲染；同时攒逐帧观测给 GBA |
| **全局优化对象 GBA**（07） | 收尾纠偏师：「完成扫描」（软件触发）才上场：读逐帧观测累加器 → Ceres GBA + 位姿图闭环 → 修正位姿回写融合层重融合（渲染冻结末帧）；算完＝工作流终结 |
| **数据管理·会话件**（06） | 开工加载 ScanConfig（enableLaser 双模式开关 / existingMarkers 基准点注入·源=app 点云仓库 / enableFinalBA GBA 收尾开关）；创建帧环形缓冲实例（双目帧），**出口按帧温查两张补偿表**随帧附标定快照（增强帧——算子无查表动作） |
| **场景内容供给器**（03） | 实时点云层，挂到公用渲染基座 |

### 【私】04 后处理的会话件（04 工作流装配 · 完成后逆序回收 · 2026-08-18 人工补充）

| 对象 | 职责与关键交互 |
|---|---|
| **后处理流水线**（07） | 离线批处理五阶段：法线重算 → 封装（网格化）→ 补洞 → 光顺(HC) → 边界优化，出 STL。输入＝02 产出的 GBA 修正后点云（06 点云仓库**内存调用**，免文件中转）；输出 STL/PLY 经 06 文件服务落盘。不走帧级调度底座（与标定计算同类的离线批处理）、不碰设备（不过设备门禁，状态机走 S6 且免疫 S7）；GBA＋重融合**不在内**（归 02，Q5 定案 2026-08-15）；记账（进度/取消/产物）归 04 自身 |

### 支撑约定（非独立对象的全局规则）

| 项 | 内容 |
|---|---|
| **环形缓冲容器**（06 · 随会话件【私】） | 帧的传送带：08 只管往里放、流水线只管从里取，采图与计算解耦互不等待；同一容器两副面孔——02 消费 lag 超限跳最新丢中间（永远追新帧），01 姿态侧顺序领取不丢帧（写满反压采集） |
| **温度表分工**（D5） | 01 生成两张温度补偿表（0.2℃ 阶梯）随标定结果落盘；02 扫描侧在缓冲出口逐帧查表消费——生成与消费分离 |
| **会话记账**（01/02 工作流自身） | 不设独立对象：01 只记起止时间+结果存档（帧数/姿态数归算子内部）；02 记帧数+覆盖率+起止时间（经事件只记数值）；均**无自动定期存档**——存档仅人工手动触发 |
