# 模块功能目录


## 1. 模块目录

| # | 模块 | 简介 | 文档 |
|---|------|------|------|
| 1 | 标定工作流 | 相机/激光标定流程 | [`01-标定工作流.md`](docs/模块功能/01-标定工作流.md) |
| 2 | 扫描工作流 | 双模式扫描流水线 | [`02-扫描工作流.md`](docs/模块功能/02-扫描工作流.md) |
| 3 | 渲染显示 | OSG+Qt 3D 渲染（点云/网格/标志点加载显示） | [`03-渲染显示.md`](docs/模块功能/03-渲染显示.md) |
| 4 | 后处理 | 离线批处理五阶段（法线→封装→补洞→光顺→边界优化）出 STL；GBA/重融合归 02 扫描收尾 | [`04-后处理.md`](docs/模块功能/04-后处理.md) |
| 5 | 编辑 | 点云编辑（套索选中/删除/撤销），从模块3解耦 | [`05-编辑.md`](docs/模块功能/05-编辑.md) |
| 6 | 文件管理与数据管理 | 文件 IO + 运行时数据管理（容器/Sink 契约/产物仓库） | [`06-文件管理.md`](docs/模块功能/06-文件管理.md) |
| 7 | 流水线管理 | 并行调度底座 + 5 条业务流水线对象（姿态判断/标定计算/扫描处理/全局优化/后处理），供 01/02/04 会话装配 | [`07-流水线管理.md`](docs/模块功能/07-流水线管理.md) |
| 8 | 设备管理 | 相机/MCU/按键/温度监控与控制 | [`08-设备管理.md`](docs/模块功能/08-设备管理.md) |
| 9 | 算子库 | 纯计算叶子算子集合（详见算子目录.md） | [`09-算子库.md`](docs/模块功能/09-算子库.md) |
| 10 | 可观测性/运维 | 状态机门禁 + 统一命令通道 + 故障链 + 日志/性能/崩溃捕获（六件已落地） | [`10-可观测性运维.md`](docs/模块功能/10-可观测性运维.md) |
| 11 | 安装部署 | 安装包/卸载，首运行配置向导 | [`11-安装部署.md`](docs/模块功能/11-安装部署.md) |

---

## 2. 代码对象速查（按实际实现提炼 · 2026-08-21）

> 每对象三行式：①编号＋对象＋文件位置；②「职责：」通俗描述＋技术描述；③「要点：」实现要点。文件路径相对工程根，**不含**测试类与 pImpl 内部细节。

### 2.0 对象总目录（明细索引：编号 · 模块 · 简要职责 · 文件）

| 编号 | 模块 | 简要职责 | 文件 |
| --- | --- | --- | --- |
| base-1 | base 共享内核 | 广播站——事件订阅/发布总线 | base/EventBus.h/.cpp |
| base-2 | base 共享内核 | 公共类型词汇表（Result/Pose/Event…） | base/types.h |
| 01-1 | 01 标定 | 标定指挥员——编排 07 A/B＋记账 | modules/01_calibration/CalibrationWorkflow.h/.cpp |
| 01-2 | 01 标定 | 旧式棋盘格标定函数库（遗留） | modules/01_calibration/calib_workflow.h/.cpp |
| 01-3 | 01 标定 | 标定类型选择弹窗（两按钮） | modules/01_calibration/CalibDialog.h/.cpp |
| 01-4 | 01 标定 | 标定画面场景工具集＋2D 板控件 | modules/01_calibration/CalibDisplay.h/.cpp |
| 01-5 | 01 标定 | 相机调试面板（采集/曝光/显示） | modules/01_calibration/IntegrateTestDialog.h/.cpp |
| 02-1 | 02 扫描 | 扫描指挥员——装配 07 C＋记账 | modules/02_scanning/ScanWorkflow.h/.cpp |
| 03-1 | 03 渲染 | 3D 显示画布（点云/网格/套索编辑） | modules/03_rendering/OSGWidget.h/.cpp |
| 04-1 | 04 后处理 | 后处理指挥员——五阶段出 STL | modules/04_postprocessing/PostProcessWorkflow.h/.cpp |
| 06-1 | 06 文件 | 帧传送带——原子槽位环 | modules/06_fileio/SlotRing.h |
| 06-2 | 06 文件 | 通用线程安全 FIFO 环 | modules/06_fileio/RingBuffer.h |
| 06-3 | 06 文件 | 帧缓冲池（IFrameSink，旧链） | modules/06_fileio/FrameBuffer.h/.cpp |
| 06-4 | 06 文件 | 点云仓库——内存保险柜【公】 | modules/06_fileio/PointCloudBuffer.h/.cpp |
| 06-5 | 06 文件 | 设备状态公告板（最新一条） | modules/06_fileio/DeviceStateCache.h/.cpp |
| 06-6 | 06 文件 | 扫描会话行李箱——装表＋查表入环 | modules/06_fileio/ScanSessionData.h/.cpp |
| 06-7 | 06 文件 | 标定会话行李箱——三键＋姿态环 | modules/06_fileio/CalibSessionData.h/.cpp |
| 06-8 | 06 文件 | 标定结果档案柜——JSON＋门禁【公】 | modules/06_fileio/CalibrationRepository.h/.cpp |
| 06-9 | 06 文件 | 增强帧——带温度匹配参数 | modules/06_fileio/EnhancedFrame.h |
| 06-10 | 06 文件 | 采集周期全家福（marker+激光+温度） | modules/06_fileio/CycleUnit.h |
| 06-11 | 06 文件 | 出口查表装配工（按帧温选档） | modules/06_fileio/FrameEnricher.h/.cpp |
| 06-12 | 06 文件 | 温补表数据壳（输入类型） | modules/06_fileio/TempTableTypes.h |
| 06-13 | 06 文件 | 数据接收契约三接口 | modules/06_fileio/IFrameSink.h 等 3 头 |
| 06-14 | 06 文件 | 工作流统一遥控器 | modules/06_fileio/IWorkflow.h |
| 06-15 | 06 文件 | 软件参数小账本（variant） | modules/06_fileio/ParameterManager.h/.cpp |
| 06-16 | 06 文件 | 点云/网格存取读写器（PLY/STL…） | modules/06_fileio/file_io.h/.cpp |
| 06-17 | 06 文件 | 产物仓库空架子（预留未接线） | modules/06_fileio/WorkflowArtifactStore.h |
| 07-1 | 07 流水线 | CPU 探测器——P/E 核＋绑核 | modules/07_pipelinemgmt/sched/CpuTopology.h/.cpp |
| 07-2 | 07 流水线 | P 核劳务派遣——CPU 重活线程池 | modules/07_pipelinemgmt/sched/PCoreBroker.h/.cpp |
| 07-3 | 07 流水线 | GPU 车位租借处——cudaStream 槽池 | modules/07_pipelinemgmt/sched/GpuSlotService.h/.cpp |
| 07-4 | 07 流水线 | 取帧策略两副面孔（抓新/顺序） | modules/07_pipelinemgmt/sched/IFrameSource.h |
| 07-5 | 07 流水线 | 结果出货口——MPMC 队列 | modules/07_pipelinemgmt/sched/FrameResultQueue.h/.cpp |
| 07-6 | 07 流水线 | 并行调度总调度室——lane 线程编排 | modules/07_pipelinemgmt/sched/SchedulerRuntime.h/.cpp |
| 07-7 | 07 流水线 | 姿态/扫描两模式差异配置 | modules/07_pipelinemgmt/sched/SchedConfig.h |
| 07-8 | 07 流水线 | 五流水线统一开关面板 | modules/07_pipelinemgmt/pipelines/IPipelineObject.h |
| 07-9 | 07 流水线 | 依赖注入插座排（EventBus/仓库…） | modules/07_pipelinemgmt/pipelines/PipelineDeps.h |
| 07-10 | 07 流水线 | 渲染推送投递口 | modules/07_pipelinemgmt/pipelines/ISceneFeed.h |
| 07-11 | 07 流水线 | 统一广播话筒——事件上报出口 | modules/07_pipelinemgmt/pipelines/PipelineEventSink.h/.cpp |
| 07-12 | 07 流水线 | A 姿态判断——认 25 目标姿态 | modules/07_pipelinemgmt/pipelines/posture/PosturePipeline.h/.cpp |
| 07-13 | 07 流水线 | 姿态打卡登记簿——连续命中确认 | modules/07_pipelinemgmt/pipelines/posture/PostureConfirmTable.h/.cpp |
| 07-14 | 07 流水线 | A→B 交棒成绩单（会话数据） | modules/07_pipelinemgmt/pipelines/posture/PostureTypes.h |
| 07-15 | 07 流水线 | B 标定计算总指挥——两线程总装 | modules/07_pipelinemgmt/pipelines/calibcompute/CalibComputePipeline.h/.cpp |
| 07-16 | 07 流水线 | 相机计算半区（内/外参/矫正） | modules/07_pipelinemgmt/pipelines/calibcompute/CameraChain.h/.cpp |
| 07-17 | 07 流水线 | 激光计算半区（PJC/映射表） | modules/07_pipelinemgmt/pipelines/calibcompute/LaserChain.h/.cpp |
| 07-18 | 07 流水线 | 标定质检员——8 项阈值门禁 | modules/07_pipelinemgmt/pipelines/calibcompute/QualityGate.h/.cpp |
| 07-19 | 07 流水线 | 标定结果 JSON 打包器 | modules/07_pipelinemgmt/pipelines/calibcompute/CalibSerialize.h/.cpp |
| 07-20 | 07 流水线 | C 扫描引擎——双链并行算帧 | modules/07_pipelinemgmt/pipelines/scan/ScanPipeline.h/.cpp |
| 07-21 | 07 流水线 | 三段工序单（GPU/P/E 钩子） | modules/07_pipelinemgmt/pipelines/scan/ScanChains.h/.cpp |
| 07-22 | 07 流水线 | 点云滚雪球线程——融合＋推渲染 | modules/07_pipelinemgmt/pipelines/scan/FuseConsumer.h/.cpp |
| 07-23 | 07 流水线 | GBA 观测记账本（逐帧累积） | modules/07_pipelinemgmt/pipelines/scan/FrameObsAccumulator.h/.cpp |
| 07-24 | 07 流水线 | 显存块回收站——GPU 点云块池 | modules/07_pipelinemgmt/pipelines/scan/GpuPointCloudPool.h/.cpp |
| 07-25 | 07 流水线 | C 类型定义集（配置/结果/观测） | modules/07_pipelinemgmt/pipelines/scan/ScanTypes.h |
| 07-26 | 07 流水线 | D 收尾纠偏师——GBA＋重融合 | modules/07_pipelinemgmt/pipelines/globaloptim/GlobalOptimObject.h/.cpp |
| 07-27 | 07 流水线 | E 点云变模型——五阶段出 STL | modules/07_pipelinemgmt/pipelines/postprocess/PostProcessPipeline.h/.cpp |
| 07-28 | 07 流水线 | E 阶段 0 法线重算实装 | modules/07_pipelinemgmt/pipelines/postprocess/NormalStage.h/.cpp |
| 08-1 | 08 设备 | 双目相机遥控器约定（HAL 接口） | modules/08_devicemgmt/IScannerCamera.h |
| 08-2 | 08 设备 | 下位机遥控器约定（HAL 接口） | modules/08_devicemgmt/IMCU.h |
| 08-3 | 08 设备 | 串口水管工——纯 Win32 IO | modules/08_devicemgmt/serial/SerialPort.h/.cpp |
| 08-4 | 08 设备 | 字节翻译官——v2/v3 成拆帧 | modules/08_devicemgmt/serial/FrameCodec.h/.cpp |
| 08-5 | 08 设备 | 命令快递员——ACK 对账重传 | modules/08_devicemgmt/serial/CommandChannel.h/.cpp |
| 08-6 | 08 设备 | 上行分拣架——帧类型＋SPSC 环 | modules/08_devicemgmt/serial/McuFrame.h/.cpp |
| 08-7 | 08 设备 | 下位机驱动组装壳（: IMCU） | modules/08_devicemgmt/MCUDriver.h/.cpp |
| 08-8 | 08 设备 | 设备大管家总门面【公】 | modules/08_devicemgmt/DeviceManager.h/.cpp |
| 08-9 | 08 设备 | 按键手势识别器（短按/双击/长按） | modules/08_devicemgmt/KeyManager.h/.cpp |
| 08-10 | 08 设备 | 按键裁判——转移表定动作 | modules/08_devicemgmt/KeySemantics.h/.cpp |
| 08-11 | 08 设备 | 菜单记账本（显式状态） | modules/08_devicemgmt/MenuLogic.h/.cpp |
| 08-12 | 08 设备 | 参数唯一账本（确认更新制） | modules/08_devicemgmt/ParamStore.h/.cpp |
| 08-13 | 08 设备 | 预热看火员（稳/超时） | modules/08_devicemgmt/WarmupSequence.h/.cpp |
| 08-14 | 08 设备 | 模式黑板（request/commit） | modules/08_devicemgmt/ModeController.h/.cpp |
| 08-15 | 08 设备 | 大恒相机驾驶人（Galaxy SDK） | modules/08_devicemgmt/CameraControl.h/.cpp |
| 08-16 | 08 设备 | 硬件体检员——1s 巡检纯读取 | modules/08_devicemgmt/HardwareMonitor.h/.cpp |
| 08-17 | 08 设备 | 电脑自检采集器（PDH/NVML） | modules/08_devicemgmt/SelfCheckCollector.h/.cpp |
| 09-1 | 09 算子 | 双链公共词汇表（11 共享头） | modules/09_operatorlib/core/common/ |
| 09-2 | 09 算子 | 跨帧记忆（AtomicFrameState） | modules/09_operatorlib/core/scheduler/ |
| 09-3 | 09 算子 | 前端三道工序（掩膜/滤帧/CCL） | modules/09_operatorlib/core/vision/ |
| 09-4 | 09 算子 | 标记点一条龙（12 算子） | modules/09_operatorlib/core/marker/ |
| 09-5 | 09 算子 | 激光线一条龙（4 算子） | modules/09_operatorlib/core/laser/ |
| 09-6 | 09 算子 | 相机标定链（5 算子） | modules/09_operatorlib/calibration/camera/ |
| 09-7 | 09 算子 | 虚拟相机标定链（8 算子，PJC） | modules/09_operatorlib/calibration/laser_calib/ |
| 09-8 | 09 算子 | 设备姿态估计 | modules/09_operatorlib/calibration/posture/ |
| 09-9 | 09 算子 | 温补三件套（内/外参/激光） | modules/09_operatorlib/calibration/temp/ |
| 09-10 | 09 算子 | 激光/标记点掩膜分离 | modules/09_operatorlib/scanning/preprocess/ |
| 09-11 | 09 算子 | 激光匹配扫描（查温度表） | modules/09_operatorlib/scanning/laser/ |
| 09-12 | 09 算子 | 融合＋法线（CPU/CUDA 双轨） | modules/09_operatorlib/scanning/fusion/ |
| 09-13 | 09 算子 | 全局 BA 优化器（Ceres） | modules/09_operatorlib/scanning/global_optim/ |
| 10-1 | 10 可观测 | 全局红绿灯——7 态门禁【公】 | modules/10_observability/StateMachine.h/.cpp 等 |
| 10-2 | 10 可观测 | 统一命令大门（双口） | modules/10_observability/CommandGate.h/.cpp |
| 10-3 | 10 可观测 | 首批 7 命令花名册 | modules/10_observability/DefaultCommands.h |
| 10-4 | 10 可观测 | 故障接警台——档案＋安全停 | modules/10_observability/FaultHandler.h/.cpp |
| 10-5 | 10 可观测 | 健康预警器（双级告警） | modules/10_observability/PerfMonitor.h/.cpp |
| 10-6 | 10 可观测 | 日志管家——轮转＋排障包 | modules/10_observability/ObsLogger.h/.cpp |
| 10-7 | 10 可观测 | 日志快捷键宏（JMW_LOG_*） | modules/10_observability/jmw_logging.h |
| 10-8 | 10 可观测 | 崩溃黑匣子——MiniDump | modules/10_observability/CrashHandler.h/.cpp |
| app-1 | app 装配 | 程序入口——初始化一切 | app/main.cpp |
| app-2 | app 装配 | 总装配车间（组合根） | app/AppContext.h/.cpp |
| app-3 | app 装配 | 工作流工具箱提手（依赖入口） | app/WorkflowContext.h/.cpp |
| app-4 | app 装配 | 主界面驾驶舱（LeadScan K2） | app/MainWindow.h/.cpp |
| app-5 | app 装配 | 扫描控制小窗（启停/滑条） | app/ScannerWindow.h/.cpp |
| app-6 | app 装配 | 外部依赖桩（旧演示链用） | app/stubs/（5 桩头） |

### 2.1 base/ — 共享内核（lib `base`，命名空间 `Scanner` / `Scanner::infra`）

**base-1 `EventBus`**（EventBus.h/.cpp）
职责：广播站——消息发出去、谁关心谁订阅，发收双方互不认识。线程安全订阅/发布总线（subscribe/unsubscribe/publish/publishSync），app 全局拥有。
要点：publishSync 现与 publish 等同（锁内同步分发，无异步队列）；07/10/UI 均经它旁观事件。

**base-2 `types.h` 类型集**
职责：全工程公共的「词汇表」——谁都要用的类型定义。`Result`/`QualityFlag`/`Pose`/`Event`/`DeviceState` 等纯值类型集中一处。
要点：`Result` 有 ok/fail/degraded/warning 四工厂；`EventType` 含 mod10 扩展码；`HealthMetrics`＝08 采集→10 消费的健康快照（10 字段）；`UPtr`/`SPtr` 指针别名。

### 2.2 01_calibration/ — 标定工作流与 UI（**直编 scan_demo**）

**01-1 `CalibrationWorkflow`**
职责：标定流程的指挥员——自己不算数，指挥 07 A/B 对象干活并记账。IWorkflow 编排/记账壳：持 06 CalibSessionData（load 会话档三键），A 集齐 25 姿态后异步移交 calibThread_ 跑 B 批算。
要点：.cpp 内 RepositoryWriter 适配器直写 06 仓库；A/B 初始参数单源注入；setOnFinished 回报由 app 注入 lambda→notifyCompleted 合账切 S2（01 不 include 10）。

**01-2 `calib_workflow`**
职责：早期棋盘格标定的老函数库（遗留）。旧式标定纯函数集：runCameraCalibration/runLaserCalibration＋save/load。
要点：与 07 B 批算链并存；MainWindow stubs 旧链消费。

**01-3 `CalibDialog`**
职责：选「相机标定」还是「激光器标定」的两按钮弹窗。标定类型选择对话框。
要点：发 cameraCalibClicked/laserCalibClicked 两信号后 accept；实际标定逻辑在 MainWindow lambda（走 stubs 旧链）。

**01-4 `CalibDisplay`**（`calib_display::`）
职责：搭建标定画面（板＋姿态模型＋偏差数字）的工具集。buildCalibScene 产 osg::Group 喂 OSGWidget。
要点：白底板＋网格＋42 标志点（绿已扫/红未扫）＋STL 姿态模型＋六轴偏差 HUD；**CalibBoard2D**＝Qt 2D 标定板控件（paintEvent 纯绘制）。

**01-5 `IntegrateTestDialog`**
职责：相机调试面板——开相机、调曝光、看画面。CameraControl 采集＋参数调节＋图像显示一体对话框。
要点：绑 08 CameraControl 裸指针直调；异步帧经 invokeMethod 回主线程显示；1s FPS 计数；「标定操作」4 按钮占位。

### 2.3 02_scanning/ — 扫描工作流（**直编 scan_demo**）

**02-1 `ScanWorkflow`**
职责：扫描流程的指挥员——装配 07 C 流水线并记账，自己不碰帧。IWorkflow 编排壳：装配序 assemble→ScanConfig→attachRing→attachCalib→configure→start。
要点：scanMode→enableLaser；snapshotMarkers 取 06 点云仓库作续扫基准；会话记账归自身成员（D6）；**声明序铁律** session_ 先于 pipeline_（逆序析构）；pause/resume 真实现。

### 2.4 03_rendering/ — 渲染显示（**直编 scan_demo**）

**03-1 `OSGWidget`**
职责：3D 显示画布——加载点云/网格并支持旋转缩放。Qt QOpenGLWidget 内嵌 osgViewer 的 3D 视图控件。
要点：loadPointCloud/WithNormals(VBO 动态)/loadMesh(经 06 fileio)/**loadFromPointCloudBuffer**(只读快照直读)/PLY 流式（5 万点/批＋PCA 自适应）；**套索编辑**（2D 多边形→HitTestCullCallback 命中→高亮/删除/撤销栈）；HUD 轴指示器；16ms QTimer 驱动。

### 2.5 04_postprocessing/ — 后处理工作流（**直编 scan_demo**）

**04-1 `PostProcessWorkflow`**
职责：后处理流程的指挥员——取点云、跑五阶段、出 STL。IWorkflow 编排壳：06 PointCloudBuffer.getSnapshot→MeshData→pipeline run。
要点：skipStages 位掩码透传；STL 经 exportStlViaFileIo 适配（07 StlExportFn→06 fileio::exportSTL）；postThread_ 阻塞批算＋进度透传＋产物记账；用户取消按 ok=true 报（正常终止非故障）。

### 2.6 06_fileio/ — 文件与数据管理（lib `mod_fileio`，命名空间 `Scanner::data`）

**——容器与会话件——**

**06-1 `SlotRing<T>`**（SlotRing.h）
职责：帧的传送带——一格一格循环放帧，采图与计算互不等待。原子槽位环＝07 抓帧策略的缓冲底座。
要点：Overwrite（满覆盖最旧）/ Backpressure（满阻塞写侧）两写策略；单生产者约定；槽为 shared_ptr 原子换入。

**06-2 `RingBuffer<T>`**（RingBuffer.h）
职责：通用先进先出队列。线程安全 FIFO 环。
要点：DropOldest/DropNewest/Block 三溢出策略（mutex＋双 cv）；FrameBuffer 内部包装它。

**06-3 `FrameBuffer`**（FrameBuffer.h/.cpp）
职责：原始帧的蓄水池。帧缓冲池，`IFrameSink` 实现。
要点：默认 60 槽 DropOldest；采集线程 pushFrame→工作流消费（旧链遗留，新链走 ScanSessionData）。

**06-4 `PointCloudBuffer`**（PointCloudBuffer.h/.cpp）
职责：扫描成果的保险柜——点云存在内存里，谁要谁取快照。全局点云仓库【公】，`IPointCloudSink` 实现＋marker 独立通道。
要点：云/marker 双版本号快照；shared_mutex 读写锁；MarkerRecord 保 globalId 只走内存不落盘；exportCloud/exportMarkers＝人工导出唯一出口。

**06-5 `DeviceStateCache`**（DeviceStateCache.h/.cpp）
职责：设备状态公告板——永远只放最新一条。按 deviceId 存最新设备状态，`IDeviceStateSink` 实现。
要点：08 侧写、UI 轮询温度/fps；mutex＋unordered_map。

**06-6 `ScanSessionData`**（ScanSessionData.h/.cpp）
职责：扫描会话的行李箱——装标定表，帧发出前先按温度配好参数。02 扫描会话件【私】：assemble 装表＋pushFrame 出口查表入环。
要点：Overwrite 环默认 16 槽（构造期定死）；enrich 失败丢帧计数挂 10 故障桥。

**06-7 `CalibSessionData`**（CalibSessionData.h/.cpp）
职责：标定会话的行李箱——装配置和 25 目标姿态表，内嵌姿态传送带。01 标定会话件【私】。
要点：load 会话档 JSON 三键（initialParams 原文/targets 25×4×4/boardPoints）；Backpressure 姿态环 8 槽（写满反压 08 采集侧）。

**06-8 `CalibrationRepository`**（CalibrationRepository.h/.cpp）
职责：标定结果的档案柜——算完落盘存 JSON，下次开工先来体检。标定结果仓库【公】：结果落盘＋参数查询两副面孔。
要点：`readyForScan` 五样门禁（内参 L/R、外参 R/T、立体温度表、激光档表、imageSize——缺哪报哪，内参 L/R 分列）；write＝临时文件＋原子改名。

**——数据类型与查表器——**

**06-9 `EnhancedFrame`/`CalibSnapshot`**（EnhancedFrame.h）
职责：带着「体温匹配参数」出厂的帧。增强帧＝gray L/R host 副本＋帧温＋命中两表的标定快照。
要点：CalibSnapshot 含 R1/R2/P1/P2/Q＋档索引；算子直接用 snapshot，无查表动作。

**06-10 `CycleUnit`**（CycleUnit.h）
职责：一个采集周期的全家福——标记点帧＋激光帧＋温度打成一包。采集周期单元。
要点：marker L/R＋N×2 激光管帧（偶 L 奇 R）＋周期温度；A 流水线命中姿态时整单元保存。

**06-11 `enrich()`/FrameEnricher**（FrameEnricher.h/.cpp）
职责：发帧前按温度查表配参数的装配工。出口查表纯函数：按帧温取最近档填 CalibSnapshot 产出 EnhancedFrame。
要点：0.2℃ 阶梯、并列取低温档；单表空 warning、两表空 fail、越界 clamp＋warning。

**06-12 `StereoTempTable` 等**（TempTableTypes.h）
职责：两张温度补偿表的数据壳。温补表输入类型集。
要点：立体侧持 Matx 档数据；激光侧仅档温索引（Ref 后缀，表数据归 07/09 GpuMat）。

**——契约 / 框架 / IO——**

**06-13 `IFrameSink`/`IPointCloudSink`/`IDeviceStateSink`**
职责：数据投递的收件口约定——设备只认口，不管背后是谁。数据接收契约【接口】。
要点：08 经依赖倒置推送，不反向依赖 Data 实现。

**06-14 `IWorkflow`**（IWorkflow.h）
职责：工作流的统一遥控器。工作流接口【接口】：initialize/start/pause/resume/stop/getState/进度回调。
要点：01/02/04 三条工作流实现；Pipeline/Stage 原语已删。

**06-15 `ParameterManager`**（ParameterManager.h/.cpp）
职责：软件参数的小账本。variant 参数账本（int/double/bool/string）＋相机/扫描默认预设组。
要点：mutex＋unordered_map；app/Service 装配期与运行期读写。

**06-16 `file_io`＋`MeshData`**（file_io.h/.cpp）
职责：点云/网格文件的存取读写器。导入导出自由函数集。
要点：ply/pcd/xyz/txt 自动识别＋STL/OBJ（已解 OSG 依赖）；PointCloudBuffer 导出与 04 STL 落盘共用。

**06-17 `WorkflowArtifactStore`**（WorkflowArtifactStore.h）
职责：产物仓库的空架子（占位）。L4 产物仓库抽象（put/get/list）【接口】。
要点：**预留未接线**（框架整体 §1 DataStore/ADR 7.10）。

### 2.7 07_pipelinemgmt/ — 流水线管理（lib `mod_pipelinemgmt`，命名空间 `Scanner::pipeline`）

**——sched/ 并行调度底座（`Scanner::pipeline::sched`）——**

**07-1 `CpuTopology`**（sched/CpuTopology.h/.cpp）
职责：CPU 探测器——查有几个性能核/能效核、怎么绑核。CPU 拓扑探测＋绑核/实时优先级工具。
要点：lanes 数＝clamp(min(P-1,E),1,8) 纯函数；失败仅 warn 不抛。

**07-2 `PCoreBroker`**（sched/PCoreBroker.h/.cpp）
职责：性能核劳务派遣——重活外包给 P 核干。P 核任务代理：X-1 worker 线程池，E 核 lane 提交 CPU 重活返 future。
要点：worker 可绑 P 核＋提优先级；有界队列 64 满阻塞提交方；shutdown 排空后 join。

**07-3 `GpuSlotService`**（sched/GpuSlotService.h/.cpp）
职责：GPU 车位租借处——借一条 cudaStream 用，用完自动还。cudaStream 槽池。
要点：空闲池恒等式承担计数信号量＋RAII `SlotGuard` 归还；每运行周期重建，不可重启。

**07-4 `IFrameSource`＋`GrabLatestSource`/`SequentialSource`**（sched/IFrameSource.h）
职责：从传送带取帧的两种取法——扫描抓最新、姿态按序全收。SlotRing 之上的薄抓帧策略。
要点：GrabLatest＝各 lane 抓最新（lag 超限跳帧计 skipped）；Sequential＝claim→waitFor→read 顺序全取＋done 腾位；⚠ Sequential 不得配 Overwrite 环（死循环空转）。

**07-5 `FrameResultQueue<T>`**（sched/FrameResultQueue.h/.cpp）
职责：算完结果的出货口。流水线结果输出 MPMC 队列。
要点：满覆盖最旧＋dropped 计数，生产侧永不阻塞；C 流水线→FuseConsumer。

**07-6 `SchedulerRuntime`**（sched/SchedulerRuntime.h/.cpp）
职责：并行流水线的总调度室——每条 lane 一个线程按固定节拍跑三段工序。E 核编排组合运行时（A/C 总装核心）。
要点：每 lane「互斥串行抓帧→acquire GPU guard→gpuChain（ccl 就绪点提交 pChain，激光链与 P 链帧内并行）→eFinalize」；组合 Broker/GpuService/Source；钩子异常即停；drain 后可 restart（lastCounter 消费水位注入）。

**07-7 `SchedConfig`**（sched/SchedConfig.h）
职责：姿态/扫描两模式的参数差异表。A/C 唯一差异配置。
要点：lanes/gpuSlots（姿态 1 扫描 2）/queueCapacity/dropThreshold/gpuAcquireTimeout。

**——pipelines/ 公共件（`Scanner::pipeline`）——**

**07-8 `IPipelineObject`**（pipelines/IPipelineObject.h）
职责：五条流水线的统一开关面板。公共轻量生命周期【接口】。
要点：configure（只接线不拥有）/start/stop/isRunning。

**07-9 `PipelineDeps`＋`IAcquisitionControl`/`ICalibRepoWriter`/`ICloudRepoWriter`**（pipelines/PipelineDeps.h）
职责：流水线对外部世界的插座排——EventBus/渲染/停采/仓库都从这接。装配期依赖注入窄接口集。
要点：07 定义、01/02 侧实现适配；裸指针只接线不拥有。

**07-10 `ISceneFeed`**（pipelines/ISceneFeed.h）
职责：往画面里送数据的投递口。渲染推送窄接口【接口】。
要点：pushPostureView/pushCloudSnapshot/notifyFreeze；pushPostureView 可能多 lane 并发，实现须线程安全。

**07-11 `PipelineEventSink`/`EventBusEventSink`**（pipelines/PipelineEventSink.h/.cpp）
职责：流水线的统一广播话筒——异常/降级/完成都从这喊出去。07 事件上报出口抽象＋EventBus 适配。
要点：FaultOccurred（sourceId=0x07）；帧级 Normal 静默、完成事件 Normal→Info；抽象可替换（测试）。

**——五流水线对象（均【私】会话件，stop 后不重启）——**

**07-12 A `PosturePipeline`**（pipelines/posture/）
职责：认姿态的——看每周期帧，判断设备摆到了 25 个目标中的哪一个。姿态判断流水线：三段链逐周期处理 CycleUnit。
要点：Runtime(sequential)×SequentialSource；gpu 段 mask_extract→frame_filter→ccl；p 段标记点 9 算子；e 段三级降级配准→pose_estimate→确认簿记→pushPostureView；不丢帧（环写满反压 08 写侧）；集齐 25 姿态→收口 watcher：drain→stopAcquisition→continueWithB 叫醒 B。

**07-13 `PostureConfirmTable`**（pipelines/posture/）
职责：25 个姿态的打卡登记簿——连续命中几次才算数，打满 25 个收工。姿态确认簿记。
要点：ΔT＋角度双阈值命中候选→streak 连续 N 次确认（CAS 保证同一姿态恰确认一次）；per-pose 细粒度行锁。

**07-14 `PostureSessionData`/`PostureData`**（pipelines/posture/PostureTypes.h）
职责：A 交棒给 B 的成绩单——25 个姿态的完整数据。会话收口数据。
要点：25×{完整周期＋命中姿态 R/T＋椭圆中心}；ConfirmTable 写入→takeSessionData 导出→B attachSession 消费。

**07-15 B `CalibComputePipeline`**（pipelines/calibcompute/）
职责：标定计算总指挥——相机/激光两路并行算完、质检、落盘。标定计算两线程总装。
要点：CameraChain(0..50)∥LaserChain(50..100) promise/future 衔接→QualityGate→serializeCalib→ICalibRepoWriter 落盘；run 阻塞或 start 后台线程；进度单调门 0..100 不回退。

**07-16 `CameraChain`**（pipelines/calibcompute/）
职责：相机参数计算半区——内参/外参/矫正表。相机链编排。
要点：逆畸变→内参(L/R)→外参→立体矫正（set promise 供激光链起跟随）＋旁支三温度表；算子间查 cancel。

**07-17 `LaserChain`**（pipelines/calibcompute/）
职责：激光参数计算半区——前段先跑，等相机结果出来再算后段。激光链编排。
要点：前段 4-1..4-4（不等相机先行）→等 future（100ms 轮询带取消）→后段→PJC 联合标定→平面映射表；LaserOps 为依赖注入缝（真算子/测试假算子）。

**07-18 `gate::evaluate()`/QualityGate**（pipelines/calibcompute/QualityGate.h/.cpp）
职责：标定结果的质检员——8 项指标判合格/降级/不合格。质量门禁纯函数。
要点：rms/重投影/PJC 条件数与改善比/表档数/ROI 八项阈值合成 Normal/Degraded/Fault；阈值可经 Config 覆盖（默认待联调）。

**07-19 `serializeCalib()`**（pipelines/calibcompute/CalibSerialize.h/.cpp）
职责：把算好的标定结果打包成 JSON。B 输出→仓库载荷序列化。
要点：stereo/tempTables/pjc/planeMap/quality 五顶层键；GpuMat 设备数据与 PJC denoisedPoints 不落盘。

**07-20 C `ScanPipeline`**（pipelines/scan/）
职责：扫描主力——抓帧、双链并行算，点云越滚越大。扫描处理引擎总装。
要点：Runtime(GrabLatest)×ScanChains×FuseConsumer＋obs＋激光显存池；`seed`（续扫先于首帧）；pause/resume（消费水位持久，已消费帧不重扫）；异常停惰性收敛 Faulted＋Fault(1604)。

**07-21 `ScanChains`**（pipelines/scan/）
职责：扫描的三段工序单（GPU 段/P 核段/收尾段）定义。C 三钩子工厂。
要点：GPU 链 mask_separation→ccl→【frontReady】激光链→入池；P 链标记点 9 算子＋配准（prevState 原子快照三级降级）；E 段 fut.get 汇合组装 FrameResult；per-lane 算子集惰性创建跨帧复用（09 算子每实例非线程安全）。

**07-22 `FuseConsumer`**（pipelines/scan/）
职责：点云滚雪球线程——把每帧结果融进总点云并推给画面。独立融合消费线程。
要点：pop 队列→marker/laser 融合→渲染节流 pushCloudSnapshot（每 N 帧）→obs.push；单消费线程独占（算子单线程约定）；单帧异常兜底 Fault(1602) 丢帧续跑；hpGlobalIds 标高精度点。

**07-23 `FrameObsAccumulator`**（pipelines/scan/）
职责：给 GBA 攒观测的记账本——每帧的位置/观测先记着，收尾用。GBA 逐帧观测累加器＋激光帧 host 缓存。
要点：字节预算，超限停累加记 Degraded 不整体失败；D run 时 snapshot 消费；obsList_ 无上限增长（万帧级待评估）。

**07-24 `GpuPointCloudPool`**（pipelines/scan/，仅 JMW_BUILD_CUDA）
职责：GPU 显存块回收站——借出去用完自动收回循环用。GPU 点云显存块池。
要点：构造预分配 GpuMat 块；acquire 返 shared_ptr、deleter 自动回池（LIFO 复用）；池须长于所有在飞块。

**07-25 `ScanTypes`**（pipelines/scan/ScanTypes.h）
职责：C 流水线自家的类型定义集。
要点：`ScanConfig`（enableLaser 双模式/最终 BA/缓存预算/续扫已有点）、`FrameResult`（队列元素）、`FrameObs`（GBA 观测）、`GpuPointCloudBlock`；laser 字段 CUDA 守卫。

**07-26 D `GlobalOptimObject`**（pipelines/globaloptim/，BUILD_GLOBAL_OPTIM 守卫）
职责：收尾纠偏师——扫完整体校一遍位姿，再重算一遍点云。全局优化「GBA＋重融合」成对批算。
要点：freeze→装配 GlobalBAInput（观测快照＋软先验＝setExistingPrior∪高精度 id）→Ceres GBA 修正位姿→marker/laser 重融合重放→cloudRepo 写→解冻推送；GBA 失败沿初值重融合兜底（Degraded）；GbaFn/重放工厂可注入测试。

**07-27 E `PostProcessPipeline`**（pipelines/postprocess/）
职责：点云变模型——五阶段网格加工出 STL。离线后处理五阶段编排。
要点：法线重算→封装→补洞→光顺→边界→STL 导出；不走底座不碰设备；网格四族 09 待建以 `IMeshStageOp` 桩占位（degraded "operator pending"）；skipStages 位掩码跳段；取消＝degraded（用户意图非故障）。

**07-28 `NormalStage`**（pipelines/postprocess/）
职责：五阶段第 1 段（法线重算）的实装。E 阶段 0 算子。
要点：MeshData.xyz→临时累积器（自适应体素）→09 laser_cloud_normal_cpu 全体素重算→映射回写（退化 ≥10% degraded）；多实例可并行。

### 2.8 08_devicemgmt/ — 设备管理（lib `mod_devicemgmt`，命名空间 `Scanner::device` / `serial` / `hal`）

**08-1 `IScannerCamera`**（IScannerCamera.h）【接口，`Scanner::hal`】
职责：双目相机的统一遥控器约定。相机抽象接口（open/参数/标定注入/同步异步采集/温度）。
要点：标定参数**注入式**（app 从 06 仓库喂入，08 不做第二真相源）；实现＝CameraControl。

**08-2 `IMCU`**（IMCU.h）【接口，`Scanner::hal`】
职责：下位机的统一遥控器约定。typed N10–N16 命令＋上行三分流出口接口。
要点：所有 send\* **非阻塞**，结果经 DoneCb 异步回告（v2 恒立即 ok「未确认」）；pump() 回调内不得再调 pump/send（短平快铁律）。

**08-3 `SerialPort`**（serial/SerialPort.h/.cpp）
职责：串口线的水管工——只管字节进出，不懂协议。纯 Win32 串口 IO 封装。
要点：8N1＋50ms 间隔超时＋4096 缓冲；**写单线程铁律**（首调线程记属主，他线程调 fail）；close 靠 CloseHandle 令阻塞 read 退出；HANDLE 藏 void\* 不进头。

**08-4 `FrameCodec`**（serial/FrameCodec.h/.cpp）
职责：串口字节的翻译官——把字节流拼成帧、校验、拆包。v2/v3 帧成拆器。
要点：**协议开关收敛此处**——v2 直通无校验（兼容现状固件）；v3＝seq 2 位 hex＋CRC16-CCITT 查表＋半帧超时 50ms；载荷禁 `$` `;`、帧长≤32B。

**08-5 `CommandChannel`**（serial/CommandChannel.h/.cpp）
职责：下发命令的快递员——送不到就重发，三次送不到算失败。下行可靠通道。
要点：非阻塞 send＋tick 驱动 ACK 对账/重传（总尝试 1+3）；**命令组步链**（前条 ACK 回调里发下条，任一步 3 败整组短路）；待确认表容量 8；v2 降级发不等恒 ok「未确认」。

**08-6 `McuFrame` 类型集＋`SpscRing`**（serial/McuFrame.h/.cpp）
职责：上行数据的分拣架——按类型拆开放，逻辑线程慢慢取。上行帧类型＋解析＋SPSC 环。
要点：Temp/RawKey/Status/Ack 四类＋4 载荷解析函数；SpscRing＝满丢新有界 SPSC 环（丢新＋dropCount，生产者永不碰 head 根除竞态）。

**08-7 `MCUDriver`**（MCUDriver.h/.cpp）
职责：下位机驱动的组装壳——串口三件套拧成一体。三小层组合壳（: public IMCU）。
要点：rx 线程零业务 read→feed→按首字符入 4 环（K/S/A 各 64＋T 遥测 8）；channelTick 单独出口（对账归门面逻辑线程驱动）；记账口：lastRxTime 心跳/seqGapCount/keyDropCount/parseFailCount。

**08-8 `DeviceManager` 总门面**（DeviceManager.h/.cpp）【公】
职责：设备大管家——外面只找它，它管相机/下位机/按键/温度所有硬件。设备管理总门面＋逻辑线程。
要点：post 编队（容量 64 满丢新）＋logicTick 一拍＝排空队列→pump 环→按键 dispatch→channelTick→Warmup tick→v2 温度兜底→故障巡检→参数快照；切模式＝命令组步链（**组成功回调才擦板**）；**故障 8 码**（0x0801 相机掉线…0x0808 seq 跳变，边沿锁＋恢复清锚防爆屏、只报不动手）；参数双口（get 互斥快照/set 编队）；open 一条龙相机→MCU→N12Z1→逻辑线程，任一步败倒序关。

**08-9 `KeyManager`**（KeyManager.h/.cpp）
职责：按键手势识别器——分清短按/双击/长按。PC 侧按键手势判定（S/D/H）。
要点：判定用 **MCU 时刻**（排队延迟不扰手感）；事件优先＋全局扫，tick 仅兜底；每键独立 4 态状态机互斥出 S/D/H；消抖 20ms；零业务状态（不门控不映射）。

**08-10 `KeySemantics`**（KeySemantics.h/.cpp）
职责：按键裁判——这个手势在这个时刻该干什么活。手势＋当前状态→转移表→动作。
要点：纯函数式（无内部计数、弃奇偶）；11 出口动作；启停键不问门禁，菜单/模式/调节键 gate 关一律丢弃；左右键短按菜单优先于调节。

**08-11 `MenuLogic`**（MenuLogic.h/.cpp）
职责：屏幕菜单的记账本——记当前在第几层、光标在哪项。菜单显式状态机。
要点：层(1/2)/游标①~④/调节上下文/模式光标(1 精细/2 深孔/3 普通交叉，默认 3，**唯一真相源**)；纯记账不判该不该不发命令；会话级不持久化，Reset 全回默认。

**08-12 `ParamStore`**（ParamStore.h/.cpp）
职责：设备参数的唯一账本——对方确认收到才入账。设备参数账本。
要点：**确认更新制**——v3＝ACK 成功才改账＋广播、3 败保旧值；v2＝立即改账 confirmed=false（期望值）；在途后值胜出（gen 判据）；入口即钳回 spec 范围；序列化手写 "key=value;..."。

**08-13 `WarmupSequence`**（WarmupSequence.h/.cpp）
职责：预热看火员——温度稳了才喊开工，超时也喊。预热时序（待命→加热→稳/超时）。
要点：双点锚点法（偏出锚点重锚重计时）；窗满且离目标≤nearTargetC 才判稳；超时兜底（温度停更也能超）；**只报不停加热**。

**08-14 `ModeController`**（ModeController.h/.cpp）
职责：当前模式的黑板——写着空闲/标定/扫描，改两笔才算数。模式黑板＋采集子态。
要点：Idle/Calibrating/Scanning 原子读写；**request/commit 两段式**（request 只问门禁，commit 在命令组成功回调调）；same-mode commit 不广播；isCapturing 为 S4/S5 子态真相源（10 不记账）。

**08-15 `CameraControl`**（CameraControl.h/.cpp）
职责：大恒相机的实际驾驶人。Galaxy SDK 立体相机对（: public IScannerCamera）。
要点：SDK 回调→双缓冲**左右配对交付**（ready 原子位，frameId 取 max）；右图可 180° 旋转；setGain 按 GainRaw 原生单位（dB 语义上层换算）；标定缓存注入式。

**08-16 `HardwareMonitor`**（HardwareMonitor.h/.cpp）
职责：硬件体检员——每秒抄一遍表，不做诊断。硬件状态周期巡检（纯读取）。
要点：默认 1s；MCU 温度改注入快照直传（不持 IMCU 指针不发命令）；**08 不链 10**——HealthMetrics 快照供 app 侧适配器喂 PerfMonitor；心跳经注入回调上报。

**08-17 `SelfCheckCollector`**（SelfCheckCollector.h/.cpp）
职责：电脑自检采集器——CPU/内存/磁盘/GPU 抄表。上位机自检→HealthMetrics。
要点：PDH（CPU 占用，首次返 -1 建基线）＋内存/磁盘 WinAPI＋**NVML 动态加载**（无 dll→GPU 字段 -1）；Honest 原则取不到留 -1 不编数；线程心跳注册制；头文件零 Win32 依赖（pimpl 藏 .cpp）。

### 2.9 09_operatorlib/ — 算子库（lib `mod_operatorlib`，命名空间 `calib::`）

> 统一契约：三元组（XXXParams〔validate/toJson/fromJson〕＋XXXResult＋Operator〔Execute/Warmup/SetParams/Destroy，pImpl 禁拷贝〕）＋ `Stream&` 流亲和；CUDA 算子五件套，受 BUILD_CUDA 门控。逐算子说明见 `算子目录.md`。

**09-1 core/common — 11 个共享头（非算子）**
职责：两条算子链共用的词汇表。双链公共类型与契约。
要点：含 calib_result_types.h 跨应用数据契约；形态：纯头。

**09-2 core/scheduler — prev_frame_state**
职责：跨帧记忆——上一帧算到哪了。AtomicFrameState 跨帧调度状态。
要点：形态：CPU 单头。

**09-3 core/vision — mask_extract / frame_filter / ccl**
职责：两条链共用的前端三道工序——抠掩膜、挑帧型、数连通块。视觉前端。
要点：掩膜提取/帧类型过滤（排除激光线帧）/连通域分析；形态：CUDA。

**09-4 core/marker — image_split…marker_cloud_fuse_cpu（12 个）**
职责：标记点识别配准一条龙——从分割到亚像素到三维重建再到配准融合。标记点链（12 算子）。
要点：分割→Zernike 亚像素→合并→去畸变→椭圆拟合→双目匹配→极线交点→边缘匹配→三维重建→[单帧兜底 | 光流默认]→体素哈希融合；形态：CPU。

**09-5 core/laser — steger / undistort_cuda / epipolar_interp / laser_reconstruct**
职责：激光线处理一条龙——中心线提取到三维重建。激光线链（4 算子）。
要点：Steger 中心亚像素→CUDA 去畸变→极线插值→三维重建；形态：CUDA。

**09-6 calibration/camera — inverse_distort…stereo_rectify_temp_table（5 个）**
职责：相机标定计算链——内参外参矫正表一条龙。相机标定链（5 算子）。
要点：逆畸变→内参→外参→立体矫正→温补矫正表；形态：CPU。

**09-7 calibration/laser_calib — laser_label…plane_map_temp_table（8 个）**
职责：激光虚拟相机标定链——从编号到平面映射表。虚拟相机/投影机标定（8 算子）。
要点：编号→匹配→端点→光心→外参优化→**PJC 联合标定（独立替代端点链）**→平面映射表→温补查找表；形态：CUDA/混合。

**09-8 calibration/posture — pose_estimate**
职责：算设备当前姿态的。设备姿态估计。
要点：A 流水线 e 段消费；形态：CPU。

**09-9 calibration/temp — intrinsic/extrinsic/laser_extrinsic_compensate（3 个）**
职责：温度变化打的补偿三件套。内参/外参/激光外参温度补偿。
要点：形态：CPU。

**09-10 scanning/preprocess — mask_separation**
职责：把激光和标记点的掩膜分开。联合掩膜分离。
要点：C GPU 链首；形态：CUDA。

**09-11 scanning/laser — laser_match_scan**
职责：扫描时的激光线匹配（查温度表）。激光线匹配扫描。
要点：查温度映射表；形态：CUDA。

**09-12 scanning/fusion — laser_cloud_fuse(+_cuda) / laser_cloud_normal(+_cuda)（4 个）**
职责：激光点云的融合与法线估计（两套实现）。点云融合＋法线。
要点：CPU/CUDA 双轨并列；形态：CPU＋CUDA。

**09-13 scanning/global_optim — global_ba_cpu＋两残差头；gba_dataset_runner**
职责：全局 BA 优化器——D 收尾用。全局 Bundle Adjustment。
要点：**准三元组**（无 Warmup）＋软先验（useSoftPrior）；global_ba_cpu.cpp＋残差头入库（BUILD_GLOBAL_OPTIM）；runner 为独立 exe 不入库；形态：CPU＋Ceres。

### 2.10 10_observability/ — 可观测性（lib `mod_observability`，命名空间 `Scanner::service`）

**10-1 `StateMachine`＋`IState`**（StateMachine.h/.cpp、IState.h）【公·实例 app 造】
职责：全系统唯一的红绿灯——现在能干什么它说了算。7 态表驱动全局门禁。
要点：S1 Init…S6 PostProcessing（免疫故障转态）/S7 FaultSelfCheck；18 条合法边入表＋**CAS 原子转态**；S7 出口回 lastHealthy 续作；canOperate 键表（calibrate/scan/postprocess/edit 仅 S2，scanning＝S4/S5…）。

**10-2 `CommandGate`＋`Spec`**（CommandGate.h/.cpp）
职责：所有开工命令的统一大门——进门先查状态，干完回来销账。统一命令通道（双口）。
要点：submit 走三关（注册/门禁/pre 前置谓词）→即切态→handler 仅点火毫秒级返回；notifyCompleted 是唯一切态收尾口（start→finish 成对）；触发型命令只点火不切态；handler 同步失败回滚；不开线程。

**10-3 `makeDefaultCommandSpecs()`**（DefaultCommands.h）
职责：首批 7 条命令的花名册。默认命令注册表。
要点：system_ready/start_calibration/start_scan/finish_scan/start_postprocess/finish_postprocess/finish_calibration；handler/pre 留空待 app 装配方。

**10-4 `FaultHandler`**（FaultHandler.h/.cpp）
职责：故障接警台——记档、亮红灯、必要时安全停车。故障处理器。
要点：档案表＋来源标识＋1s 聚合防风暴＋S7 故障链；**两口分工防死锁**（reportFault 直调＝完整链，EventBus 订阅口仅记档）；红灯走 LedControl；clearFault 仅清档，恢复走 selfCheckPassed。

**10-5 `PerfMonitor`＋`IHealthProvider`**（PerfMonitor.h/.cpp）
职责：电脑健康预警器——过热/爆盘提前喊话，个别情况直接拉闸。健康指标消费端（采集归 08）。
要点：poll 拉取式（避免总线锁重入死锁）；双级告警——预警级 HealthReport、故障级（磁盘停写保护）reportFault 完整链；持续类规则连续越限满 N 秒才降级、边沿发事件防刷屏；指标 -1 未知跳过。

**10-6 `ObsLogger`**（ObsLogger.h/.cpp）
职责：统一日志管家——写文件、自动轮转、能打包排障包。日志装配＋排障包导出。
要点：stdout＋rotating_file（10MB×5）双 sink 组 spdlog default logger，幂等；obsExportDiagnosticsPackage 收 logs＋dump＋version（不引 zip）；shutdown 须在删日志目录前调。

**10-7 `JMW_LOG_*` 宏**（jmw_logging.h）
职责：全工程统一的打日志快捷键。日志宏集。
要点：spdlog＋fmt 直透；tag 显式传参输出 "[模块-对象] msg" 前缀；与 09 CALIB_LOG_\* 同构同底，新代码一律 JMW_LOG。

**10-8 `install()`/`detectResidualDump()`**（CrashHandler.h/.cpp）
职责：崩溃黑匣子——崩了留 dump，下次启动再分析。SEH 崩溃捕获。
要点：filter 内只做最小安全动作（MiniDumpWriteDump＋标记文件；**禁堆分配/锁/日志**——SEH filter 内不安全）；dump 路径 install 时缓存进 static 缓冲；残留检测返回最旧一份 .dmp 不删除；install 幂等。

### 2.11 app/ — 应用装配层（直编 scan_demo.exe）

**app-1 `main.cpp`**
职责：程序第一脚——初始化一切再开主窗口。应用入口。
要点：ObsLogger init→crash::install＋残留检测→Qt/OSG 初始化（插件路径 env、8×MSAA、dark.qss）→AppContext.initialize→MainWindow 全屏→exec 后 shutdown。

**app-2 `AppContext`**（AppContext.h/.cpp）
职责：总装配车间——造出所有零件并接好线。组合根：创建并拥有全部运行时组件，经 WorkflowContext 窄接口注入。
要点：装配序 EventBus→Data（FrameBuffer/PointCloudBuffer/DeviceStateCache/CalibrationRepository＋load calibration.json）→Service（StateMachine/FaultHandler[safeStop→DeviceManager::toIdle]/CommandGate[注册 7 命令：start_scan pre＝06 仓库 readyForScan 门禁＋handler＝stereo()→ScanCalibration 转接注入]/PerfMonitor）→HAL（DeviceManager open 一条龙＋S1 自检回填；HardwareMonitor start(1000)；HealthAdapter 匿名桥接 08→10）→三工作流创建＋setOnFinished 注入（lambda→notifyCompleted 切 S2）；自检清单 camera/serialPort/license→system_ready 恰一次；**声明序＝析构序**（gate/perf 后于 SM/FH；门面先于巡检件）。

**app-3 `WorkflowContext`**（WorkflowContext.h/.cpp）
职责：给工作流的工具箱提手——一个口提走所有依赖。工作流统一依赖入口。
要点：窄接口聚合 Data 四件＋StateMachine/ParameterManager＋EventBus（ADR7.7）；纯 setter/getter 不拥有；HAL 撤口（设备经组合根取门面）。

**app-4 `MainWindow`**（＋ArrowSlider）
职责：主界面驾驶舱——标题栏/导航/项目树/3D 视图全在这。无边框主窗口「LeadScan K2」。
要点：持 OSGWidget＋CalibDialog＋ScannerWindow＋CalibBoard2D；标定分屏（buildCalibScene＋2D 板＋ArrowSlider 彩条）；**新旧双链并存**——标定/扫描按钮走 stubs 旧演示链（LEADSCANSeries），ScannerWindow 走 gate＋门面新链；信息面板 m_infoTimer 1s＝perfMonitor poll＋DeviceStateCache＋点云计数；cloudTimer 500ms 点云快照刷新。

**app-5 `ScannerWindow`**
职责：扫描控制小窗——开关设备、启停采集、拉参数滑条。扫描仪控制窗口。
要点：设备一律经 AppContext→DeviceManager 门面（**A-T17 红线**，不私摸串口/相机）；帧出口 pushFrameToBuffer（06 FrameBuffer 入队最轻量）；Start→setScanMode(MarkerOnly)＋submit("start_scan")；Stop→submit("finish_scan")＋门禁拒后兜底直停；滑条按 ParamStore spec 运行时钳位；开关设备回填 notifySelfCheckItem。

**app-6 `app/stubs/`（5 个桩头）**
职责：外部依赖的占位假件——真东西没来先拿它编过。桩头集。
要点：LEADSCANSeries.h / CameraControl.h / camera_calib_workflow.h / laser_calib_workflow.h / scan_workflow.h——人工提供；MainWindow 旧演示链消费；条件构建守卫。
