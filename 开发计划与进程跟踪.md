# JEAMMWARE 开发计划与进程跟踪

> **快照日期**: 2026-07-06 ｜ **阶段**: 骨架已完成（P0），进入业务实现期（P1-P7）
> **工程**: `E:\JEAMMWARE260705`（3D 扫描仪交付级产品软件）
> **读者**: 开发者 / AI 协作者 ｜ **维护方式**: 滚动更新，状态变更即改，快照日期 = 末次变更日期
> **依据**: `docs/architecture/software-architecture-skeleton-design.md` ｜ `框架整体.md` ｜ `算子目录.md` ｜ `工程目录地图.md`
>
> **构建验证（2026-07-06 实测）**：现有 `build/` 目录为 **Debug-only**（`CMAKE_CONFIGURATION_TYPES=Debug` + Debug OpenCV `C:/opencv-cuda-4.13.0-debug`）。`ctest -C Debug` = **42/42 全绿（96.95s）**。⚠ 注意：本文件/AGENTS.md 主推的 **Release 命令对现有 build/ 目录无效**（报 MSB8013 / "Test not available in Release"）；Release 需另建独立构建目录（见附录 B）。

---

## 1. 文档说明

本文档是 JEAMMWARE 工程的**单一开发计划与进程跟踪中枢**，回答四个问题：

1. **要建什么** —— 11 个核心模块各自的目的（§4）
2. **拿什么建** —— 每模块所需的参考资料/输入资料（§4 各节「参考资料来源」）
3. **建到哪了** —— 每模块已完成项 + 全局状态总览（§2）
4. **接下来建什么** —— 依赖驱动的动工顺序（§3）+ 每模块待办里程碑（§4）

**状态徽章约定**：✅ 已完成 ｜ 🟡 进行中 ｜ ⬜ 未开始 ｜ 🔵 部分完成（迁入但待集成）

**架构回顾**（5 层 + 11 模块 + 部署期 wrapper + 侧边设施 + 横切）：

```
部署期wrapper(模块11) → 接入端UI(模块3渲染/5编辑/6文件/8设备) → 横切(权限+模块10可观测)
   → Workflow层(模块1标定/2扫描/4后处理) → Service层(模块7会话/5编辑/6文件/8设备) ∥ Algorithm层(模块9算子库)
   → Data层(RingBuffer/双缓冲/标定IO/文件IO/ArtifactStore) → HAL层(模块8设备管理)
侧边设施: EventBus / GpuRuntime / Scheduler / Watchdog
```

---

## 2. 状态总览表

| # | 模块 | 主层 | 状态 | 完成度 | framework 层依赖 | 模块/阶段依赖 | 所属阶段 |
|---|------|------|------|--------|------------------|---------------|----------|
| 09 | operatorlib 算子库 | Algorithm | ✅ | 100% | fw_algorithm | — | P0 |
| 03 | rendering 渲染显示 | UI | 🔵 | display 组件已迁入并入构建；UI 集成 + Data 直读待 P5 | fw_ui, fw_algorithm, fw_data | 09 | P0/P5 |
| 08 | devicemgmt 设备管理 | HAL | ⬜ | 0% | fw_hal, fw_service, fw_ui, fw_data, fw_crosscut | — | P1/P2/P6 |
| 06 | fileio 文件管理 | Data | ⬜ | 0% | fw_data, fw_service, fw_ui | — | P2/P5 |
| 01 | calibration 标定 | Workflow | ⬜ | 0% | fw_workflow, fw_algorithm, fw_data | 09 + P1/P2/P3 | P4 |
| 02 | scanning 扫描 | Workflow | ⬜ | 0% | fw_workflow, fw_algorithm, fw_data, fw_infra | 09 + 08 + P1/P2/P3 | P4 ★ |
| 04 | postprocess 后处理 | Workflow | ⬜ | 0% | fw_workflow, fw_algorithm, fw_data | 09 + P2/P3 | P4 |
| 07 | session 会话管理 | Service | ⬜ | 0% | fw_service, fw_workflow, fw_ui, fw_data | 01, 02 | P5 |
| 05 | editing 编辑 | UI | ⬜ | 0% | fw_ui, fw_service, fw_data | — | P5 |
| 10 | observability 可观测性 | 横切 | ⬜ | 0% | fw_crosscut | — | P6 |
| 11 | deploy 安装部署 | 部署期 | ⬜ | 0% | — | 全部业务模块就绪 | P7 |

> ★ = 关键路径（最长杆 + 最高风险）。M02 扫描 180fps 实时是全工程关键路径。

**framework 层契约（9 个 fw_* 库）**：✅ 全部就位 —— `fw_common`(STATIC，含共享类型) + 8 个 INTERFACE 层契约（`fw_ui`/`fw_workflow`/`fw_service`/`fw_algorithm`/`fw_data`/`fw_hal`/`fw_infra`/`fw_crosscut`）。均为桩。骨架构建 + smoke 测试通过（Debug 42/42 绿，见顶部构建验证）。
> ⚠ 注：设计稿 §6 与 AGENTS.md 写"8 fw_*"是把 `fw_common` 排除在外（仅数 8 个架构层）；实际库目标 9 个。

---

## 3. 动工顺序（依赖驱动 · 8 阶段路线）

### 3.1 依赖 DAG

```
P0 地基 ✅ ──→ P1 HAL底座 ──┐
                            ├─→ P3.5 垂直smoke ──→ P4 业务工作流 ★ ──┐
P0 ──→ P2 Data主体 ──┐      │                                       ├─→ P5 Service+UI ──→ P6 横切收口 ──→ P7 交付硬化
                     ├─→ P4 │                                       │
P0 ──→ P3 Workflow编排 ┘    │                                       │
                            ┘                                       │
P0 ──→ 09算子库 ✅ ──────────────────────────────────────────────────┘ (P4 直接消费)
P0 ──→ 03渲染 🔵 ────────────────────────────────────────────────────→ (P5 UI 集成)
```

> **并行性**：P1/P2/P3 三者都只依赖 P0、相互弱耦合，**可并行启动**（不同人/分支）。P4 必须等 P1+P2+P3+09 齐备。
> ★ **关键路径**：P0 → (P1∥P2∥P3) → P4（M02 扫描）→ P5 → P6 → P7。M02 是长杆。
> **framework 层实现是独立工作流**：P1/P2/P3 的主体是"把 fw_* 桩实现成真实代码"（fw_hal/fw_data/fw_workflow/fw_infra），**不归任何业务模块**——M08/M06 等只是消费方。需把 framework 层实现当作与 11 模块并列的工作流来排期。

### 3.2 阶段路线

| 阶段 | 目标 | 涉及模块 / framework | 关键产出 | 状态 |
|------|------|----------------------|----------|------|
| **P0 地基** | 架构骨架可构建可测试 | framework 9 库(桩) + app + sdk + 09 + 03 | 契约桩 + exe + Debug 42/42 ctest 绿 | ✅ 完成 |
| **P1 HAL/设备底座** | 硬件可接入、帧可采集 | **fw_hal 实现** + 08(HAL侧消费) | ICamera/IMCU/IPlatform 真实驱动、CaptureDriver | ⬜ |
| **P2 Data 层主体** | 数据可流动可持久化 | **fw_data 实现** + 06/08(Data侧消费) | RingBuffer/双缓冲/标定IO/文件IO/ArtifactStore/操作历史 | ⬜ |
| **P3 Workflow 编排** | 流水线可跑 | **fw_workflow + fw_infra 实现** | Pipeline/Stage/Scheduler/Watchdog/GpuRuntime 真实实现 | ⬜ |
| **P3.5 垂直切片 smoke** | 端到端最小链路打通 | fw_hal+fw_data+fw_workflow(各1实现) + 09(1算子) + 03(渲染) | HAL采1帧→1算子→Data→渲染 显示出来（早期风险验证） | ⬜ |
| **P4 业务工作流** ★ | 标定/扫描/后处理三条流水线打通 | 01, 02(★), 04 | 25 姿态标定、180fps 双链扫描、七阶段后处理出 STL | ⬜ |
| **P5 Service + UI 接入** | 操作员可交互 | 05, 06, 07 + UI 主壳(Qt) | 编辑撤销、文件导入导出、会话控制、主窗口 | ⬜ |
| **P6 平台/横切** | 可运维可鉴权 | 10 + 用户权限 + 08(许可证侧) | 统一日志/性能/崩溃、角色鉴权、许可证绑定 | ⬜ |
| **P7 交付硬化** | 可出厂 | 11 + 出厂级 | 安装包/首配向导、看门狗/自检/审计/多语言 | ⬜ |

> **路线图来源声明**：本 8 阶段（P0-P7）是依设计稿 §4 模块→framework 依赖表**自行推导**，未经 `3DSCANNER260622` 的"工程缺口分析与出厂路线图（8 切片）"逐项对齐（该文件在外部工程，本仓库未含）。如需严格对齐，需拉取该文件对照。

---

## 4. 核心模块详述

> 每节六字段：目的与职责 / 参考资料来源 / 依赖关系 / 已完成 / 待办（里程碑） / 风险

---

### M09 · operatorlib 算子库  ✅

- **目的与职责**：全部算子（core/calibration/scanning）+ GBA，纯计算叶子，标定/扫描双模式复用；含融合/法线等跨 workflow 共用算子。Algorithm 层。
  > ⚠ **计数说明**：源文档不一致——`框架整体.md`/设计稿记"39 算子（core17/calib16/scan6）"，`09_operatorlib/README.md` 记"42 算子+GBA"，`算子目录.md` 统计表 core17+calib16+scan6=39 但逐项列 core 实为 18。**以代码实测为准，不写死数字**。
- **参考资料来源**：
  - 源码：`modules/09_operatorlib/`（已迁入，命名空间 `calib::`）
  - 算子文档：`docs/算子说明文档/`（core/calibration/scanning/display，A-K 模板）+ `算子目录.md` + `算子规范.md` v2.0
  - 来源工程：`E:\3DSCANNER260622`（原研发工程）
  - 外部库：OpenCV 4.13(+CUDA+Contrib)、Eigen 3.4.1、Ceres 2.2(GBA)、CUDA 12.6
- **依赖关系**：仅依赖 fw_algorithm（取共享 QualityFlag）；纯叶子，不依赖 Data/EventBus
- **✅ 已完成**：
  - [x] 全部算子源码迁入单库 `mod_operatorlib`（core+calibration+scanning）
  - [x] GBA 全局 Bundle Adjustment 入库（`BUILD_GLOBAL_OPTIM`）
  - [x] ctest 全绿（**2026-07-06 实测 Debug 42/42 通过，96.95s**）
  - [x] 算子说明文档就位
  - [x] 修 System32 MSVCP140 旧版崩溃问题
- **⬜ 待办**：（本模块基本完成，仅维护）
  - [ ] 后处理网格算子（封装/补洞/光顺/边界优化）按需补建（M04 触发）
- **⚠ 风险**：无。本模块是后续 M01/M02/M04 的算子来源，已就位。

---

### M03 · rendering 渲染显示  🔵

- **目的与职责**：点云实时 3D 渲染（CUDA-GL 零拷贝），标记点/激光点云 viewer。UI 层。
- **参考资料来源**：
  - 源码：`modules/03_rendering/display/`（scanner_viewer / laser_cloud_renderer / marker_cloud_renderer + point_expand_kernel）
  - 文档：`docs/算子说明文档/display/`（3 份）+ `docs/plans/2026-07-05-display-migration-design.md`
  - 外部库：OSG（OpenSceneGraph）、CUDA-GL interop
- **依赖关系**：fw_ui + fw_algorithm + fw_data；待 UI 集成（P5）
- **✅ 已完成**：
  - [x] display 渲染组件迁入 `mod_rendering`
  - [x] point_expand_kernel（CUDA 点扩展核）
  - [x] 并入工程构建
- **⬜ 待办**：
  - [ ] 提交未 track 文件（`modules/03_rendering/display/` + 2 份 migration 计划）
  - [ ] 与 Qt UI 主壳集成（P5）
  - [ ] 接入 Data 层只读直读（ADR 7.8 IPointCloudReadView）
- **⚠ 风险**：有未提交文件，git status 显示 `modules/03_rendering/display/` 未 track，需尽快提交固化。

---

### M08 · devicemgmt 设备管理（含许可证/激活）  ⬜

- **目的与职责**：下位机/PC 状态监控（温度/帧率/按键/故障）与设备控制；含设备绑定许可证/激活（加密狗/软授权、权益、试用）。HAL 层（主），跨 Service/UI/Data/crosscut。
- **参考资料来源**：
  - 架构：`框架整体.md` §3.2、`docs/architecture/...-skeleton-design.md` §3.4/§3.7
  - 契约：`framework/hal/ICamera.h` `IMCU.h` `IPlatform.h`、`framework/crosscut/IAuth.h`
  - 待确认：下位机通讯协议（MCU 串口/USB）、相机驱动 SDK、加密狗/软授权方案
  - 外部库：待定（依硬件）
- **依赖关系**：fw_hal + fw_service + fw_ui + fw_data + fw_crosscut
- **✅ 已完成**：（无）
- **⬜ 待办**：
  - [ ] 定义 HAL 真实驱动接口实现（CaptureDriver/LightDriver/LedDriver/HardwareMonitor）
  - [ ] 相机 ICamera1/2 实现（采集、触发、参数）
  - [ ] MCU IMCU 实现（通讯链、按键、温度上报）
  - [ ] 平台 IPlatform 实现（Windows / Jetson 双平台编译时多态）
  - [ ] 设备状态缓存 + Data 层 Sink 回填
  - [ ] 许可证/激活存储与权益检查（P6）
  - [ ] 设备管理 UI 界面（P5）
- **⚠ 风险/待决策**：① 下位机通讯协议文档是否齐备（关键阻塞）；② 加密狗选型 vs 软授权方案未定；③ Windows/Jetson 双平台优先级。

---

### M06 · fileio 文件管理  ⬜

- **目的与职责**：PLY/STL/标定参数导入导出，工程/工件文件组织。Data 层（主），跨 Service/UI。
- **参考资料来源**：
  - 架构：`框架整体.md` §3.1、设计稿 §3.4（WorkflowArtifactStore ADR 7.10）
  - 契约：`framework/data/IDataStore.h` `DataContext.h` `WorkflowArtifactStore.h`
  - 外部库：PLY/STL 解析（待定，PCL/Open3D/自写）、nlohmann_json（标定参数）
  - 待确认：工程/工件文件目录结构约定、标定参数文件格式（3DSCANNER 是否已有）
- **依赖关系**：fw_data + fw_service + fw_ui
- **✅ 已完成**：（无）
- **⬜ 待办**：
  - [ ] 定义文件格式规范（PLY/STL/标定参数 JSON schema）
  - [ ] PLY 导入导出实现
  - [ ] STL 导出实现（M04 后处理产出）
  - [ ] 标定参数导入导出（内/外参/plane_map/温度表）
  - [ ] 工程/工件文件组织（目录树、元数据）
  - [ ] WorkflowArtifactStore 产物落地（ADR 7.10）
  - [ ] 文件管理 UI 界面（P5）
- **⚠ 风险/待决策**：① 标定参数文件格式是否复用 3DSCANNER 既有格式；② PLY/STL 库选型。

---

### M01 · calibration 标定  ⬜

- **目的与职责**：25 姿态采集 → 解算相机内外参/立体矫正/激光平面映射表(plane_map)/温度补偿表，产出可复用标定参数（离线 batch）。Workflow 层。
- **参考资料来源**：
  - 算子：`modules/09_operatorlib/calibration/`（camera 链 5 + laser_calib 链 7 + posture 1 + temp 3 = 16 算子）
  - 算子文档：`docs/算子说明文档/calibration/`（16 份）
  - 架构：`框架整体.md` §3.1、设计稿 §3.3（Workflow/Pipeline）
  - 契约：`framework/workflow/IWorkflow.h` `WorkflowContext.h` `Pipeline.h`
- **依赖关系**：fw_workflow + fw_algorithm + fw_data + **mod_09_operatorlib**；前置 P1(硬件采集) + P2(标定数据存储) + P3(批处理编排)
- **✅ 已完成**：（算子已就位，工作流未建）
- **⬜ 待办**：
  - [ ] 定义标定工作流接口（ICalibWorkflow，ADR 7.7 窄角色接口）
  - [ ] 25 姿态采集流程（调 M08 HAL 采集）
  - [ ] 相机标定链编排（intrinsic→extrinsic→rectify→temp_table）
  - [ ] 虚拟相机标定链编排（laser_label→match→endpoint→vcam_pose→optimize→plane_map→temp_table）
  - [ ] 姿态估计 + 温度补偿表生成
  - [ ] 标定结果落盘（调 M06）+ 质量评估
  - [ ] 标定导引 UI（P5）
- **⚠ 风险/待决策**：① 标定质量评估准则（重投影误差阈值）；② 温度补偿表的标定流程（温箱？现场？）。

---

### M02 · scanning 扫描（双模式）  ⬜

- **目的与职责**：实时扫描 180fps：标记点链（配准求 R/T）∥ 激光链（三维重建，纯标记点模式关）双链并行。Workflow 层。**整个产品的核心实时路径**。
- **参考资料来源**：
  - 算子：`modules/09_operatorlib/core/`（vision 2 + marker 12 + laser 4 = 共享前端）+ `scanning/`（mask_separation + laser_match_scan + fusion 4 = 6 算子）
  - 算子文档：`docs/算子说明文档/core/`（17）+ `scanning/`（6）
  - 架构：`框架整体.md` §3.1、设计稿 §3.3（Pipeline ADR 7.2 背压/丢帧）
  - 契约：`framework/workflow/Pipeline.h` `Stage.h`、`framework/infra/Scheduler.h` `Watchdog.h` `GpuRuntime.h`、`framework/data/IFrameSink.h`
  - 关键 ADR：7.1(背压) 7.2(丢帧) 7.3(急停硬实时) 7.4(双缓冲) 7.6(FusionStateHandle) 7.11(ResourceLifecycleManager)
- **依赖关系**：fw_workflow + fw_algorithm + fw_data + fw_infra + **mod_09_operatorlib** + M08(HAL 采集)；前置 P1+P2+P3
- **✅ 已完成**：（算子已就位，实时流水线未建）
- **⬜ 待办**：
  - [ ] 双链 Pipeline 搭建（标记点链 + 激光链，纯标记点模式可关激光链）
  - [ ] 帧缓冲 RingBuffer 接入（Data 层 P2）
  - [ ] Scheduler/Watchdog/GpuRuntime 真实实现（infra P3，含 CUDA Stream 调度）
  - [ ] FusionStateHandle 跨帧状态（融合哈希归 Data，ADR 7.6）
  - [ ] 背压/丢帧策略落地（ADR 7.2，有界队列）
  - [ ] 急停硬实时路径（ADR 7.3）
  - [ ] 点云双缓冲前台/后台（ADR 7.4，供 M03 渲染直读）
  - [ ] 覆盖率统计（喂给 M07 会话）
- **⚠ 风险/待决策**：① 180fps 实时性是最高风险项，需 Scheduler/Watchdog 落地后压测；② 双链同步策略；③ 急停硬实时实现路径（独立线程/中断）。

---

### M04 · postprocess 后处理  ⬜

- **目的与职责**：离线 batch 七阶段（全局标记点优化→重融合→法线计算→封装→补洞→光顺→边界优化），出 STL。Workflow 层。
- **参考资料来源**：
  - 算子：`modules/09_operatorlib/scanning/global_optim/`（GBA 已入库，`BUILD_GLOBAL_OPTIM`）+ scanning/fusion（重融合/法线，已就位）
  - 待建算子：封装/补洞/光顺/边界优化（网格算子，M09 按需补建）
  - 架构：`框架整体.md` §3.1（七阶段）、设计稿 §3.6
  - 契约：`framework/data/WorkflowArtifactStore.h`（ADR 7.10）、`framework/workflow/`
  - 外部库：网格处理（PCL/libigl/CGAL/Open3D 待选）
- **依赖关系**：fw_workflow + fw_algorithm + fw_data + **mod_09_operatorlib**；前置 P2(ArtifactStore) + P3(批处理编排)；消费 M02 扫描产物
- **✅ 已完成**：GBA 算子已入库
- **⬜ 待办**：
  - [ ] 定义后处理工作流（七阶段 Pipeline，批处理模式）
  - [ ] 阶段1：全局标记点优化（GBA 调用）
  - [ ] 阶段2：重融合（laser_cloud_fuse 离线版）
  - [ ] 阶段3：法线计算（laser_cloud_normal，已就位）
  - [ ] 阶段4：封装（网格算子，待建）⚠
  - [ ] 阶段5：补洞（网格算子，待建）⚠
  - [ ] 阶段6：光顺（网格算子，待建）⚠
  - [ ] 阶段7：边界优化（网格算子，待建）⚠
  - [ ] STL 导出（调 M06）
- **⚠ 风险/待决策**：① 网格算子（封装/补洞/光顺/边界优化）尚未实现，是本模块最大缺口；② 网格库选型（PCL vs libigl vs CGAL）。

---

### M07 · session 会话管理  ⬜

- **目的与职责**：扫描启/停/暂停/恢复、覆盖率统计、断电续扫、会话元数据。Service 层（主），跨 Workflow/UI/Data。
- **参考资料来源**：
  - 架构：`框架整体.md` §3.1、设计稿 §3.4（IService/StateMachine/FaultHandler）
  - 契约：`framework/service/IService.h` `StateMachine.h` `FaultHandler.h`
  - 待确认：断电续扫的持久化策略（会话元数据 schema）
- **依赖关系**：fw_service + fw_workflow + fw_ui + fw_data；依赖 M01/M02（扫描工作流跑在会话内）
- **✅ 已完成**：（无）
- **⬜ 待办**：
  - [ ] 扫描状态机（Idle/Running/Paused/Stopped/Faulted）
  - [ ] 启/停/暂停/恢复控制（驱动 M02 工作流）
  - [ ] 覆盖率统计（消费 M02 输出）
  - [ ] 断电续扫（会话元数据持久化 + 恢复）
  - [ ] 故障处理（FaultHandler，对接 M10 可观测性）
  - [ ] 会话控制 UI（P5）
- **⚠ 风险/待决策**：断电续扫一致性（WAL？检查点频率？）。

---

### M05 · editing 编辑（含撤销/重做）  ⬜

- **目的与职责**：扫描时选中/标记/删除点云或标记点；命令栈支持 undo/redo + 操作历史可追溯。UI 层（主），跨 Service/Data。
- **参考资料来源**：
  - 架构：`框架整体.md` §3.1、设计稿 §3.4
  - 契约：`framework/ui/IView.h`（ADR 7.8 IPointCloudReadView）、`framework/service/`
  - 待确认：命令模式（Command pattern）的历史栈容量、点云裁剪数据结构
- **依赖关系**：fw_ui + fw_service + fw_data
- **✅ 已完成**：（无）
- **⬜ 待办**：
  - [ ] 编辑服务 + 命令栈（Command pattern，undo/redo）
  - [ ] 点云选中/标记/删除操作
  - [ ] 标记点编辑操作
  - [ ] 操作历史栈（Data 层，可追溯）
  - [ ] 点云裁剪数据结构
  - [ ] 编辑 UI（P5，与 M03 渲染集成）
- **⚠ 风险/待决策**：编辑操作与实时扫描流水线的数据一致性（编辑时扫描是否暂停）。

---

### M10 · observability 可观测性/运维  ⬜

- **目的与职责**：统一日志（配置/轮转/分级）、性能监控（延迟/帧率/计数器 dashboard）、崩溃捕获（minidump/crash）。横切层（全局）。
- **参考资料来源**：
  - 架构：`框架整体.md` §3.2、设计稿 §3.4
  - 契约：`framework/crosscut/ILogger.h` `IPerfMonitor.h` `ICrashHandler.h` `IConfig.h`
  - 外部库：spdlog 1.15（已用，未统一配置）、minidump（Windows dbghelp / Breakpad 待选）
  - 待确认：性能 dashboard 形态（独立窗口？Web？日志文件？）
- **依赖关系**：fw_crosscut；全局贯穿，P6 集中收口但 P2 起逐步落地
- **✅ 已完成**：spdlog 已接入（未统一配置）
- **⬜ 待办**：
  - [ ] 统一日志配置（分级/轮转/格式，封装 `framework/crosscut` ILogger 实现）
  - [ ] 性能监控（延迟/帧率/计数器采集）
  - [ ] 性能 dashboard 展示
  - [ ] 崩溃捕获（minidump 落盘 + 重启上报）
  - [ ] 配置管理（IConfig 实现，nlohmann_json）
  - [ ] 审计日志（出厂级）
- **⚠ 风险/待决策**：① 崩溃捕获方案（dbghelp vs Breakpad vs Crashpad）；② 性能 dashboard 形态。

---

### M11 · deploy 安装部署 + 首次配置向导  ⬜

- **目的与职责**：安装包/卸载、首运行向导（连接设备/选语言/初始标定引导）。部署期 wrapper（运行时之外）。
- **参考资料来源**：
  - 架构：`框架整体.md` §3.3、设计稿 §3.7
  - 外部库：安装包工具（NSIS / Inno Setup / WiX 待选）
  - 待确认：首配向导调用的模块（标定 M01 / 设备 M08 / 多语言）
- **依赖关系**：全部业务模块就绪（最后阶段）
- **✅ 已完成**：（无）
- **⬜ 待办**：
  - [ ] 安装包制作（exe + 依赖打包：OpenCV/CUDA/Qt 运行时）
  - [ ] 卸载流程
  - [ ] 首次配置向导（连设备→选语言→初始标定引导）
  - [ ] 上电自检（出厂级）
  - [ ] 多语言资源（出厂级）
  - [ ] 出厂级看门狗（独立于 infra Watchdog）
- **⚠ 风险/待决策**：① 安装包工具选型；② CUDA/OpenCV 运行时打包体积；③ 多语言范围。

---

### 框架结构元素（非模块，不计入 11）

| 元素 | 位置 | 说明 | 状态 |
|------|------|------|------|
| **SDK（二次开发）** | `sdk/IScannerSDK.h` | 接入端 B 的 API 门面（MES/PLM 集成/远程/CLI） | ⬜ 桩，P6/P7 实现 |
| **用户权限/鉴权** | `framework/crosscut/IAuth.h` | 角色/登录/操作授权，管 UI(+SDK) 接入 | ⬜ 桩，P6 实现 |

---

## 5. 全局风险登记表

> 汇总各模块 ⚠ 风险，便于跨模块跟踪。按严重度排序。

| # | 风险 | 涉及模块 | 严重度 | 缓解/待办 |
|---|------|----------|--------|-----------|
| R1 | **M02 扫描 180fps 实时性** —— 双链并行 + CUDA Stream 调度 + 背压/丢帧，未压测 | M02, P3 | 🔴 高 | P3 Scheduler/Watchdog 落地后立即做垂直 smoke + 帧率压测；标记为关键路径 |
| R2 | **M08 下位机通讯协议文档** —— MCU 串口/USB 协议是否齐备未知，阻塞 HAL | M08, P1 | 🔴 高 | 动工前确认协议文档；若无则需硬件方提供 |
| R3 | **M04 网格算子缺口** —— 封装/补洞/光顺/边界优化四算子未实现 | M04, M09 | 🟠 中高 | 网格库选型（PCL/libigl/CGAL）→ 补建进 M09 |
| R4 | **构建配置文档不一致** —— 现有 build/ 是 Debug-only，但 AGENTS/本文档主推 Release 命令 | 全局 | 🟠 中 | 已在本文档顶部 + 附录 B 标注；建议同步修 AGENTS.md |
| R5 | **M03 未提交文件** —— `modules/03_rendering/display/` + 2 份计划文档未 track | M03 | 🟡 中低 | 尽快 `git add` 固化 |
| R6 | **断电续扫一致性** —— 会话元数据持久化策略（WAL/检查点）未定 | M07 | 🟡 中 | P5 设计会话元数据 schema 时定 |
| R7 | **崩溃捕获方案未选** —— dbghelp vs Breakpad vs Crashpad | M10 | 🟡 中 | P6 选型 |
| R8 | **CUDA/OpenCV 运行时打包体积** —— 安装包可能过大 | M11 | 🟡 中 | P7 评估裁剪/按需安装 |
| R9 | **算子计数源文档不一致** —— 39 vs 42，core 17 vs 18 | M09 | 🟢 低 | 已在 M09 标注，以代码实测为准 |

---

## 6. 进程变更日志

> 只追加，不删除。每次更新记 日期 + 本次变更摘要。快照日期 = 末次变更日期。

| 日期 | 阶段 | 变更摘要 |
|------|------|----------|
| 2026-07-05 | 文档建立 | 初始创建。录入 P0 完成状态（framework 契约 + app + sdk + M09 算子库 + M03 display 迁入）；规划 P1-P7 八阶段路线；11 模块六字段详述 |
| 2026-07-06 | 审查修订 | **实测构建验证**：Debug ctest 42/42 绿（96.95s）。修正 3 处错误：① fw_* 库 8→**9**（fw_common STATIC + 8 INTERFACE）；② ctest 状态改为"Debug 实测绿"，标注现有 build/ 是 Debug-only、Release 命令需另建目录；③ 算子计数不写死（源文档 39/42/core17-18 冲突）。优化：§2 依赖列拆 framework/模块两列 + 标关键路径★；§3 加并行性说明、framework 层实现独立工作流、**P3.5 垂直切片 smoke** 里程碑、路线图来源声明；新增 §5 全局风险登记表（R1-R9）；附录 B 补 Debug 命令 |

---

## 附录 A · 关键参考文档索引

| 文档 | 位置 | 用途 |
|------|------|------|
| 架构骨架设计 | `docs/architecture/software-architecture-skeleton-design.md` | 5 层 + 11 模块权威设计（§3 各层契约、§4 模块依赖、§8 后续） |
| 框架整体图 | `框架整体.md` | 01.4 框架图 + 模块归位表 + 现状缺口 |
| 工程目录地图 | `工程目录地图.md` | 快速查阅目录结构 |
| 算子规范 | `算子规范.md` | 算子契约 v2.0（三元组 + 工程集成） |
| 算子目录 | `算子目录.md` | 算子索引 + 文档路径（计数有出入，见 R9） |
| 算子说明文档 | `docs/算子说明文档/` | 算子逐份说明（A-K 模板） |
| 环境配置汇总 | `环境配置汇总.md` | VS/CUDA/OpenCV/三方库路径 |
| AGENTS.md | `AGENTS.md` | AI 工程索引（环境/结构/构建命令） |
| 本文档设计稿 | `docs/plans/2026-07-05-dev-plan-tracking-design.md` | 本文档的结构设计决策记录 |
| 8 切片路线图（外部） | `E:\3DSCANNER260622\docs\plans\` | 工程缺口分析与出厂路线图（参考，本仓库未含；本计划未严格对齐） |

## 附录 B · 构建验证命令

> ⚠ **先看这个**：现有 `build/` 目录是 **Debug-only**（`CMAKE_CONFIGURATION_TYPES=Debug` + Debug OpenCV）。直接跑 Release 命令会报 `MSB8013` / `Test not available in Release`。两种选其一：

**方案 1 — 用现有 Debug 构建（推荐，立即可跑）**：
```powershell
# 现有 build/ 已 configure 为 Debug；如需重建：
cmake --build build --config Debug
# Debug 需 Debug OpenCV 在 PATH（运行期）
$env:PATH = 'C:\opencv-cuda-4.13.0-debug\x64\vc17\bin;' + $env:PATH
ctest --test-dir build -C Debug --output-on-failure     # 实测 42/42 绿
```

**方案 2 — 新建独立 Release 构建目录**（与 Debug 隔离，不串味）：
```powershell
cmake -S . -B build-rel -G "Visual Studio 17 2022" -A x64 `
  -DCMAKE_CONFIGURATION_TYPES=Release
cmake --build build-rel --config Release
ctest --test-dir build-rel -C Release --output-on-failure
```
