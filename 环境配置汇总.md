# 本机开发环境配置汇总

> 适用项目：手持式双目三维多线激光扫描仪软件框架  
> 目标平台：Windows 10/11 x64（兼顾 Jetson Orin NX/AGX Orin 移植）  
> 整理日期：2026-05-20

---

## 一、基础环境

| 项目 | 版本 | 安装路径 |
|------|------|---------|
| 操作系统 | Windows 10 22H2 (Build 19045.6456) x64 | — |
| Visual Studio 2022 | 17.14.37216 (Community) | `C:\Program Files\Microsoft Visual Studio\2022\Community\` |
| MSVC 编译器 | 14.44.35207 (v144 工具集) | `C:\Program Files\Microsoft Visual Studio\2022\Community\VC\Tools\MSVC\14.44.35207\` |
| CMake | 4.3.0-rc1 | `C:\Program Files\CMake\bin\cmake.exe` |
| Ninja | 1.x | `C:\devlibs\ninja\ninja.exe` |
| 语言标准 | C++17 (`/std:c++17`) | — |

> **注意**：文档要求 MSVC v143 工具集，本机实际为 v144。v144 向后兼容 v143，  
> 所有已编译库均使用 v144（MSVC 14.44）。建议将项目工具集统一设为 v144。

---

## 二、CUDA 与 GPU 计算栈

| 项目 | 版本 | 安装路径 |
|------|------|---------|
| CUDA Toolkit | 12.6 (V12.6.85) | `C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v12.6\` |
| cuDNN | 9.21.1 | 头文件位于 CUDA 目录下 `...\CUDA\v12.6\include\cudnn*.h` |
| GPU | NVIDIA Quadro RTX 5000 16GB (sm_75) | — |
| 显卡驱动 | 573.91 | — |

### CMake 中引用 CUDA

```cmake
set(CMAKE_CUDA_COMPILER "C:/Program Files/NVIDIA GPU Computing Toolkit/CUDA/v12.6/bin/nvcc.exe")
enable_language(CUDA)
set(CMAKE_CUDA_ARCHITECTURES 75)  # Quadro RTX 5000
```

> **重要（产品 vs 开发机）**：上面 `CMAKE_CUDA_ARCHITECTURES 75` 仅指**本开发机本机构建**
> （Quadro RTX 5000 = sm_75）。**产品需跨平台**：sm_75（本机 RTX 5000）+ sm_86（Ampere 桌面）
> + **sm_87（Jetson Orin NX/AGX Orin，见第一节目标平台）**。因此 JEAMMWARE 工程实际设为
> `CMAKE_CUDA_ARCHITECTURES "75;86;87"`，**不要**按本节示例改成只 75，否则会破坏
> Jetson Orin（sm_87）移植目标。本机多编 sm_86/87 的代价是构建变慢，但为部署所需。

> **CUDA 工具链注意**：本机同时安装了 v12.6（PATH/CUDA_PATH 默认）与 v12.4。工程用
> `CMAKE_CUDA_COMPILER` 显式 pin 到 v12.6 的 nvcc，避免旧构建缓存误用 v12.4。


### CUDA 环境变量（已自动配置）

| 变量 | 值 |
|------|---|
| CUDA_PATH | `C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v12.6` |
| CUDA_BIN_PATH | `%CUDA_PATH%\bin` |
| CUDA_LIB_PATH | `%CUDA_PATH%\lib\x64` |
| CUDA_INCLUDE_PATH | `%CUDA_PATH%\include` |

---

## 三、第三方库详细路径

### 1. OpenCV 4.13.0 + CUDA + Contrib

| 资源 | 路径 |
|------|------|
| 安装根目录 | `C:\opencv-cuda-4.13.0\` |
| 头文件 | `C:\opencv-cuda-4.13.0\include\` |
| 导入库 (63个 .lib) | `C:\opencv-cuda-4.13.0\x64\vc17\lib\` |
| 运行时 DLL (63个 .dll) | `C:\opencv-cuda-4.13.0\x64\vc17\bin\` |
| CMake 配置文件 | `C:\opencv-cuda-4.13.0\OpenCVConfig.cmake` |
| 编译器工具集 | vc17 (MSVC 14.4x) |

**CMake 引用方式：**

```cmake
set(OpenCV_DIR "C:/opencv-cuda-4.13.0")
find_package(OpenCV 4.13 REQUIRED)
target_include_directories(your_target PRIVATE ${OpenCV_INCLUDE_DIRS})
target_link_libraries(your_target PRIVATE ${OpenCV_LIBS})
```

**运行时需要将以下目录加入 PATH：** `C:\opencv-cuda-4.13.0\x64\vc17\bin`

---

### 2. Open3D (main 分支, CUDA 已启用)

| 资源 | 路径 |
|------|------|
| 安装根目录 | `C:\devlibs\open3d-install\` |
| 头文件 | `C:\devlibs\open3d-install\include\open3d\` |
| 导入库 | `C:\devlibs\open3d-install\lib\Open3D.lib` (101.9 MB) |
| 运行时 DLL | `C:\devlibs\open3d-install\bin\Open3D.dll` (163.5 MB) |
| CMake 配置文件 | `C:\devlibs\open3d-install\CMake\Open3DConfig.cmake` |
| 源码目录 | `C:\devlibs\Open3D-main\` |
| 构建目录 | `C:\devlibs\open3d-build\` |
| CUDA 架构 | sm_75 |

**CMake 引用方式：**

```cmake
set(Open3D_DIR "C:/devlibs/open3d-install/CMake")
find_package(Open3D REQUIRED)
target_link_libraries(your_target PRIVATE Open3D::Open3D)
```

**运行时需要将以下目录加入 PATH：** `C:\devlibs\open3d-install\bin`

---

### 3. PCL 1.15.1

| 资源 | 路径 |
|------|------|
| 安装根目录 | `C:\devlibs\pcl-1.15.1-install\` |
| 头文件 | `C:\devlibs\pcl-1.15.1-install\include\pcl-1.15\pcl\` |
| 导入库 (18个 .lib) | `C:\devlibs\pcl-1.15.1-install\lib\` |
| 运行时 DLL (18个 .dll) | `C:\devlibs\pcl-1.15.1-install\bin\` |
| CMake 配置目录 | `C:\devlibs\pcl-1.15.1-install\cmake\` |

**主要库文件列表：**

| 模块 | 导入库 | 运行时 DLL |
|------|--------|-----------|
| pcl_common | pcl_common.lib | pcl_common.dll |
| pcl_io | pcl_io.lib | pcl_io.dll |
| pcl_io_ply | pcl_io_ply.lib | pcl_io_ply.dll |
| pcl_kdtree | pcl_kdtree.lib | pcl_kdtree.dll |
| pcl_octree | pcl_octree.lib | pcl_octree.dll |
| pcl_search | pcl_search.lib | pcl_search.dll |
| pcl_filters | pcl_filters.lib | pcl_filters.dll |
| pcl_features | pcl_features.lib | pcl_features.dll |
| pcl_surface | pcl_surface.lib | pcl_surface.dll |
| pcl_registration | pcl_registration.lib | pcl_registration.dll |
| pcl_segmentation | pcl_segmentation.lib | pcl_segmentation.dll |
| pcl_sample_consensus | pcl_sample_consensus.lib | pcl_sample_consensus.dll |
| pcl_keypoints | pcl_keypoints.lib | pcl_keypoints.dll |
| pcl_recognition | pcl_recognition.lib | pcl_recognition.dll |
| pcl_tracking | pcl_tracking.lib | pcl_tracking.dll |
| pcl_stereo | pcl_stereo.lib | pcl_stereo.dll |
| pcl_ml | pcl_ml.lib | pcl_ml.dll |
| pcl_outofcore | pcl_outofcore.lib | pcl_outofcore.dll |

**CMake 引用方式：**

```cmake
set(PCL_DIR "C:/devlibs/pcl-1.15.1-install/cmake")
find_package(PCL 1.15 REQUIRED)
target_include_directories(your_target PRIVATE ${PCL_INCLUDE_DIRS})
target_link_libraries(your_target PRIVATE ${PCL_LIBRARIES})
```

**运行时需要将以下目录加入 PATH：** `C:\devlibs\pcl-1.15.1-install\bin`

---

### 4. Eigen 3.4.1 (header-only)

| 资源 | 路径 |
|------|------|
| 安装根目录 | `C:\devlibs\eigen-3.4.1-install\` |
| 头文件 | `C:\devlibs\eigen-3.4.1-install\include\eigen3\` (含 Eigen, Dense, SVD 等) |
| CMake 配置文件 | `C:\devlibs\eigen-3.4.1-install\share\eigen3\cmake\Eigen3Config.cmake` |

**CMake 引用方式：**

```cmake
set(Eigen3_DIR "C:/devlibs/eigen-3.4.1-install/share/eigen3/cmake")
find_package(Eigen3 3.4 REQUIRED)
target_link_libraries(your_target PRIVATE Eigen3::Eigen)
```

---

### 5. Boost 1.84.0

| 资源 | 路径 |
|------|------|
| 安装根目录 | `C:\devlibs\boost-1.84.0-install\` |
| 头文件 | `C:\devlibs\boost-1.84.0-install\include\boost-1_84\boost\` |
| 导入库 (11个) | `C:\devlibs\boost-1.84.0-install\lib\` |

**已编译的库文件（均为 vc144-mt-x64）：**

| 库文件 | 说明 |
|--------|------|
| libboost_atomic-vc144-mt-x64-1_84.lib | 原子操作 |
| libboost_chrono-vc144-mt-x64-1_84.lib | 时钟/时间 |
| libboost_container-vc144-mt-x64-1_84.lib | 容器 |
| libboost_date_time-vc144-mt-x64-1_84.lib | 日期时间 |
| libboost_exception-vc144-mt-x64-1_84.lib | 异常 |
| libboost_filesystem-vc144-mt-x64-1_84.lib | 文件系统 |
| libboost_iostreams-vc144-mt-x64-1_84.lib | IO 流 |
| libboost_random-vc144-mt-x64-1_84.lib | 随机数 |
| libboost_serialization-vc144-mt-x64-1_84.lib | 序列化 |
| libboost_thread-vc144-mt-x64-1_84.lib | 线程 |
| libboost_wserialization-vc144-mt-x64-1_84.lib | 宽字符序列化 |

**CMake 引用方式：**

```cmake
set(BOOST_ROOT "C:/devlibs/boost-1.84.0-install")
set(BOOST_INCLUDEDIR "C:/devlibs/boost-1.84.0-install/include/boost-1_84")
set(BOOST_LIBRARYDIR "C:/devlibs/boost-1.84.0-install/lib")
find_package(Boost 1.84 REQUIRED COMPONENTS filesystem thread serialization)
target_link_libraries(your_target PRIVATE Boost::filesystem Boost::thread Boost::serialization)
```

---

### 6. FLANN 1.9.2

| 资源 | 路径 |
|------|------|
| 安装根目录 | `C:\devlibs\flann-1.9.2-install\` |
| 头文件 | `C:\devlibs\flann-1.9.2-install\include\flann\` |
| 导入库 | `C:\devlibs\flann-1.9.2-install\lib\` |

**库文件：**

| 文件 | 说明 |
|------|------|
| flann_s.lib | FLANN C 静态库 (59.6 MB) |
| flann.lib | FLANN C 导入库 |
| flann_cpp_s.lib | FLANN C++ 静态库 |
| flann_cpp.lib | FLANN C++ 导入库 |

**CMake 引用方式：**

```cmake
set(FLANN_ROOT "C:/devlibs/flann-1.9.2-install")
set(FLANN_INCLUDE_DIR "C:/devlibs/flann-1.9.2-install/include")
set(FLANN_LIBRARY "C:/devlibs/flann-1.9.2-install/lib/flann_s.lib")
```

---

### 7. libigl 2.6.0 (header-only)

| 资源 | 路径 |
|------|------|
| 安装根目录 | `C:\devlibs\libigl-2.6.0\` |
| 头文件 | `C:\devlibs\libigl-2.6.0\include\igl\` |

**CMake 引用方式：**

```cmake
set(LIBIGL_INCLUDE_DIR "C:/devlibs/libigl-2.6.0/include")
target_include_directories(your_target PRIVATE ${LIBIGL_INCLUDE_DIR})
```

或使用 igl 提供的 CMake 配置（如果有）：

```cmake
set(libigl_DIR "C:/devlibs/libigl-2.6.0/cmake")
find_package(libigl REQUIRED)
```

---

### 8. CGAL 6.1.1

| 资源 | 路径 |
|------|------|
| 安装根目录 | `C:\devlibs\CGAL-6.1.1\` |
| 头文件 | `C:\devlibs\CGAL-6.1.1\include\CGAL\` |
| CMake 配置文件 | `C:\devlibs\CGAL-6.1.1\CGALConfig.cmake` |
| 辅助模块 | `C:\devlibs\CGAL-6.1.1\auxiliary\` |

> CGAL 6.x 以 header-only 为主，核心功能无需链接库文件。

**CMake 引用方式：**

```cmake
set(CGAL_DIR "C:/devlibs/CGAL-6.1.1")
find_package(CGAL 6.1 REQUIRED)
target_link_libraries(your_target PRIVATE CGAL::CGAL)
```

---

### 9. Qt 5.15.2

| 资源 | 路径 |
|------|------|
| 安装根目录 | `C:\devlibs\Qt-5.15.2-msvc2019_64\5.15.2\msvc2019_64\` |
| 头文件 | `...\msvc2019_64\include\` (含 QtWidgets, QtCore, QtGui 等) |
| 导入库 | `...\msvc2019_64\lib\` (Qt5Core.lib, Qt5Widgets.lib, Qt5Gui.lib 等) |
| 运行时 DLL | `...\msvc2019_64\bin\` (Qt5Core.dll, Qt5Widgets.dll, Qt5Gui.dll 等) |
| qmake | `C:\devlibs\Qt-5.15.2-msvc2019_64\5.15.2\msvc2019_64\bin\qmake.exe` |
| 平台插件 | `...\msvc2019_64\plugins\platforms\qwindows.dll` |
| 预编译版本 | msvc2019_64 |

**CMake 引用方式：**

```cmake
set(CMAKE_PREFIX_PATH "C:/devlibs/Qt-5.15.2-msvc2019_64/5.15.2/msvc2019_64")
find_package(Qt5 5.15 REQUIRED COMPONENTS Core Widgets Gui)
target_link_libraries(your_target PRIVATE Qt5::Core Qt5::Widgets Qt5::Gui)
```

**运行时需要将以下目录加入 PATH：** `C:\devlibs\Qt-5.15.2-msvc2019_64\5.15.2\msvc2019_64\bin`

> Qt 使用 msvc2019 预编译包，VS2022 可通过设置平台工具集为 v142 兼容使用。  
> 如果遇到 ABI 兼容性问题，建议用 VS2022 从源码重新编译 Qt 5.15.2。

---

### 10. OpenSceneGraph 3.6.5

| 资源 | 路径 |
|------|------|
| 安装根目录 | `C:\devlibs\osg-install\` |
| 头文件 | `C:\devlibs\osg-install\include\` (含 osg/, osgDB/, osgViewer/ 等) |
| 导入库 (18个 .lib) | `C:\devlibs\osg-install\lib\` |
| 运行时 DLL (17个 osg161-*.dll) | `C:\devlibs\osg-install\bin\` |
| 插件 DLL (78个 osgdb_*.dll) | `C:\devlibs\osg-install\bin\osgPlugins-3.6.5\` |
| 工具程序 | `C:\devlibs\osg-install\bin\` (osgviewer.exe, osgconv.exe, osgarchive.exe) |
| 源码目录 | `C:\devlibs\OpenSceneGraph-OpenSceneGraph-3.6.5\` |
| 构建目录 | `C:\devlibs\osg-build\` |

**核心库文件：**

| 模块 | 导入库 | 运行时 DLL |
|------|--------|-----------|
| OpenThreads | OpenThreads.lib | ot21-OpenThreads.dll |
| osg (核心) | osg.lib | osg161-osg.dll |
| osgDB (数据I/O) | osgDB.lib | osg161-osgDB.dll |
| osgUtil | osgUtil.lib | osg161-osgUtil.dll |
| osgGA (交互) | osgGA.lib | osg161-osgGA.dll |
| osgViewer | osgViewer.lib | osg161-osgViewer.dll |
| osgText | osgText.lib | osg161-osgText.dll |
| osgFX | osgFX.lib | osg161-osgFX.dll |
| osgManipulator | osgManipulator.lib | osg161-osgManipulator.dll |
| osgParticle | osgParticle.lib | osg161-osgParticle.dll |
| osgShadow | osgShadow.lib | osg161-osgShadow.dll |
| osgSim | osgSim.lib | osg161-osgSim.dll |
| osgTerrain | osgTerrain.lib | osg161-osgTerrain.dll |
| osgAnimation | osgAnimation.lib | osg161-osgAnimation.dll |
| osgVolume | osgVolume.lib | osg161-osgVolume.dll |
| osgWidget | osgWidget.lib | osg161-osgWidget.dll |
| osgUI | osgUI.lib | osg161-osgUI.dll |
| osgPresentation | osgPresentation.lib | osg161-osgPresentation.dll |

**CMake 引用方式：**

```cmake
set(OSG_ROOT "C:/devlibs/osg-install")
set(OSG_INCLUDE_DIR "${OSG_ROOT}/include")
set(OSG_LIB_DIR "${OSG_ROOT}/lib")
set(OSG_BIN_DIR "${OSG_ROOT}/bin")

target_include_directories(your_target PRIVATE ${OSG_INCLUDE_DIR})
target_link_libraries(your_target PRIVATE
    ${OSG_LIB_DIR}/osg.lib
    ${OSG_LIB_DIR}/osgDB.lib
    ${OSG_LIB_DIR}/osgViewer.lib
    ${OSG_LIB_DIR}/osgGA.lib
    ${OSG_LIB_DIR}/osgUtil.lib
    ${OSG_LIB_DIR}/OpenThreads.lib
)
```

**运行时环境变量：**

```
PATH 加入: C:\devlibs\osg-install\bin
OSG_LIBRARY_PATH=C:\devlibs\osg-install\bin\osgPlugins-3.6.5
```

---

### 11. OpenCASCADE 7.9.1 (OCCT)

> 从源码编译（tag `V7_9_1`），方案 B（混合依赖）：FreeType/oneTBB/Draco 源码自建保 v144 一致，
> RapidJSON 纯头文件，FreeImage 官方预编译 x64。开启 FreeImage+TBB+Draco(glTF)+RapidJSON。
> 关闭 Draw 模块（`BUILD_MODULE_Draw=OFF`，无 Tcl/Tk 依赖）。

| 资源 | 路径 |
|------|------|
| 安装根目录 | `C:\devlibs\opencascade-7.9.1-install\` |
| 头文件 | `C:\devlibs\opencascade-7.9.1-install\inc\` (TopoDS_Shape.hxx, BRepPrimAPI_MakeBox.hxx 等) |
| 导入库 (50个 .lib) | `C:\devlibs\opencascade-7.9.1-install\win64\vc14\lib\` |
| 运行时 DLL (50个 .dll) | `C:\devlibs\opencascade-7.9.1-install\win64\vc14\bin\` |
| CMake 配置文件 | `C:\devlibs\opencascade-7.9.1-install\cmake\OpenCASCADEConfig.cmake` |
| 源码目录 | `C:\devlibs\OCCT-7.9.1\` |
| 构建目录 | `C:\devlibs\occt-7.9.1-build\` |
| 编译器工具集 | vc14 (MSVC 14.44 / v144) |

**核心 toolkit（OCCT 7.9.x 命名）：**

| 模块 | toolkit | 说明 |
|------|---------|------|
| 内核/数学 | TKernel, TKMath | 基础类型、gp 几何 |
| 几何数据 | TKG2d, TKG3d, TKGcc, TKGeomBase, TKBRep | 2D/3D 几何、B-Rep |
| 几何算法 | TKGeomAlgo, TKTopAlgo, TKPrim, TKBO, TKBool, TKShHealing, TKMesh | 布尔、网格化、缝合 |
| 可视化 | TKService, TKV3d, TKOpenGl | 3D 显示（OpenGL） |
| 应用框架 | TKernel 内含 CDF/LCAF → TKCDF, TKLCAF, TKCAF, TKBinL, TKXmlL 等 | 文档/标签 |
| 数据交换 | TKDE, TKXSBase, TKDESTEP, TKDEIGES, **TKDESTL**, TKDEOBJ, TKDEPLY, TKDEVRML, TKRWMesh, TKDEGLTF | STEP/IGES/STL/OBJ/PLY/glTF |

> **重要**：OCCT 7.9.0+ 重构了 DataExchange 模块，旧 toolkit 名已变更：
> `TKSTL→TKDESTL`、`TKSTEP→TKDESTEP`、`TKIGES→TKDEIGES`、`TKOBJ→TKDEOBJ`、
> 新增 `TKDEGLTF`(glTF)、`TKDEPLY`。从旧代码迁移时注意更新链接名。

**第三方依赖（OCCT 编译时已用）：**

| 依赖 | 版本 | 安装路径 |
|------|------|---------|
| FreeType | 2.13.3 | `C:\devlibs\freetype-2.13.3-install\` (源码编译) |
| oneTBB | 2022.0.0 | `C:\devlibs\onetbb-2022.0-install\` (源码编译) |
| Draco | 1.5.7 | `C:\devlibs\draco-1.5.7-install\` (静态库) |
| RapidJSON | 1.1.0 | `C:\devlibs\rapidjson-1.1.0\include\` (纯头文件) |
| FreeImage | 3.18.0 | `C:\devlibs\FreeImage-3.18.0\` (官方预编译 x64) |

**CMake 引用方式：**

```cmake
set(OpenCASCADE_DIR "C:/devlibs/opencascade-7.9.1-install/cmake")
find_package(OpenCASCADE REQUIRED CONFIG)
target_link_libraries(your_target PRIVATE
    TKernel TKMath TKG2d TKG3d TKBRep TKGeomBase
    TKGeomAlgo TKTopAlgo TKPrim TKBO TKBool TKMesh TKShHealing
    TKDESTL TKDESTEP TKDEIGES TKRWMesh          # 数据交换
    TKService TKV3d TKOpenGl)                    # 可视化
```

> OCCT 的 imported target 名与 toolkit 名一致（如 `TKPrim`、`TKDESTL`），按需链接即可。

**运行时需要将以下目录加入 PATH：**
```
C:\devlibs\opencascade-7.9.1-install\win64\vc14\bin
C:\devlibs\freetype-2.13.3-install\bin
C:\devlibs\onetbb-2022.0-install\bin
C:\devlibs\FreeImage-3.18.0\bin
```
（Draco 为静态库，RapidJSON 为纯头文件，无需运行时 DLL）

---

## 四、CMake 项目集成总览

以下是一个综合的 CMakeLists.txt 参考配置，集成所有库：

```cmake
cmake_minimum_required(VERSION 3.24)
project(HandheldScanner VERSION 1.0 LANGUAGES CXX CUDA)

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CUDA_STANDARD 17)

# === CUDA ===
set(CMAKE_CUDA_COMPILER "C:/Program Files/NVIDIA GPU Computing Toolkit/CUDA/v12.6/bin/nvcc.exe")
set(CMAKE_CUDA_ARCHITECTURES 75)

# === 第三方库路径 ===

# OpenCV
set(OpenCV_DIR "C:/opencv-cuda-4.13.0")

# Open3D
set(Open3D_DIR "C:/devlibs/open3d-install/CMake")

# PCL
set(PCL_DIR "C:/devlibs/pcl-1.15.1-install/cmake")

# Eigen
set(Eigen3_DIR "C:/devlibs/eigen-3.4.1-install/share/eigen3/cmake")

# Boost
set(BOOST_ROOT "C:/devlibs/boost-1.84.0-install")
set(BOOST_INCLUDEDIR "C:/devlibs/boost-1.84.0-install/include/boost-1_84")
set(BOOST_LIBRARYDIR "C:/devlibs/boost-1.84.0-install/lib")

# FLANN
set(FLANN_INCLUDE_DIR "C:/devlibs/flann-1.9.2-install/include")
set(FLANN_LIBRARY "C:/devlibs/flann-1.9.2-install/lib/flann_s.lib")

# libigl (header-only)
set(LIBIGL_INCLUDE_DIR "C:/devlibs/libigl-2.6.0/include")

# CGAL
set(CGAL_DIR "C:/devlibs/CGAL-6.1.1")

# Qt5
set(CMAKE_PREFIX_PATH "${CMAKE_PREFIX_PATH};C:/devlibs/Qt-5.15.2-msvc2019_64/5.15.2/msvc2019_64")

# OSG
set(OSG_ROOT "C:/devlibs/osg-install")

# OpenCASCADE
set(OpenCASCADE_DIR "C:/devlibs/opencascade-7.9.1-install/cmake")

# === find_package ===
find_package(OpenCV 4.13 REQUIRED)
find_package(Open3D REQUIRED)
find_package(PCL 1.15 REQUIRED)
find_package(Eigen3 3.4 REQUIRED)
find_package(Boost 1.84 REQUIRED COMPONENTS filesystem thread serialization)
find_package(CGAL 6.1 REQUIRED)
find_package(Qt5 5.15 REQUIRED COMPONENTS Core Widgets Gui OpenGL)
find_package(OpenCASCADE REQUIRED CONFIG)

# === 目标链接 ===
target_include_directories(${PROJECT_NAME} PRIVATE
    ${PCL_INCLUDE_DIRS}
    ${FLANN_INCLUDE_DIR}
    ${LIBIGL_INCLUDE_DIR}
    ${OSG_ROOT}/include
)

target_link_libraries(${PROJECT_NAME} PRIVATE
    ${OpenCV_LIBS}
    Open3D::Open3D
    ${PCL_LIBRARIES}
    Eigen3::Eigen
    Boost::filesystem Boost::thread Boost::serialization
    ${FLANN_LIBRARY}
    CGAL::CGAL
    Qt5::Core Qt5::Widgets Qt5::Gui Qt5::OpenGL
    ${OSG_ROOT}/lib/osg.lib
    ${OSG_ROOT}/lib/osgDB.lib
    ${OSG_ROOT}/lib/osgViewer.lib
    ${OSG_ROOT}/lib/osgGA.lib
    ${OSG_ROOT}/lib/osgUtil.lib
    ${OSG_ROOT}/lib/OpenThreads.lib
    TKernel TKMath TKG2d TKG3d TKBRep TKGeomBase
    TKGeomAlgo TKTopAlgo TKPrim TKMesh TKDESTL TKDESTEP TKRWMesh
    TKService TKV3d TKOpenGl
)
```

---

## 五、运行时 PATH 配置

程序运行时需要以下目录在系统 PATH 中（或在同一目录下放置 DLL）：

```
C:\opencv-cuda-4.13.0\x64\vc17\bin
C:\devlibs\open3d-install\bin
C:\devlibs\pcl-1.15.1-install\bin
C:\devlibs\osg-install\bin
C:\devlibs\Qt-5.15.2-msvc2019_64\5.15.2\msvc2019_64\bin
C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v12.6\bin
C:\devlibs\opencascade-7.9.1-install\win64\vc14\bin
C:\devlibs\freetype-2.13.3-install\bin
C:\devlibs\onetbb-2022.0-install\bin
C:\devlibs\FreeImage-3.18.0\bin
```

建议通过 CMake 的 `install` 命令将所有依赖 DLL 复制到项目的 `bin/` 输出目录，  
或设置系统环境变量 `PATH` 包含以上路径。

---

## 六、构建环境初始化命令

编译项目前需在命令行中初始化 VS2022 环境：

```batch
call "C:\Program Files\Microsoft Visual Studio\2022\Community\Common7\Tools\VsDevCmd.bat" -arch=amd64
set "PATH=C:\devlibs\ninja;%PATH%"
```

或使用 CMake 直接配置：

```batch
cmake -S . -B build -G "Visual Studio 17 2022" -A x64
cmake --build build --config Release
```

---

## 七、目录结构总览

```
C:\devlibs\
├── boost-1.84.0-install\          # Boost 1.84.0
├── CGAL-6.1.1\                    # CGAL 6.1.1 (源码目录即安装目录)
├── draco-1.5.7\                   # Draco 源码
├── draco-1.5.7-install\           # Draco 1.5.7 (静态库, OCCT 依赖)
├── eigen-3.4.1-install\           # Eigen 3.4.1
├── fake-git\                      # 辅助工具：模拟 git 命令
├── flann-1.9.2-install\           # FLANN 1.9.2
├── FreeImage-3.18.0\              # FreeImage 3.18.0 (官方预编译, OCCT 依赖)
├── freetype-2.13.3\               # FreeType 源码
├── freetype-2.13.3-install\       # FreeType 2.13.3 (OCCT 依赖)
├── libigl-2.6.0\                  # libigl 2.6.0
├── ninja\                         # Ninja 构建工具
│   └── ninja.exe
├── OCCT-7.9.1\                    # OpenCASCADE 7.9.1 源码
├── occt-7.9.1-build\              # OCCT 构建目录
├── opencascade-7.9.1-install\     # OpenCASCADE 7.9.1 安装目录
├── oneTBB-2022.0.0\               # oneTBB 源码
├── onetbb-2022.0-install\         # oneTBB 2022.0.0 (OCCT 依赖)
├── Open3D-main\                   # Open3D 源码 (main 分支)
├── open3d-build\                  # Open3D 构建目录
├── open3d-install\                # Open3D 安装目录
├── OpenSceneGraph-OpenSceneGraph-3.6.5\  # OSG 源码
├── osg-build\                     # OSG 构建目录
├── osg-install\                   # OSG 安装目录
├── pcl-1.15.1-install\            # PCL 1.15.1
├── Qt-5.15.2-msvc2019_64\         # Qt 5.15.2
│   └── 5.15.2\msvc2019_64\
└── rapidjson-1.1.0\               # RapidJSON 1.1.0 (纯头文件, OCCT 依赖)

C:\opencv-cuda-4.13.0\             # OpenCV 4.13.0 + CUDA + Contrib
```

---

## 八、已知差异与注意事项

1. **MSVC 工具集**：文档要求 v143，本机为 v144 (14.44)。建议统一使用 v144。
2. **Qt ABI 兼容性**：Qt 使用 msvc2019 预编译包，VS2022 可通过 v142 平台工具集兼容。若项目整体用 v144，建议后续从源码编译 Qt 5.15.2。
3. **GPU 计算能力**：Quadro RTX 5000 为 sm_75 (Turing)。文档示例中 8.6/8.9/9.0 针对较新 GPU，sm_75 完全可用。
4. **Open3D CUDA**：使用 main 分支编译，CUDA 12.6 + MSVC 14.44 编译通过，已验证无 is_proxy_reference 错误。
5. **OSG 补丁**：源码中 `OSGA_Archive.cpp` 的 `_FPOSOFF` 宏已手动修复（兼容 MSVC 14.44）。如果重新解压源码需重新应用。
6. **OCCT toolkit 改名**：OCCT 7.9.0+ 重构 DataExchange 模块，`TKSTL→TKDESTL`、`TKSTEP→TKDESTEP`、`TKIGES→TKDEIGES`、`TKOBJ→TKDEOBJ`，并新增 `TKDEGLTF`/`TKDEPLY`。从旧版 OCCT 代码迁移时需更新链接名。
7. **OCCT 关闭 Draw**：本机构建时 `BUILD_MODULE_Draw=OFF`（`USE_TCL=OFF` 被产品依赖扫描强制覆盖，须用 Draw 模块开关），未生成 DRAWEXE 测试台；核心几何/网格/IO/可视化 toolkit 齐全。如需 DRAWEXE，需额外安装 Tcl/Tk 并重新配置。
8. **OCCT 运行时依赖**：OCCT 自身 DLL 在 `win64\vc14\bin`，但其依赖的 FreeType/oneTBB/FreeImage 的 DLL 需单独加入 PATH（见第五节）。
9. **GitHub 下载**：本机 `github.com` 直连受阻（连接重置），OCCT 及其依赖源码通过代理 `https://gh-proxy.com/` 拉取。SourceForge（FreeImage）与 savannah（FreeType）可直连。
