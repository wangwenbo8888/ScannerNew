# 算子提炼模板

> **版本**: v1.0
> **用途**: 为每个算子生成 1-2 页精炼的**开发参考卡**，开发者拿到后能快速掌握算子全貌并开始编码
> **算子总数**: 35 个（不含算子 0 初始化、算子 1 图像采集循环）

---

## 模板定义

每个算子按以下 **12 个段落**组织，段落顺序固定，内容用表格或代码块填充。

### 段落总览

| 段落 | 名称 | 内容 | 填写说明 |
|:----:|------|------|---------|
| A | 标识 | 编号、名称、目录名、平台、所属流程、精度档次 | 全部必填 |
| B | 数据流 | 输入→输出的核心数据类型 | 全部必填 |
| C | 算法 | 核心数学方法 / OpenCV 调用 | 写关键函数名，不写推导过程 |
| D | 依赖 | 上下游算子编号 + 共享复用关系 | 明确"谁→谁" |
| D2 | 衔接 | 跨算子数据传递细节 | 字段→方法重载、指针/值、必用/可选 |
| E | 架构 | 代码结构、pImpl、文件、核心类名/方法名 | CUDA 算子必须标注桥接文件 |
| J | 环境 | 第三方库依赖 + 编译约束 | 列出 OpenCV 子模块，标注是否需要 CUDA |
| F | 参数 | operator_params.json 中的关键配置项 | 表格：参数名/类型/默认值/说明 |
| G | 约束 | 精度、性能、数据量、线程安全、实例隔离 | 定量指标 |
| K | 质量 | QualityFlag 语义 + 错误处理模式 | 列出各 QualityFlag 的触发条件 |
| H | 风险 | 已知缺陷、注意事项、坑 | 从对比分析报告提取 |
| I | 状态 | 现有代码复用判定 | 可直接用 / 需重构 / 需新开发 |

---

## A. 标识

| 字段 | 填写内容 |
|------|---------|
| 算子编号 | `2-1`（阶段号-序号） |
| 中文名称 | 激光线或标记点掩膜区域提取 |
| 英文目录名 | `mask_extract_cuda` |
| 运行平台 | CUDA / CPU |
| 所属流程 | `标定流程1① / 标定流程2❶（共用）` — 标注在哪个流程、第几步 |
| 精度档次 | `①`（亚像素级，~0.01 pixel）/ `②`（亚像素级，~0.05 pixel）/ `③`（像素级） |

---

## B. 数据流

| 方向 | 数据描述 | 数据类型 |
|------|---------|---------|
| **输入①** | 单帧灰度图像（标记点或激光线） | `cv::Mat`（CV_8UC1） |
| **输入②** | （如果有多个输入，继续列出） | — |
| **输出** | 特征区域二值掩膜 | `shared_ptr<cv::cuda::GpuMat>`（CUDA）/ `cv::Mat`（CPU） |

---

## C. 算法

**核心流程**（用伪代码或步骤列表）：

```
Step 1: Host→Device 上传（cv::Mat → GpuMat）
Step 2: GPU 二值化 → cv::cuda::threshold
Step 3: GPU 腐蚀（去噪）→ cv::cuda::erode
Step 4: GPU 膨胀（恢复形状）→ cv::cuda::dilate
Step 5: （可选）面积过滤
```

**关键 OpenCV/第三方函数**：

| 函数 | 用途 |
|------|------|
| `cv::cuda::threshold()` | 二值化 |
| `cv::cuda::erode()` | 腐蚀去噪 |
| `cv::cuda::dilate()` | 膨胀恢复 |

---

## D. 依赖

**上下游算子**：

```
上游 → 本算子 → 下游
（无） → 2-1 mask_extract → 2-2 region_analyze
                         → 4-2 region_analyze（激光标定复用）
```

**共享/复用关系**：

| 共享对象 | 说明 |
|---------|------|
| 与 4-1 共享同一模块 | 目录名同为 `mask_extract_cuda`，通过参数区分标记点/激光线场景 |

---

## D2. 衔接

**上游→本算子**：

| 来源 | 传递方式 | 说明 |
|------|---------|------|
| 直接输入 `cv::Mat` | 值传递 | 本算子是 GPU 处理链入口，内部自动上传到 GPU |

**本算子→下游**：

| 输出字段 | 传递给 | 下游方法 | 传递方式 | 必用/可选 |
|---------|--------|---------|---------|---------|
| `result.d_cleanedMask` | 2-2 RegionAnalyzerCUDA | `analyze(shared_ptr<GpuMat>)` | `shared_ptr` | 必用 |
| `result.success` | Pipeline 调度 | — | 值 | 必用 |
| `result.qualityFlag` | Pipeline 调度 | — | 值 | 可选 |

---

## E. 架构

**文件结构**：

```
mask_extract_cuda/
├── CMakeLists.txt
├── mask_extract_cuda.h               # 公开头文件（纯 C++，不含 CUDA 类型）
├── mask_extract_cuda_pimpl.h         # CUDA 桥接头文件（仅 CUDA 算子）
├── mask_extract_cuda.cpp             # 桥接实现
├── mask_extract_cuda_impl.cu         # CUDA 实现（仅 CUDA 算子）
├── config/
│   └── operator_params.json
└── tests/
    └── test_mask_extract_cuda.cpp
```

**核心 API**：

| 项目 | 名称 |
|------|------|
| 核心类 | `LaserMaskExtractorCUDA` |
| 核心方法 | `extract(const cv::Mat& grayImage, cv::cuda::Stream stream)` |
| 参数结构体 | `LaserMaskParams` |
| 结果结构体 | `LaserMaskResult` |
| 日志标签 | `"01-LaserMaskExtractorCUDA"` |

---

## J. 环境

| 依赖项 | 版本 | 说明 |
|--------|------|------|
| OpenCV + CUDA | 4.13.0 | 需要 `opencv_cudaimgproc`、`opencv_core` |
| CUDA Toolkit | 12.6 | sm_75（RTX 5000），兼容 Jetson Orin（sm_87） |
| nlohmann_json | ≥ 3.11 | 参数序列化 |
| spdlog | ≥ 1.15 | 日志 |
| GoogleTest | ≥ 1.14 | 单元测试（FetchContent） |
| C++ 标准 | C++17 | **禁止 C++20** |

**编译特性**：

- CUDA 算子：`BUILD_CUDA` 条件编译宏
- `_NEIGHBOR_DEPS` 指向上游算子 `build/_deps` 复用 FetchContent 源码
- 平台兼容：Windows 10 x64 + Jetson Orin NX/AGX Orin

---

## F. 参数

**operator_params.json 关键参数**：

| 参数名 | 类型 | 默认值 | 范围 | 说明 |
|--------|------|--------|------|------|
| `threshold` | int | 80 | [0, 255] | 二值化阈值 |
| `erodeSize` | int | 5 | 奇数 | 腐蚀核大小 |
| `laserDilateSize` | int | 3 | 奇数 | 激光膨胀核大小 |
| `minArea` | int | 100 | > 0 | 最小区域面积阈值 |
| `maxArea` | int | 100000 | > minArea | 最大区域面积阈值 |

**预设配置**：`bright_laser` / `dark_laser` / `noisy_environment` / `thin_laser` / `wide_laser`

---

## G. 约束

| 约束类型 | 指标 |
|---------|------|
| 目标精度 | — （掩膜提取无精度指标） |
| 单帧性能 | ≤ 2ms（GPU，1080p） |
| 显存占用 | ~12 MB（1080p，单实例） |
| 数据规模 | 图像 1800×1200 ROI |
| 线程安全 | **非线程安全**，Debug 模式 `atomic<bool>` 检测并发调用 |
| 实例隔离 | 左右相机各持独立实例，总显存约 24 MB |

---

## K. 质量

**QualityFlag 语义**：

| 标记 | 含义 | 触发条件 |
|------|------|---------|
| `Normal` | 正常处理 | 掩膜提取成功，面积过滤后仍有有效区域 |
| `Degraded` | 质量降级 | 掩膜提取成功但有效区域减少（如面积过滤后剩余少） |
| `Warning` | 警告 | 输入为空或 CUDA 错误 |

**错误处理模式**：

| 错误类型 | 处理方式 |
|---------|---------|
| 参数非法 | 抛出 `std::invalid_argument` |
| 输入为空 | `result.success = false` |
| CUDA 错误 | `result.success = false` + 错误信息 |

---

## H. 风险

| 严重程度 | 风险描述 | 影响 |
|:--------:|---------|------|
| 🟡 中 | 面积过滤 `filterByArea` 是 TODO stub，两个分支都执行 `copyTo`，未实际过滤 | 掩膜含噪声区域 |
| 🟡 中 | 仅处理激光线掩膜，未适配标记点掩膜提取场景 | 需分支处理 |
| 🟡 中 | 激光线断裂连接功能未实现 | 断裂激光线可能被识别为多条 |
| 🟢 低 | 阈值未自适应调整 | 不同亮度场景需手动调参 |

---

## I. 状态

| 项目 | 说明 |
|------|------|
| **判定** | 🔧 需要重构 |
| **现有模块** | `06_jgyanmo`（`LaserMaskExtractorCUDA`） |
| **复用方式** | 核心算法（GPU 阈值 + 腐蚀 + 膨胀）可直接复用，需增加：(1) 面积过滤实现 (2) 标记点分支 (3) 断裂连接 |
| **重构优先级** | P0-1（整条流水线入口） |

---

> **模板结束**
