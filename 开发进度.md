# 开发进度 · AI 接力状态

> 🔒 **阅读规则（默认限读）**：AI 默认只读「当前快照」+「关键决策」+ 最近 **10** 条会话日志。
> **禁止全篇通读**，除非：①人工明确授权"可通读"；②当前任务确需历史，且近期记录与其他文档都无法解答。
>
> 静态结构查 `工程目录地图.md`；算子详情查 `docs/算子说明文档/`；流水线查 `docs/流水线/`；算子契约查 `算子规范.md`。
> **本文件只记动态**（现在做到哪 / 卡在哪 / 下一步），不重复静态信息。

---

<!-- SNAPSHOT BEGIN -->
## 当前快照（每次会话结束前覆盖更新；只改有变化的行）

- **更新于**：2026-08-21 · 模块 08_devicemgmt 落地（分支 feat/mod08-devicemgmt，24 提交，待人工验收合并；**叠在 feat/mod07 之上**）
- **现状/阶段**：08 设备管理全量落地——serial/ 协议层三小层（SerialPort/FrameCodec v2·v3 开关+CRC16+ACK 重传/CommandChannel 命令组步链）+ 按键链（KeyManager/MenuLogic/KeySemantics 弃奇偶）+ DeviceManager 门面（逻辑线程 post 编队/故障 8 码/ParamStore 确认更新制/Warmup/ModeController）+ 监控（CameraControl 收敛/SelfCheckCollector PDH+NVML 动态加载/HardwareMonitor 三件套+快照）+ app 装配收拢（三行门面/ScannerWindow 串口旁路删净/N10 组链/HealthAdapter→PerfMonitor）。设计 `docs/plans/2026-08-20-设备管理模块08设计方案.md`（含落地记录附录）+ 实施计划同日
- **当前分支**：feat/mod08-devicemgmt（基于 feat/mod07-pipelinemgmt @af98dda；**两分支均未合并，待人工验收**——合并序：先 07 后 08）
- **构建/测试**：Debug 全量构建 ✅（含 scan_demo.exe）+ ctest **90/90** ✅（68→90：08 新增 13 个测试 exe）
- **进行中（≤5，★ 标队首 = 下一步首要）**：
  - ★ 人工验收 feat/mod07-pipelinemgmt → feat/mod08-devicemgmt 并依序合并 master
  - [ ] 08 真机联调（COM3 实链路/N10·N11 对拍/按键手感/温度上行/预热实测——代码就绪待硬件）
  - [ ] v3 协议定稿跟进（设计方案 §8 十四项待协议方：ACK 双 seq/S 码表/加热停止/温度 4 路/T·V 语义等——定稿后删 v2 兼容开关）
  - [ ] 真机联调（07 侧：08 帧源→SlotRing / 06 出口查表 / 03 渲染）
  - [ ] 09 网格四族算子（E 消费，桩待替换）
- **已知阻塞 / 待决（如有）**：无阻塞。08 遗留三口已登记 08 文档 §3：帧率三件套 process/drop 计数器（07 接入期）/热插拔（真机项）/许可证（预留 D6）；相机状态快照口（HardwareMonitor 相机行暂无源）在 AppContext.cpp:196 注释
- **易踩坑（如有，≤5，一行提醒非完整命令）**：
  - Release 必须用独立目录 `build-rel`（直接在 `build/` 跑报 MSB8013）
  - github 封锁时加 `-DJMW_GH_MIRROR=https://ghfast.top/`
  - **PowerShell `Set-Content -Encoding UTF8` 腐蚀 `#`→`i`**——文件改写用 Edit/Write 工具或 git checkout+Move-Item
  - ctest 跑 Debug 测试前必须 `$env:PATH` 前置 `C:\opencv-cuda-4.13.0-debug\x64\vc17\bin`——缺 DLL 时 ctest 假挂死（WerFault 弹窗阻塞）；全量 ctest 慢例 calib_camera/PJC 各 2-4 分钟非挂死
  - **子代理验证构建必须查 `$LASTEXITCODE`**——只看输出尾部会假绿（08 之 T17 曾中招）
- **只读保护区**：`factory_calib/` AI 不得改动（见 `factory_calib/AGENTS.md`）
<!-- SNAPSHOT END -->

---

## 🧭 关键决策（持久 · 除非人工推翻，勿再讨论）

> 晋升门槛：仅当某决策**会被未来会话重新质疑**时才提升至此；否则留在日志即可。
> 清除义务：失效的易踩坑、被推翻的决策，**当会话即移除**。

- 算子契约统一为**三元组**（Params/Result/Operator）+ pImpl + `Stream&` 流亲和 → 详见 `算子规范.md`
- `global_optim`（Ceres BA）**不入算子三元组**，为独立组件（`BUILD_GLOBAL_OPTIM` 开关）
- `factory_calib` 为已交付独立子工程，AI 只读保护区
- 07 调度底座**零算子知识**：GPU 链经 LaneHooks.gpuChain 注入，其第 4 参 `frontReady()` 回调＝"ccl 就绪点"（调用即提交 P 链、GPU 激光链并行；不调则 gpuChain 返回后兜底提交）——后续改链勿破坏此契约
- ScanPipeline **pause/resume 消费水位持久**（SchedulerRuntime startCounter/lastCounter）——resume 不得从 0 重扫（错误 R/T 污染点云+obs 重复）；A 姿态 streak"连续命中"以**判定完成序**计（非采集序），乱序窗口≤在飞周期数
- 07_session 已退役（2026-08-20）：StateMachine/IState 迁 10_observability；会话记账归 01/02 工作流自身（D6/D7）——勿再复活 SessionService

---

## 会话日志（倒序：新在上；默认仅读最近 10 条）

<!-- LOG INSERT: 紧贴本标记线【正下方】插入新条目，标记本身不动。状态标记：⚔进行中 / ⚠阻塞（AI 基于客观事实可标）；✅完成 禁止 AI 自标（需人工验证后由人工填）。标题须含可搜索关键词（模块/算子名）。 -->

### 2026-08-21 · 模块08_devicemgmt 落地（串口协议层v3+按键链+DeviceManager门面+监控全量+app收拢）· ⚙（全绿，待人工验收合并）
- 改动/决策：分支 `feat/mod08-devicemgmt` 24 提交（基于 feat/mod07 @af98dda）。设计两轮审查定稿（`docs/plans/2026-08-20-设备管理模块08设计方案.md`，A 类 6+B 类 7 修复）+ 实施计划 18 任务 subagent 三重审（实现/规格/质量）执行：**S** serial/ 三小层（FrameCodec v2/v3 开关+CRC16-CCITT+半帧超时；CommandChannel 非阻塞 ACK 对账+重传 3 败+命令组步链；SpscRing 满环丢新）；**K** KeyManager（PC 手势判定）+MenuLogic（弃奇偶显式账本）+KeySemantics（转移表裁判）；**D** ModeController/ParamStore（确认更新制+gen 后值胜出）/WarmupSequence/DeviceManager 门面（**post 编队逻辑线程**——审查抓出 Critical 跨线程竞态后修复+读口快照化）+故障 8 码边沿接线；**H** CameraControl 收敛（setGain/注入式标定/删三越界方法）+SelfCheckCollector（PDH/NVML 动态加载）+HardwareMonitor（三件套+HealthMetrics 快照）；**A** app 三行门面+串口旁路删净+N10 断链修复（终审抓出）。口径变更 6 条回写设计附录
- 验证：Debug 全量构建零错（含 scan_demo.exe）+ ctest **90/90**（68→90）；过线三条（门面不漏零件/AppContext 三行/08 不链 06·10）终审实证；07/09/10 模块与 🔒 定稿文档零改动
- 续作点：①人工验收合并（先 07 后 08）；②真机联调（COM3/协议对拍/按键/预热）；③v3 定稿跟进（§8 十四项待协议方，定稿后删 v2 开关）

### 2026-08-20 · 模块07_pipelinemgmt 落地（五流水线+调度底座+工作流移交+07_session 退役）· ⚙（全绿，待人工验收合并）
- 改动/决策：分支 `feat/mod07-pipelinemgmt` ~40 提交。按人工确认设计（`docs/plans/2026-08-19-流水线管理模块07设计方案.md`）与实施计划（同日实施计划.md P1~P6/31 任务）执行：**P1** sched/ 底座 7 组件（CpuTopology/PCoreBroker/GpuSlotService/SlotRing[06]/IFrameSource 两面孔/FrameResultQueue/SchedulerRuntime）+装配公共件；**P2** C 扫描（06 出口查表 EnhancedFrame/FrameEnricher、09 seed()、ScanChains/FuseConsumer/ScanPipeline）；**P3** A 姿态（06 CycleUnit、PostureConfirmTable streak+CAS、集齐自动收口 watcher）；**P4** B 标定计算（相机链/激光链‖PJC/质量门禁+落盘）+ D 全局优化（GBA+重融合成对、09 软先验扩展）；**P5** E 后处理（五阶段编排壳+法线实接，网格四族桩待 09）；**P6** 状态机迁 10、三工作流帧处理移交 07（ScanWorkflow/CalibrationWorkflow/PostProcessWorkflow 只留编排/记账）、07_session 退役、CUDAToolkit pin 上移根 CMakeLists。全程 subagent 实施+两阶段审查（规格/质量），审查抓出并修复：C1 submit×shutdown 竞态、T8 ccl 就绪点语义返工、resume 水位重扫、测试灭活残留+mkParams UB、quality 非单调等
- 验证：Debug 全量构建零错 + ctest **68/68**（43→68）+ scan_demo 链接绿；真 GPU 冒烟（mask_separation/ccl/steger 真跑）；合成数据收敛（B 相机链 Δfx<1px/角差 0.04°、GBA 漂移 1.5°→<0.2°、法线球面中位<3°）；factory_calib 零改动；🔒 定稿文档零改动
- 续作点：①人工验收合并；②真机联调（08→ring / 06 逐温 K/D / 03 渲染 / existingMarkers——TODO 已标）；③09 网格四族；④阈值联调标定（streak/σ/帧率）。设计/实施两文档已入库 docs/plans/

### 2026-08-13 · framework 退役 Phase 5+7 完成 · ⚙（整条迁移收尾，待人工验收合并）
- 改动/决策：feature/base-foundation 分支推进至 12 提交。Phase 5（务实版）：WorkflowContext+3工作流编译归属 → app，ScannerWindow → app，fw_workflow 清空（**暂未做 stages 去 ctx / WorkflowContext retire**——保留作 app DI 聚合器，留后续清理）。Phase 7：**framework/ 整树删除**；7 处 include 改 base/；app+08 的 fw_* 链接 → base；根 CMake 去 add_subdirectory(framework)。
- 中途额外完成：装 Qt5 Svg/SerialPort（自 download.qt.io 下 .7z+7z 解压）+ OSG/osgText/Eigen 集成修复 + stubs/（人工提供 5 个头）+ copy_dlls.bat 路径修复 → **scan_demo.exe 首次全量构建(5.4MB)**。
- 验证：构建绿 + ctest 43/43。分层 base/←06←08←07←业务←app 单向就位。
- 续作点：★ 待人工验收 12 提交并合并 master；后续可选清理（stages 去 ctx、工作流物理迁 app、types.h 拆分）。本会话改动已全 commit。

### 2026-08-13 · framework 退役迁移 Phase 1-4+6 + 按键协议v2/分层设计 · ⚙（Phase5/7 阻塞于 Qt）
- 改动/决策：新分支 `feature/base-foundation`（8 提交，全程构建绿+ctest 43/43，**未合并 master**）。Phase1 建 base/ 底座(types.h+EventBus)；Phase2 framework/data 容器+Sink、IWorkflow/Pipeline/Stage、ParameterManager→mod_fileio(06)，CalibStore→mod_calibration(01)；Phase3 hal(IScannerCamera/IMCU)→mod_devicemgmt(08)；Phase4 service(StateMachine/Session/IState)→mod_session(07)；Phase6 FaultHandler→mod_observability(10)。framework/ 仅剩 workflow 层+死代码+转发壳。配套文档：按键协议v2(`docs/plans/2026-08-12-按键交互协议-v2-design.md`+已落`08-设备管理.md §3.1`)、分层总方案(`2026-08-13-分层与功能划分-design.md`，**六轮代码级审查**)、退役实施方案+阶段总结(`2026-08-13-framework退役-*.md`)。另：基线前工作树是未完成集成重构，修 3 处 CMake（sdk 引用/Qt5 find_package/Galaxy SDK 路径）+ scan_demo 条件构建。
- 验证：每 Phase cmake 构建+ctest 43/43 绿（scan_demo 跳过）。六轮审查靠 grep/read 实证纠正 6 类错误（CalibStore 非孤儿、Stage 被模块继承须下沉06非app、ScannerWindow 深度耦合AppContext、04无独立stages、core/与09/core/撞名→改base/ 等）。
- 续作点：**Phase 5/7 阻塞于 Qt5 Svg/SerialPort 未装**（scan_demo 不可构建→UI 侧改动不可验证）。续作：装 Qt → Phase5(工作流+ScannerWindow→app, WorkflowContext退役, stages去WorkflowContext化) → Phase7(删framework/整树+文档同步)。**教训**：PS `Set-Content -Encoding UTF8` 腐蚀 `#`→`i`，后续用 Edit/Write 工具。

### 2026-08-06 · 01-标定工作流 核心功能描述完善 · ✅
- 改动/决策：`docs/模块功能/01-标定工作流.md` 重写为核心功能描述（对标 02/08 同级）。修 3 处事实错误：①1.3 误用 `mask_separation`(扫描算子)→应为 `mask_extract`+`ccl`+`laser_label`；②1.3 旧端点链路 4-9/4-10/4-11→`projector_joint_calib`(v2.2 替代)；③接口过期 `executeTempCalib` 独立按钮→废弃(v2.2 阶段5已解散)。补：运行时温度补偿查询 `getCompensatedParams`(Service层)、主层/跨层归属、两条独立流水线控制流视角、产物消费者、25姿态多帧确认控制流、姿态优先(原误"速度优先")、frame_filter(2-1b)；当前状态由"核心已实现"修正为"算子已实现/编排待建"。**未提交**（待人工确认）
- 验证：纯文档；交叉核对 `客户端标定流水线.md`(v2.2) + `框架整体.md`(§3.1/§3.5) + `modules/01_calibration/`(空桩) + `算子规范.md`(§0.0 C6/C7)
- 下次注意：编排层实现依赖 `framework/workflow` 契约具体化（前置）；运行时查询服务返回类型/契约待编排阶段定义；原计划"代码生成规格"因依赖空桩框架暂缓，降级为核心功能描述

### 2026-08-06 · 模块功能细化 拆分 · ✅
- 改动/决策：`模块功能细化.md` 转为索引页；11 模块拆至 `docs/模块功能/01~11-*.md`（标题层级升一级）；`工程目录地图.md` docs 树补全（+模块功能/architecture/plans）+ 表行更新；复盘补 3 条维护约定（结构同步/进行中界定/未提交清单）；**未提交**（待人工确认）
- 验证：11 文件落地、索引↔模块双向链接通；纯文档无构建/测试影响
- 下次注意：模块文档待人工逐个完善；本次改动未 commit

### 2026-08-06 · 建立开发进度文档 · ✅
- 改动/决策：新增 `开发进度.md`（根目录）；定两区结构（快照覆盖 + 日志倒序追加）+ 限读规则（默认快照+关键决策+近 3 条）+ 持久「关键决策」区；`工程目录地图.md` 加双向链接
- 验证：无（文档类）；SNAPSHOT/LOG 标记已平衡
- 下次注意：后续会话按「维护约定」追加；平淡会话用一行模式；⚔ 中断会话补 `续作点`

### 2026-08-06 · frame_filter 收尾（示例条目，可删） · ✅
- 改动/决策：CUDA impl + pImpl 桥接；空输入 pin 为 `success=true`（commit 02555d8）
- 验证：5/5 绿
- 下次注意：待集成进姿态判断链（链 2-1b）

<!-- 更旧的记录在下方；超 50 条时把最旧一批移至 docs/plans/开发进度归档-YYYYMM.md -->

---

## 维护约定

- **会话开始**：读「当前快照」+「关键决策」+ 最近 10 条日志即可上手（除非人工授权通读）。
- **会话结束前**：①更新 `SNAPSHOT` 块（**只替换 BEGIN/END 之间**，只改有变化的行）；②在 `LOG INSERT` 线**正下方**插一条新日志；③按需维护「关键决策」「易踩坑」（晋升/清除）。
- **简洁纪律**：每字段一行；决策只记"决定 + 一句理由"，不记讨论过程；能一句话说清不展开。
- **一行模式**（无实质改动的探索/调研会话）：`### 日期 · 目标短语 · 状态 — 一句话结果。下次: …`，不强填四字段。
- **⚔ 续作点**：进行中会话**必须**加一行 `续作点：…`（改到哪 + 剩余文件/搜索词），便于恢复现场。
- **完成同步**：完成某「进行中」项时，①从快照移除/勾选；②完成它的日志条目标题引用该任务，避免快照与日志脱节。
- **状态标记权限**：日志条目状态中，**✅完成 禁止 AI 自标**（完成需人工验证）；⚔进行中 / ⚠阻塞 AI 可基于客观事实标注。
- **进行中界定**：「进行中」只放**跨会话未竟**工作；一次性单会话任务直进日志，不入「进行中」。
- **结构同步**：新增/变更目录结构（新文件/新文件夹/重命名）→ 同步 `工程目录地图.md`（静态地图），进度文档只记动态。
- **未提交清单**：本会话改动若未提交，「改动/决策」须**枚举 touched 文件/路径**，便于下个会话精确 `git add`。
- **构建/测试可信度**：状态行带 `✅干净` / `⚠未验证`；⚠ 时附"最后绿提交 `<hash>`"。
- **日志 ≥ 50 条**：把最旧（底部）一批移至 `docs/plans/开发进度归档-YYYYMM.md`；快照与关键决策不归档。
- **并行会话**：快照 last-write-wins；并发时各自只追加日志，最后统一合并快照。
