# 交付级 3D 扫描仪软件框架 · 整体设计（01.4 框架图）

> **日期**: 2026-07-05
> **步骤**: 01.4（01.3 基础上：许可证/激活管理 合并进 设备管理；模块数 12→11，可观测性 11→10、安装部署 12→11）
> **目的**: 搭建交付级产品
> **层脊依据**: 沿用 `2026-07-02-scanner3d-framework-design.md` v1.1 的 5 层架构（权威，含 11 条 ADR）

---

## 1. 框架设计 vs 工程现状（分析）

| | 2026-07-02 蓝图（权威） | 2026-03-22（废弃） | 工程现状 |
|---|---|---|---|
| 层脊 | 5 层 UI/Workflow/Service∥Algorithm/Data/HAL | 同 5 层 | **仅 Algorithm 层有实现；Scheduler/Watchdog 为空类桩** |
| 改进 | 11 条 ADR（流控/急停硬实时/双缓冲/产物交接/资源生命周期…） | 无 | — |
| 落地 | §3.9 自承「未落地，现状是算子脊梁」 | — | Algorithm 层 ✅（算子库已迁入）/ Scheduler·Watchdog·GpuRuntime 仅空类桩 / **UI·HAL·Service·Workflow 编排·Data 主体（含 RingBuffer）❌ 全空** |

**结论**：蓝图层脊稳健（ADR 充分），本框架 = 把抽象蓝图落成**具体框架**——**11 个产品模块（业务 7 + 平台 3 + 交付 1）+ SDK/用户权限作框架结构元素**（融合/法线等算子归模块9 算子库），归位到 5 层 + 部署期 wrapper，使其可增量实现（对应 `工程缺口分析与出厂路线图` 的 8 切片）。

---

## 2. 整体框架图

```
╔══════════════════════════════════════════════════════════════════════════════╗
║   交付级 3D 扫描仪软件框架 · 5 层主架构 + 部署期 wrapper + 侧边设施 + 横切    ║
║   接入端 UI(+SDK 架构位) · 11 产品模块(3类) · ★现有件                        ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌─ 部署期 wrapper（运行时之外）─────────────────────────────────────────────────┐
│ 〔模块11·交付〕安装包/卸载 · 首次配置向导(连设备/选语言/初始标定引导)         │
└──────────────────────────────┬───────────────────────────────────────────────┘
                               ↓ 安装后启动运行时 ↓
   ┌─ 接入端: UI 层 (操作员·人机) ─────────────────────────────────────────────┐
   │ 主窗口 · 标定导引 · 信息面板                                              │
   │ 〔模块3·业务〕渲染显示: 点云视图(跨层直读 Data)                            │
   │ 〔模块5·业务〕编辑(含撤销重做): 选中/标记/删除点云或标记点                 │
   │ 〔模块6〕文件管理界面  〔模块8〕设备管理(含许可证/激活)界面                 │
   │ ── 架构位: 接入端 B SDK(二次开发, 非模块) ──                              │
   └──────────────────────────────┬────────────────────────────────────────────┘
                                  │ 启动 Workflow / 订阅事件
   ╔══════════════════════════════╧════════════════════════════════════════════╗
   ║  〔横切: 用户权限/鉴权(架构元素) + 〔模块8〕许可证检查 — 管 UI 接入〕      ║
   ╚══════════════════════════════════════════════════════════════════════════╝
                                  │
┌─────────────────────────────────▼───────────────────────────────────────────┐
│ Workflow 层 (编排) —— WorkflowContext 统一入口 (窄接口注入, ADR 7.7)          │
│  Pipeline 引擎: Stage 组合 / 有界队列 / 背压 / 线程调度 〔EcoreOrchestrator 待建〕│
│  ┌──────────┬──────────────────────────┬───────────┐                         │
│  │〔模块1〕 │〔模块2 扫描·双模式〕主   │〔模块4〕 │ ← 3 条工作流             │
│  │ 标定     │ 纯标记点 / 标记点+激光   │ 后处理   │  （均跑在〔模块7〕       │
│  │ 工作流   │  工作流                   │ 工作流   │    会话管理内）          │
│  └──────────┴──────────────────────────┴───────────┘                         │
└──────────────────────────────┬───────────────────────────────────────────────┘
               ╔═══════════════╧═══════════════╗
  ┌────────────▼─────────────┐   ┌──────────────▼─────────────────────────────┐
  │ Service 层 (状态/业务)    │   │ Algorithm 层 (纯计算叶子, 无状态)            │
  │ · 状态机/故障/按键        │   │ ┌──────────────────────────────────────────┐ │
  │ · 温度补偿服务            │   │ │〔模块9·平台 算子库〕★现有 39 算子+GBA★     │ │
  │ ·〔模块7〕扫描会话管理    │   │ │  core17 + calib16 + scan6 + GBA          │ │
  │   (启/停/暂停/恢复/覆盖率 │   │ │  （渲染件归模块3, 非本库算子）                    │ │
  │   /断电续扫)              │   │ │  + 融合算子(模块2 实时/模块4 离线重融合) │ │
  │ ·〔模块6〕文件管理服务    │   │ │  + 法线算子(模块2/4, 已就位)             │ │
  │ ·〔模块8〕设备管理服务    │   │ │  纯计算叶子 · 标定/扫描双模式复用        │ │
  │   (含许可证/激活/权益)    │   │ │  不持有跨帧状态(融合哈希归 Data 层)      │ │
  │ ·〔模块5〕编辑服务        │   │ │  Result 携带 status(不抛异常)           │ │
  │   (含撤销/重做/操作历史)  │   │ └──────────────────────────────────────────┘ │
  │ 依赖 Data + EventBus      │   │  不依赖 Data/EventBus; 由 Workflow 调用      │
  └────────────┬─────────────┘   │  数据以参数注入; Result 携带 status         │
               │ 访问 Data        └──────────────┬─────────────────────────────┘
               │                                  │ (纯叶子, 不参与依赖链)
  ┌────────────▼─────────────────────────────────▼─────────────────────────────┐
  │ Data 层 (数据/持久化)                                                         │
  │  帧缓冲 RingBuffer(待建) · 点云缓冲(前后台双缓冲 ADR7.8) · GPU 显存(体素哈希)      │
  │  标定数据(内/外参/plane_map/温度表)                                           │
  │  〔模块6 文件管理·IO〕PLY/STL/标定参数 导入导出 · WorkflowArtifactStore(产物) │
  │  〔模块8〕设备状态缓存 + 许可证存储 · 〔模块5〕点云裁剪/操作历史栈 · Sink 回填│
  └──────────────────────────────┬──────────────────────────────────────────────┘
                                 │ [依赖倒置] Data 拥有 Sink; HAL 仅依赖抽象回填
  ┌──────────────────────────────▼──────────────────────────────────────────────┐
  │ HAL 层 (硬件抽象 · 纯硬件操作 · 编译时平台多态)                              │
  │  接口: ICamera1/2 · IMCU · IPlatform · ICudaContext                          │
  │  驱动: CaptureDriver · LightDriver · LedDriver · HardwareMonitor             │
  │  〔模块8 设备管理·硬件侧〕下位机/相机/CUDA/平台 · 实现: Windows / Jetson     │
  └──────────────────────────────────────────────────────────────────────────────┘

╔═ 侧边基础设施 (跨层共用) ════════════════════════════════════════════════════╗
║  EventBus (控制/通知通道, 不携带载荷)                                         ║
║  GpuRuntime: ICudaContext + ResourceLifecycleManager (GPU 资源生命周期)       ║
║  Scheduler + Watchdog: 线程池/CPU亲和/CUDA Stream/Stage心跳  〔仅空类桩〕       ║
╚══════════════════════════════════════════════════════════════════════════════╝
╔═ 横切关注点 ═════════════════════════════════════════════════════════════════╗
║  〔模块10·平台 可观测性/运维〕: 统一日志(spdlog★升级·配置/轮转/分级) ·        ║
║            性能监控(延迟/帧率/计数器 dashboard) · 崩溃捕获(minidump/crash)   ║
║  用户权限/鉴权 (架构元素) — 角色/登录/操作授权, 管 UI 接入                    ║
║  配置管理                                                                     ║
║  〔出厂级〕看门狗 · 上电自检 · 审计日志 · 多语言                              ║
╚══════════════════════════════════════════════════════════════════════════════╝
```

---

## 3. 模块 → 层 归位表（11 产品模块，3 类分组）

> 分类轴：**平台类 = 监控/运维/支撑性质**；**业务类 = 操作员日常扫描工作流的一部分**。许可证设备绑定，归入设备管理。

### 3.1 业务功能模块（操作员扫描工作流，7 个）

| # | 模块 | 功能说明 | 主层 | 跨层涉及 |
|---|------|---------|------|---------|
| 1 | 标定 | 25 姿态采集 → 解算相机内外参/立体矫正/激光平面映射表(plane_map)/温度补偿表，产出可复用标定参数（离线 batch） | Workflow | Algorithm(标定算子) · Data(标定数据) |
| 2 | 扫描（双模式：纯标记点 / 标记点+激光） | 实时扫描 180fps：标记点链（配准求 R/T）∥ 激光链（三维重建，纯标记点模式关）双链并行 | Workflow | Algorithm(core 全算子) · Data(RingBuffer 待建) |
| 3 | 渲染显示 | 点云实时 3D 渲染（CUDA-GL 零拷贝），标记点/激光点云 viewer | UI | 渲染组件本模块自含(display/) · Data(只读直读) |
| 4 | 后处理 | 离线 batch 七阶段（全局标记点优化→重融合→**法线计算**→封装→补洞→光顺→边界优化），出 STL | Workflow | Algorithm(GBA★/法线算子★/网格算子待建) · Data(ArtifactStore) |
| 5 | 编辑（含撤销/重做 + 操作历史） | 扫描时选中/标记/删除点云或标记点；命令栈支持 undo/redo + 操作历史可追溯 | UI | Service(编辑服务+命令栈) · Data(点云裁剪/历史) |
| 6 | 文件管理 | PLY/STL/标定参数导入导出，工程/工件文件组织 | Data | Service(导入导出服务) · UI(界面) |
| 7 | 会话管理 | 扫描启/停/暂停/恢复、覆盖率统计、断电续扫、会话元数据 | Service | Workflow(扫描工作流跑在会话内) · UI(会话控制) · Data(会话元数据) |

### 3.2 平台/基础设施模块（监控/运维/支撑底座，3 个）

| # | 模块 | 功能说明 | 主层 | 跨层涉及 |
|---|------|---------|------|---------|
| 8 | 设备管理（含许可证/激活） | 下位机/PC 状态监控（温度/帧率/按键/故障）与设备控制；含设备绑定许可证/激活（加密狗/软授权、权益、试用） | HAL | Service(状态监控+许可证) · UI(设备+激活界面) · Data(状态缓存+许可证存储) |
| 9 | 算子库 | 39 算子 + GBA（core17/calib16/scan6），纯计算叶子，标定/扫描双模式复用；含融合/法线等跨 workflow 共用算子（模块2/4 调用）；渲染件归模块3 | Algorithm | —（纯叶子，已就位） |
| 10 | 可观测性/运维 | 统一日志（配置/轮转/分级）、性能监控（延迟/帧率）、崩溃捕获（minidump） | 横切 | 全局 |

### 3.3 接入与交付模块（1 个）

| # | 模块 | 功能说明 | 主层 | 跨层涉及 |
|---|------|---------|------|---------|
| 11 | 安装部署 + 首次配置向导 | 安装包/卸载、首运行向导（连接设备/选语言/初始标定引导） | 部署期 wrapper（运行时外） | 首配向导调 标定/设备/语言 |

### 3.4 框架结构元素（非模块，不计入 11）

| 元素 | 位置 | 说明 |
|------|------|------|
| SDK（二次开发） | 接入端 B（与 UI 并列的架构位） | 稳定版本化 API 门面：MES/PLM 集成/远程/批处理 CLI；经用户权限+许可证(模块8) |
| 用户权限/鉴权 | 横切 | 角色/登录/操作授权，管 UI（+SDK）接入 |

---

## 4. 现状 vs 框架（一眼看清建什么）

- ✅ **已就位**：Algorithm 层（算子库 39+GBA，含融合/法线算子）· spdlog（未统一配置）
- ❌ **全空待建**：
  - UI 接入端主壳（主窗口/标定导引/信息面板）· 模块5 编辑/模块6·8 界面 · SDK 接入端（注：模块3 渲染组件 display/ 已迁入，待 UI 集成）
  - HAL 层 · Service 层（含模块7 会话管理/模块5 编辑服务+撤销/模块8 设备管理+许可证）· Data 层主体（点云双缓冲/标定IO/文件IO/ArtifactStore/操作历史/设备状态+许可证存储）
  - Workflow 编排（标定/扫描/后处理三条工作流全待建；EcoreOrchestrator 原型未迁入）· Data 层 RingBuffer 帧缓冲 · Scheduler/Watchdog/GpuRuntime 真实实现（现仅空类桩）
  - 模块10 可观测性（统一日志/性能/崩溃）· 用户权限 · 部署期 wrapper（模块11 安装包/首配）
  - 〔出厂级〕看门狗/自检/审计/多语言
- 本图与 `工程缺口分析与出厂路线图` 的 8 切片一一对应；模块就近并入切片：
  - 模块7 会话管理 → 切片6（状态机/编排）
  - 模块5 编辑(撤销重做) → 切片7（UI/编辑）
  - 模块8 设备管理(含许可证) → 切片2（HAL/设备监控基础）+ 切片6（故障/按键编排）+ 切片8（许可证/硬化）
  - 模块10 可观测性 → 横切，贯穿切片2起逐步落地
  - 模块11 部署+首配 → 切片8（硬化/交付）

---

## 5. 后续

本框架图为步骤 01.4（图 + 归位表，不展开细节）。后续步骤按层/模块展开细节设计（各层接口、模块职责、数据流、ADR 落地），逐层对接 8 切片路线图增量实现。

> 框架/模块审核告一段落。如还需补充其他模块（如尺寸检测/报告生成/扫描引导覆盖图/逆向工程等之前列举但本次未纳入的），告知后继续更新。
