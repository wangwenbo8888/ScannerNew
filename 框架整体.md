# 交付级 3D 扫描仪软件框架 · 整体设计（01.4 框架图）

> **日期**: 2026-07-05 ｜ **修订**: 2026-07-22（§2 架构一致性修订）｜ **增补**: 2026-08-10（§6 数据流程端到端追踪）
> **步骤**: 01.4（01.3 基础上：许可证/激活管理 合并进 设备管理；模块数 12→11，可观测性 11→10、安装部署 12→11）
> **目的**: 搭建交付级产品
> **层脊依据**: 沿用 `2026-07-02-scanner3d-framework-design.md` v1.1 的 5 层架构（权威，含 11 条 ADR）
>
> **本次修订要点（仅 §2 自身可读性，不动架构决策）**：
> - A2 Pipeline/Scheduler 术语消歧（"线程调度"→"Stage 执行调度"）
> - A3 部署期 wrapper 拆分"运行时之外/之内"标注
> - A4 ICudaContext 双归拆分（HAL=IGpuDevice，GpuRuntime=ICudaContext）
> - A5 Data 层拆 DataPlane(hot) / DataStore(cold) 子区
> - B1 横切"管 UI 接入"→"管 UI+SDK 接入"
> - B3 侧边设施标注"Workflow 层持有"
> - C1 模块8 跨层标注（待复审主层）
> - C2 模块5 主层 UI→Service（与模块7 标准对齐）
> - C3 侧边设施归属标注"待定"
> - C4 故障处理：7/8 主动响应 + FaultHandler 聚合（2026-07-23 定）
> - A6 横切元素统一标"开发级/出厂级"
> - B2 SDK 补"门面/组合各层能力"说明
>
> **2026-07-22 架构一致性修订（依据 framework 合约 + 独立审计）**：
> - 补 Workflow→Data 热路径(skip-layer) 与 Service→Workflow 编排方向；删 Algorithm 错位 ▼（合约：Algorithm 不依赖 Data）
> - 侧边设施去"归属待定"，对齐 `fw_infra` 独立层（`framework/infra/EventBus.h`）
> - 模块8 主层定 Service（HAL 仅留驱动/枚举子集）；模块3 直读标注 ADR7.8 形式化
> - 新增"读图规则"图例（纵向=逻辑分组，非依赖方向）；修正双缓冲误标 ADR7.8
> - resolved: C1/C3/C4/e-stop(弃用) ｜ open: A4/A5（详见 §7.1）
>
> **2026-08-10 增补（§6 数据流程·端到端追踪）**：
> - 新增 §6：基于算子库代码（`modules/09_operatorlib`）+ 标定/扫描两条主工作流，端到端追踪相机帧→点云产物的数据流
> - 含每环节精确数据类型（对照 `core/common/*.h` 契约头文件）+ 内存/显存流转关系（Host/Device 跨域转换↑↓⊕）
> - §6.0 总览 / §6.1 标定链 / §6.2 交接契约 / §6.3 扫描链 / §6.4 类型字典 / §6.5 内存显存图谱
> - 原 §6「后续」顺延为 §7（含 §7.1 修订遗留项）

---

## 1. 框架设计 vs 工程现状（分析）

| | 2026-07-02 蓝图（权威） | 2026-03-22（废弃） | 工程现状 |
|---|---|---|---|
| 层脊 | 5 层 UI/Workflow/Service∥Algorithm/Data/HAL | 同 5 层 | **仅 Algorithm 层有实现；Scheduler/Watchdog 为空类桩** |
| 改进 | 11 条 ADR（流控/急停硬实时（已弃用，见 §5.4）/双缓冲/产物交接/资源生命周期…） | 无 | — |
| 落地 | §3.9 自承「未落地，现状是算子脊梁」 | — | Algorithm 层 ✅（算子库已迁入）/ Scheduler·Watchdog·GpuRuntime 仅空类桩 / **UI·HAL·Service·Workflow 编排·Data 主体（含 RingBuffer）❌ 全空** |

**结论**：蓝图层脊稳健（ADR 充分），本框架 = 把抽象蓝图落成**具体框架**——**11 个产品模块（业务 7 + 平台 3 + 交付 1）+ SDK/用户权限作框架结构元素**（融合/法线等算子归模块9 算子库），归位到 5 层 + 部署期 wrapper，使其可增量实现（对应 `工程缺口分析与出厂路线图` 的 8 切片）。

---

## 2. 整体框架图

```
╔══════════════════════════════════════════════════════════════════════════════╗
║   交付级 3D 扫描仪软件框架 · 5 层主架构 + 部署期 wrapper + 侧边设施 + 横切    ║
║   接入端 UI(+SDK 架构位) · 11 产品模块(3类) · ★现有件                        ║
╚══════════════════════════════════════════════════════════════════════════════╝

读图规则:
  · 纵向位置 = 逻辑分组(UI/Workflow/Service∥Algorithm/Data/HAL), 非严格依赖方向;
  · 实线箭头 = 实际依赖边(调用/数据/控制), 允许 skip-layer(如 Workflow→Data 热路径、ADR7.8 直读口);
  · 控制方向与查询方向可并存(如 Service 编排 Workflow 生命周期 / Workflow 查询 Service 状态);
  ·〔模块n〕= 业务模块主层; (跨层…) = 该模块还触及的层。

┌─ 部署期 wrapper ─────────────────────────────────────────────────────────────┐
│ 〔模块11·交付·运行时之外〕安装包/卸载                                        │
│ 〔模块11·交付·运行时之内〕首次配置向导(连设备/选语言/初始标定引导)            │
│   ※ 首配向导演示需运行相机/MCU/算子，实为运行时活动，仅入口由安装包触发       │
└──────────────────────────────┬───────────────────────────────────────────────┘
                               ↓ 安装后启动运行时 ↓
   ┌─ 接入端: UI 层 (操作员·人机) ─────────────────────────────────────────────┐
   │ 主窗口 · 标定导引 · 信息面板                                              │
   │ 〔模块3·业务〕渲染显示: 点云视图(经 ADR7.8 只读窄接口直读 DataPlane, 非旁路) │
   │ 〔模块5 编辑·UI 面〕(主层=Service): 选中/标记/删除点云或标记点             │
   │ 〔模块6〕文件管理界面  〔模块8〕设备管理(含许可证/激活)界面                 │
   │ ── 架构位: 接入端 B SDK(门面, 组合各层能力, 非模块) ──                     │
   │     ※ SDK 经横切(权限+许可证) 接入, 依赖箭头见各层服务接口                 │
   └──────────────────────────────┬────────────────────────────────────────────┘
                                  │ 启动 Workflow / 订阅事件
   ╔══════════════════════════════╧════════════════════════════════════════════╗
   ║  〔横切: 用户权限/鉴权(架构元素) + 〔模块8〕许可证检查 — 管 UI+SDK 接入〕  ║
   ╚══════════════════════════════════════════════════════════════════════════╝
                                  │
┌─────────────────────────────────▼───────────────────────────────────────────┐
│ Workflow 层 (编排) —— WorkflowContext 统一入口 (窄接口注入, ADR 7.7)          │
│  Pipeline 引擎: Stage 组合 / 有界队列 / 背压 / Stage 执行调度(消费 Scheduler 线程) │
│  ※ Pipeline=数据流控(Stage 组合/丢帧), Scheduler=线程资源供给(线程池/亲和), 互补│
│  ※ Stage0 从 DataPlane 读帧(RingBuffer); 输出写点云双缓冲(热路径 skip-layer→Data)│
│  ※ 生命周期由 Service(StateMachine/会话) 编排 execute/stop/pause(07_session 链 fw_workflow)│
│  ※ 模块2 ScanMode 驱动:                                                     │
│    纯标记点  → mask_extract + 单链(标记点链), HAL 激光 OFF                  │
│    标记点+激光 → mask_separation + 双链(标记点∥激光), HAL 激光 ON           │
│    (两模式共享 ccl+标记点链9算子+配准+融合 ~70%, 不拆模块)                    │
│  ┌──────────┬──────────────────────────┬───────────┐                         │
│  │〔模块1〕 │〔模块2 扫描·双模式〕主   │〔模块4〕 │ ← 3 条工作流             │
│  │ 标定     │ 纯标记点 / 标记点+激光   │ 后处理   │  （均跑在〔模块7〕       │
│  │ 工作流   │ ※入口算子+激光链由       │ 工作流   │    会话管理内）          │
│  │          │  ScanMode 驱动(见下注)   │          │                         │
│  └──────────┴──────────────────────────┴───────────┘                         │
└──────────────────────────────┬───────────────────────────────────────────────┘
               ╔═══════════════╧═══════════════╗
  ┌────────────▼─────────────┐   ┌──────────────▼─────────────────────────────┐
  │ Service 层 (状态/业务)    │   │ Algorithm 层 (纯计算叶子, 无状态)            │
  │ · 状态机/按键             │   │ ┌──────────────────────────────────────────┐ │
  │ · FaultHandler(7/8主动    │   │ │〔模块9·平台 算子库〕★现有 42 算子+GBA★     │ │
  │   响应+汇聚/状态转换)     │   │ │  core19 + cal17 + scan6 + GBA            │ │
  │ · 温度补偿服务            │   │ │  （渲染件归模块3, 非本库算子）                    │ │
  │ ·〔模块7〕扫描会话管理    │   │ │  + 融合算子(模块2 实时/模块4 离线重融合) │ │
  │   (启/停/暂停/恢复/覆盖率 │   │ │  + 法线算子(模块2/4, 已就位)             │ │
  │   /断电续扫)              │   │ │  纯计算叶子 · 标定/扫描双模式复用        │ │
  │ ·〔模块6〕文件管理服务    │   │ │  不持有跨帧状态(融合哈希归 Data 层)      │ │
  │ ·〔模块8〕设备管理服务    │   │ │  Result 携带 status(不抛异常)           │ │
  │   (含许可证/激活/权益)    │   │ └──────────────────────────────────────────┘ │
  │ ·〔模块5〕编辑服务(主层)  │   │  不依赖 Data/EventBus; 由 Workflow 调用      │
  │   (含撤销/重做/操作历史)  │   │  数据以参数注入; Result 携带 status         │
  │ 依赖 Data + EventBus      │   │  (纯叶子, 不参与依赖链)                       │
   └────────────┬─────────────┘   └──────────────┴─────────────────────────────┘
                │ 冷路径: 状态/产物(秒-永久)         (Algorithm=叶子, 无 Data 下行边)
                ▼   ╭── 热路径 Workflow→Data(skip-layer): 读 RingBuffer 帧/写点云双缓冲 ─╮
    ┌────────────▼───┴───────────────────────────────────────────────────────────╯─┐
   │ Data 层 (数据/持久化)                                                         │
   │ ┌─ DataPlane (hot path · 高速数据通路, μs-ms) ─────────────────────────────┐ │
    │ │  帧缓冲 RingBuffer(待建) · 点云缓冲(前后台双缓冲) · GPU 显存(体素哈希)      │ │
   │ │  Sink 回填(IFrameSink/IDeviceStateSink, HAL→Data 依赖倒置)                │ │
   │ └──────────────────────────────────────────────────────────────────────────┘ │
   │ ┌─ DataStore (cold path · 持久化/状态, 秒-永久) ────────────────────────────┐ │
   │ │  标定数据(内/外参/plane_map/温度表) · WorkflowArtifactStore(产物 ADR7.10)  │ │
   │ │  〔模块6 文件管理·IO〕PLY/STL/标定参数 导入导出                              │ │
   │ │  〔模块8〕设备状态缓存 + 许可证存储 · 〔模块5〕点云裁剪/操作历史栈            │ │
   │ └──────────────────────────────────────────────────────────────────────────┘ │
   └──────────────────────────────┬──────────────────────────────────────────────┘
                                 │ [依赖倒置] Data 拥有 Sink; HAL 仅依赖抽象回填
   ┌──────────────────────────────▼──────────────────────────────────────────────┐
   │ HAL 层 (硬件抽象 · 纯硬件操作 · 编译时平台多态)                              │
   │  接口: ICamera1/2 · IMCU · IPlatform · IGpuDevice(设备枚举)                  │
   │  驱动: CaptureDriver · LightDriver · LedDriver · HardwareMonitor             │
    │  (模块8 主层已定 Service; HAL 仅留上方驱动/枚举硬件子集, 不含许可证/状态监控)  │
   └──────────────────────────────────────────────────────────────────────────────┘

╔═ fw_infra 层 (独立基础设施层 · 跨层注入使用 · 运行期由 app/main 装配) ════════╗
║  EventBus   : 控制/通知通道, 承载事件信号(ADR7.9 AlgorithmFault/HardwareFault)   ║
║  GpuRuntime : ICudaContext + ResourceLifecycleManager(ADR7.11), 注入给 Algorithm ║
║  Scheduler  : 线程池/CPU亲和/CUDA Stream, 供 Workflow 消费                       ║
║  Watchdog   : Stage 心跳监控(独立于被监者)                                       ║
║  ※ 计划口径归"P3 Workflow编排"工作项, 代码口径=fw_infra; 非Workflow私有, 非待定  ║
╚══════════════════════════════════════════════════════════════════════════════╝
╔═ 横切关注点 ═════════════════════════════════════════════════════════════════╗
║  〔模块10·平台·开发级〕可观测性/运维: 统一日志(spdlog★) · 性能监控 · 崩溃捕获 ║
║  〔开发级〕用户权限/鉴权 (架构元素) — 角色/登录/操作授权, 管 UI+SDK 接入      ║
║  〔开发级〕配置管理 (待细化 schema/版本化/热加载策略)                         ║
║  〔出厂级〕看门狗 · 上电自检 · 审计日志 · 多语言                              ║
╚══════════════════════════════════════════════════════════════════════════════╝
```

---

## 3. 模块 → 层 归位表（11 产品模块，3 类分组）

> 分类轴：**平台类 = 监控/运维/支撑性质**；**业务类 = 操作员日常扫描工作流的一部分**。许可证设备绑定，归入设备管理。

### 3.1 业务功能模块（操作员扫描工作流，7 个）

| # | 模块 | 功能说明 | 主层 | 跨层涉及 |
|---|------|---------|------|---------|
| 1 | 标定 | 25 姿态采集 → 解算相机内外参/立体矫正/激光平面映射表(plane_map)/温度补偿表，产出可复用标定参数（离线 batch）；**运行时温度补偿查询接口**（`getCompensatedParams(temp)`，模块8 喂温度/模块2 消费） | Workflow | Algorithm(标定算子) · Data(标定数据) · Service(温度补偿查询) |
| 2 | 扫描（双模式：纯标记点 / 标记点+激光） | 实时扫描 180fps。**ScanMode 驱动**：纯标记点用 mask_extract+单链(激光 OFF)；标记点+激光用 mask_separation+双链并行(激光 ON)。两模式共享 ccl+标记点链+配准+融合，不拆模块 | Workflow | Algorithm(core/mask_extract · scanning/mask_separation+激光链) · HAL(激光器开关) · Data(RingBuffer 待建) · Service(ScanConfig 持有模式) |
| 3 | 渲染显示 | 点云实时 3D 渲染（CUDA-GL 零拷贝），标记点/激光点云 viewer | UI | 渲染组件本模块自含(display/) · Data(只读直读) |
| 4 | 后处理 | 离线 batch 七阶段（全局标记点优化→重融合→**法线计算**→封装→补洞→光顺→边界优化），出 STL | Workflow | Algorithm(GBA★/法线算子★/网格算子待建) · Data(ArtifactStore) |
| 5 | 编辑（含撤销/重做 + 操作历史） | 扫描时选中/标记/删除点云或标记点；命令栈支持 undo/redo + 操作历史可追溯 | **Service** | UI(编辑界面) · Data(点云裁剪/历史) |
| 6 | 文件管理 | PLY/STL/标定参数导入导出，工程/工件文件组织 | Data | Service(导入导出服务) · UI(界面) |
| 7 | 会话管理 | 扫描启/停/暂停/恢复、覆盖率统计、断电续扫、会话元数据 | Service | Workflow(扫描工作流跑在会话内) · UI(会话控制) · Data(会话元数据) |

### 3.2 平台/基础设施模块（监控/运维/支撑底座，3 个）

| # | 模块 | 功能说明 | 主层 | 跨层涉及 |
|---|------|---------|------|---------|
| 8 | 设备管理（含许可证/激活）⚠ 跨4层 | 下位机/PC 状态监控（温度/帧率/按键/故障）与设备控制；上电自检流程；含设备绑定许可证/激活（加密狗/软授权、权益、试用）。**注：跨4层（HAL/Service/UI/Data）+ 横切，CMake 链全 5 层，警示"上帝模块"信号；主层已定 Service（许可证/状态监控归 Service），HAL 仅留驱动/枚举硬件子集（见 §7.1 C1）** | Service（已定） | HAL(驱动/枚举子集) · UI(设备+激活界面) · Data(状态缓存+许可证存储) |
| 9 | 算子库 | 42 算子 + GBA（core19/cal17/scan6），纯计算叶子，标定/扫描双模式复用；含融合/法线等跨 workflow 共用算子（模块2/4 调用）；渲染件归模块3 | Algorithm | —（纯叶子，已就位） |
| 10 | 可观测性/运维 | 统一日志（配置/轮转/分级）、性能监控（延迟/帧率）、崩溃捕获（minidump）、系统级审计日志 | 横切 | 全局 |

### 3.3 接入与交付模块（1 个）

| # | 模块 | 功能说明 | 主层 | 跨层涉及 |
|---|------|---------|------|---------|
| 11 | 安装部署 + 首次配置向导 | 安装包/卸载（运行时之外） + 首运行向导（连接设备/选语言/初始标定引导，**实为运行时之内**，仅入口由安装包触发） | 部署期 wrapper（拆分：安装包=运行时外 / 首配向导=运行时内） | 首配向导调 标定/设备/语言 |

### 3.4 框架结构元素（非模块，不计入 11）

| 元素 | 位置 | 说明 |
|------|------|------|
| SDK（二次开发） | 接入端 B（与 UI 并列的架构位） | 稳定版本化 API **门面**：组合各层能力（参考 §3.7）；MES/PLM 集成/远程/批处理 CLI；**经横切(用户权限+模块8 许可证) 接入**，与 UI 走同一道门禁 |
| 用户权限/鉴权 | 横切 | 角色/登录/操作授权，管 **UI + SDK** 接入（不区分接入端） |
| 配置管理 | 横切 | schema/版本化/热加载；`IConfig` 已在 fw_crosscut（`framework/crosscut/IAuth.h`）；性质同用户权限 |

### 3.5 灰色地带元素归类（2026-07-23 审计定）

> 以下元素原在 §2 图中出现但无模块号、也不在 §3.4，归属未决。本次逐一显式归类，消除歧义。

| 元素 | §2 原位置 | 归类决定 | 理由 |
|------|----------|---------|------|
| 配置管理 | 横切〔开发级〕 | §3.4 框架结构元素（已补入上方表） | `IConfig` 已在 fw_crosscut，性质同用户权限 |
| 主窗口/UI 外壳 | UI 层（无模块号） | **app/ 层职责**（非模块、非框架元素） | 各模块 UI 的宿主壳，天然属应用入口层（app/main） |
| 温度补偿服务 | Service 层（无模块号） | **模块1** 子功能（运行时补偿查询接口） | 模块1 持有补偿表，提供 `getCompensatedParams(temp)` 最内聚；模块8 喂温度、模块2 消费 |
| 审计日志 | 横切〔出厂级〕 | **模块10** 子功能 | 审计是日志子类，模块10 已有统一日志基础设施 |
| 上电自检 | 横切〔出厂级〕 | **模块8** 子功能 | 硬件初始化检查，模块8 持有全部硬件接口（已在模块8 待办） |
| 多语言 | 横切〔出厂级〕 | 横切框架元素（i18n 字符串表/本地化机制）；首配语言选择入口归模块11 | i18n 是框架级机制；模块11 首配向导仅是语言选择入口 |

---

## 4. 现状 vs 框架（一眼看清建什么）

- ✅ **已就位**：Algorithm 层（算子库 42+GBA，含融合/法线算子）· spdlog（未统一配置）
- ❌ **全空待建**：
  - UI 接入端主壳（主窗口/标定导引/信息面板）· 模块5 编辑/模块6·8 界面 · SDK 接入端（注：模块3 渲染组件 display/ 已迁入，待 UI 集成）
  - HAL 层 · Service 层（含模块7 会话管理/模块5 编辑服务+撤销/模块8 设备管理+许可证）· Data 层主体（点云双缓冲/标定IO/文件IO/ArtifactStore/操作历史/设备状态+许可证存储）
  - Workflow 编排（标定/扫描/后处理三条工作流全待建；EcoreOrchestrator 原型未迁入）· Data 层 RingBuffer 帧缓冲 · Scheduler/Watchdog/GpuRuntime 真实实现（现仅空类桩）
  - 模块10 可观测性（统一日志/性能/崩溃）· 用户权限 · 部署期 wrapper（模块11 安装包/首配）
  - 〔出厂级〕看门狗/自检/审计/多语言
- 本图与 `工程缺口分析与出厂路线图` 的 8 切片一一对应；模块就近并入切片：
  - 模块7 会话管理 → 切片6（状态机/编排）
  - 模块5 编辑(撤销重做) → 切片7（UI/编辑）
  - 模块8 设备管理(含许可证) → 切片2（HAL/设备监控基础）+ 切片6（故障/按键编排）+ 切片8（许可证/硬化）
  - 模块10 可观测性 → 横切，贯穿切片2起逐步落地
  - 模块11 部署+首配 → 切片8（硬化/交付）

---

## 5. 设备状态机（7 状态模型 · 操作员视角）

> 7 个顶层状态**两两互斥、绝不并行**，描述扫描仪运行时的全部处境。仅文档描述（操作员视角）；现有代码 `framework/service/IState.h::ScannerState` 枚举暂不动，后续另排期对齐（对齐项见 §5.7）。完整推演及与现有枚举的差异对照见 `docs/plans/2026-08-09-设备状态机-design.md`。

### 5.1 七个状态

| 代号 | 状态 | 说明 |
|---|---|---|
| **S1** | 初始化 | 软件启动自检：通讯 / 加密狗 / 软件授权文件 |
| **S2** | 待机 | 空闲就绪，所有功能入口 |
| **S3** | 标定 | 多姿态采集→解算标定参数 |
| **S4** | 扫描标记点 | 纯标记点扫描（激光 OFF，ScanMode 二选一） |
| **S5** | 扫描标记点+激光 | 标记点+激光点扫描（激光 ON，ScanMode 二选一） |
| **S6** | 后处理 | 离线 batch 七阶段→出 STL |
| **S7** | 设备故障+自检中 | 设备故障（通讯/温度/GPU）触发；安全停机 + 自检恢复 |

### 5.2 转换图

```
                     ┌──────────────────────────────┐
                     │ S1 初始化                     │
                     │ 自检: 通讯/加密狗/授权文件     │
                     └──────────────┬───────────────┘
                          自检通过  │  设备故障 → S7
                                    ▼
        ┌──────────────────────► S2 待机 ◄──────────────────┐
        │        启动标定/扫描/后处理 │                        │
        │        (扫描按 ScanMode     │                        │
        │         二选一 S4 或 S5)    ▼                         │
   ┌────┴───┐  ┌──────────┐  ┌───────────┐  ┌──────┐  完成/    │
   │ S3 标定│  │ S4 扫描   │  │ S5 扫描    │  │ S6   │ 停止      │
   │        │  │ (标记点)  │  │(标记点+激光)│  │后处理│(图标)    │
   └────┬───┘  └────┬─────┘  └─────┬─────┘  └──┬───┘           │
        └───────────┴──────────────┴────────────┘               │
                      (各状态完成→返回 S2)                       │

   ┌──────────────────────────────────────────┐
   │ S1–S5 检测到设备故障 → S7 设备故障+自检中 │
   │   S7 自检通过 → 回"退出前状态"，续作同一会话│
   │   ※ S6 免疫：不进 S7（离线，无设备可停/自检）│
   └──────────────────────────────────────────┘
```

### 5.3 转换表（合法跳转；空 = 不允许，需经 S2）

| 从＼到 | S1 | S2 | S3 | S4 | S5 | S6 | S7 |
|---|---|---|---|---|---|---|---|
| **S1** Init | — | 自检通过 | | | | | 设备故障 |
| **S2** Standby | | — | 启动标定 | 启动扫描(标记点) | 启动扫描(标记+激光) | 启动后处理 | 设备故障 |
| **S3** Calib | | 标定完成 | — | | | | 设备故障 |
| **S4** ScanM | | 上位机图标 | | — | | | 设备故障 |
| **S5** ScanML | | 上位机图标 | | | — | | 设备故障 |
| **S6** Post | | 后处理完成 | | | | — | **—（免疫）** |
| **S7** Fault | （恢复目标之一） | （恢复目标之一） | （恢复目标之一） | （恢复目标之一） | （恢复目标之一） | | — |

> S7 恢复 = 回退出前状态（S1/S2/S3/S4/S5 之一）并续作同一会话；**永不回 S6**。

### 5.4 关键规则

1. **互斥**：任意时刻只处于 7 状态之一；S3/S4/S5/S6 间无直接跳转，必经 S2。
2. **扫描二选一**：S4、S5 由 ScanMode 在 S2 入口决定；切换需经 S2 重选。
3. **故障恢复续作**：S7 自检通过后回退出前状态 + 续作同一会话（接续断点，留缺口）。⚠ 依赖检查点机制（`SessionService::loadCheckpoint`，当前 TODO）。
4. **S6 免疫**：离线 batch，不走硬件采集路径，**不进 S7**；其 GPU/算法失败由 `Result.status` 返回、操作员处置。
5. **S7 安全停机**：进入 S7 即下位机采集全停——**相机停 + 补光灯关 + 激光关**。
6. **S1 非设备类失败**（加密狗/授权）不进 S7，留 S1 阻塞告警。
7. **急停不采用**；扫描启停纯操作员驱动（覆盖率仅显示，不触发自动退出）。

### 5.5 S4 / S5 子状态机（就绪 / 采集）

```
                 上位机图标（从 S2 进入）
                         │
                         ▼
                  ┌─────────────┐  扫描仪按键   ┌─────────────┐
                  │  就绪 Ready  │ ──────────► │ 采集 Capture │
                  │ (持连接,不出帧)│ ◄────────── │(实时出帧+算子链)│
                  └──────┬──────┘  扫描仪按键   └──────┬──────┘
                         │                             │
                         └──────── 上位机图标 ─────────┘
                              （就绪/采集任一，点击即退出→ S2）
```

- **就绪↔采集**：扫描仪硬件按键 toggle（MCU `KeyEvent`，`MCUDriver` 解析 `"K1;/K0;"`，已实现）
- **任一→S2**：上位机图标（UI 待建）
- **设备故障**：就绪/采集任一子态均可 → S7

### 5.6 决策记录摘要

| 主题 | 决策 |
|---|---|
| S4/S5 关系 | 二选一模式（切换经 S2） |
| 暂停/急停/标定完成 | 子状态/标志/出口事件，不升顶层 |
| S7 恢复目标 | 回退出前状态 + 续作同一会话 |
| S6 与 S7 | S6 免疫，顶层仍互斥（不引入并行） |
| S7 范围 | 设备故障（通讯/温度/GPU），非仅通讯 |
| S7 进入动作 | 安全停机：相机+补光灯+激光全停 |
| S6 进入前提 | 操作员全权判定（无系统门禁） |
| S6 GPU 故障 | 不进 S7（走 `Result.status`） |
| 急停 / 自动扫描完成 | 均不采用；纯操作员驱动 |

### 5.7 落地依赖（按优先级）

1. 检查点机制（`SessionService::loadCheckpoint`）— S7 续作硬依赖
2. 模块8 上电自检 + 许可证检查 — S1 实质功能
3. 故障主动响应链（模块8 → `FaultHandler::reportFault`）— S7 上报
4. 覆盖率计算（体素哈希）— S4/S5 显示
5. UI（自检画面/扫描图标/故障告警/覆盖率）
6. （另排期）`ScannerState` 枚举对齐：含就绪/采集子态、删 EmergencyStop

---

## 6. 数据流程（端到端追踪）

> **日期**: 2026-08-10 ｜ **定位**: §2 给静态架构（谁在哪层），§5 给控制流（状态怎么跳），§6 给数据流（一帧数据从哪来、经过谁、变成什么、到哪去）——三者正交。
>
> **依据**: 算子库代码（`modules/09_operatorlib`）+ 标定/扫描两条主工作流流水线文档。数据类型以 `core/common/*.h` 契约头文件为准。

### 6.0 数据旅程总览

一帧数据从相机触发到点云产物，跨三条路径：

```
〔离线·冷路径〕标定                 〔交接〕               〔运行时·热路径〕扫描
(Workflow·Algorithm·DataStore)     (DataStore→内存/显存)   (HAL→DataPlane→Workflow→Algorithm)

相机帧 → Stage0 配置读入            标定产物                相机帧 → Stage0 预处理分叉
      → Stage1 采集循环            ┌────────────┐                → 双分支(标记点∥激光)
      → Stage2 姿态判断 ──────►    │ 两张温度表  │ ────注入────► → 融合 → 显示渲染
      → Stage3 相机标定            │ (DataStore) │                → GlobalOptimStage(批处理)
      → Stage4 激光标定            └────────────┘                  → 点云产物
      (秒~分钟, 离线一次性)                              (μs~ms/帧, 180fps 流式)
```

**标注**：
- 层归属：标定=Workflow 编排 Algorithm 算子；扫描=HAL→DataPlane(skip-layer)→Workflow→Algorithm
- 两条独立标定流水线：第一=持续循环(姿态采集)，第二=一次性(参数计算)
- A/B 扫描模式变点：①激光链(04~08)+激光融合，A跳过/B运行（`enableLaser` 开关）
- GlobalOptimStage：扫描与后处理之间的批处理(GBA+重融合)，A/B 均必跑
- skip-layer 热路径：Workflow→Data 写点云双缓冲（不经 Service 冷路径）

---

### 6.1 标定数据流追踪（离线·冷路径）

> **导读**：标定是离线一次性流程（秒~分钟级），产出两张温度补偿表供扫描查表。分两条独立流水线：第一持续循环采集姿态，第二集齐25姿态后一次性计算参数。算子级分叉详图见 `docs/流水线/客户端标定流水线.md`。

#### 6.1.1 第一流水线（持续循环）Stage0 → Stage1 → Stage2

**Stage0 初始化**（DataStore 冷读入）：
- 入：配置文件 → 出：初始内参(fx/fy/cx/cy) + 外参(R/T) + 立体矫正参数 + 标定板参数（均 **Host**）

**Stage1 图像采集循环**（HAL 硬触发 → DataPlane）：
- 下位机 I/O 硬触发，每帧只含一种特征：标记点帧(补光灯ON) / 激光管N帧(激光管N ON)
- 出：标记点灰度图 + 激光管灰度图 + 温度（双副本：Host `cv::Mat` + Device `GpuMat`）
- 帧率 180fps，每帧记录相机内部温度 + 骨架温度

**Stage2 姿态判断链**（帧级流水线，14 算子，GPU段→CPU段）：

| 算子 | 设备 | 输入类型 (域) | 输出类型 (域) | 跨域 |
|---|:---:|---|---|:---:|
| 2-1 `mask_extract` | CUDA | `cv::Mat` CV_8UC1 (**Host**) | `MaskExtractResult`{d_grayImage, d_laserMask, d_cleanedMask} (**Device** GpuMat) | ↑H2D |
| 2-1b `frame_filter` | CUDA | d_cleanedMask (**Device**) | `FrameFilterResult`{isMarkerFrame, maskRatio} + d_cleanedMask 透传 | — |
| 2-2 `ccl` | CUDA | d_cleanedMask (**Device**) | `RegionAnalysisResult`{d_labeledMask CV_32SC1 + components} | — |
| 2-3 `image_split` | CPU | cv::Mat(**Host**灰度副本) + components(**Host**) | `ImageSplitCPUResult`{splitImages: vector<cv::Mat>} | ↓灰度用Host副本 |
| 2-4 `zernike_edge` | CPU | splitImages[i] cv::Mat (**Host**) | `ZernikeEdgeCPUResult`{edgePoints} | — |
| 2-5 `image_merge` | CPU | vector<vector<EdgePoint>> + ROI (**Host**) | `ImageMergeCPUResult`{mergedEdgePoints + groupIds} | — |
| 2-6 `undistort_cpu` | CPU | EdgePoints + groupIds + 初始内参 (**Host**) | `StereoUndistortResult`{rectifiedPoints + R1/R2/P1/P2/Q} | — |
| 2-7 `ellipse_fit` | CPU | rectifiedPoints (**Host**) | `EllipseFitCPUResult`{center, major/minorAxis, angle} | — |
| 2-8 `marker_match` | CPU | L/R Point2f (**Host**) | `MarkerMatchCPUResult`{centerMatches} | — |
| 2-9 `epipolar_intersect` | CPU | EllipseFitCPUResult + centerMatches (**Host**) | `EpipolarIntersectCPUResult`{ellipseResults} | — |
| 2-10 `edge_match` | CPU | 极线交点 + centerMatches (**Host**) | `EdgeMatchCPUResult`{ellipseResults} | — |
| 2-11 `point_reconstruct` | CPU | EdgeMatchCPUResult + groupIds (**Host**) | `PointReconstructCPUResult`{markerResults: 3D中心+法线} | — |
| 2-12 `frame_fuse` | CPU | MarkerPointSet × 2 (**Host**) | `FrameFuseCPUResult`{R, T, transform} | — |
| 2-13 `pose_estimate` | CPU | R/T + 标定板目标矩阵 (**Host**) | `PoseEstimateCPUResult`{currentPose, anyMatched} | — |

- **GPU→CPU 分界**（2-2→2-3）：`ccl` 的 components 已在 Host；灰度图用 Host 副本，标记点链此后全 Host
- **输出**：命中 25 目标姿态 → 保存完整采集循环图像（标记点帧 + 激光管帧 + 温度）
- **灰度图双副本**：标记点帧灰度图同时驻留 Host（供 CPU 算子）和 Device（供 CUDA 算子），免二次上传

> **2-6⇄3-1 一致性**：`undistort_cpu`(2-6) 和 `inverse_distort`(3-1) 必须用同一组初始内参/畸变/R1/P1。

#### 6.1.2 第二流水线（一次性）Stage3 ∥ Stage4

> 集齐 25 姿态后一次性执行。Stage3 先行，Stage4 以数据驱动跟随（4-5 起消费 Stage3 输出）。

**Stage3 相机标定**（全 CPU，全程 **Host**）：

| 算子 | 设备 | 输入类型 (域) | 输出类型 (域) |
|---|:---:|---|---|
| 3-1 `inverse_distort` | CPU | 2-7 center(Point2f) + 初始内参 (**Host**) | `InverseDistortResult`{originalPoints} |
| 3-2 `intrinsic_calib` | CPU | L/R 多帧原始畸变中心 + 标定板3D点 (**Host**) | `IntrinsicCalibResult`{camera_matrix, dist_coeffs} |
| 3-3 `extrinsic_calib` | CPU | 内参+畸变(←3-2) + 角点 + 物方点 (**Host**) | `ExtrinsicCalibCpuResult`{R, T, E, F} |
| 3-4 `stereo_rectify` | CPU | 内参+畸变+R/T (**Host**) | `StereoRectifyCpuResult`{R1, R2, P1, P2, Q} |
| **3-5** `stereo_rectify_temp_table` | CPU | 内参+外参+CTE (**Host**) | **`StereoRectifyTempTable`** ★交接产物(§6.2表1) |
| 5-1 `intrinsic_compensate` | CPU | 内参(←3-2) (**Host**) | `IntrinsicCompensateCPUResult`{每温度 fx/fy/cx/cy} |
| 5-2 `extrinsic_compensate` | CPU | 外参(←3-3) (**Host**) | `ExtrinsicCompensateCPUResult`{每温度 T 补偿} |

**Stage4 激光标定**（GPU段 → CPU优化段）：

| 算子 | 设备 | 输入类型 (域) | 输出类型 (域) | 跨域 |
|---|:---:|---|---|:---:|
| 4-1 `mask_extract` | CUDA | cv::Mat (**Host**) | `MaskExtractResult`{d_grayImage, d_cleanedMask} (**Device**) | ↑H2D |
| 4-2 `ccl` | CUDA | d_cleanedMask (**Device**) | `RegionAnalysisResult`{d_labeledMask} | — |
| 4-3 `laser_label` | CUDA | d_labeledMask (**Device**) | `LaserLabelResult`{d_labeledMask 重编号} | — |
| 4-4 `steger` | CUDA | d_grayImage + d_labeledMask (**Device**) | `StegerResult`{d_centerPoints CV_32FC2 + d_line_ids} | — |
| 4-5 `undistort_cuda` | CUDA | d_centerPoints + 标定后内参 (**Device**) | `UndistortPointsResult`{d_rectifiedPoints} | — |
| 4-6 `epipolar_interp` | CUDA | d_rectifiedPoints (**Device**) | `EpipolarInterpResult`{d_interpPoints} | — |
| 4-7 `laser_match` | CUDA | L/R d_interpPoints (**Device**) | `LaserMatchResult`{d_matched L/R} | — |
| 4-8 `laser_reconstruct` | CUDA | d_matched + Q(←3-4) (**Device**) | `LaserReconstructResult`{d_points3d CV_32FC3} | — |
| [编排层跨姿态聚合] | — | d_points3d (**Device**) | vector<PosePointSet> (**Host**) | **↓D2H** |
| `projector_joint_calib` | CPU | poses + f + 主点 + initialT (**Host**) | `ProjectorJointCalibResult`{projectorT, emissionCurve} | — |
| 4-12 `plane_map` | CPU+CUDA | virtualK/R/T + StereoCalib(←3-4) | `PlaneMapResult`{d_left_to_right, d_right_u} | 混合 |
| **4-13** `plane_map_temp_table` | CUDA | PJC参数 + 立体标定 + CTE | **`LaserPlaneMapTempTable`** ★交接产物(§6.2表2) | — |
| 5-3 `laser_extrinsic_compensate` | CPU | 外参×2(←PJC+双目外参) (**Host**) | `LaserExtrinsicCompensateCPUResult` | — |

- **GPU→CPU 分界**（4-8→聚合→PJC）：`d_points3d`(GpuMat) download 成 Host `vector<PosePointSet>` 喂 CPU LM 优化
- **Stage3→Stage4 依赖**：4-5 用 3-2/3-3 标定后内参；4-8 用 3-4 的 Q 矩阵

---

### 6.2 标定→扫描交接契约

> 这是全系统**唯一**的标定→扫描数据耦合点。代码契约定义在 `core/common/calib_result_types.h`。两表均按温度阶梯（默认 0.2°C 步长）预计算；模块8 喂当前温度，扫描算子 `lookup(tempC)` 消费——温度是贯穿标定/扫描的一等轴。

| 标定产物 | 产出算子 | 扫描消费算子 | 数据类型 | 查表域 | 存储路径 |
|---|---|---|---|---|---|
| **温度补偿立体矫正表** | 3-5 `stereo_rectify_temp_table` | 标记点 `undistort_cpu` + 激光 `undistort_cuda` | `StereoRectifyTempTable`（map<double, `StereoRectifyParams`{R1,R2,P1,P2,Q}>） | **Host 内存**查表（`lookup(tempC)` 返回 Host 指针） | DataStore(冷) → 启动加载到内存 |
| **温度补偿激光面映射表** | 4-13 `plane_map_temp_table` | 激光 `laser_match_scan`（`SetTempTable` 注入） | `LaserPlaneMapTempTable`（map<double, `LaserPlaneMap`{leftToRightMap, rightUMap}>） | **Host 内存**存储；`SetTempTable` 内部转 GPU 扁平表示，**Device 查表** | DataStore(冷) → Host 内存（`SetTempTable` 时内部转 Device） |
| 内/外参补偿表 | 5-1/5-2/5-3 `*_compensate` | （已并入上两表，运行时不单独查） | `*CompensateCPUResult` | — | DataStore(归档) |

> **`lookup` 不抛异常**（对齐算子规范 §9）：表空返回 nullptr，由调用方处置。取温度最接近条目。
>
> **已知集成缺口**：`LaserPlaneMap.leftToRightMap` 生产者（4-13）尚未按契约产出该字段，仅 JSON 路径当前可用（`calib_result_types.h:74` 标注）。

---

### 6.3 扫描数据流追踪（运行时·热路径）

> **导读**：扫描是运行时流式流程（180fps，μs~ms/帧），数据走热路径（HAL→DataPlane→Workflow skip-layer→Algorithm）。算子级详图见 `docs/流水线/客户端扫描流水线.md`。

#### 入口（HAL → DataPlane）

- 下位机硬触发 → 相机帧写入 DataPlane RingBuffer（☐待建）
- **灰度图双副本**：Host 内存（供 CPU 标记点链）+ Device 显存（供 CUDA 激光链），免二次上传

#### 预处理分叉

| 算子 | 设备 | 输入类型 (域) | 输出类型 (域) | 跨域 |
|---|:---:|---|---|:---:|
| 预处理-01 `mask_separation` | CUDA | 灰度图(读 **Device** 副本) | `LaserMarkingSeparationResult`{d_markingPointMask, d_laserMask} (**Device**) | — |
| `ccl`(标记点) | CUDA | d_markingPointMask (**Device**) | `RegionAnalysisResult`{d_labeledMask + components} | — |

#### 标记点分支（CPU，全程 **Host**）

| 算子 | 设备 | 输入类型 (域) | 输出类型 (域) | 备注 |
|---|:---:|---|---|---|
| 03 `image_split` | CPU | Host灰度 + components(**Host**) | `ImageSplitCPUResult`{splitImages} | — |
| 04 `zernike_edge` | CPU | splitImages[i] (**Host**) | `ZernikeEdgeCPUResult`{edgePoints} | — |
| 05 `image_merge` | CPU | L/R EdgePoints (**Host**) | `ImageMergeCPUResult`{mergedEdgePoints + groupIds} | — |
| 06 `undistort_cpu` | CPU | EdgePoints + groupIds (**Host**) | `StereoUndistortResult`{rectifiedPoints} | **查§6.2表1**(Host内存) |
| 07 `ellipse_fit` | CPU | rectifiedPoints (**Host**) | `EllipseFitCPUResult`{center, axis} | — |
| 08 `marker_match` | CPU | L/R center (**Host**) | `MarkerMatchCPUResult`{centerMatches} | — |
| 09 `epipolar_intersect` | CPU | 椭圆参数 + centerMatches (**Host**) | `EpipolarIntersectCPUResult` | — |
| 10 `edge_match` | CPU | 极线交点 + centerMatches (**Host**) | `EdgeMatchCPUResult` | — |
| 11 `point_reconstruct` | CPU | EdgeMatchCPUResult (**Host**) | `PointReconstructCPUResult`{markerResults: 3D+法线} | — |
| 配准-01 `optical_flow_fuse` | CPU | 标记点3D + 上帧状态 (**Host**) | R/T (Matx33d+Vec3d) | 默认；失败↓兜底 |
| `frame_fuse`(兜底) | CPU | MarkerPointSet × 2 (**Host**) | R/T | — |

#### 激光分支（CUDA，全程 **Device**）①变点(A跳过/B运行)

| 算子 | 设备 | 输入类型 (域) | 输出类型 (域) | 备注 |
|---|:---:|---|---|---|
| 04 `steger`(Flat) | CUDA | d_laserMask + d_grayImage (**Device**) | `StegerResult`{d_centerPoints CV_32FC2 + d_line_ids占位} | Flat模式不分组 |
| 05 `undistort_cuda` | CUDA | d_centerPoints + d_line_ids (**Device**) | `UndistortPointsResult`{d_rectifiedPoints} | **查§6.2表1** |
| 06 `epipolar_interp`(false) | CUDA | d_rectifiedPoints (**Device**) | `EpipolarInterpResult`{d_interpPoints} | lineIdCheck=false |
| 07 `laser_match_scan` | CUDA | L/R d_interpPoints (**Device**) | `LaserMatchScanResult`{d_matched L/R + d_matched_line_ids} | **查§6.2表2**(Host存储→内部转Device查表); 产出line_id |
| 08 `laser_reconstruct` | CUDA | d_matched + Q (**Device**) | `LaserReconstructResult`{d_points3d CV_32FC3} | — |

> **激光链双模式（扫描 vs 标定）**：扫描激光线断裂+无序，`steger` 用 Flat 模式（统一占位 line_id），`epipolar_interp` 用 `lineIdCheck=false`（仅几何邻近），`laser_match_scan` 全局查表匹配时**产出** line_id（其前算子不依赖 line_id 的标定含义）。

#### 汇合 → 融合（skip-layer 写 DataPlane 点云双缓冲）

| 算子 | 设备 | 输入类型 (域) | 输出类型 (域) | 持久性 |
|---|:---:|---|---|---|
| 融合-02M `marker_cloud_fuse_cpu` | CPU | `MarkerFuseInput*` + R/T (**Host**) | `vector<MarkerCloudPoint>& fusedPoints_` (**Host** 跨帧累积) | 自持久化 |
| 融合-02 `laser_cloud_fuse_cuda` | CUDA | d_points3d + R/T (**Device**) | `LaserCloudFuseDeviceContext`{d_keys, d_fusedIdx, d_fusedXyz} (**Device** 常驻) | 自持久化 |
| 融合-03 `laser_cloud_normal_cuda` | CUDA | DeviceContext + 新体素 (**Device**) | 写 ctx.d_fusedNormal (**Device**) | — |

#### 显示渲染

| 算子 | 设备 | 输入 (域) | 输出 | 跨域 |
|---|:---:|---|---|:---:|
| 显示-02 `marker_cloud_renderer` | CPU | fusedPoints_ (**Host**) | osg::Geometry* VBO | **↑Host→VBO上传** |
| 显示-01 `laser_cloud_renderer` | CUDA | DeviceContext (**Device**) | osg::Drawable* GL VBO | **⊕零拷贝**(CUDA-GL interop) |
| 显示-00 `scanner_viewer` | OSG | Geometry / Drawable | 屏幕像素 | — |

#### 收尾 GlobalOptimStage（批处理，扫描结束后一次性）

- **入**：逐帧 {R_init, t_init, markerObs[]}（**Host**，从调度层逐帧 obs 缓冲取，**非** `fusedPoints_`——后者是体素去重云，已丢逐帧结构）
- `global_ba_cpu`（CPU + Ceres）→ 修正位姿（**Host**）
- **重融合**：用修正位姿重跑 `marker_cloud_fuse` / `laser_cloud_fuse`
- **display 行为**：GBA 期间（秒~分钟级）冻结末帧

#### A/B 模式变点

| 标记 | 图中节点 | 情况A(纯标记点) | 情况B(标记点+激光) | 开关 |
|---|---|---|---|---|
| ① | 激光链 04~08 **及** 激光融合 `laser_cloud_fuse`/`laser_cloud_normal` | 整段跳过 | 运行 | `enableLaser` |

> A/B 均**必跑** GlobalOptimStage（A 靠它压实光流链式配准的累积漂移；B 标记点位姿同样需 GBA 压实）。

---

### 6.4 数据类型字典（速查附录）

> 按"链"分组，每个类型标：定义头文件 / 产出算子 / 消费算子 / 内存域。

**标定→扫描契约**（`core/common/calib_result_types.h`）：

| 类型 | 产出 | 消费 | 域 |
|---|---|---|:---:|
| `StereoRectifyParams`{R1, R2, P1, P2, Q} | 3-4 `stereo_rectify` | (中间态，并入温度表) | Host |
| `StereoRectifyTempTable`{map<double, StereoRectifyParams>} | 3-5 | 扫描 `undistort_cpu`/`undistort_cuda` | Host(查表) |
| `LaserPlaneMap`{leftToRightMap, rightUMap} | 4-12 `plane_map` | (中间态，并入温度表) | Host/Device |
| `LaserPlaneMapTempTable`{map<double, LaserPlaneMap>} | 4-13 | 扫描 `laser_match_scan` | Device(查表) |

**标记点链**（`core/common/zitai_result_types.h` + 各算子头文件）：

| 类型 | 产出 | 消费 | 域 |
|---|---|---|:---:|
| `EllipseFitCPUResult`{center, axis} | `ellipse_fit` | `marker_match`/`epipolar_intersect` | Host |
| `EpipolarIntersectCPUResult`{ellipseResults} | `epipolar_intersect` | `edge_match` | Host |
| `EdgeMatchCPUResult`{ellipseResults} | `edge_match` | `point_reconstruct` | Host |
| `PointReconstructCPUResult`{markerResults: 3D+法线} | `point_reconstruct` | `frame_fuse`/`optical_flow_fuse` | Host |

**管线聚合**（`core/common/pipeline_types.h`）：

| 类型 | 产出 | 消费 | 域 |
|---|---|---|:---:|
| `CclOutput`{labeledMask(GpuMat), componentRects} | `ccl` | 标记点链(`image_split`) | Device+Host |
| `MarkerChainOutput`{markerPoints3D, R, T} | `pose_estimate`/`optical_flow_fuse` | (会话层) | Host |

**扫描融合**（各算子头文件）：

| 类型 | 产出 | 消费 | 域 |
|---|---|---|:---:|
| `MarkerCloudPoint`{x, y, z, nx, ny, nz, whiteRadius} | `marker_cloud_fuse_cpu` | `marker_cloud_renderer`/GBA | Host |
| `LaserCloudFuseDeviceContext`{d_keys, d_fusedIdx, d_fusedXyz, d_fusedNormal} | `laser_cloud_fuse_cuda` | `laser_cloud_normal`/`laser_cloud_renderer` | Device常驻 |

---

### 6.5 内存/显存流转图谱

#### 双副本投递机制

```
相机帧读入 RingBuffer 时：
  灰度图 → Host 内存 (cv::Mat CV_8UC1)     ──► 供 CPU 标记点链
        ↘ Device 显存 (GpuMat CV_8UC1)     ──► 供 CUDA 算子(mask_separation/steger/...)
  免二次上传；标定/扫描均遵循
```

#### 关键跨域转换点

| # | 位置 | 转换 | 数据 | 说明 |
|---|---|:---:|---|---|
| T1 | 采集 → RingBuffer | H→D ↑ | 灰度图 | 双副本写入，免后续重复上传 |
| T2 | 标定 2-2→2-3 / 扫描 ccl→标记点链 | 域切换 | components(ccl 内部产 Host) + 灰度(用预存 Host 副本) | GPU 段结束→CPU 段；无显式 download（灰度双副本早驻 Host） |
| T3 | 标定 4-8→聚合→PJC | D→H ↓ | d_points3d(GpuMat) → vector<PosePointSet> | GPU激光重建结果 download 喂 CPU 优化 |
| T4 | 扫描激光链全程 | D→D | d_centerPoints → … → d_points3d | 不 download，直喂 Device 融合 |
| T5 | 显示·标记点 | H→D ↑ | fusedPoints_(Host) → GL VBO | CPU 展点上传显卡 |
| T6 | 显示·激光 | D→GL ⊕ | d_fusedXyz → GL VBO | **零拷贝**(CUDA-GL interop) |
| T7 | 扫描启动·表注入 | H→D ↑ | `LaserPlaneMapTempTable`(Host) → GPU 扁平表示(Device) | `laser_match_scan::SetTempTable` 内部转换，一次性（非逐帧） |

#### 跨帧持久缓冲（驻留域不同，不可混）

| 缓冲 | 域 | 类型 | 持有者 |
|---|:---:|---|---|
| 标记点融合累积器 `fusedPoints_` | **Host** | `vector<MarkerCloudPoint>` | `marker_cloud_fuse_cpu` 实例内部 |
| 激光融合累积器 | **Device** | `LaserCloudFuseDeviceContext`(d_keys/d_fusedXyz/d_fusedNormal) | `laser_cloud_fuse_cuda` 实例内部 |
| 逐帧 obs 缓冲(GBA用) | **Host** | `vector<{R_init, t_init, markerObs[]}>` | 调度层维护 |

> 两融合算子各维护一个体素哈希累积器，空间栅格一致（共原点/分辨率），位置可互查。标记点 `fusedPoints_` 在 Host，激光 `LaserCloudFuseDeviceContext` 在 Device——两者驻留域不同。

#### 查表路径（温度补偿表）

| 表 | 查表域 | 消费算子 | 机制 |
|---|:---:|---|---|
| `StereoRectifyTempTable` | **Host 内存** | `undistort_cpu` / `undistort_cuda` | `lookup(tempC)` 返回 Host 指针；CUDA 算子用时上传参数 |
| `LaserPlaneMapTempTable` | **Host 存储 → Device 查表** | `laser_match_scan` | `SetTempTable` 内部转 GPU 扁平表示（Host→Device），运行时 Device 查表 |

---

## 7. 后续

本框架图为步骤 01.4（图 + 归位表，不展开细节）。后续步骤按层/模块展开细节设计（各层接口、模块职责、数据流、ADR 落地），逐层对接 8 切片路线图增量实现。

### 7.1 修订遗留待复审项

- ✅ **C1 模块8 主层**（2026-07-22 解决）：主层定为 **Service**（许可证/状态监控归 Service）；HAL 仅留驱动/枚举硬件子集。图 §2 + §3.2 已同步
- ✅ **C3 侧边基础设施归属**（2026-07-22 解决）：EventBus/GpuRuntime/Scheduler/Watchdog 归属 **fw_infra 独立层**（`framework/infra/EventBus.h` 已落），运行期由 app/main 装配；图 §2 已同步去"待定"。注：计划口径仍归"P3 Workflow编排"工作项，代码口径=fw_infra
- ☐ **A4 IGpuDevice/ICudaContext 拆分**：HAL 改 IGpuDevice（设备枚举），GpuRuntime 保留 ICudaContext（资源生命周期），需在 framework 层契约落实
- ☐ **A5 Data 层子分**：图上已拆 DataPlane/DataStore，需在 framework/data 的 CMake/头文件落实
- ✅ **C4 故障处理模型**（2026-07-23 定）：采用**主动响应**——模块7/8 检测故障后**先主动响应**（模块7：暂停/停止扫描；模块8：停激光/标记设备离线），再上报 FaultHandler；FaultHandler(ADR7.9) 聚合 → 发 FaultOccurred 事件 → StateMachine 转「故障」态 → 通知 UI/日志。图 §2 已同步
- ✅ **(新) e-stop / 硬实时（已弃用）**：经 §5.4 设备状态机决策，急停功能不采用；§1 "急停硬实时" ADR 相应弃用（2026-08-09）

> 框架/模块审核告一段落。如还需补充其他模块（如尺寸检测/报告生成/扫描引导覆盖图/逆向工程等之前列举但本次未纳入的），告知后继续更新。
